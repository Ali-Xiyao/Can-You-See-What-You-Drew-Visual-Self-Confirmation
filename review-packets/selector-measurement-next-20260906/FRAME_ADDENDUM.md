# 固定措辞的空描述条件

2026-09-06，在查看任何新条件的回答内容或效果统计之前登记；只查看过进度计数。

原 prompt_off 调用直接提交原题，删除了生成描述及整个自绘上下文包装。
它估计完整上下文移除的影响，不能把差异只归因于描述内容。

追加 prompt_blank：与 prompt_on 使用逐字相同的 PROMPTED_PREAMBLE，仅把 format 的
prompt 参数改为空字符串。保留“你被要求画图”“这是你画的图片”和据图回答的措辞及引号。
同图、同题、相同图像 latent 与解码设置；直接新请求，不含任何对话历史。

prompt_blank 减 prompt_on 是主要的生成描述内容干预；prompt_off 减 prompt_on
保留为整段上下文移除的补充干预。空描述条件仍不是完全无提示，题面和自绘措辞都在。
在 base、Naive16/32、Gold16/32 上追加，80张图、606题/每checkpoint，总3030条新增回答。
不会依据原两条件的效果大小决定是否追加，五个checkpoint全部执行。

追加数据独立写入 matched-frame；原 plan、题目、两条件数据及其执行代码不改。
两条件 controller 完成并释放锁后，追加 controller 取得同一锁再启动。无新训练、图像或梯度。
分析同时公开三条件，以场景配对报告，不挑选效应较大的无描述条件。
