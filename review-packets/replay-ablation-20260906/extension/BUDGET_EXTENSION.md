# 用户批准的运行时间延长

用户在2026-09-06约16:54 UTC问“那是不是可以延长硬截止”，本次按其继续完成实验的意思执行。
这是一项在已看过部分结果后批准的预算变更，不能称为事前预注册。原6小时协议、代码、数据和退出记录均保留。

只将最后一组 naive-alternate_ids 的完成窗口延长最多2小时：原截止17:14:46 UTC，新截止19:14:46 UTC，仍受原项目60小时总预算约束。
不增加优化步数（仍为32），不换样本/随机种子，不改问题、主要终点、阈值或置信区间计算。

原控制器和worker已经把截止时间存入进程内存，不支持热更新。新接续控制器等待它们真实退出，再取得同一原实验监督器锁。
如果原任务已完整完成，就不再运行GPU任务；否则只有在原预算到期引起退出时，才从最后一组最新的完整checkpoint接续。
使用同一GPU1，完整恢复adapter、Adam状态、学习率调度和全部RNG；不得从base重新开始。起点至少为step24，最多重算8个逻辑优化步。

输出在本extension目录。已完整保存的checkpoint之前的训练记录、探针、测量按字节复制并记哈希，构成可审计的逻辑运行前缀。
checkpoint之后原进程来不及保存的更新保留在旧目录，标为被接续重算取代的尾部，不重复计入32步。重复区间单独比较，不按结果选择某次运行。
原目录的失败/部分文件不删除、不补造complete。新接续结果有自己的complete及来源说明。

分析仍执行冻结的统计代码，只把最后一组的输入目录解析到接续后的逻辑运行；这个唯一的路径替换会机械核验并记录源码哈希。
其他五组读取原文件。最终报告同时披露预算延长、恢复步数、重复计算区间、完整/未完成状态及原控制器退出原因。
旧自然生成实验继续保持step32暂停，不启动新生成、梯度或新训练条件。新截止到达后仍不自动加时或重试。
