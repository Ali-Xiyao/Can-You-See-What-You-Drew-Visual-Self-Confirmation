# B运行恢复登记

2026-09-07，用户明确要求“可以 那你处理一下”“处理并继续”。此次授权执行一次有边界的故障处理和续跑；旧失败仍保留，不自动延长原24小时预算。

第一阶段仅针对InternVL唯一失败图做一次同模型、同512 token上限、同指令、同解码参数的诊断复跑，保存完整原文。恢复前的错误只保存回复前200字符，因此不能从它推断格式修复或生成截断。新的完整回复必须经过原冻结解析器；若仍失败，停止诊断，先核原文再决定，不拼凑对象、不从Qwen结果补InternVL。

诊断前冻结诊断代码及旧输入哈希，全部输出写到本恢复目录。900张PNG保持原路径；生成清单、完成记录和23640条观察回答按原字节复制，恢复输出另存。原149条成功InternVL结果不重新挑选，不因图像对错决定复跑对象。

观察恢复保留原100/50场景GPU分片、四权重、三上下文、每图/条件种子和问题顺序。完整模型先核原哈希；未完成模型核完整有序前缀并对一条已存条件同seed复现，通过后才接写。重放验证单独计数，不能混入108000条正式回答。所有旧失败和原回答哈希在恢复末尾再次核对。

正式续跑只在诊断成功、CPU恢复测试通过并冻结recovery-plan.json后启动，使用原截止2026-09-08 06:15:54 UTC。显存不足等候计入预算，不停止其他项目。代码只改运行容错和完整日志，不改研究题目、模型、评分规则或unknown处理。计算/解析再失败时保留完整错误，不无限循环重试。

原B分析代码通过显式路径适配使用新的逻辑目录；指标、分母、原10pp/±5pp规则不变。未裁定图仍未知，不能因恢复而转成确定标签。B结果不等于D*或D_g确认。
