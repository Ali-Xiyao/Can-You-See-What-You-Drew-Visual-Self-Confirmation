# B 执行清单（新图和回答产生前冻结）

沿用父目录PROTOCOL.md全部判据、150场景、6张/池、四模型和三上下文，不重排修复候选，不增加训练。A已完成且独立核验通过；三个配置均有可测输出响应，因此进入B。A结果不等于质量改善或D*。

GPU0现空闲，承担全部900张base生成，然后观察预先分配的100场景。GPU1目前由无关项目占用，等待显存足够后承担双检测器和crop，随后观察另50场景。观察分片按场景ID哈希排序、每个题型结构分别50/25场景，四模型/三上下文对同一场景全部落在同一GPU。统计始终对150场景等权，不能按设备速度改变分母。生成分为6个150图批次，GPU1可流水线裁定已完成批次。生成全部在同一GPU0，不混入A图或旧图。

GPU空闲只用nvidia-smi读取；至少18000MiB可用才加载。等待计入24小时总预算，不在超时后强行加载，不停止无关进程。各任务记录PID、设备、原始输出及完成清单。遇计算/数据错误保存失败并停止拥有的进程树，无自动重试、覆盖或预算延长。显存检查不能保证之后没有其他程序抢占，真实分配失败照实记录。

吞吐用A的实际生成秒数与回答latency估计。基准约20秒/图，900图约5 GPU小时；108000个回答按A均值估算，GPU0承担72000条、GPU1承担36000条。GPU1存在额外检测/crop与等待时间，24小时是预算上限而非完成保证。磁盘预留至少8GiB；不写新checkpoint。

新增负计数模板为“How many apple objects ...”。旧事实解析器把最后一个词当名词，会错读成object。B只在事实解析适配器中把严格匹配、且atom_id一致的这一模板转换为旧解析器支持的规范名词范围；实际观察题面、问题ID、A代码与语料不改。测试覆盖确实多画负名词时必须计出正数、颜色范围以及存在争议不能当零。A从未使用事实gold，因此此适配不改变A结论。

外部检测器保持现有Qwen3-VL-8B/InternVL3.5-8B及既有指令。冻结本地模型权重、配置、分词器和远程代码的SHA256，并记录文件大小/mtime；载入前后检查本地快照，最终再次全哈希。检测器只看到像素。图像数据同时核验文件SHA和RGB SHA；缺少任一检测结果是运行错误，不退化成单检测器裁定。整图pending_human保持未知，agreed_verdict的争议事实保持未知，不把删除后的交集当完整事实。

统计5000次场景bootstrap、seed20260907。主选择终点使用原问题的最高分集合均匀期望正确率；固定seed决胜、并列大小、基础率、请求匹配分并报，扩展评分仅描述。每个未知图标签y∈[0,1]，对池内加权选择差直接取线性函数上下界，再对配对场景bootstrap，不分别补两个模型的gold。能力分组使用事实yes/no、正计数/0（包含原题和预定负题）；主要条件prompt_on/off。未知事实可能改变分组，按每场景最不利±1差值及未知成员数给保守界，不能用只剩简单题的已知子集验收。至少150实例/50场景及10pp容差保持不变。

能力保留全部通过后才能判选择等效；点估计增加不能代替±5pp区间。报告实际95%区间半宽与未知补全范围。150池达不到精度就报不足。统计区间描述本次150个新组合式场景，不支持训练种子总体推断。D*、D_g均不在本次结论范围。

自动裁定后形成独立审查包：包括所有整图未知或题级事实未知的图片。原请求清楚标示，但事实录入应按实际物体/颜色/数量填写；也可保留无法确定。审查包不依据训练模型回答筛选。自动结果和人工补充分别保存，未裁定不强填为正确或错误。
