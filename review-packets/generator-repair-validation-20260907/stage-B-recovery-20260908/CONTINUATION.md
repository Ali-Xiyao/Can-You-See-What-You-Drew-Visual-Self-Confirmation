# 调度日志故障后的有界续接

2026-09-08。前次恢复在05:02:55 UTC完成batch-01并准备启动GPU1观察时，调度器event(kind, **value)与启动记录中的kind字段重名，触发TypeError。其失败清理已停止原观察和刚启动的GPU1工人。模型未报推理错误；这是调度代码缺陷。原stage-B-recovery-20260907目录，包括源码、失败、日志、全部数据和前次交接记录，保留原字节，不删除失败标记后续跑。

本目录是用户既有“处理并继续”授权下、已批准截止内的一次有界恢复。截止仍为2026-09-08 19:00 UTC（本地9月8日12:00），不加时、不扩样、不改变模型、解码设置、问题、gold、统计阈值或科学终点。原32步自然训练继续暂停，无新优化步。

原观察、检测、事实解析及主分析代码逐字节复制；rcommon依自身位置把逻辑输出改到新目录，OLD仍指最初stage-B。900张PNG仍引用最初绝对路径，不复制或修改图像。两批完成的双检测、crop、verification及完成标记直接复用，其余四批未开始的manifest与生成完成记录复用。新控制器验证后从已完成集合跳过两批。

已写的42,156条回答完整复用：base18,000和original18,000有完成记录；balanced6,156条、633个条件未完成。每份新文件先与前次源文件逐字节核验，新reuse-inventory记录新路径、旧来源、冻结前缀长度及哈希。新目录的可追加文件不以整文件哈希锁死；旧来源整体冻结、已复用前缀在观察续接及最终分析再次检查。balanced先重放最后一个完整条件（索引632），保存单独回放记录，语义完全一致才从633继续；回放不进108,000分母。GPU1没有可复用回答，从其原50场景分片起步。

此时已无旧控制器或工人运行，不再做进程交接。新控制器在原supervisor锁下同时启动固定GPU0/GPU1观察分片；各卡观察结束后按批ID和空闲状态领取剩余检测整批。完成事件和真实退出码/完整性标记均校验，失败只清理本次拥有的进程。event参数改为event_type，载荷中的kind继续表示工作类型。测试必须经过实际Controller.start与完成后调度、最终分析路径，不仅测试纯队列函数。

原次要分层登记和CODE_FREEZE保持不动。独立路由wrapper只把后续完整批/最终结果入口显式指向本恢复目录，并调用原分层、共享gold配对边界和人工工作量函数；输出新目录与路由哈希。不得改写原注册时间，不在主结果完整前查看四模型选择比较。

本次恢复不把前次失败回填为成功。未裁定和解析隔离仍未知，原解析隔离政策与全部题级人工队列保留；机器分析完成不代表人工裁定或D*、D_g成立。
