# Can You See What You Drew? Visual Self-Confirmation in Unified Multimodal Models

**Version 3.4 · 2026-09-01 · 目标会议 ICLR 2028**

---

## 摘要

统一多模态模型正在用自身的理解能力评价自己的生成结果，把「模型能从自己画的图中读回原意」当作图像正确的证据，并以此作为自训练奖励 [1–8]。本文检验这个奖励信号何时失效。

我们首先建立一把标尺：**分叉点 D\***——内部循环一致性仍在上升、而外部可验证正确率停止上升的最早训练步。但 D\* 的测量需要外部真值，而真正需要这个结论的从业者恰恰没有真值可用。因此本文的核心交付是一个**不依赖 ground-truth verifier 的预警信号**：把「某个 reward 的梯度」定义为该 reward 所选样本上 SFT loss 的梯度，在同一候选池上只改变选择准则，监控泄漏式自一致性与隔离盲观察两条更新方向的夹角。我们证明该夹角的转折点 **D_g 早于 D\***，给出可操作的早停规则。

随后我们把分叉归因到意图泄漏，并评估反馈原则 **Render–Forget–Observe（RFO）** 能否将其推后：强制自我评价穿过真实 RGB 瓶颈，移除对原始意图的访问，只接受盲观察者能稳定确认的原子事实。RFO 在本文中同时扮演两个角色——它既是修法，其隔离观察方向也正是构造上述检测器所需的对照。

核心主张：**自一致性只在模型仍允许自己被实际渲染结果「惊讶」的那段区间内值得信任，而这段区间的位置可以被无标签地测量。**

---

## 1. 一级主张

论文按三步推进，顺序即叙事顺序：

| | 主张 | 主要证据 |
|---|---|---|
| **①  预测** | 分叉可被**不依赖 ground-truth verifier** 地提前检测：`cos(g_naive, g_rfo)` 的转折点 D_g 早于 D\* | Lead = D\* − D_g > 0，配对 bootstrap CI 不跨 0 |
| **②  机理** | 分叉主要由意图泄漏驱动，共享感知盲点为次因 | 静态配对对照已测得泄漏本身（§3.4，0.654 → 0.336，n=667）；上下文消融对 D_g / D\* 的位移量分解；per-block 梯度余弦定位 |
| **③  延后** | RFO 把 D\* 推后或消除，位移量与②的归因同量级 | compute-matched 下 external correctness 提升，D\* 右移 |

D\* 是**标尺不是卖点**：「代理指标升、真指标不升」的曲线形状在 reward overoptimization 文献中已是常识 [18]。D\* 的唯一作用是给①的无标签信号提供一把用程序化真值刻好的尺子。

**这项工作值得做的原因**：现有 overoptimization 早停方法全部依赖 ground-truth queries [22]，无标签替代（loss plateau 等）已被证明与正常收敛不可区分 [23]；梯度层检测在文本/RLVR 上刚起步 [19–21]，在统一多模态模型上是空白。而文本 overoptimization 的 gold 是一个更大的 reward model，它自己就在漂 [18]；本文受控层的真值由构造过程给出（像素反事实），而主池的真值由两个冻结外部视觉模型加人工裁定确定，对 72 张盲审标签实测 F1 0.986 / 0.978。**想到测梯度分歧不难，但只有在这里才测得准。**

---

## 2. 关于「无标签」的准确表述

检测器需要构造两个方向：`g_naive`（泄漏式自一致性所选样本）与 `g_rfo`（隔离盲观察所选样本）。为避免循环论证，构造 `g_rfo` 的观察配置必须与训练所用的配置解耦（§7.3）。

因此本文**不主张零外部依赖**。准确表述是：

> 检测器不需要 ground-truth verifier、不需要任务标签、不需要人工标注。它需要一个冻结的异构观察者。这严格弱于需要 verifier——观察者只需回答原子视觉问题，不需要判定「是否满足目标」，也不需要比生成器更强。

**我们把这一点做成一个实验而不是一段辩解**：报告检测器在观察者比生成器**更弱**时是否仍然有效（§8.4）。若在弱观察者下 Lead 仍显著为正，主张①的适用范围比"需要 verifier"宽得多；若不成立，如实报告检测器对观察者质量的依赖曲线。

---

## 3. 已完成的前置工作与它决定的设计

v2.x 阶段已在本地完成三个 backbone 的就绪审计与一次工程闭环。完整数据见 `docs/EVIDENCE_LOG.md`。三个结果直接决定了 v3.0 的设计，写在这里而不是藏在 appendix：

### 3.1 backbone 已由实测选定；问题族的角色已改变

`showlab/show-o2-1.5B`（rev `07ec1658`，432px 原生）为主 backbone——**普通版，不是 HQ**。v4 三批语料全部由它生成，Gate A 在 v4 仪器上通过（p=0.2201）。它在 v2.2 的就绪门上曾判红，但那一代的金标准已被 §11 / §13 判定为缺陷所在；换掉金标准后同一模型在 Gate A 上是绿的，因此不再作为负对照。

Show-o2-7B 已审计：Oracle@4 提升到 0.92，但首候选整体覆盖率降到 0.767，固定种子覆盖摆动恶化到 16pt（**测于 HQ** 的对照值为 10pt）。**放大规模没有修复生成域可测量性**，7B 与 Show-o v1 作为冻结负对照。注意这组对比的基准是 HQ 而非本文主 backbone：它支持的结论是「放大到 7B 没用」，不是关于普通版与 7B 的直接比较。

**问题族不再由「哪一族能被 verifier 打分」决定。** v2.x 的族选表（existence/color/spatial 入主、count/binding 入 hard tier、absolute size 排除）是在几何 verifier 下测出的，而那个 verifier 的对象级 F1 只有 0.754（对 72 张盲审人工标签，§11）。用一把这样的尺子去决定「哪些族可测」，测出来的是尺子的分辨率，不是模型的能力边界。该表作为不可变记录保留在 `docs/EVIDENCE_LOG.md`，**不再用于任何设计决策**。

v4 把两件被 v2.x 混为一谈的事分开：

| | 作用 | 由谁定 |
|---|---|---|
| **问题族** | 向模型提的那一问，是实验条件 | 设计者。族之间要能比较，所以必须多于一个 |
| **图像正确性金标准** | 给像素打分，判定这张图画对没有 | 与族无关的**单一规则** |

金标准是：检测到的 (物体, 颜色) 多重集必须等于 spec 的多重集。v3 给每个族配一套自己的金标准，而每一套都是当时那个几何检测器还能分辨的**最小主张**——最小主张的问题在于，少画一点就能满足。实测漏画换来的「正确」判定占比：existence 15%、spatial 37.4%、binding 44.8%。更糟的是同一张图会因为恰好被问了哪一族而适用不同的标准。多重集相等这条规则里**没有任何否定式主张**，所以那个漏洞不是被补上，是从构造上不存在。

主问题族固定为五条，全部采用 2AFC（二选一）形式：existence / counting / binding / spatial / absence。选 2AFC 不是为了更难，是因为它和 yes/no 携带同样的 1 bit，却**不存在一个恒定的安全答案**；而且回答是单个 token，不需要第二个模型来判卷。

五条族**不是一个同质的集合**，这一点在 §5 展开：自然池只能给 absence 与 counting 稳定供给诊断性试次，binding 与 existence 稀薄，spatial 在自然池上恒为 0。这是生成器错误分布的性质，不是族的缺陷——所以处理办法是补一层由构造给出真值的刺激（Tier B），不是删族。

### 3.2 候选供给曾是瓶颈，但原因不是我们以为的那个

在几何刺激上用随机 K=4 采样构造候选池，**42 个 prompt 中只有 9 个池同时包含正确与错误的候选**（21%）。
v2.x 由此得出结论：在这个尺度上模型对某个 prompt 的正确性接近确定性，基于选择的自训练没有可利用的方差。
这个结论被提升为第一道门（Gate A），并驱动了后续一切围绕难度旋钮的调整。

**v4 的实测表明，这是几何刺激的性质，不是模型的性质。** 同一定义、同为自然采样的平衡池率：

| 刺激与金标准 | p | 平衡池率 | 若候选独立则应为 | 实测 / 独立预测 |
|---|---:|---:|---:|---:|
| 几何 + 每族原子 | 0.495 | 0.225 | 0.875 | **0.26** |
| 几何 + 场景匹配 | 0.253 | 0.220 | 0.685 | **0.32** |
| **可核验自然场景 + 场景匹配** | **0.188** | **0.513** | 0.564 | **0.91** |

「若候选独立则应为」是 `1 − p⁴ − (1−p)⁴`，即同一 prompt 的四个候选彼此独立时该有的平衡率。

关键不在 p 那一列，在最后一列。几何刺激下 p=0.495——正落在产出带正中——平衡率却只有 0.225，
是独立抽样的四分之一。**同一条 prompt 的四个候选高度相关：模型要么四张都画对，要么四张都画错。**
换成自然场景后这个比值升到 0.91，候选之间近乎独立，平衡率随之翻倍到 0.513。

这改变了 Gate A 的含义。它不是在问「p 是否落在带内」——p=0.495 时就已经落在带内而门是红的；
它在问**同一条 prompt 的候选之间是否有差异可供选择**。白底几何形状是一个近乎确定性的渲染任务，
所以答案是否；自然场景不是，所以答案是。这也解释了为什么此前所有围绕难度旋钮（物体数、视觉词汇、
金标准定义）的调整都没能救活 Gate A：它们调的是 p，而问题不在 p。

### 3.3 原设计的梯度噪声地板控制是错的

v2.x 用「同准则、**不相交**半批之间的梯度余弦」作为噪声地板。实测两次：`[-0.082, 0.322]`（n=9 池）与 `[0.135, 0.290]`（n=12 对）。

这个控制量与被测量不在同一尺度上：跨准则余弦是在**同一批 prompt**上比较（噪声大部分共享、配对抵消），半批余弦是在**不相交样本**上比较（噪声独立且加倍）。证据是同一次运行里 identical 与 naive/rfo 的配对余弦精确等于 1.000，而半批余弦只有 0.08。**用后者给前者划地板，是拿一把量错东西的尺子。**

v3.0 的噪声控制改为**在 prompt 层做配对 bootstrap**：对 prompt 集有放回重采样，在同一重采样集上重算 `cos(g_naive, g_rfo)`，直接给出该余弦自身的 CI。不相交半批余弦降级为独立报告的「单样本梯度信噪比」诊断量，不再充当门槛。

这项修正使主终点比 v2.x 设想的**更可测**，而不是更难测。

### 3.4 意图泄漏已被直接测到，训练还没开始

主张②说分叉由意图泄漏驱动。在 v4 的 `2+1` 半边上，这个机制已经作为一个**静态现象**被单独测到——不需要训练，也不需要 D\*。

228 条 spec × K=4 = 912 张图，其中 23 张里有不是任何东西的东西、按 §6.2 不出题，
余下 889 张给出 3623 道 2AFC 配对试次（`2+1` 与 `1+1+1` 两个半边独立跑完同一条流水线）。同一批图、同一批题、同一个模型，唯一差别是问题前面有没有那句「你是照这个描述画的：……」：

| | image-only | prompted | 配对 McNemar |
|---|---:|---:|---|
| 图与 spec 一致（2281 道） | 0.979 | **0.991** | 9 → 35 反向，χ²=14.2，p=1.6×10⁻⁴ |
| 图与 spec 冲突（667 道） | 0.654 | **0.336** | **221 → 9，χ²=194，p=5.3×10⁻⁴⁴** |

**两行方向相反，这是全部的分量所在。** 若那句描述只是干扰上下文、只是加长了 prompt，两行都该下降。实际上图与描述一致时它略有帮助，只有在像素与描述冲突时才崩塌：加上描述后 221 道从对变错，只有 9 道从错变对。自我确认率（诊断性试次中按被要求的场景而非画出的场景作答的比例，随机 = 0.5）**0.346 → 0.664**。

**两个半边各自成立，反向那一行也各自成立**（一致行 +0.009 与 +0.014，冲突行 −0.440 与 −0.214）。分族（合并）：counting 0.825 → 0.250（n=188）、binding 0.797 → 0.493（69）、absence 0.557 → 0.355（366）、existence 0.500 → 0.296（44）；四族全部显著同向。

**效应量在两个半边差一倍，原因是错法的种类而不是族。** 拆到 counting 的「要求几个 / 画了几个」：

| 要求 / 画出 | n | image-only | prompted |
|---|---:|---:|---:|
| 2 / 1（少画了） | 102 | 0.804 | **0.049** |
| 1 / 2（多画了） | 73 | 0.740 | 0.534 |

要求两个、只画了一个，把描述放回上下文，模型 95% 的时候答「两个」——**它在断言一个自己没画出来的物体存在**。反过来要求一个而画了两个，它只有 41% 的时候答「一个」。absence 族方向一致（那一族问的就是「被要求但没画出来的物体」）：缺席的东西比多余的东西更容易被 prompt 补上。`1+1+1` 的物体全是单例，`(2,1)` 这个格子在它里面不存在，效应量减半即由此而来。

这条不写得比证据硬：`(2,1)` 只出现在一个半边，所以这个比较里混着「哪个半边」；两边都有的 `(1,2)` 格子在 `2+1` 是 0.353（n=17）、在 `1+1+1` 是 0.589（n=56）。要干净地测「少画 vs 多画」，需要同一批 spec 同时供给两种错法——这是 v5 的语料设计要求，不是本文能下的结论。

程序性检查：prompted 条件下弃答 0 道，image-only 下 42 道——**那句描述不只改变答案，它还让模型每次都给出答案**。位置分布：prompted 选 A 0.500 / 0.529（gold A 0.497 / 0.510），无偏置；image-only 选 A 0.452 / 0.442，比 gold 低约 5 个点（≈3.5 σ），即无描述时略偏向选 B。这不构成对主结论的解释——主张是**同一道题在两个条件下的配对差**，而一个恒定的选项偏好在配对里两边都在——但它是真的，如实记在这里。

这条结果对设计的影响有三处：

- **§7 的执行顺序不变，但②不再完全押在训练上。** 意图泄漏在 step 0 就存在且量级巨大，训练过程中的分叉是它随选择压力被放大的结果，不是它的起点。论文里这条现象先立住，D\* 与 D_g 是关于它如何随训练演化的命题。
- **RFO 的 Forget 分量有了直接的效应量。** 「移除 Forget」在 Gate D-② 里要求效应量大于「移除 hard render」；本测量给出前者在静态条件下的上界：0.651 vs 0.201。
- **单一观察条件读不出机制。** 此前只跑了 image-only 一条。`observe_atoms` 按 RFO 隔离契约只喂图和问题，生成期的描述根本不在上下文里——在那种条件下，「照着 prompt 答而不是照着像素答」这个假设**没有可答的东西**，0.982 vs 0.651 那个落差只能读作「画错的图更难读」，是难度不是服从。矩阵里 Fresh-context prompted 那一行本来就写着，是漏跑了。

**边界**：单一模型、单一 seed 组，描述性结果，不是训练结论。金标准已全部落定——70 张争议图（占 7.7%）人工裁定完毕，两个半边都不再有 `pending`（裁定率 5.98% / 9.46%，均在锁定的 ≤10% 内）；裁定前后所有行的方向与显著性不变，最大的一处变化是冲突行 0.661 → 0.654。配对分析见 `scripts/v4_compare_conditions.py`——`v4_analyze.py` 一次只看一个条件，而主张在两个文件的逐试次差上。

---

## 4. 问题形式化

**Visual Self-Confirming Failure**：对某张生成图像，(a) 判别阶梯（§6.2）判定其检测到的 (物体, 颜色) 多重集不等于 spec 的多重集，且 (b) 生成模型自身在被问及该图像时给出与原始 prompt 一致、而与像素不一致的回答。

**意图泄漏（Intention Leakage）**：观察阶段仍可访问原始 prompt、同一会话历史、生成 latent / image tokens / KV cache，或来源标签。

**共享感知盲点（Shared Perceptual Blind Spot）**：即使在 fresh context + image-only 条件下，同模型或同家族观察者仍在相同视觉模式上共同失误。

### 4.1 指标

| 指标 | 定义 | 角色 |
|---|---|---|
| **GDA-free** | `cos(g_naive, g_rfo)`：泄漏式自一致性与隔离盲观察两条更新方向的夹角余弦 | **核心信号** |
| **D_g** | GDA-free 的分段回归断点，且该点余弦的配对 bootstrap CI 不含起点值 | 主终点分量 |
| **Lead = D\* − D_g** | 无标签信号相对指标分叉的领先步数 | **主终点** |
| GDA-gold | `cos(g_naive, g_gold)`：需要 verifier，不可部署 | 机制参照，①→②的桥梁 |
| **D\*** | internal cycle score 仍显著上升、external verifiable correctness 停止上升或下降的最早训练步；若预算内未出现则取 T_max | **次主终点 / 标尺** |
| ΔSCFR | 从训练起点到 D\*+Δ 步，错误确认率的变化 | 次主终点 |
| SCFR | 外部判错图像中模型仍判正确的比例 | 主诊断量 |
| POE | 已知像素反事实上，回答跟随原意而非修改后像素的比例 | 因果证据 |
| CSR | 收到原子反馈后目标错误被修复且正确属性未被破坏的比例 | 实用价值 |

**D\* 的兜底定义**（`T_max`）使 Lead 在「未观察到分叉」时依然有定义。这解决了 v2.x 的一个结构缺陷：主终点不能依赖一个可能不存在的量。

### 4.2 梯度量的等价定义

主线不使用可微 reward，因此不存在显式 reward gradient。采用：

> **某个 reward 的梯度 ≔ 该 reward 所选出的样本集上 SFT loss 的梯度。**

这是模型实际收到的参数更新方向，比 policy gradient 更直接、方差更低。关键设计：在**同一批 prompt、同一个候选池**上只改变选择准则，得到完美配对的三个梯度，唯一差异来源是「谁选的样本」。

| 记号 | 选择准则 | 需要 verifier |
|---|---|---|
| `g_gold` | 判别阶梯（§6.2） | 是 |
| `g_naive` | 朴素 cycle score | 否 |
| `g_rfo` | 隔离盲观察 | 否 |

### 4.3 测量协议

1. **固定 probe set**，全程不变，规模以**informative pool 数**计（不是 prompt 数）；
2. **在 LoRA 子空间计算**，同时报告 per-block 余弦以定位分歧层段；
3. **checkpoint 序列 EMA 平滑**后再做断点估计，窗口预先固定；
4. **报告范数比** `‖g_naive‖/‖g_rfo‖`，区分方向分歧与幅度分歧；
5. **配对 bootstrap** 给出每个 checkpoint 余弦的 CI（§3.3）。

---

## 5. 数据：SelfSight-Bench v4

v3 的刺激是白底上的彩色几何形状，由模板在固定的 shape × color × size 词表上组合而成。它给了精确的控制，代价是外部有效性：论文的主张是关于图像生成的，而刺激是形状测验——没有人会这样用一个生成模型。

v4 换成**可核验的自然场景**：日常物体放在一个表面上的照片。「可核验」是一条硬性定义，不是形容词——prompt 里的每一句话都必须能对着像素判定对错。所以 prompt 只说物体、颜色、数量，不说氛围、风格、光线、质感。一条 prompt 里出现了 spec 没有声明的东西，这条场景就被拒。

**场景由 LLM 直接撰写**（Qwen3-VL-8B-Instruct，`scripts/v4_author_corpus.py`），并且它必须把 prompt 和机器可读的 spec **一起**输出。这不是额外的一道工序，这就是输出格式：没有 spec 就没有难度旋钮、没有 `image_correct`、也没法构造「画出来的和要求的不一样」那类试次。让模型写语料的已知风险不靠机制去消除，而是去测量——`distribution_report` 报告实际落地的数量、颜色与物体分布，偏斜是被看见的，不是被假设掉的。

**规模旋钮由实测确定，不是选的。** 48 条 prompt × K=4 候选过 **Show-o2-1.5B-HQ**（512px），由 Qwen3-VL-8B 按多重集规则评分。这批标定测于 HQ 而非本文主 backbone，那 48 条 prompt 未进版本库、无法逐字节复现；旋钮迁到普通版后的实测后果单独报告（见下）：

| 场景总物体数 | n | p | C(p) at K=4 |
|---:|---:|---:|---:|
| 2 | 8 | 0.500 | 0.438 |
| 3 | 12 | 0.417 | 0.468 |
| 4 | 52 | 0.154 | 0.334 |
| 5 | 20 | 0.000 | 0 |
| ≥6 | 100 | 0.010 | ~0 |

这是**断崖不是斜坡**。选择上限 `C(p) = (1−(1−p)^K) − p` 要求 p 大致落在 [0.13, 0.70]；五个物体起，每个候选都是错的，选择实验没有任何可操作空间。**场景总物体数固定为 3。**

单条目的数量另算：要求 1 个时画对 75.4%，要求 2 个时 31.2%，要求 3 个时 21.3%。因此数量上限为 2——不是因为数错不有趣，而是到 3 已经几乎必错，试次不再有区分度。保留 2 恰恰是因为它错得够频繁，能供给「画出的数量 ≠ 要求的数量」的试次。

| 层 | 规模 | 作用 |
|---|---:|---|
| 主池 | 240 条 spec（`2+1` 与 `1+1+1` 各半） | 主测量场 |
| Tier B 像素反事实 | 计划 300 对，实得 306 对（83 翻转 + 56 改色 + 97 删除 + 70 sham），其中诊断性 153 对 | 因果证据；构造真值下的服从上界 |
| Tier B 确证语料 | 第二批 1368 张 → 159 对删除；第三批 3192 张 → 362 对删除（三批种子按构造两两不相交，只用于两次预注册检验） | 服从分界的两次独立复制，均未确证 |

`1+1+1` 供给 spatial（需要两个单例物体）与 binding，`2+1` 供给 counting 的真实错配试次。实际产出 `2+1` 117 条、`1+1+1` 111 条（LLM 撰写后过结构与颜色白名单检查，通过率约 16%）。

**Tier B 构造**：选外部验证为正确的生成图 → 对一个原子属性做最小可控编辑 → 保持原 prompt 不变 → 用 2AFC 询问实际事实。真值完全由构造过程给出，不依赖生成器恰好犯了那个错。

自然池上诊断性试次的实际供给决定了 Tier B 要补哪几族。912 张图、3623 道题里，2268 条 spec 条目层面的结果是：**颜色画对 1447、画错 85、整个物体没画出来 603、同名画成两色 133**。颜色几乎免费而物体常常缺席，所以 absence（366 道诊断）与 counting（199 道）够用，binding（69）与 existence（48）稀薄，spatial 为 0。

| 编辑 | 目标族 | 真值来源 | 产率 | 需要新权重 |
|---|---|---|---:|---|
| 水平翻转 | spatial | 镜像后所有 left/right 关系精确取反 | 1.00 | 否 |
| 改色 | binding | 编辑本身指定了新颜色 | 0.700（56/80） | 否 |
| 删除物体 | existence | 编辑本身 | 0.752（97/129） | 是（inpainting，已批准下载） |
| 空编辑（sham） | 无（痕迹对照） | 不适用，两成员金标准相同 | 0.909（70/77） | 同上 |

删除臂晚于前两种加入：它要 inpaint 被删区域，权重是单独批准后才下载的（`Carve/LaMa-ONNX`，见 §权重与许可）。**产率不是 1.00**——初稿把它记成 1.00 是错的，那假定掩码一定删得干净、填充一定看不出来，两条都不成立（见下文的面积上限）。它和改色一样要过双检测器的门，产率待测。

两种编辑测的**不是同一件事**，不能混报：

- **水平翻转是纯像素敏感度对照。** 本语料的 spec 不带空间关系，所以翻转后的图与原图**同样符合 prompt**——两个成员在描述面前地位相同，这里没有服从可测。它测的是更基本的一层：一个只会背 prompt 的模型对这一对会给出**相同**的答案，因为它的依据没变。答案随像素翻转的比例就是像素敏感度，是整个主张的地板。
- **改色是 binding 的诊断题，两个条件都跑。** spec 说「一个绿梨」，原图经外部验证确实画了绿梨，编辑把它改成红的。此时编辑后的图在**恰好一个原子事实**上与 prompt 冲突，其余全等。image-only 下正确答案是红，prompted 下若模型答绿，那就是服从，且这次「它本来画的是什么」没有任何解释空间——是我们改的。

**改色的验收门**：色相在检测框内旋转（保留 S 与 V，因而保留明暗与纹理），目标色取自该名词的 `PLAUSIBLE_COLORS` 且与场景里其他物体的颜色不重合；编辑完用**同样那两个检测器**重跑，只接受「目标物体读作新颜色 **且** 其余每个 (物体, 颜色) 一字不变」的那些。没通过的丢弃并计数——产率是要报告的数字，不是要藏的。用一把比主池松的尺子去验收 Tier B，等于用构造真值的名义放进一批没验过的图。

**实际产出 306 对，但诊断性的只有 153 对。** 计划里的 300 对指的是诊断性对数，所以这一栏没有达标，不能用总数掩过去：翻转的两个成员同样符合描述、sham 的两个成员金标准相同，两者都不诊断。底料是两个半边 912 张图里外部验证为正确且不待裁定的图（翻转与改色臂建于 209 张，删除臂在人工裁定补齐后建于 242 张），这个数由 p 决定，不由预算决定。验收产率：改色 0.700（56/80）、删除 0.752（97/129）、sham 0.909（70/77）。差额如实记，不靠放宽验收标准去凑数——补到 300 道诊断对要再生成一批候选图。

**两臂的结果，以及一个必须写进限制一节的构造性代价：**

| | image-only | prompted | n |
|---|---:|---:|---:|
| 改色 / 原图（与描述一致） | 1.000 | 1.000 | 56 |
| 改色 / 改色后（与描述冲突） | 1.000 | **0.964** | 56 |
| 翻转 / 两个成员 | 0.789 | 0.735 | 166 |

**改色臂上服从几乎不存在**（56 道翻转 2 道），而自然池的 binding 诊断题是 0.792 → 0.500。翻转臂的下降不显著（21 → 12，p=0.164）；它本来也测不到服从（spec 不带关系，两个成员在描述面前地位相同），它测的是答案是否跟着像素走：配对层面两个都对 0.590、只对一个 0.398，与「两次作答独立、单次 p=0.789」的预期 0.62 / 0.33 几乎一致——**翻转对没有查出隐藏的位置偏置或场景先验**，这是一个干净的阴性对照。

「Tier B 的题太清楚所以不服从」这个解释**是错的**：counting 的诊断题在 image-only 下也有 0.825，一点不含糊，prompted 下却掉到 0.249。像素清楚而服从极强的格子存在。留下的候选解释是错法不同（counting 要断言的是一个不在图里的物体，改色问的是在图里的物体）、可归因性（一只毫不含糊的绿杯子不是模型合理会犯的错，少画一个梨是）、以及编辑痕迹。**能分辨其中第一条的编辑是删除物体**，第三条则要一个空编辑对照，两者都已在建（见下）。

- **删除物体是 existence 的诊断题，并自带一个空编辑对照。** spec 说「一个红苹果」，原图确实画了，编辑把它整个移走，prompt 不动。此时 prompted 下若模型仍答「有」，那就是服从，且与改色臂的区别只有一条：变的是**有没有**，不是**是什么颜色**。这正是 §上文三条候选解释里的第一条。

**删除的编辑器与验收门**：掩码取「目标检测框（外扩 12%）减去周围物体各自的像素掩码」——整框会把邻居一起铲掉，物体自己的颜色掩码会留下梗、高光和接触阴影，两种失败面互补，所以都不是收紧参数能救的。白、灰、黑按饱和度与明度分割（无彩色没有色相），否则邻居退化成整框保护，在静物里会把删除掩码挖出一个洞。洞用 LaMa（`Carve/LaMa-ONNX`，CPU）补，**填充质量随洞的面积断崖式下降**：占画面 0.16 时干净，0.90 时整只盘子被换成一堵暗墙。因此在跑检测器**之前**卡两条：洞 ≤ 画面的 20%，且掩码覆盖目标框中心 ≥ 80%（后者拒绝「邻居坐在目标正中间」，例如躺在盘子上的勺子）。剩下的用**同样那两个检测器**验收：目标物体消失 **且** 其余每个 (物体, 颜色) 一字不变。填充器最常犯的错不是删不干净——掩码是整个框，删不掉是不可能的——而是**凭空补出一个新物体**（试点里一只红杯子被填成了一个米色的蛋），这正是验收门要挡的东西。**编辑器可以粗糙，因为门是严的**，负担落在与主池同一件仪器上。

**空编辑（sham）对照**：同一个填充器、同样大的面积、落在没有任何检测框的背景上，物体清单不动。没有它，删除对里两个成员的差异既可能是「少了个物体」，也可能是「一张过了网络、一张没过」——那正是第三条候选解释。sham 的两个成员金标准相同，因此它是对痕迹的对照，不计入诊断性试次。

**删除臂的问法**：自然池的 existence 是「两个名词里哪个在画面里」，这个形式撑不住删除对——无论配哪个第二名词，总有一个成员是两个都在或两个都不在。改用对被删物体的是非题「Is there a red apple in this picture? A. yes B. no」，两个成员问法完全一样、金标准相反。是非题会引入 acquiescence 偏置，措辞救不了；救得了的是这是**配对的、同图内的**比较——恒定的「倾向答 yes」在两个成员上同样偏移，做差时抵消，而 yes/no 哪个当 A 按 pair 随机，使该偏置无法与位置偏好对齐。

**删除臂的结果（97 对删除 + 70 对 sham，两个条件）**：

| | image-only | prompted | 配对 McNemar |
|---|---:|---:|---|
| 删除·原图（与描述一致） | 0.969 | 0.990 | 0 → 2，p=0.48 |
| **删除·编辑图**（与描述冲突） | 0.979 | **0.835** | **14 → 0，p=5.1×10⁻⁴** |
| sham·原图 | 0.957 | 0.971 | 0 → 1，p=1 |
| sham·编辑图 | 0.957 | 0.971 | 0 → 1，p=1 |

**第三条候选解释（编辑痕迹）被排除。** sham 用同一个填充器、同样大的洞、同一批图，两个成员都没有掉。删除对上那 14 次翻转因此不可能是「一张过了网络、一张没过」。这个排除不依赖对填充质量的信任，只依赖两臂用的是同一个填充器。

**第一条（错误类型）有支持。** 编辑器、验收门、问法风格全固定，只换被改的原子：改色掉 0.036（2 次翻转，56 对），删除掉 0.144（14 次翻转，97 对）。像素在两臂上同样清楚（image-only 都 ≥0.964），服从强弱仍差约四倍，差的是错法。

**第二条（可归因性）没有被分开，本文如实标注。** 删除同时是「不同的错法」和「更像模型自己会犯的错」，这一臂动了两个变量。自然池的 existence 诊断题（0.500 → 0.292，n=44）问的是同一件事而错来自模型自己，但那批图本身含糊（image-only 仅 0.500），绝对值与构造删除（0.979）不可比。分开这两条需要一个让错法固定、只让「错是谁犯的」变化的对照，本轮没有做。

另需注意量级：自然池 prompted 下 counting 0.256、absence 0.355、existence 0.292，而构造删除是 0.835。**被我们抠掉一个物体时模型基本照实说，被它自己漏画时才大面积改口**——这个落差的方向指向第二条解释，但如上所述本轮分不开。

**服从跟着什么走：不是「谁犯的错」——而「是不是我会犯的错」这条尚未确证。**

（术语：本文的**分叉点指 D\***，训练步上的量。本小节讲的是服从的**分界变量**，属于主张②的静态证据，不是 D\*。）

为分开可归因性，建了 26 对同 spec 配对（一个种子漏画、另一个种子画对后由我们删掉；同 prompt、同问句、同答案，只差缺失的作者）。结果与假设方向相反：自己漏画的掉 0.308，我们删的掉 0.385，掉幅之差 −0.077（16 对严格子集 −0.188）。**服从不跟着作者身份走。**

真正分开两者的是删除臂内部的拆分。那 97 对全是我们删的，作者恒定；变化的是该 (spec, 物体) 在**别的种子**上是否被生成器漏画过（被打分的图本身外部验证为正确，划分变量不使用它）：

| | n | image-only | prompted | 掉幅 | McNemar |
|---|---:|---:|---:|---:|---|
| 生成器漏画过该物体 | 43 | 0.977 | **0.744** | +0.233 | 10 → 0，p=0.0044 |
| 从没漏画过 | 54 | 0.981 | 0.907 | +0.074 | 4 → 0，p=0.134 |

image-only 两组相同，像素同样清楚，服从差三倍。三条替代解释均不成立：高服从组的洞更小（0.128 vs 0.151，编辑痕迹更少而服从更多）、严格门占比更高（0.74 vs 0.67）、只取两组共有的 9 个名词重算仍为 0.222 vs 0.067。

这条读法是：服从跟随模型对自身错误分布的先验，而非对错误作者的识别。它也解释了构造删除（掉 0.144）与自然 counting（掉 0.568）之间的落差——漏画是该生成器最常犯的错，自然诊断题几乎全部落在其高先验区。

**该结论标注为探索性，两次预注册的确证检验都没有通过。** 划分变量是在看到配对臂结果之后选定的；确证方案在生成新语料之前写定，检验代码的提交早于任何答案存在，随后在 1368 张全新种子的语料上执行：同一批 228 个 spec、K=6、种子与旧语料不相交，分层标注**只由旧语料计算**，流水线与全部门槛一律不动，不做人工裁定，主检验按图聚类后做 2×2 Fisher 精确检验。

| | n | image-only | prompted | 掉幅 |
|---|---:|---:|---:|---:|
| 生成器漏画过该物体 | 77 | 0.974 | 0.688 | +0.286 |
| 从没漏画过 | 62 | 1.000 | 0.839 | +0.161 |

**单侧 p=0.062（预先声明的阈值 0.05），双侧 p=0.106。** 方向与效应量都复现了（0.125 对原先的 0.159），三条辅助失败条件也都通过（两层 image-only 差 0.026、排除率差 0.004、方向一致），但主检验不显著，**按预注册降为「探索性观察，未获确证」，不改用次要分析或分层阈值抢救**。

第一次未通过的原因是功效而非效应消失：预注册按 0.159 定的每层约 74 对，实得 77 / 62 已贴着下限，而实测 0.125 要求每层约 136 对（单侧 80% 功效）。

**第二次预注册确证在第三批语料上执行，同样未通过，而这次不能归因于功效。** 方案按第二批的实测效应量重新定 n（每层 136 对），设计与第一次逐字相同，只改样本量与种子；第三批 3192 张新图（同一批 228 个 spec、K=14），实得每层 173 / 149，**两层都超过要求**。

| | n | image-only | prompted | 掉幅 |
|---|---:|---:|---:|---:|
| 生成器漏画过该物体 | 173 | 0.983 | 0.861 | +0.121 |
| 从没漏画过 | 149 | 0.987 | 0.886 | +0.101 |

**单侧 p=0.342，双侧 p=0.598。** 掉幅之差在三次取样上单调塌缩：0.159（探索性，划分变量是看过数据后选的）→ 0.125 → **0.020**。两条预先声明的次要分析也都不支持它：按 (spec, 名词) 格子聚类这个**更严**的单位上方向反了过来（0.111 vs 0.145，单侧 p=0.814），第二批与第三批的固定效应合并为 Mantel-Haenszel OR 1.537、单侧 p=0.074，**合并之后仍不显著**。三条辅助失败条件依旧干净通过（两层 image-only 差 0.004、排除率差 0.003、方向一致），所以这既不是仪器失灵，也不是差别性流失——两个子语料的未裁定率差了近两倍，分层排除率却是 0.091 vs 0.095。

因此本文陈述为：**该分界是一个探索性观察，两次预注册确证均未通过；在样本量充足的第三批上效应塌向零。** 不做第四批：连着两次方向对而阈值不到，说明效应比设计能力小，继续加语料是在买显著性。上一段那条读法（服从跟随模型对自身错误分布的先验）在文中保留为对 counting / 删除落差的一个**未获确证支持的解释**，不承担任何主张的证据重量。

**构造真值与难度不可兼得。** 双检测器要能确认「颜色变了、别的没变」，编辑就必须做得干脆，于是题必然容易（image-only 1.000）。只旋转一部分色相就能出难题，代价是「算黄还是算绿」不再由构造给出，得回去要人工标签。**同一道门既保证了标签，也保证了题目容易**——这是构造类刺激的结构性代价，不是本次实现的瑕疵。

**金标准来自图像，永远不来自 spec。** 问的是「你画了什么」，不是「别人要求你画什么」。要求两个梨而画了三个时，正确答案是「三」，照着 prompt 复述「二」的模型被判错。**这类试次是整个实验里信息量最大的一批**，在数据里以 `gold_source: image_differs_from_spec` 标出。

**但这类试次不会自己出现，必须构造出来。** 干扰项若与 spec 无关，读像素与背 prompt 就给同一个答案，该试次对主张的贡献为零。所以每一族的干扰项都取自 **spec 里没被满足的那条声明**：

| 族 | 诊断性试次的构造 | 干扰项来源 | 实测诊断率（912 张图） | 诊断题数 |
|---|---|---|---:|---:|
| absence | 要求蜡烛，一根没画 | 未画出的请求物体 | 0.431 | 366 |
| counting | 画了三个，要求两个 | 请求的数量 | 0.236 | 199 |
| binding | 要求绿梨，画成红梨 | 请求的颜色 | 0.088 | 69 |
| existence | 多画了瓶子、漏画了蜡烛 | 未画出的请求物体 | 0.056 | 48 |
| spatial | 要求杯在苹果左，画在右 | spec 关系 | 0 | 0（本语料不带关系） |

整体诊断性试次占比 **0.188**（3623 题中 682 道）。三点必须在论文里写明：

- **existence 只把「替换」算作诊断。** 若图里画出的选项本身也在 spec 里，背 prompt 的模型在两个选项中都看见自己的指令，它是掷硬币而非答错——把这种试次计入会用无法区分两个假设的题稀释主结果。
- **binding 的诊断率低是测量结果。** 逐条统计 spec 条目：颜色画对 722、画错 14、没画 168、同名画成两色 32。颜色几乎免费，与 §11 的失败分层一致（count → object → colour）。
- **spatial 在自然池上被降级，不再算作诊断族。** 让它在自然池里出诊断题必须在 spec 里写空间关系，而每多一条关系就是多给生成器一个必须做对的约束，直接压 p——p 是这条路线上最稀缺的资源，v4 的 Gate A 全靠它撑。因此自然池上的 spatial 只承担一件事：**测「能否读出自己画的布局」**。这恰恰是 image-only 条件下唯一不被 prompt 拉扯的能力基线，有单独的价值，但它不测服从。spatial 的诊断性试次全部由 Tier B 的水平翻转对提供（见下），那里真值由构造给出，产率 100%，且不花 p 的预算。

**观察条件矩阵**：

| 条件 | 原始 prompt | 同一会话 | 生成隐状态 | 状态 |
|---|---|---|---|---|
| Fresh-context prompted | 有 | 无 | 无 | 已跑（`2+1`），§3.4 |
| Fresh-context image-only | 无 | 无 | 无 | 已跑（`2+1`），§3.4 |
| Counterfactual prompt | 冲突 | 无 | 无 | 待跑，依赖 Tier B |
| 独立冻结观察者 | 无 | 无 | 无 | 已跑（核验器即是） |

前两行是配对的：同一批图同一批题，只有那句描述在与不在。**它们必须一起跑**，理由见 §3.4 末段。

v3 的矩阵里还有一行 `Same-session self-check`，标注为「生成隐状态可访问」。**该行已删除**，两个理由:它与 §6 的 Render 约束直接矛盾（那里明令禁止把 hidden state 或 KV cache 传给观察器），而且实现从来没有提供过它——`generate_images` 落盘 PNG，观察端从像素重新编码，中间没有任何状态被携带。留着它等于在论文里承诺一个不存在的条件。

---

## 6. 方法：Render–Forget–Observe

RFO 不改变基础架构，只重新设计反馈通道。

- **Render**：图像 tokens / latent 必须真正 decode 为 RGB、写入文件、从磁盘重新读取、断开原生成计算图。禁止把 hidden state、semantic token 或 KV cache 传给观察器。
- **Forget**：观察器在独立进程与独立会话中运行；不提供原始 prompt、生成历史、来源标签；不复用 cache；不使用「是否满足这段 prompt」式诱导提问。
- **Observe**：观察器参数冻结，生成器可更新（避免两者共同演化出私有协议）。`RFO-Self` = 同基础模型的冻结 step-0 副本，image-only fresh context。**「冻结副本」不是修辞**：三个对照臂在代码里的区别就是哪一组参数接收更新（`analysis/figure1.py` 的 `arm`），生成端与观察端之间不存在任何被携带的状态。
- **Compare**：目标事实来自**检测到的像素**，不来自 scene graph。整条流水线里只有一步交给模型判断——读像素；其下游（比多重集、查金标准、把 A/B 判对错）全部是确定性算术，并且保持如此。把不确定性集中在一个地方是有意的，因为那个地方才能被审计。比较器不输出模糊总分，只输出明确差异（要求：两个绿梨 / 观察：三个 / 错误类型：数量 / 其余属性：正确）。
- **Repair**：三种部署形态——`RFO-Rerank`（免训练，按事实一致性择优）、`RFO-Repair`（推理时，最小修正指令）、`RFO-PostTrain`（主线，rejection-sampling SFT）。主结果不含 RL。

**训练原则**：只奖励能够通过硬渲染、上下文隔离与盲观察之后仍被确认的事实，而不是奖励模型对原始意图的内部恢复。

### 6.1 候选库搜索（v3.0 新增，Gate A 的实现）

随机 K=4 只有 21% 的池是 informative（§3.2）。v3.0 改为有界候选库搜索：

1. 对每个 prompt，用与梯度/dropout **不相交**的搜索种子生成 M=16 个候选（上限预先固定）；
2. 用 §6.2 的判别阶梯给每个候选打分；
3. 从库中取出 K=4 呈现给全部选择准则：尽可能 2 个 verifier-正确 + 2 个 verifier-错误；
4. 无法凑齐平衡池的 prompt 从**全部 arm 中对称移除**；
5. 同一个 K=4 池原封不动地交给 naive / rfo / gold 三个选择器。

### 6.2 判别器：两个模型，一条升级裁定阶梯

图像正确性由**冻结的外部视觉模型**判定，不再由程序化几何检测器判定。对 72 张盲审人工标注图像的实测（对象级）：

| 判别器 | precision | recall | F1 | 整图物体清单全对 |
|---|---:|---:|---:|---:|
| 几何检测器（v3） | 0.750 | 0.757 | 0.754 | 0.486 |
| Qwen3-VL-4B-Instruct | 0.964 | 0.922 | 0.943 | 0.806 |
| **Qwen3-VL-8B-Instruct** | 0.981 | 0.990 | **0.986** | **0.944** |
| **InternVL3.5-8B** | 0.985 | 0.971 | 0.978 | 0.903 |

4B 被排除的理由不是分数低，是**它的错误方向恰好是本实验的混淆项**：它的失败集中在漏检物体（12 例，8B 为 1 例），而「判别器漏看了」与「模型少画了」在数据里不可区分。8B 主判别器 + InternVL3.5-8B 次判别器；两者的错误方向相反（Qwen 偏向虚构，InternVL 偏向漏检），共同错误只有 1 例，适合互为对照。

**一致性是标记，不是筛选条件。** 三级阶梯，每一张图都要有结论：

1. 两者一致 → 采纳（实测约 89%）；
2. 不一致 → 只把有争议的那个物体裁切放大后重问（实测争议全部是单个物体的多报/漏报或类别之争，裁切提供的是**新像素**，而第三个模型只是又一张相关的选票）；
3. 仍不一致 → 人工裁定（实测 `2+1` 6.0%、`1+1+1` 9.5%）。裁定界面是三选一：
   采纳主判别器的列表 / 采纳次判别器的列表 / **`X`：图里有东西不是任何东西**。
   不显示 prompt 与 spec；哪个检测器排在上面按图随机。

**第三个选项是一个结论，不是「跳过」。** 生成器有时把两个物体融成一个体、或画出没有稳定形状的
东西，两个检测器于是各自给出自信而不同的名词——这是图会走到人眼前的一大来源——而评审员同样
没法诚实地写出一个名词。这类图记为 `unnameable`（`src/selfsight/v4/spec.py`），**算生成失败，
计入 p 的分母与「错」的一侧**：不论那东西是什么，它都不是 spec 要的东西。曾经给「整张图读不了」
另设过一个词让它退出 p 的分母，那是错的，已删除——p 问的是生成有没有画出要求的东西，读不出来
的图显然没有，把它从分母里拿掉等于把生成器最差的输出从它自己的分数里删掉。人读不出来是关于
这次生成的证据，不是仪器的空白；真正没有判定的只有文件损坏或根本没渲染出来。它唯一不做的事
是出题：「你画了几个梨」在候选是半个梨时没有正确答案，所以这类图供给 p 与平衡池比率（判定在
那里有定义），不供给任何 trial（在那里没有定义）。该比率单独报一行，因为「画错了物体」与
「画出了非物体」是两种失败，而 p 把它们加成了一个数。

**为什么不改成「只用一致的子集跑主结果」**：两个模型会吵起来的图，正是遮挡、模糊、含混的那些。悄悄丢掉它们会把语料重采样到简单场景一侧、抬高 p，并且——由于杂乱程度在各族之间分布不均——让族与族之间比较的是不同的总体。而且这些难图恰恰是选择实验唯一有操作空间的地方。判别过程如何逐图收敛，作为一行覆盖率指标写进论文，这是方法的强项而不是免责声明。

**定位说明**：候选库是**测量仪器的标定刺激**，不是训练分布。因此——

- **梯度探针**使用库平衡池（保证 GDA 有动态范围）；
- **训练与外部正确率曲线**使用模型的自然 K=4 采样（保证 D\* 反映真实自训练）。

两者必须在论文中显式区分，并报告库平衡池上估出的 D_g 是否预测了自然池上测出的 D\*。**这个跨分布的验证本身就是①最强的证据**：如果一个用标定刺激测出的信号能预警自然训练中的退化，那它就是一个真正的仪器。

---

## 7. 实验计划

### 7.1 实验组

| 组别 | 反馈方式 | 看到 prompt | hard render | 冻结观察器 |
|---|---|---|---|---|
| Base | 无自反馈 | – | – | – |
| **Naive Cycle** | prompt→image→恢复 prompt | 间接 | 是 | 否 |
| **RFO-Gold** | 判别阶梯选择（§6.2） | 否 | 是 | 是（真值） |
| **RFO-Self** | 冻结 step-0 盲原子观察 | 否 | 是 | 是 |
| Fresh Blind | fresh context image-only | 否 | 是 | 是 |
| Same-Context Judge | 同会话总分判断 | 是 | 否 | 否 |

**Base / Fresh Blind / Same-Context Judge 为推理时或消融组，不占主训练预算。**

### 7.2 机制优先的执行顺序

先跑 **RFO-Gold vs Naive**，而不是 RFO-Self vs Naive。理由：RFO-Gold 是**阳性对照**——它用完美选择器、相同候选池、相同更新预算。若一个完美选择器都无法在这个 setup 里产生可测的训练优势，那么限制因素不是观察者质量，而是修正目标 / 样本权重 / 梯度探针，此时增加算力或换 backbone 都是浪费。

```
Pass 1   A800-0: Naive        ‖  A800-1: RFO-Gold      → Gate C
Pass 2   A800-0: RFO-Self     ‖  A800-1: checkpoint 评测 → Gate D
```

### 7.3 检测器与训练器的解耦（硬性要求）

| 用途 | 使用的隔离观察配置 |
|---|---|
| 训练（RFO-PostTrain 样本选择） | `RFO-Self`：同基础模型冻结 step-0 副本，image-only fresh context |
| **检测（构造 `g_rfo`）** | **冻结异构 VLM（Qwen2-VL-2B），全程不参与任何训练样本选择** |

若不解耦，「检测器报警 → 换 RFO 有效」构成同义反复。此外，非平凡内容不在「两个梯度不同」——它们在任何时刻都不同——而在**时间结构**：夹角在训练早期落在其配对 CI 内，随后单调扩大，且这一扩大发生在外部正确率退化**之前**。这个时序命题无法由定义差异推出，并且可以真实失败（Gate D）。

### 7.4 实验清单

| 编号 | 内容 | 硬件 |
|---|---|---|
| **E0** | 候选库搜索与 informative pool 供给统计（Gate A） | 3090 ×2 |
| **E1** | 五种观察条件下的 SCFR / POE / PSFP；Tier B 因果证据 | 3090 ×2 |
| **E2** | 主训练曲线 + 梯度分歧监控（**第一主实验**） | A800 ×2 |
| **E3** | compute-matched 价值验证：rerank / repair / posttrain vs 全部 baseline | A800 ×2 |
| **E4** | 机制消融：去 hard render、保留 prompt、观察器共同更新、原子问题→整体打分 | A800 ×2 |
| **E5** | 理解能力保持 + Tier C 方向抽查 | 3090 ×2 |

**E2 每个 checkpoint 记录**：internal cycle score / external verifiable correctness（→D\*）/ SCFR / GDA-free 及范数比及 per-block 分解（→D_g）/ GDA-gold（机制上界）/ 配对 bootstrap CI / 观察答案熵（作为竞争基线信号，须被 GDA-free 击败）。

### 7.5 基线

无自反馈 Base；same-context self-judge；fresh-context self-judge；naive cycle consistency（主对手）；冻结异构 VLM judge；CLIP / VQAScore rerank；random best-of-K；UniRL / UniCorn / ASG 风格自生成反馈的最接近可复现版本 [5–7]。全部在**相同 K 与相同算力**下比较。

### 7.6 消融

| 消融项 | 验证的问题 | 预期方向 |
|---|---|---|
| 去掉 hard render | 渲染屏障是否必要 | D\* 提前 |
| 保留 prompt（不 Forget） | 意图泄漏的贡献 | SCFR 上升、D\* 提前 |
| 观察器与生成器共同更新 | 冻结观察器是否必要 | internal 升更快、external 不升 |
| 原子问题 → 整体打分 | 反馈粒度 | CSR 下降、collateral damage 上升 |
| GDA-free vs 熵类无标签信号 | 梯度信号是否优于更简单替代 | GDA-free 的 Lead 更大 |
| GDA 全参数 vs LoRA 子空间 | 投影是否必要 | 全参数余弦退化到无信息量 |
| 检测器 = 训练器 vs 解耦 | 循环论证的影响量 | 不解耦时 Lead 被系统性高估 |
| 弱观察者构造 `g_rfo` | §2 的适用范围 | Lead 随观察者能力单调变化 |
| 库平衡池 vs 自然池 | §6.1 的跨分布有效性 | D_g 一致，D\* 不同 |

---

## 8. 统计方案

**主线先跑单主 seed**（`20260901`）。主 seed 的作用是判断曲线形状是否分离、仪器是否工作；**不从单 seed 做显著性主张**。只有主 seed 显示可分离形状后，才投入 seed 2/3 做区间估计。

- 主比较以 prompt 为配对单位，报告 paired bootstrap 95% CI；
- 二元错误使用 McNemar 检验；
- **D\* 与 D_g 的估计**：分段线性回归 + profile likelihood 断点，估计流程与超参在实验开始前写入代码并冻结；
- **Lead 的推断**：直接对 `(D* − D_g)` 做配对 bootstrap，不分别给 CI 再目测重叠（那会严重低估功效）；
- **GDA 余弦的所有报告必须伴随同 checkpoint 的配对 bootstrap CI**，不得单独出现；
- **SCFR 必须伴随观察者准确率与原始分母**一同报告；
- 所有曲线图绘出单 seed 而非只画均值；
- 不把 1 个百分点以内的无方差差异写成确定提升；
- 主终点 `Lead`、次主终点 `D*` 与 `ΔSCFR` 在实验前指定，结果出来后不得替换。

**多 seed 的触发条件**：主 seed 上 Gate C 与 Gate D 均为绿，且 Naive 与 RFO 曲线在 external correctness 上的差距超过 checkpoint 内方差。否则先诊断，不加 seed。

---

## 9. Go / No-Go

四道门，每道门只在有真实失败可能时才存在。

### Gate A — 候选供给（第一道门）

有界候选库搜索（M ≤ 16）能否在主问题族上稳定产出平衡的 K=4 池。

- 平衡池产出率 ≥ 40%；
- 梯度探针可凑齐 ≥ 128 个 informative pool；
- 搜索种子与梯度/dropout 种子不相交，库上限在看到任何梯度输出之前固定。

**该门在 v3 上判红，但那次判定用的是已作废的仪器**（几何检测器 + 每族一套金标准），因此不构成 v4 的结论。v4 的难度旋钮已按 §5 的实测定在总物体数 3，对应 p ≈ 0.42、C(p) ≈ 0.47，落在产出带内；Gate A 用新仪器重测。

**不过：** 基于选择的自训练在此 backbone 上没有可利用的方差。停止，产出物是能力地板报告，不是主线论文。

### Gate B — 仪器有效

- 判别器 precision 由盲审人工标签验证 ≥ 0.95（**已达成，n=72 盲审**：Qwen3-VL-8B 0.981 / InternVL3.5-8B 0.985；注意 v2.x 报告的「各族 1.00」是几何检测器在 n=28 上的数字，该检测器实测 F1 0.754，那一行不再有效）；
- 判别阶梯覆盖每一张图，**人工裁定率必须报告且 ≤ 10%**，且**不得**用一致性筛掉任何图像；

  这条取代了原先「判别器须先过 120 图参考观察审计」的判据。后者在 v4 下无法执行：参考观察审计是对程序渲染的参考图做的，而 v4 的刺激是生成的自然场景，不存在参考图。改用人工裁定率有一个实质上的好处而不只是权宜——阶梯把**每一张两个模型意见不合的图**送到人眼前，覆盖的正是真正难判的那些；而随机抽 120 张做一次性审计，其中绝大多数是两个模型毫无异议的图，同样的人工工时买到的保障更少。裁定率本身也是一个有信息量的量：它超过 10% 就说明语料的杂乱程度已经超出两个 8B 判别器的分辨能力，那是应当在论文里报告的事实，不是需要掩盖的缺口。
- identical-selection 梯度余弦 ≥ 0.999（已达成：1.000）；
- **GDA-free 的配对 bootstrap CI 宽度 ≤ 0.10**（v3.0 新控制量，见 §3.3）；
- LoRA 子空间投影下余弦不退化到 0 附近。

**不过：** 增大 probe 的 informative pool 数直到 CI 收窄；若增大到预算上限仍不收窄，主张①退回熵类替代信号，如实报告梯度信号在此规模下不可测。

### Gate C — 机制阳性对照（**生死实验**）

主 seed 上，RFO-Gold 在相同候选池与相同更新预算下：

- external verifiable correctness 高于 Naive，差距超过 checkpoint 内方差；
- 选择一致率与 Naive 显著不同（已知：0.444）。

**不过：** 完美选择器都无法产生训练优势 ⇒ 限制因素是修正目标/样本权重/梯度探针，**不是**观察者质量、模型规模或算力。停止扩展，回到目标函数诊断。这是项目的**真实失败条件**。

### Gate D — 主张成立

主 seed 上：

- **①** `Lead = D* − D_g > 0`，且 D_g 处 GDA-free 的配对 CI 不含起点值；早期 GDA-free 落在起点 CI 内（分歧是后天出现的）；GDA-free 与 GDA-gold 的 checkpoint 级 Spearman ρ ≥ 0.7；GDA-free 的 Lead 优于熵类基线；
- **②** 至少一项上下文消融显著提前 D\* 与 D_g，且「移除 Forget」的效应量大于「移除 hard render」；
- **③** compute-matched 下 RFO-Self 的 D\* 晚于 Naive（或预算内不出现），external correctness 提升 ≥ 3 pt。

**不过：** ①不成立 ⇒ 如实报告梯度信号在本设置下无预警价值，论文重心回落到②③。②不成立 ⇒ RFO 失去理论动机，§6 改写为纯经验方法，限制一节明写「我们能预警但不能解释」。

---

## 10. 硬件与预算

| | 3090 × 2（本地 Windows，24GB，无 NVLink） | A800 × 2（服务器 Linux，80GB） |
|---|---|---|
| 角色 | 数据/候选库生成、冻结观察者服务、Gate A/B 筛查、E1、E5、图表 QA | E2 主训练曲线、E3、E4 |
| 约束 | 两卡是独立 worker，不是 48GB 池；单模型不跨卡切分 | 两卡并行跑两个 arm，不做分布式 |

LoRA backward/resume 实测峰值约 **10.15 GiB**（测于 HQ；普通版与 HQ 的 LoRA 目标清单逐条相同——同样 196 个模块、`selection_digest` 同为 `033751f8…`，故该上界同样适用），两种卡都不受显存约束。

| 阶段 | 内容 | 估算 GPU-hours |
|---|---|---:|
| P0 | 候选库搜索 + Gate A/B（3090） | 40–70 |
| P1 | Tier A/B 构建 + E1（3090） | 60–100 |
| P2 | **E2 主 seed：Naive ‖ RFO-Gold，然后 RFO-Self**（A800） | 200–320 |
| P3 | E3 compute-matched + 全部 baseline（A800） | 90–150 |
| P4 | E4 机制消融（**每项消融 = 一次完整训练**） | 250–400 |
| P5 | E5 + Tier C + 图表 + 补实验 | 60–100 |
| | **主 seed 完成度合计** | **700–1140** |
| | 追加 seed 2/3（仅 P2，条件触发） | +400–640 |

**关于预算的诚实说明**：v2.x 报的 450–750 小时把 E4 严重低估了——「报告消融对 D\* 的影响」意味着**每项消融一次完整训练**，不是一次评测。上表按完整训练计。P4 是最大的单项，可在①②③确立后按需裁剪消融项数。

**MVP（先做这一段）**：P0 + P1 + E2 短程（60–100）= **160–270 GPU-hours**。它回答三个决定性问题：候选库能不能供上（Gate A）、GDA 的配对 CI 够不够窄（Gate B）、以及 **RFO-Gold 在短程内是否已经拉开 external correctness**（Gate C 预告）。**没有看到这三个结果之前不投入剩余算力。**

存储：生成图像与公共视图 100–200GB，checkpoints/日志 150–300GB，模型权重 100–150GB，建议 400–650GB。

---

## 11. 时间线

| 时间 | 里程碑 | 门 |
|---|---|---|
| 2026.09 | 候选库搜索实现；Gate A 供给统计；梯度仪器配对 CI 改造 | **A / B** |
| 2026.10 | Tier A/B v3 冻结；E1 五种观察条件 | – |
| 2026.11 | A800 环境与迁移 canary；**E2 主 seed Pass 1（Naive ‖ RFO-Gold）** | **C** |
| 2026.12 | E2 主 seed Pass 2（RFO-Self）；D\*/D_g/Lead 估计与 Figure 1 | **D** |
| 2027.01–02 | 条件触发：seed 2/3；E3 compute-matched | – |
| 2027.03–05 | E4 机制消融；per-block 定位 | – |
| 2027.06–07 | E5、Tier C、人类评测、failure cases | – |
| 2027.08–09 | 写作、内部审稿、补实验、复现包 | – |

---

## 12. 关键图表

| 图 | 内容 | 来源 |
|---|---|---|
| **Figure 1** | 预警图：internal / external / GDA-free 三条曲线随训练步叠加；两条竖线标出 D_g 与 D\*，中间阴影为 Lead；下方平行轴为 SCFR | E2 |
| Figure 1b | 梯度仪器有效性：配对 bootstrap CI 带 + GDA-free/GDA-gold 双曲线 + per-block 余弦热力图 | E2 |
| Figure 2 | 判别器审计表（对 n=72 盲审人工标签）+ 难度旋钮曲线（p 与 C(p) 对场景总物体数） | §5, §6.2 |
| Figure 3 | RFO 流程示意 | – |
| Figure 4 | 像素覆盖因果图：固定像素改 prompt vs 固定 prompt 改像素 | E1 |
| Figure 5 | 候选库供给：自然池 vs 库平衡池的 informative 率与 D\* / D_g | E0 + E2 |

**Figure 1 必须来自真实短程数据。** MVP 阶段就要能画出雏形；如果短程中两条曲线完全重叠且无分开迹象，立即进入 Gate C 诊断，而不是先把 200 GPU-hours 跑完。

---

## 13. 计划贡献

最终论文只在实验支持时主张：

1. **【① 预测】** 一个不需要 ground-truth verifier 的分叉预警信号 GDA-free，证明它领先于外部指标退化（Lead > 0），可直接作为自训练系统的早停规则，并给出它对观察者质量的依赖曲线；
2. **【② 机理】** 把分叉归因到意图泄漏为主、共享感知盲点为辅，给出可加分解与层级定位；
3. **【③ 延后】** Render–Forget–Observe 原则与后训练流程，展示它将分叉点推后或消除；
4. 统一多模态模型中自一致性奖励**分叉点**的可重复测量（作为①的标尺）；
5. 一套受控自反馈测量协议：能力地板表、去偏提问库、配对梯度 CI、候选库供给统计；
6. SelfSight-Bench v4，可核验自然场景 + 与问题族无关的单一金标准，用像素反事实与跨观察者控制区分反馈失败来源。

---

## 14. 预期审稿问题

**Q1：你怎么知道不是模型太小看不清？**
判别器 precision 对 n=72 盲审人工标签为 0.981（Qwen3-VL-8B）与 0.985（InternVL3.5-8B），且主结论是**同一个模型在训练过程中的时间序列变化**——能力地板在各 checkpoint 上是公共因子，无法解释两条曲线的**反向**运动。五个问题族全部进主分析,不设 hard tier:族是实验条件,不是按「哪族打得动分」筛出来的名单。

**Q2：GDA-free 既由 RFO 构造、RFO 又是你的修法，这不是循环论证吗？**
检测器与训练器在设计上强制解耦（§7.3）。且非平凡内容不在「两个梯度不同」——它们在任何时刻都不同——而在**时间结构**：夹角早期落在其配对 CI 内，随后单调扩大，且扩大发生在外部正确率退化**之前**。这个时序命题无法由定义差异推出，可以真实失败（Gate D）。

**Q3：候选库搜索是不是在制造不存在的方差？**
是的，而且我们明说了。候选库是**仪器的标定刺激**，只用于梯度探针；训练与 external correctness 曲线用自然采样池。跨分布验证（库上估的 D_g 是否预测自然池上的 D\*）本身是①的核心证据（§6.1）。

**Q4：只训了一个 1.5B 模型。**
backbone 由预注册的就绪审计选定而非事先指定，7B 已审计且更差（§3.1）。这是计算约束下的选择，不对更大规模做定量外推。

**Q5：RFO 是不是只是 rejection sampling 加集成？**
RFO-Self 只用同模型冻结副本，不引入额外能力。贡献在于反馈信息的**约束**：hard render、blind observation、原子比较。全部 baseline 在相同 K 与相同算力下比较。

**Q6：分叉会不会只是过拟合？**
外部正确率在 held-out prompts 上测量，模板与属性组合严格隔离。若只是普通过拟合，internal cycle score 应当同时在 held-out 上下降；我们预期它继续上升。这两种情形在图上可直接区分。

---

## 15. 参考文献

[1] Xie et al. *Show-o: One Single Transformer to Unify Multimodal Understanding and Generation*. arXiv:2408.12528, 2024/2025.
[2] Wu et al. *Janus: Decoupling Visual Encoding for Unified Multimodal Understanding and Generation*. arXiv:2410.13848, 2024.
[3] Chen et al. *Janus-Pro: Unified Multimodal Understanding and Generation with Data and Model Scaling*. arXiv:2501.17811, 2025.
[4] Deng et al. *Emerging Properties in Unified Multimodal Pretraining (BAGEL)*. arXiv:2505.14683, 2025.
[5] Mao et al. *UniRL: Self-Improving Unified Multimodal Models via Supervised and Reinforcement Learning*. arXiv:2505.23380, 2025.
[6] Han et al. *UniCorn: Towards Self-Improving Unified Multimodal Models through Self-Generated Supervision*. arXiv:2601.03193, 2026.
[7] Thawkar et al. *Ask, Solve, Generate: Self-Evolving Unified Multimodal Understanding and Generation via Self-Consistency Rewards*. arXiv:2606.27376, 2026.
[8] Shan et al. *CyCLeGen: Cycle-Consistent Layout Prediction and Image Generation in Vision Foundation Models*. arXiv:2603.14957, 2026.
[9] Mollah et al. *The Telephone Game: Evaluating Semantic Drift in Unified Models*. arXiv:2509.04438, 2025.
[10] Chu et al. *CycleGAN, a Master of Steganography*. arXiv:1712.02950, 2017.
[11] Spiliopoulou et al. *Play Favorites: A Statistical Method to Measure Self-Bias in LLM-as-a-Judge*. arXiv:2508.06709, 2025.
[12] Wataoka et al. *Self-Preference Bias in LLM-as-a-Judge*. arXiv:2410.21819, 2024.
[13] Ghosh et al. *GenEval: An Object-Focused Framework for Evaluating Text-to-Image Alignment*. arXiv:2310.11513, 2023.
[14] Xie et al. *MME-Unify: A Comprehensive Benchmark for Unified Multimodal Understanding and Generation Models*. arXiv:2504.03641, 2025.
[16] Amodei et al. *Concrete Problems in AI Safety*. arXiv:1606.06565, 2016.
[17] Skalse et al. *Defining and Characterizing Reward Hacking*. arXiv:2209.13085, 2022.
[18] Gao et al. *Scaling Laws for Reward Model Overoptimization*. arXiv:2210.10760, 2022.
[19] Ackermann et al. *Gradient Regularization Prevents Reward Hacking in RLHF and Verifiable Rewards*. arXiv:2602.18037, 2026.
[20] *Detecting and Suppressing Reward Hacking with Gradient Fingerprints (GRIFT)*. arXiv:2604.16242, 2026.
[21] *Proxy Reward Internalization and Mechanistic Exploitation (PRIME)*. arXiv:2606.09711, 2026.
[22] Moskovitz et al. *Confronting Reward Model Overoptimization with Constrained RLHF*. arXiv:2310.04373, 2023.
[23] *EvalStop: Using World Feedback to Detect and Correct Reward Overoptimization*. arXiv:2606.04145, 2026.

> **注：** [6][7][8][15][19][20][21][23] 的 arXiv 编号为 2026 年条目，投稿前需逐条核验编号、作者与标题。[15] EvoTok 在 v3.0 正文中已无引用点，若不恢复引用则删除。
