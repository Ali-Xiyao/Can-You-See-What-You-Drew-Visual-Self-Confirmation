# STATUS

**唯一入口。** 想知道「现在什么成立」只读这一页；要证据细节再去 `docs/EVIDENCE_LOG.md` 对应节。

**2026-09-08 03:29 PDT（10:29 UTC）：用户明确要求暂停。** 已停止R2控制器及其两卡检测工人，
定时跟进 `automation` 已设为PAUSED，不自动恢复。108000条观察回答全部完成、8份模型完成记录及
回答SHA均核对通过；检测batch-00至03共600图已完成，batch-04/05的部分输出与全部日志原样保留。
两张3090已释放计算任务；本轮主分析及第四批的次要汇总尚未执行，D*、D_g仍未确认。
暂停记录与文件清单见 `review-packets/generator-repair-validation-20260907/stage-B-recovery-20260908/`
下的 `user-pause-request.json`、`controller-paused.json`、`pause-inventory.json`。以后恢复须由用户另行授权，
不得按旧心跳继续或把暂停当成完成。

最后更新：2026-09-06（America/Los_Angeles） · 分支 `codex/decoupling-pilot-20260906` · backbone `showlab/show-o2-1.5B` rev `07ec1658`（432px）

> **2026-09-03 更正：主 backbone 是普通版 1.5B，不是 HQ。** 全部 v4 语料实际由普通版生成
> （图为 432×432，HQ 是 512px）。此前本页多处写 HQ 是错的。v2.x/v3 时代的就绪审计与难度标定
> 确实跑在 HQ 上，那些记录原样保留并逐条注明来源。来龙去脉见 §28。

---

## 正在跑

**最新：A阶段已完成并独立核验，B阶段已启动（2026-09-07 06:15:54 UTC，§43.24A）。**
A于05:25:40 UTC完成：128张配对图、8次base重复、256个配对损失实例、15360条回答及512个cycle。
三组生成输出及固定训练目标均有可测响应；不代表图像质量提高。去回放组离散回答漂移很小，
连续cycle仍有漂移，不能当作冻结观察器。报告见 `review-packets/generator-repair-validation-20260907/STAGE-A-RESULTS.md`。
用户已明确要求继续150池B验证；新运行位于该目录 `stage-B/`，控制器PID37840，生成worker PID30740。
12项CPU检查通过，代码/模型/语料SHA256已冻结；GPU0开始加载生成模型，GPU1在首批150图写完后接续检测。
后续以B控制器started/complete/failed文件为运行事实。D*、D_g尚未确认，旧自然训练仍暂停。

**A启动时的历史记录（2026-09-07 03:28:57 UTC，§43.24）。**
用户要求按最新讨论规划并执行。四个冻结模型为base、Naive原回放32、补no/0 32、去回放梯度32。
GPU1生成16个新场景×2seed×4模型的128张配对图及8张base重复图；GPU0测同样本/噪声的生成损失，
随后四模型交叉观察同一批新图并计算连续cycle分数。无新增优化步，旧自然生成训练和选择条件不变。
另冻结150个不同的新场景、每池6张，B阶段是否开始等A的可测响应结果；150不是精度保证。
协议和运行目录：`review-packets/generator-repair-validation-20260907/`。6项CPU设计检查通过，
输入/代码/模型哈希已冻结。下面的“GPU空闲”是上一轮结束时的历史状态，本轮两卡已启动。

**最新：回放消融六组全部结束，最终核验完成（2026-09-07 02:46 UTC后整理，§43.23C）。**
GPU实际于2026-09-06 17:32:56 UTC结束，两卡目前空闲。六组各32步、72,324条有效回答及
1,728个池级评分核验通过；最后一组从step32接续，只补测量，额外训练步数为0。
去回放梯度组通过开发集能力保留及±5pp等效规则；补no/0组仍有一个真值组下降12pp而未通过，
换样本也未解决否定退化。结果支持先修回放并在新场景验证，不能宣布D*或D_g。
报告：`review-packets/replay-ablation-20260906/RESULTS.md`。原自然生成实验仍暂停，尚未启动新采集。
以下启动/等待记录为历史进度，以本条及§43.23C为准。

**2026-09-06 17:05 UTC 用户批准最多延长2小时，补齐最后一组。** 新截止为19:14:46 UTC，
仍只完成naive-alternate_ids原定32步及固定图测量，不增加条件、步数、样本或改变判据。
原进程的截止不可热修改；原进程于17:14:51 UTC按预算退出，接续worker于17:14:53启动，
已选择完整step32 checkpoint，在同一GPU1恢复模型、Adam、scheduler及RNG，恢复校验仍待完成。
训练32步已经完成，接续只需补校验及末次测量。旧退出/部分记录保留，接续输出在
`review-packets/replay-ablation-20260906/extension/`。这是用户看过部分结果后的预算变更，
不冒称事前预注册；原自然生成实验仍停在step32。细节见该目录 `BUDGET_EXTENSION.md`。

**2026-09-06 11:14 UTC 新的隔离回放消融已启动（§43.23）。** 用户要求继续回放诊断及CPU BoN。
原动态实验仍停在step32；新实验固定旧生成样本，仅改变回放，输出在
`review-packets/replay-ablation-20260906/`。两张3090各一进程，6个对照最多32步、每步200答探针，
0/16/32步完整固定图测量；启动后6小时或原预算截止前停止，错误不自动重跑。尚无消融结论。

**2026-09-06 08:52 UTC 执行范围已收束：两臂第32步测量完成，新增训练暂停。**
用户明确要求按测量验收建议调整并开始诊断，取代§43.16的继续10轮安排。
接续调度器以 `--through-round 4` 正常退出，`round-004` 尚未启动；原60小时预算、
冻结配置和历史结果不变。44张已有Naive图片的独立裁定已完成，人工后续见§43.21；新增训练继续暂停。
真实状态与控制证据见§43.21及 `review-packets/selection-benefit-side-20260906/`。

**2026-09-06 新阶段：固定图像观察/选择诊断已完成（§43.22）。** 新产物在
`review-packets/selector-measurement-next-20260906/`。主臂Naive，Gold训练臂作匹配对照；
共享base及两臂16/32步，同80图、原题/追加否定题、三种提示上下文条件，9090回答全部完成。
32步两臂在三条件下对202道否定题均回答yes；新题没有改变该步的排序。无新训练或生成。
完整结果见该目录 `RESULTS.md`，生成D*和梯度D_g仍未确认。

**新动态 pilot 已于 2026-09-05 18:04 UTC 启动，运行状态以 `runs/v4/decoupling-pilot-20260906/state.json` 为准。**
用户明确授权接手实现、使用本地两张 3090 并持续推进至完成或预先规定的停止条件（§43）。
GPU1 生成/训练/梯度，GPU0 独立裁定；不会恢复旧 `l3-preview` 调度链。
484 项 CPU 检查通过；InternVL 在空卡完成真实图片推理。固定方案见
`docs/prereg/2026-09-06-decoupling-pilot.md`。当前尚未得到训练分叉或可靠预警结论。

2026-09-05 19:44 UTC 首轮完成：两臂各做 8 次真实更新，9/12 个 prompt 进入配对训练；
更新前 96/96 对候选 PNG/RGB 哈希一致，naive 512 条回答完整可解析。保存的两臂
checkpoint、Adam 步数、参数 delta/digest 与日志一致，见 `runtime-check/first-update-integrity.json`。
22:42 UTC 两臂的 base/step8 留出评测、主梯度、敏感性报告及首张轨迹图全部完成，
随后自动开始第二轮（round001）。当前只具备 0/8 两个测量点，没有三点行为候选或
两点梯度报警；首轮配对结果及限制见 §43.11。先训练首轮再评测已保存 base 是原定顺序。
曾有 1 张未被选中训练图片因检测解析失败缺少 verdict，覆盖率修复及边界接续见 §43.6。
2026-09-06 00:11 UTC 第二轮更新完成、两臂累计 16 步；随后发现新增轮数/累计轮号混用的
调度 bug，已停止提前生成并修复，checkpoint 核验通过。恢复目标是先测 step16，见 §43.12。

**§41/§42 的事实错误率、旧 oracle 与“短板全在观察者”解释已被 §43 更正。**
原预注册实测的未通过判定保留；原始数据不覆盖。
2026-09-06后补§42.10修复了遗漏的两份历史诊断脚本，按完整可判事实重算；用户随后
明确要求继续原pilot，取代§43.15的step24临时停止决定，执行接续见§43.16。

两卡不对等，这条是实测：GPU1 挂在 PCIe gen3 x4（GPU0 是 gen4 x16），**逐图检测慢 7.5 倍**
（25 s/张 vs 4–5 s/张，在等数据不在算），但生成只慢 7%（每张几十步扩散，算力受限）。
**传输受限的活（detect / observe）留在 GPU0，算力受限的活（生成、梯度）才给 GPU1。**

v4 两个半边都已走完 generate -> detect x2 -> crop -> verify
-> observe(image_only + prompted) -> analyze。主结果 §18、核验器缺陷 §19、复现与效应量分解 §20。

**70 张争议图已裁定完毕，v4 的金标准全部落定。** 两个半边都不再有 `pending`。
分布 Q 38 / I 9 / X 23；`verify`、`observe`（只补这 70 张）、`analyze`、`compare` 已全部重跑，
主结论一行未变（§20）。裁定表与回填脚本：`scripts/v4_human_review.py`（三选一，分片、图片内嵌、
localStorage 存盘）与 `scripts/v4_apply_review.py`（把 `A1: 1Q 2I 3X` 这样一行写回
`human_labels.jsonl`，重复号/越界号/漏答都报错）。答案码留档在各 run 的 `review.codes.txt`。

**`X` 计入 p，是失败，不是被忽略。** 一度给「整张图读不了」单设过 `unusable`，让它退出 p 的
分母；那是错的，已删除。p 问的是「生成有没有画出要求的东西」，读不出来的图显然没有，
把它从分母里拿掉等于把生成器最差的输出从它自己的分数里删掉，p 会被抬高。人读不出来是关于
这次生成的证据，不是仪器的空白；真正没有判定的只有文件损坏或根本没渲染出来，那不是评审员会报的东西。
`X` 唯一不做的事是出题——「你画了几个梨」在候选是半个梨时没有正确答案，所以
`stage_observe` 跳过它们（实测 23 张）。`ladder_summary` 另报一个 `unnameable` 计数，因为
「画错了物体」和「画出了非物体」是两种失败，p 把它们加成了一个数。

**Tier B 删除臂已跑完**（把 §21 的三条候选解释分开）。设计与门槛见 §22，结果见 §23：
**编辑痕迹这条解释被 sham 对照排除了**，错误类型这条有支持（删除掉 0.144，改色只掉 0.036）。
§24(a) 的同 spec 配对臂**推翻**了「服从跟着作者走」，方向还是反的。
§24(b) 提出「服从跟着模型对自己错误的先验走」，§25 / §27 两次预注册、§26 / §35 两次检验：
**两次都没过。** 第二批方向与效应量复现、单侧 p=0.062；第三批在充足样本下（要求每层 136，
实得 173 / 149）效应塌到 0.020、p=0.342，更严的格子级单位上方向还反了过来。
**§24(b) 永久写为「探索性观察，两次预注册确证均未通过」，不做第四批（§35）。**

**已下载的权重（用户 2026-09-02 明确批准，落在 H 盘）**：`Carve/LaMa-ONNX` 的
`lama_fp32.onnx`，revision `c3c0c9e468934d62e79c329e35d82dd09ff8c444`，208 MB，
存在 `H:\selfsight-models\lama-onnx`，用途只有一个：补删除物体后留下的洞。
CPU 推理，装了 `onnxruntime` 1.23.2 到 `envs/core`。原先问的是 `smartywu/big-lama`，
换掉是因为那个 repo 发的是 zip、要 LaMa 仓库的 hydra 配置才能加载。
**许可证未核实**——下载目录里没有 LICENSE，HF 是离线模式，需要联网时补查再写进论文的致谢/许可一节；
LaMa 代码是 Apache-2.0，但 big-lama 权重另有条款，不要凭记忆写。

**运行环境的一条事实**：两块卡都是 RTX 3090，但 GPU1 挂在 PCIe gen3 x4 上(GPU0 是 gen4 x16)，
逐图推理慢 7.5 倍(Qwen 检测 20.4 s/张 vs 2.7 s/张)，功耗只有 89 W、频率 1125 MHz——在等数据不在算。

**2026-09-01 起：所有 GPU 作业只用 GPU0，GPU1 不占。** 这是用户的指令，不是从上面那条实测推出来的。
因此各阶段串行跑，起下一个之前先看 `nvidia-smi` 显存有没有掉下来；队列长就说队列长，不许挪到 GPU1 上并行。

语料已定稿：`data/v4/corpus_2plus1.jsonl` 117 条、`corpus_1plus1plus1.jsonl` 111 条。
后者原为 120 条，剔除 9 条 `clear` / `wooden` 条目——那个进程在 12:53 启动，
内存里是加 `COLOR_WORDS` 白名单之前的模块，磁盘上的检查是对的、进程里的不是。
教训与 §15 同源：改了校验就必须重启用它的进程，否则校验只存在于源码里。

---

## 当前成立的结论

按主题组织，不按时间。括号内是证据出处。

### 1. Backbone 已定
`showlab/show-o2-1.5B` rev `07ec1658`（432px 原生）——**普通版，不是 HQ**。v4 三批语料全部由它生成，
Gate A 在 v4 仪器上通过（p=0.2201，balanced_pool_rate 0.5385 / 0.6757，balanced_pools 138）。（§28）

HQ（512px）在 v2.x 就绪门上 existence / color / spatial 三族四项指标全达标、程序化 verifier 盲审
precision = 1.00，判红**只因为**「主族 ≥4」这条阈值，而该阈值无推导依据——v3 已公开把 floor 降到 3。
那条记录成立，但它是关于 HQ 的，不是关于本文主 backbone 的。（§1）

### 2. 负对照全红，不得提回主线
Show-o v1 1.3B、Show-o2-7B 均判红，不得提回主线。（§2）

**Show-o2-1.5B rank-1（432px）已于 2026-09-03 移出此清单**，因为它就是 v4 全部语料的实际生成模型。
它当初的红来自 v2.2 的 A3 自动检查，而 §11 / §13 已判定那一代仪器的缺陷在金标准；v4 换掉金标准后，
同一个模型在 Gate A 上是绿的。移出的理由和证据在 §28，不是顺手改的。

### 3. 工程闭环已打通
LoRA 训练、E1 反事实、checkpoint 曲线在本地全部跑通。**单 seed 下没有机制信号**——这是工程可行性结论，不是机制结论。（§3）

### 4. 候选供给是真实瓶颈
随机 K=4 池天然 informative 率仅 **21%**。有界候选库搜索把 balanced_rate 从 0.135 提到 **0.344**（2.5×），但仍低于注册阈值 0.40 → Gate A 判红。失败模式与 v2.x 预期**相反**：不是模型画不对，是 color 太准（每候选正确率 0.949）凑不出 2 个错误候选。（§4, §8）

### 5. 梯度噪声控制原设计有缺陷
「不相交半批余弦」测的量与它要把关的配对跨判据余弦**尺度不同**，产生过两次误导性红灯。已改为 prompt 层配对 bootstrap；半批余弦降级为诊断，不再当门槛。（§5）

### 6. 显示词汇缺陷（2026-08-30 发现）
verifier 把长宽比 0.5–2.0 的四边形都判为 SQUARE，而 v3 manifest 声称用 `box` 措辞却有 **183/256 行**仍写 `square`。实测 **39.5%** 的 verifier-SQUARE 物体长宽比 ≥1.25（中位 1.19，p90 1.62）。方向是**系统性抬高 `oracle − naive`**，即把 Gate C 推向假绿。已修复并加 fail-closed 守卫。（§11）

### 7. 选择头room 的判据（已在看数前注册）
`Oracle@K − Naive@K` > 0.05 → go；< 0.03 → stop；之间 → 扩样本。每候选正确率 p 与天花板
`(1−(1−p)^4) − p` 的关系：p≈0.35 时最大（0.471），p→0 或 1 时坍缩。有效区间很宽——
p ∈ [0.25, 0.55] 全都 ≥0.41，所以标定不需要精细调参。

**头room 至今没有被测出来。** 本页早先写的「已测 spatial p=0.194 / existence p=0.864」
无法从任何源文件复现，已作废（§12.4）。

### 8. box 词汇下的 Gate A（2026-08-31，128 prompt/族）

| 族 | balanced_rate | informative | 自然池 informative | 自然池 p | 天花板 |
|---|---|---|---|---|---|
| existence | **0.594** 过 | 91 (0.711) | 0.461 | 0.818 | 0.181 |
| spatial | **0.266** 不过 | 65 (0.508) | **0.016** | 0.754 | 0.243 |

阈值 0.40。两族都远在有效区间之外（p 太高）。spatial 的自然池 **98.4% 是退化的**——
整体 p=0.754，但每条 prompt 要么四个全对要么全错，与 proposal §72「模型对某个 prompt 的
正确性接近确定性」一致。

绝对供给判据（128 个 informative pool）在两次运行里都被显式 defer，从未测过。

---

### 9. gold 原子必须是全函数且肯定式（2026-08-31）

标定扫描本来是要量难度的，结果量到了别的东西。

**弃权，不是判错。** binding 和 spatial 在模型生成图上大面积弃权（binding 40–44/64，
spatial 44–54/64，existence **0/64**）。`color` 和严格关系谓词要求主体解析到**恰好一个**
检测才给答案，否则返回 `None`。这在参考渲染上恒成立（自洽测试因此全绿），在生成图上
只有 12–40% 成立。于是 p 是在存活的少数样本上估的，难度扫描测的是验证器可解析性。
`exists` 没有唯一性要求，所以 existence 弃权率恒为 0——**弃权还是判错是验证器的设计
选择，不是模型的性质。**

**否定命题近乎白送。** 拆开 existence 正负例（物体数 3/5/6）：

| | 正例 p | 负例 p |
|---|---|---|
| 3 物体 | 0.312 | **0.931** |
| 5 物体 | 0.420 | **0.882** |
| 6 物体 | 0.250 | **0.819** |

模型漏画东西就自动满足否定命题。这在生成式验证里是本质属性，调参调不掉。

**修复**（`src/selfsight/data/questions.py: build_gold_atoms`）：把**判生成图的原子**和
**问模型的原子**分开。问题保持正负平衡不变；gold 原子必须同时满足

* **全函数**——新增 `exists_left_of` / `exists_above` / `exists_larger_than`
  （`verifier.py`），「不存在这样一对」是答案不是弃权；
* **肯定式**——existence 负例场景改用「场景里某个真实物体在场」，binding 用
  「目标在场 ∧ 错配色不在场」，spatial 用「关系成立 ∧ 主体在场」。

`_gold_score` 改成多原子合取（`v3/supply.py`），manifest 落 `gold_atoms` 字段，
旧 manifest 自动回落到原问题原子。空白画布永远判错，由 `tests/test_v3_families.py` 守住。

**重测**（`scripts/rescore_v3_calibration.py`，**同一批图，只换判分，零 GPU**）：

| 物体数 | 族 | p | 天花板 | 弃权 | balanced_rate |
|---|---|---|---|---|---|
| 3 | binding | 0.312 | 0.464 | **0.000** | 0.688 |
| 3 | existence | 0.531 | 0.420 | **0.000** | 0.875 |
| 3 | spatial | 0.344 | **0.471** | **0.000** | 0.750 |
| 5 | binding | 0.297 | 0.459 | 0.000 | 0.812 |
| 5 | existence | 0.641 | 0.343 | 0.000 | 0.750 |
| 5 | spatial | 0.328 | 0.468 | 0.000 | 0.938 |
| 6 | binding | 0.312 | 0.464 | 0.000 | 0.750 |
| 6 | existence | 0.594 | 0.379 | 0.000 | 1.000 |
| 6 | spatial | 0.531 | 0.420 | 0.000 | 1.000 |

弃权全部归零，balanced_rate 最低 0.688（阈值 0.40，原 spatial 是 0.000 informative）。
spatial@3 的天花板 0.471 就是 K=4 的理论最大值 0.4715。

**物体数是弱旋钮。** 标定 bank 上的检测直方图是平的：要 6 个物体时 Show-o2 最常画 3–4 个，
要 3 个时最常画 2 个。移动 p 的是原子设计，不是物体数。

> **分析脚本的坑：** `_gold_score` 走 `verify_generated_image` → `detect_generated_objects`
> （给生成图用的容差更大的轮廓检测器）。任何离线复算都必须走同一条路径；用参考图检测器
> `detect_objects` 会得到系统性不同的 p。这个错误在本次分析中犯过一次，未进入任何产物。

**难度选择是欠功效的（重要限制）。** 48 条标定场景分给 3 个族，每族只有 **16 条 prompt**。
候选在 prompt 内部有聚类（设计效应 1.5–1.9），聚类后 SE(p) ≈ **0.085**，而有效区间
[0.25, 0.55] 只有 0.30 宽。注册规则是靠 |Δp| = 0.038 在 obj3 和 obj6 之间做的选择——
**这个差值远在噪声底之下，四档之间的取舍实际上是任意的。**

规则是看数之前注册的，照章执行，结果不改。但能写进论文的结论不是「obj3 是最优难度」，
而是**四档现在全部落在可工作区间、弃权全部为零**——这个结论对档位选择不敏感。
下次要真正区分档位，标定分片每族需要的是几百条 prompt，不是 16 条。

顺带：新 gold 原子下候选池的退化程度明显下降。held-out existence 的 64 条 prompt 里
20 条「四个候选全同」，独立假设下期望 12 条（设计效应 1.48）。对比 §8 记录的旧设计
「每条 prompt 要么四个全对要么全错」，这是选择机制能起作用的前提条件。


---

### 10. Gate A 首次两条判据同时成立（2026-08-31，held-out）

难度在 calibration 分片上冻结（三族均为 3 物体），Gate A 测在**没有参与选难度**的
64 条 held-out prompt 上。产物 `runs/v3/gate-a-v2/{family}/gate_a.json`，
汇总 `runs/v3/queue2/SUMMARY.md`。

| 族 | balanced_rate | informative | 自然 informative | bank lift | p | 天花板 | 弃权 |
|---|---|---|---|---|---|---|---|
| binding | 0.672 | 53/64 | 0.609 | +0.062 | 0.340 ± 0.040 | **0.470** | 0.000 |
| existence | 0.859 | 60/64 | 0.688 | +0.172 | 0.645 ± 0.036 | 0.340 | 0.000 |
| spatial | 0.891 | 63/64 | 0.734 | +0.156 | 0.508 ± 0.039 | 0.434 | 0.000 |

* **balanced_rate 三族全过**（阈值 0.40）。
* **informative pool 合计 176 ≥ 128**——注册的绝对供给底线在本项目里第一次达标。
  §8 的两次运行都把这条显式 defer 了，从未测过。
* **弃权全部为零。** 这是 §9 的直接结果。

binding 的天花板 0.470 几乎就是 K=4 的理论最大值 0.4715，但它的 balanced_rate（0.672）
和 bank lift（+0.062）是三族里最低的：候选质量分布最集中，bank 搜索能加的东西最少。

**held-out 的 p 系统性高于 calibration（未定论）。** 三族同向：+0.027 / +0.113 / +0.164，
Stouffer 合并 z = 2.00（双尾 p = 0.045）。**这是事后构造的检验**——我在看到前两族之后
才提出这个假设，第三族只是确认了方向，所以 0.045 不能按预注册结果解读。
影响是难度在 held-out 上比预期略偏易（existence 的 0.645 已在 [0.25, 0.55] 之外），
但三族 balanced_rate 仍全过。要定论需要预注册一次专门的检验。

---

### 11. 判分器效度审计：几何检测器不可信（2026-08-31）

§10 的 Gate A 数字全部依赖 `detect_generated_objects`。本节第一次在**人工标注**上
测了它的效度，结论是它不可信，§10 的 p / 天花板不能按面值使用。
（供给判据本身抗得住，见文末。）

#### 11.1 人工基准

72 个 held-out 自然池候选（每族 24），从 768 个里按固定种子抽样，**排除**先前带检测框
看过的 48 个。标注在只有编号、无框、无判定、无 prompt 的对照图上做。尺寸沿用检测器
同一条 95px 规则；判不准的标 `?` 并从尺寸统计中剔除。产物
`scratchpad/human_labels.py`、`blind_sample.jsonl`。

**这是 §「不可恢复的损失」之后项目里唯一可用的人工基准。它必须进 git。**

#### 11.2 物体级效度

| 判分器 | precision | recall | F1 | 整图完全一致 | 形状误判 | 漏检 | 虚构 |
|---|---|---|---|---|---|---|---|
| 几何（现行） | 0.750 | 0.757 | 0.754 | **0.486** | 45 | 5 | 7 |
| 几何（顶点数规则修复） | 0.889 | 0.898 | 0.894 | 0.694 | 16 | 5 | 7 |
| **Qwen3-VL-8B-Instruct** | **0.981** | **0.990** | **0.986** | **0.944** | 1 | 1 | 3 |

**现行检测器每两张图就有一张的物体清单是错的。**

#### 11.3 三个独立缺陷

1. **立方体被判成圆**（45 次形状误判里 `square→circle` 占 21）。渲染出的 3D 立方体
   投影是六边形，正六边形 circularity ≈ 0.91，而规则是「circularity ≥ 0.72 且外接框
   接近正方 → 圆」。八边形、圆角矩形走同一个洞。
2. **背景色相撞进调色板时整组物体消失。** 背景被归入某个颜色通道后，形态学闭运算把
   背景和同色物体连成一块，再被「面积 ≥ 80% 画布」整块丢弃。实例：一张模型三个物体
   全画对的图，检测器只看见一个。
3. **碎片与重复。** 18.9% 的候选带至少一个面积不足中位数 1/10 的检测；3.0% 的候选
   有同位置重复检测。

#### 11.4 判定级效度（48 张 existence + binding，gold 原子是纯存在式，不需坐标）

| 判分器 | 与人工一致 | 一致率 | 判对但实际错 | 判错但实际对 |
|---|---|---|---|---|
| 几何（现行） | 34/48 | **0.708** | 5 | 9 |
| 几何（修复） | 41/48 | 0.854 | 3 | 4 |
| Qwen3-VL-8B | 44/48 | **0.917** | 2 | 2 |

Qwen 的 4 处分歧逐张裁定后：1 处人工标注更弱（水滴形），1 处尺寸卡在 95px 边界，
1 处画面上的圆是**青色**（不在四色调色板内，属数据缺陷），仅 1 处 Qwen 真错。
**扣除标注争议后 Qwen 的一致率约 0.96。**

#### 11.5 修复线的评估

按 112 条人工标注轮廓拟合新形状规则（三角：circularity<0.62 且 fill<0.60；
圆：eps=0.01 下顶点数 ≥10；其余为方）。顶点数比 circularity 分得干净得多——
圆中位数 14 顶点、方 5、三角 3。`square→circle` 从 21 降到 1。

**但缺陷 2、3 一个没修**（漏检 5 / 虚构 7 完全不变），它们与形状分类无关。
且阈值是在同一批 112 条上挑的，0.894 含样本内成分。
**判断：几何路线修得动但修不到 Qwen 的水平，且每个缺陷都要在同一批小样本上单独
拟合阈值，终点大概率是过拟合到这批图的检测器。**

#### 11.6 对 §10 的影响

* **Gate A 的两条供给判据仍然成立。** 它们问的是「池子里有没有对错两侧」，
  对判分器的系统性偏移不敏感。补上 spatial 的 object grounding 后实测
  balanced_rate 0.807 → 0.797，informative 176 → 175，仍然通过。
* **p、天花板、以及基于 p 的难度选择不能按面值使用。**
* **偏差方向不能简单定号。** §9 记录的漏画漏洞（原子设计）抬高 p；本节的检测器误差
  在 existence+binding 上净向下（9 次误判为错 vs 5 次误判为对）。两者方向相反。

> **上一版本记录的一处过度归因已更正：** 「检测器报 large 占 72% 而场景只要求 51%，
> 是绝对像素阈值的问题」——**说过头了**。人工标注显示画面上实际就有 64.6% 是大物体，
> 大头是模型自己画大了；两个判分器的尺寸准确率都是 0.93，阈值本身没问题。

---

### 12. 那个「held-out p 同向偏高」已被完全解释（2026-08-31）

§10 末尾标注为未定论的 Stouffer z = 2.00，查清后**没有任何真实的难度漂移**，
三个族三个不同原因：

| 族 | 原始差值 | 原因 | 校正后 |
|---|---|---|---|
| spatial | +0.164 | 成分差异 + 漏画漏洞 | **+0.008** |
| existence | +0.113 | 赢家诅咒 | — |
| binding | +0.027 | 无（成分变化反而掩盖了它） | +0.063 |

**spatial：条件 p 几乎没动**（truth=True 0.288→0.250，False 0.583→0.652），
动的是配比——标定集 81% 是「关系成立」的难题，held-out 只有 36%。按标定配比标准化后
0.325 vs 标定 0.344。而 False 一侧的高 p 来自漏画漏洞：**107 个判对的 truth=False
候选里 40 个（37.4%）画面上根本没有 object 形状。** 补上 grounding 后 spatial p
0.508 → 0.352（标定是 0.344）。

`build_gold_atoms` 的 spatial 分支注释说要堵漏画漏洞，但**只 ground 了 subject，
没 ground object**。binding 有同类漏洞且更大：87 个判对里 39 个（44.8%）画面上
没有竞争者的颜色+形状，30 个（34.5%）目标形状只画了一个（场景本该有两个）。

**existence：赢家诅咒。** 扫描四档 p = 0.531/0.656/0.641/0.594，带是 [0.25,0.55]，
**obj3 的 0.531 是唯一落在带内的，只差 0.019 出带**。held-out 实测 0.645，
而其他三档均值 0.630。真实 p 约 0.63，标定那 16 条向下抖了约 0.10，规则抓住了抖动。
**existence 的 `in_band` 判定不可复现；按真值它应触发 `knob_exhausted`。**

**生成器 truth 标志有偏（新缺陷）。** `truth` 靠「掷硬币定目标真值，不符就交换两个
物体」实现，但交换只在两个锚点该坐标不同时才翻转真值。ANCHORS 是 8 点网格，
x 有 3 组重复、y 有 3 组重复，**坐标打平时 True 不可达**：

| 关系 | 坐标打平概率 | 预测 P(True) | 实测（3000 场景） |
|---|---|---|---|
| left_of | 0.250 | 0.375 | 0.360 |
| above | 0.107 | 0.446 | 0.446 |
| larger_than | 0 | 0.500 | 0.506 |
| 合计 | | 0.440 | 0.437 |

设计意图是 50/50，实际是 44/56。另已排除构建方式差异：混合族与单族构建的 truth 率
分别为 0.446 / 0.437，统计上相同；用同种子精确复现出了两份 manifest。
标定集那 13/16 = 0.812 是**真实率 0.44 下 P ≈ 0.0016 的抽样意外**——
这是 §9「每族只有 16 条 prompt」那条限制第一次真的咬人。

**Stouffer 的 0.045 本身双重虚高**：事后构造，且假设三族独立（共用 backbone、
检测器、代码版本，相关会撑大 z；取 ρ=0.3 则 z 从 2.00 降到 1.58）。

---

### 13. 缺陷在金标准，不在检测器（2026-08-31，同一批 753 张 held-out 图）

同一批图，四种打分方式：

| 打分 | p | C(p) K=4 | 自然 K=4 平衡率 |
|---|---:|---:|---:|
| 几何检测器 + 旧每族原子（§10） | 0.495 | 0.440 | 0.225 |
| Qwen3-VL-8B + 旧每族原子 | 0.583 | 0.387 | 0.162 |
| Qwen3-VL-8B + 场景匹配（含尺寸，prereg v1） | 0.107 | 0.258 ✗ | 0.089 |
| Qwen3-VL-8B + 场景匹配（去尺寸，prereg v2） | 0.253 | 0.436 | 0.220 |

**换检测器动了 +0.09，换金标准动了 −0.48。** 我早先把「检测器不可信」当作主因，这一行否定了那个说法：
主因是金标准，检测器是次因。

失败逐层分解：数量对 0.459 → 再加形状 0.277 → 再加颜色 0.252 → 再加尺寸 0.107。
**超过一半的图物体数就是错的，而旧金标准对此完全看不见。** 颜色只花掉 2.5 个点——
颜色当初是因为几何检测器的 0.156 供给率被退役的，那是个仪器假象，退役理由不成立。

existence 的漏洞在换上更好的检测器后**变大**：0.645（几何）→ 0.844（Qwen + 旧原子），
因为 Qwen 几乎总能找到那个唯一的锚点物体。这说明「把检测器换好」不能修复一个最小主张式的金标准。

### 14. v4 难度旋钮：断崖，不是斜坡（2026-09-01）

48 条可核验自然场景 prompt × K=4 = 192 张，**Show-o2-1.5B-HQ** 生成（512px），Qwen3-VL-8B 按多重集规则评分。

> **来源注（2026-09-03 补）：本表测于 HQ，不是本文主 backbone。** 这 48 条 prompt 从未进过 git，
> 无法逐字节复现，因此没有在普通版上重测。旋钮迁移到普通版后的实测后果见 §28。

| 场景总物体数 | n | p | 95% CI | C(p) K=4 |
|---:|---:|---:|---|---:|
| 2 | 8 | 0.500 | [0.215, 0.785] | 0.438 |
| 3 | 12 | 0.417 | [0.193, 0.680] | 0.468 |
| 4 | 52 | 0.154 | [0.080, 0.275] | 0.334 |
| 5 | 20 | 0.000 | [0.000, 0.161] | 0 |
| ≥6 | 100 | 0.010 | — | ~0 |

单条目数量的画对率：要求 1 个 → 0.754，2 个 → 0.312，3 个 → 0.213。

**据此把总物体数锁在 3，单条目数量上限 2。** 组合层面 `1+1`=0.50、`2+1`=0.50、`1+1+1`=0.375，
其余全部塌到 0（每格 n 只有 4–12，但与总物体数那一列的大样本趋势一致）。

一处需要更正的早先说法：我在只看了两三张图之后说过「tier 2 太简单」。实测 tier 2 的 p=0.172。
那是抽样运气，以实测为准。

---

### 15. 让 LLM 写语料:光靠 prompt 约束不管用（2026-09-01）

第一批 119 条 `2+1` 场景通过了全部结构校验（条目数、总物体数、prompt 与 spec 一致、
无重复），人工抽检 30 条却发现一类校验看不见的缺陷：**自然物体的颜色不合常理**。

instruction 从第一版起就写着「绝不要写蓝香蕉」。实际产出的 55 条 apple 条目里，
蓝 ×10、黑 ×4、棕 ×3、白 ×2——35% 不合理；场景层面 22/119 受影响。

**为什么这不是小事**：模型的物体颜色先验会和 prompt 打架，图失败的原因就不再是被测的能力；
而且不同物体被拖累的程度不一样，物体身份因此变成难度分层的混淆项。

**处理**：`PLAUSIBLE_COLORS` 白名单写进 `_check` 强制执行，只约束自然物（水果蔬菜蛋面包），
人造物（杯/书/蜡烛/碗/盘…）不限颜色——马克杯真的可以是蓝的，苹果不行。
第一批语料移入 `data/v4/_superseded/`，重跑。

**可推广的一条**：语料由模型写时，任何「请不要 X」的软约束都必须有对应的硬校验，
否则它只是一句祝愿。这也是用户当初那句「后面你人工校验一下」抓出来的——
自动校验全绿，人眼看第 30 条时才发现。

---

### 16. v3 的瓶颈是候选不变异，不是 p 落错区间（2026-09-01，v4 首批 117 spec × K=4）

单判别器（Qwen3-VL-8B）下的首读，**尚未过三级阶梯**：

| | p | 平衡池率 | 独立伯努利预测 | 实测/预测 |
|---|---:|---:|---:|---:|
| v3 几何 + 旧每族原子 | 0.495 | 0.225 | 0.875 | **0.26** |
| v3 Qwen + 场景匹配（去尺寸） | 0.253 | 0.220 | 0.685 | **0.32** |
| **v4 可核验自然场景** | **0.188** | **0.513** | 0.564 | **0.91** |

平衡池率 = K=4 池里既有对又有错的比例，三行同一定义、同为自然采样。
独立预测 = `1 − p⁴ − (1−p)⁴`，即若同一 prompt 的四个候选彼此独立时该有的平衡率。

**Gate A 的 ≥40% 判据在 v4 上成立（0.513），在 v3 上不成立。**

**最后一列才是重点。** Proposal §3.2 断言「在这个尺度上，模型对某个 prompt 的正确性接近确定性」，
并据此把候选供给定为整条路线的真实瓶颈。这个断言对 v3 数据是对的——但它是**几何刺激的性质，
不是模型的性质**。v3 的平衡率只有独立抽样预测值的 1/4 到 1/3，同一 prompt 的四个候选高度相关；
换成自然场景后这个比值升到 0.91，候选之间几乎独立。

所以 v3 的瓶颈从来不是「p 落在错误区间」，而是**同一 prompt 内候选不变异**。
此前围绕 p 调旋钮（物体数、词汇、金标准）的全部努力都在治标：p=0.495 那一档 p 明明在带内，
平衡率却只有 0.225。这一条应写进论文，它比「换了个更好的判别器」有意思得多。

**~~一处需要记录的偏差~~ ——— 这条归因是错的，已由 §19 取代。** 原文写：实测 p=0.188 低于
校准值 0.417，是因为 LLM 产出的 prompt 比校准批次短、无照片框定、部分图偏插画风。
实际原因是核验器的两个缺陷（盘子被当成表面、cup/mug 与 notebook/book 交叉命名），
修掉之后 p = 0.244。prompt 形式的差异也许仍有一点影响，但它不是主因，
而且当时没有任何证据支持那个归因——它是在没有别的解释时顺手写下的一个解释。
**下次遇到「实测低于校准」，先去查尺子，再去猜刺激。**

失败分层（468 张）：全对 88、总数不对 312、漏画物体 68。

### 17. 「诊断性试次」不会自己出现——四个族一度全部为零

在花 GPU 让 backbone 答题之前，对 468 张已检测的图做了一次纯 CPU 的出题空跑。结果：

```
总题数 1803，平均 3.85 题/图
gold 来源: image 1344 / spec_matches_image 305 / image_differs_from_spec 154
  诊断性试次占比 0.085   ——  而且 154 道全部来自 counting
  existence 0/468   absence 0/468   binding 0/355   spatial 0/53
```

**承载整个主张的只有 8.5% 的试次。** 其余 91.5% 上「读像素」与「背 prompt」给出同一个答案，
两个假设不可分。原因是构造上的：只有 `build_counting` 会把「请求的数量」当作干扰项，
其余四族的干扰项都是从一个通用物体池里随机抽的，与这张图的 spec 无关，
于是干扰项永远既不在图里也不在 prompt 里，背 prompt 的模型不会被它抓住。

修法是让每一族都优先从 **spec 里没被满足的那条声明** 取干扰项：

| 族 | spec 说 | 画出来 | 问 | 背 prompt 的人答 |
|---|---|---|---|---|
| existence | 蜡烛 | 瓶子（多画的） | 「图里有瓶子还是蜡烛」 | 蜡烛 ✗ |
| absence | 蜡烛 | 没画 | 「图里有蜡烛吗」 | 有 ✗ |
| binding | 绿梨 | 红梨 | 「梨是什么颜色」 | 绿 ✗ |
| counting | 两个梨 | 三个 | 「几个梨」 | 两个 ✗ |
| spatial | 杯在苹果左 | 画在右 | 「杯在左还是右」 | 左 ✗ |

修完重跑同一批图：

```
总题数 1860，平均 3.97 题/图
gold 来源: image 182 / spec_matches_image 1264 / image_differs_from_spec 414
  诊断性试次占比 0.223（154 → 414 道）
  existence  81/468 = 0.173
  counting  160/459 = 0.349
  absence   159/468 = 0.340
  binding    14/412 = 0.034
  spatial      0/53 = 0.000
```

三件必须一起记下的事：

**(a) existence 只算「替换」为诊断。** 若图里画出的那个选项本身也在 spec 里（漏画了另一个），
背 prompt 的模型在两个选项里都看见自己的指令，它是**掷硬币而不是答错**。
把这种试次也标成诊断会用无法区分两个假设的题去稀释主结果，所以只有
「画了 spec 没要的 × 漏了 spec 要的」这种替换才标 `image_differs_from_spec`（81 道），
其余标 `image`（在元数据里留 `present_in_spec` 以便复查）。

**(b) binding 的诊断率低是测出来的事实，不是构造缺陷。** 逐条统计 spec 条目：
颜色画对 722、画错 14、根本没画 168、同名物体画成两种颜色 32。
颜色几乎是免费的——这与 v3 的分层一致（count 0.459 → object 0.277 → colour 0.252）。
所以 binding 这一族在本语料上基本不产诊断题，报告时不应掩饰。

**(c) spatial 在本语料上永远为 0，且是有意的。** 它要成为诊断题必须 spec 里带关系，
而带关系就等于多给生成器一个必须做对的约束，直接压 p，而 p 是这条路线上最稀缺的资源
（Proposal §5：不带关系的 prompt 让 spatial 题「白拿」）。
`build_spatial` 里读 spec 关系的分支已经写好并有测试覆盖，换成带关系的语料无需改代码。

**可推广的教训**：v3 的族是按「能问什么」划分的，v4 的族必须按
「**这一问能否把读像素和背 prompt 分开**」重新审一遍。前者是题型，后者才是实验。
一个族问得再对，只要它的干扰项与 spec 无关，它对主张的贡献就是零。
### 18. 主结果：prompt 在场时，模型答自己画错的那张图，正确率从 0.651 掉到 0.201

`2+1` 半边，117 条 spec × K=4 = 468 张图，1783 道 2AFC 配对试次，同一批图、同一批题，
唯一差别是问题前面有没有那句「你是照这个描述画的：……」。

| | image-only | prompted | 配对检验 |
|---|---:|---:|---|
| 图与 spec 一致（1244 道） | 0.982 | **0.993** | 3 → 17 反向，χ²=8.4, p=0.0037 |
| 图与 spec 冲突（307 道） | 0.651 | **0.201** | **140 → 2，χ²=132, p=1.4e-30** |
| 总体 | 0.905 | 0.824 | |

**两个方向相反，这才是关键。** 若 prompt 只是干扰上下文，两行都该下降；实际上图与 spec 一致时
prompt 略有帮助（+0.011，显著），只有在像素与描述冲突时才崩塌。加了 prompt 之后
140 道从对变错、只有 2 道从错变对；而在一致的那批上是 3 → 17，方向反过来。

自我确认率（诊断性试次中按被要求的场景而非画出的场景作答的比例，随机 = 0.5）：
**0.349 → 0.799。**

分族看诊断性试次：

| 族 | image-only | prompted | n |
|---|---:|---:|---:|
| counting | 0.815 | **0.109** | 135 |
| absence | 0.510 | 0.262 | 145 |
| binding | 0.688 | 0.312 | 16 |
| existence | 0.455 | 0.357 | 11 |

counting 最干净：**不给 prompt 时模型数得清自己画的（0.815）**，给了 prompt 之后
「你被要求画两个梨」→ 它答两个，尽管它画了三个、而且被明确要求「回答图里实际有什么」。
这不是看不见，是看见了不采信。

程序性检查：prompted 条件下弃答率 0.000，选 A 比例 0.499（gold A 0.496），无位置偏置。
排除 28 张待人工裁定的图后，诊断性正确率 0.6515 → 0.6445 / 0.2013 → 0.2020，结论不变。

**这个对照是补跑的，而且是必须补的。** 在此之前只跑了 image-only 一条，而
`observe_atoms` 按 RFO 隔离契约只喂图和问题、不带任何生成期状态——描述根本不在上下文里。
那种条件下「照着 prompt 答而不是照着像素答」这个假设**没有可答的东西**，
0.982 vs 0.651 那个落差只能说成「画错的图更难读」，是难度不是服从。
Proposal §5 的观察条件矩阵本来就写着 Fresh-context prompted 这一行，是我漏跑了。
一条只有单一条件的曲线读不出机制，这一条要记住。

**边界**：只有 `2+1` 半边；单一模型、单一 seed 组；binding 与 existence 的诊断 n 只有 16 和 11，
不单独下结论。这是描述性结果，不是训练结论。
### 19. 核验器的三个缺陷，以及一个「低到该去查」的数字

`2+1` 半边跑了三遍，每遍都是因为尺子有问题，不是因为结论不喜欢。三版记录全部留档
（`runs/v4/main-2plus1/*.v1-no-box-clause.*`、`*.v2-plate-as-surface.*`）。

| | v1 | v2 | v3 定稿 |
|---|---:|---:|---:|
| p | 0.197 | 0.192 | **0.244** |
| 平衡池率 | — | 0.496 | **0.573** |
| 清单一致率 | — | 0.496 | **0.573** |
| 人工裁定率 | 0.335 | 0.077 | **0.060** |

**(a) Qwen 在 468 张里 276 张一个 bbox 都不给。** 不是解析错、不是 key 名不同——字段直接省略，
object 和 color 照常正确。`parse_reply` 有框就存、没框不报错，全程没有任何东西失败。
框是 spatial 族和裁剪重问赖以工作的东西，所以这两处一直只跑在 41% 的行上：
spatial 从 468 张图只出了 53 题，我一度归因于 `2+1` 组成里单例物体少，**那个归因也是错的**。
指令加一句「元素缺 box 即无效」，6/6 原本没框的图都给了框。detection 行现在存原始回复。

**(b) 指令让检测器跳过「物体所在的表面（table, tray, counter, cloth）」，而 21% 的场景要求画盘子。**
盘子既是物体又是表面，于是这些图无论生成器画得多好都必然被判漏画：仅此一项 57/468。
反过来把托盘说成物体又制造了新的假阳性（20/468 多出一个「第四个物体」）。
最终形态是：指令里明说盘/碗/碟是物体，同时**在比对时用一张表面词表强制过滤** table/tray/cloth/mat
——软约束必须配硬检查，这已经是第三次（§15 颜色白名单、§17 诊断干扰项、这里）。

**(c) 名词粒度。** cup↔mug 交叉命名 30 次、notebook↔book 21 次，多重集按精确名词比对，
同一个东西当成两个，又 50 张。`canonical_noun` 只并这两对加复合词取中心词，
**故意做得很小**：lemon 与 lime 不并、spoon 与 fork 不并——那些是检测器的错误，值得被测出来。
撰写词表每对同义词只留一个，canonical 后撞车的 spec 直接拒。

**这三个是怎么被发现的：不是靠 code review，是靠一个太低的数字。**
absence 族的诊断性试次正确率 0.385，显著低于随机。分物体一看：
plate 0.182（n=33）、cup 0.303（n=33）、notebook 0.550（n=20）——占 205 道里的 86 道。
那些东西**本来就在图里**，模型答「有」是对的，错的是金标准。修完这一族回到 0.510。

**一个族的数字低到反常时，先去看它，再去写它。** 如果当时把 0.385 当成发现写进论文，
写的是核验器的缺陷，署名是模型的失败。
### 20. `1+1+1` 半边复现了主结果，效应量只有一半，原因查出来了

`1+1+1` 走完同一条流水线（111 spec × K=4 = 444 张）。核验器不再有 §19 那三个缺陷，
一次跑成，没有返工：

下表是 70 张争议图**人工裁定完成后**的定稿值，括号里是裁定前（按主判别器暂记）的值：

| | `2+1` | `1+1+1` | 合并 |
|---|---:|---:|---:|
| p | 0.2201 (0.2436) | 0.3131 (0.3288) | 0.2653 |
| C(p) | 0.4099 | 0.4643 | — |
| 平衡池率 | 0.5385 (0.5726) | **0.6757** (0.6847) | 0.6053 |
| 清单一致 / 判决一致 | 0.573 / 0.885 | 0.475 / 0.840 | — |
| 人工裁定率 | 0.0598 | **0.0946** | 0.0768 |
| 待裁定 | 0 | 0 | 0 |

两个半边 Gate A 都过（≥0.40），人工裁定率都在锁定的 ≤10% 以内，但 `1+1+1` 只剩 0.5pt 余量——
它三个单例物体、颜色各不相同，两个检测器在清单层面更容易分歧。crop 那一级问了 120 个争议物体，
**0 个因为没有 bbox 而问不成**（§19(a) 修好之前这个数是 59%）。

**70 张的裁定分布：Q（千问）38、I（InternVL）9、X（不是任何东西）23。**
千问在争议图上以 38:9 压过 InternVL，这与盲审 72 张上两者几乎持平（0.981 / 0.985）不矛盾——
难图上的胜负与平均精度是两回事，而这批图正是 InternVL 的失败方向（漏检）最容易暴露的地方。
`X` 那 23 张占 912 张的 **2.5%**：Show-o2-1.5B 有百分之二点五的生成不是任何东西的图。
这是下界（只在争议图里数的），但它已经解释了两个 8B 检测器为什么会自信地给出不同的名词——
**它们在给没有名字的东西起名字**。这 23 张计入 p 的失败侧，不供给任何 trial（§6.2）。
两个半边的 X 率差别很大（12/28 = 43% vs 11/42 = 26%），语料难度不同，如实分开报。

**配对对照复现，方向完全一致：**

| | image-only | prompted | 配对 McNemar |
|---|---:|---:|---|
| 图与 spec 一致（1078 道） | 0.974 | **0.988** | 6 → 21 反向，χ²=7.3，p=0.007 |
| 图与 spec 冲突（360 道） | 0.667 | **0.453** | 84 → 7，χ²=63.5，p=1.6×10⁻¹⁵ |

**关键的那个对照——一致行反向上升——在独立的一半语料上又出现了一次**（+0.014，与 `2+1` 的
+0.009 几乎同值）。「加长上下文造成的干扰」这个解释要成立，得解释为什么它只在像素与描述冲突时才生效。

合并 3623 道配对试次（889 张出题图）：一致 0.979 → 0.991（9 → 35，p=1.6×10⁻⁴），
冲突 0.654 → 0.336（221 → 9，χ²=193.6，p=5.3×10⁻⁴⁴）。自我确认率 **0.346 → 0.664**。
分族（合并）：counting 0.825 → 0.250（n=188）、binding 0.797 → 0.493（69）、
absence 0.557 → 0.355（366）、existence 0.500 → 0.296（44）。
**§18 里 n 太小不敢单独说的 binding 与 existence，现在 69 和 44，都显著且同向。**
以上全部是 70 张争议图人工裁定落定之后的值；裁定前后没有一行改变方向或显著性，
最大的一处变化是合并冲突行 0.661 → 0.654。

**但冲突行的落差在两半边差了一倍**（−0.440 vs −0.214），这不能不解释。分解到 counting 的
「要求几个 / 画了几个」组合上，答案是干净的：

| 要求 / 画出 | n | image-only | prompted | 出现在 |
|---|---:|---:|---:|---|
| 2 / 1（少画了） | 102 | 0.804 | **0.049** | 只有 `2+1` |
| 1 / 2（多画了） | 73 | 0.740 | 0.534 | 两个半边都有 |

**少画比多画容易否认得多。** 要求两个、只画了一个，再把描述放回上下文，模型有 95% 的时候
答「两个」——它在断言一个自己没画出来的物体存在。反过来要求一个、画了两个，它只有 41%
的时候答「一个」。这与 absence 族的方向一致（那一族问的正是「被要求但没画出来的物体」，
0.557 → 0.355）：**缺席的东西比多余的东西更容易被 prompt 补上。**

`1+1+1` 的所有物体都是单例，`(2,1)` 这个格子在它里面根本不存在，效应量减半就是这么来的。

**这条不要写得比证据硬。** `(2,1)` 只出现在一个半边，所以「少画 vs 多画」的比较里混着
「哪个半边」；两边都有的 `(1,2)` 格子在 `2+1` 是 0.353（n=17）、在 `1+1+1` 是 0.589（n=56），
半边效应确实存在，只是没被分离出来。要干净地测它，得让同一批 spec 同时供给两种错法。

**一个附带的观察**：image-only 条件下有 42 道弃答，prompted 条件下 0 道。
那句描述不只是改变答案，它还让模型每一次都给出答案。

配对分析独立成脚本 `scripts/v4_compare_conditions.py`，因为 `v4_analyze.py` 一次只看一个文件，
而主张在两个文件的**逐试次差**上——非配对地比两个准确率会把配对信息扔掉，在这里方差会被放大数倍。
### 21. Tier B：构造真值拿到了，服从却没出现——而且「像素清楚就不服从」这个解释是错的

从 209 张外部验证为正确的图上建了 139 对：**83 对水平翻转 + 56 对改色**（Proposal 原写 300 对，
底料只有 209 张，达不到；差额如实记，不靠放宽标准凑）。

**改色臂（56 对，两个条件）**

| | image-only | prompted |
|---|---:|---:|
| 原图（与描述一致） | 1.000 | 1.000 |
| 改色图（与描述冲突） | 1.000 | **0.964** |

56 道里只有 2 道翻转。**服从几乎不存在**，而自然池的 binding 诊断题是 0.792 → 0.500。
两个验收门下结论一样（严格 44 对 0.977，阶梯 12 对 0.917），受语义约束的名词（水果，n=13）
与不受约束的（杯碗瓶，n=43）也一样。弃答两个条件都是 0。

**我先给出的解释是错的，在这里更正。** 我一度写道「服从是像素证据弱时填进来的东西」——
Tier B 的题在 image-only 下 1.000，自然池的诊断题只有 0.669，看上去正好对上。
**counting 直接推翻它**：counting 的诊断题在 image-only 下是 0.825，一点也不含糊，
prompted 下却掉到 0.249。像素清楚而服从极强，这个格子存在，所以「清楚就不服从」不成立。

留下的候选解释，都还没测：

- **错法不同。** counting 的主力格子是「要求 2、画了 1」（§20），模型要断言的是一个**不在图里**的
  物体；改色问的是一个确实在图里的物体是什么颜色。前者也许更像它可能犯的错。
- **可归因性。** 一只毫不含糊的绿杯子，在「你被要求画蓝杯子」面前，不是一个模型合理会犯的错误；
  少画一个梨是。服从也许跟着「这个错我可能犯过吗」走，而不是跟着像素清晰度走。
- **编辑痕迹。** 旋转色相后的物体饱和度整齐，也许携带了「这不是我画的」的线索。

**能分辨这三条的那个编辑，恰好是这一轮做不了的那个**：删除一个物体，直接造出「要求了但图里没有」，
也就是 counting 与 absence 的那种错法。删除必须 inpaint，本地没有权重，按 Hard Stop 要先批准。
这一条要记住——**范围决定是有代价的，代价出现在这里，不是出现在批准的时候。**

**翻转臂（83 对）：一个干净的阴性对照**

image-only 逐图 0.789，prompted 0.735（21 → 12，χ²=1.94，p=0.164，不显著）。
配对层面 image-only 两个都对 0.590、只对一个 0.398、都错 0.012。若两次作答独立且 p=0.789，
预期是 0.62 / 0.33 / 0.045——**实测与独立几乎一致，翻转对没有查出隐藏的位置偏置或场景先验**。
本语料 spec 不带空间关系，两个成员在描述面前地位相同，所以这一臂本来就测不到服从，
它测的是「答案是否跟着像素走」，答案是：跟，但 spatial 自读本身只有 0.79，不高。

**构造真值与难度不可兼得。** 双检测器要能确认「颜色变了、别的没变」，编辑就必须做得干脆，
于是题必然容易（image-only 1.000）。想让 Tier B 出难题就得只旋转一部分色相，
可那样「到底算黄还是算绿」就不再由构造给出，得回去要人工标签。
**同一道门既保证了标签，也保证了题目容易**，这是构造类刺激的结构性代价，要写进论文的限制一节。

**三个工程缺陷，都已修并补测试：**

- `check` 的续跑键只有 `pair_id`、不含检测器名。Qwen 写完 80 行，InternVL 认为全做过了，
  于是 80 张改色图全卡在 `not_checked_by_both`，产率报成 **0.000**——一个「什么都没做错」的零。
- 验收门按整份清单比对，19/80 的拒绝来自两个检测器对**别的物体**的分歧，而不是编辑本身。
  这正是 §19 修过一次的错（按清单判 vs 按被判定的事判）。改成：两个检测器都要确认被编辑的那个事实，
  其中一个确认别的没动。产率 0.550 → 0.700，两个门的结论一致，两个数都报。
- `questions` 用顺序 RNG。加了 12 对之后，**已答过的 254 道里 136 道选项顺序变了**，
  而 `observe` 的续跑按 question_id 判——旧答案会被配到新选项上。改成按 `pair_id` 播种，
  阶段幂等；旧答案留档 `answers.sequential-rng.jsonl`，278 道全量重跑。
  **续跑的正确性依赖上游阶段是幂等的，这个依赖之前没有被写下来，也没有被测。**
---

### 22. 删除臂的编辑器：三种掩码试错，最后靠面积卡候选（2026-09-02）

§21 的空结果留下三条候选解释：①错误类型（颜色变了模型跟得上，东西没了跟不上）、
②可归因性、③编辑痕迹。删除物体分开①，配一个 sham（空编辑）分开③。

**掩码试了三种，前两种失败面互补，所以都不是「调紧一点就能用」的方案。**

- **整框**：把邻居一起铲掉。八张试点图里四张不能用——瓶子被切平，一本红书跟着旁边的蜡烛一起消失。
- **物体自己的颜色掩码**：留下残骸。梗、高光、接触阴影都不是主体的色相，
  苹果删完剩一团白斑加一根梗，胡萝卜从自己的绿缨子底下被删走。
- **整框减邻居像素**（采用）：物体必然消失，邻居靠自己的掩码被保护下来。

**让第三种真正能用的是给白/灰/黑加分割。** 无彩色没有色相，原来直接退化成
「保护邻居整个框」，而在静物里那个框和目标重叠，等于在删除掩码上挖了个洞——
半瓶红瓶子就那样立在它原来站的白盒子上。改成按饱和度+明度分割后，退化成整框保护变成罕见情况。

**剩下的失败在填充器，不在掩码，而且是面积问题。** 洞占画面 0.16 时补得干净，
0.67 糊掉，0.90 时 LaMa 把一只盘子换成一堵暗墙、勺子浮在上面。
先试过「围着洞裁窗口再补」——**输出逐位相同**，因为在这个语料的洞尺寸下，
留够上下文的窗口就是整张图。所以设 `MAX_DELETE_SHARE = 0.20`，在跑检测器**之前**卡掉，
不把 GPU 花在已知不可用的编辑上。0.20 时 690 个候选里剩 292 个、覆盖 188 张图，供给够。
另设 `MIN_DELETE_CENTRE = 0.80`：邻居坐在目标正中间（勺子躺在要删的盘子上）就拒绝，
否则会得到一张「半个物体还在」的图。

**一次记录在案的判断失误**：我一度诊断说 LaMa 的输入约定要求先把被删区域置零，
并这样告诉了用户。按这个改完，输出与改之前逐位相同——这个诊断不成立，
ONNX 图内部已经处理了。真正的两条原因是上面那两条。

**编辑器可以粗糙，因为门是严的。** 一条编辑成为试次的条件不变：两个检测器都看到那个物体没了，
且至少一个看到别的什么都没动。试点里一只红杯子被填成了一个米色的蛋——门会拒绝它。
加了无彩色分割之后重新抽 8 例：**7 例干净，1 例失败**——胡萝卜的橙色身子没了、
绿缨子悬在半空。这类残骸掩码救不了：缨子整个长在检测框外面，框再外扩也够不着，
要语义分割才行。它交给验收门（下节的 `delete_collateral_change` 就包含这种）。

**sham（空编辑）**：同一个填充器、同样大的面积、落在没有任何检测框的背景上，物体清单不动。
没有它，删除对里两个成员的差异既可能是「少了个物体」，也可能是「一张过了网络、一张没过」，
而那正是③。sham 的两个成员金标准相同，`gold_source` 记为 `no_spec_claim`——它是对痕迹的对照，不是试次。

**问法**：自然池的 existence 是「两个名词里哪个在画面里」，这个形式撑不住删除对
（无论配哪个第二名词，总有一个成员是两个都在或两个都不在）。改成对被删物体的是非题
「Is there a red apple in this picture? A. yes B. no」，两个成员问法完全一样、金标准相反。
是非题会有 acquiescence 偏置，措辞救不了；救得了的是这是**配对的、同图内的**比较——
恒定的「倾向答 yes」在两个成员上同样偏移，做差时抵消。yes/no 哪个当 A 也按 pair 随机。

### 23. 删除臂：服从出现了，而且不是编辑痕迹造成的（2026-09-02）

242 张外部验证为正确的源图，建出 **129 对删除 + 77 对 sham**；过双检测器门后
**97 对删除（产率 0.752）+ 70 对 sham（0.909）**，334 道题，两个条件各跑一遍。
删除被拒的 32 对：附带清单改动 21、两个检测器都看不出变化 8、目标还在 2。

| | image-only | prompted | 配对 McNemar |
|---|---:|---:|---|
| **删除·原图**（与描述一致） | 0.969 (94/97) | 0.990 (96/97) | 0 → 2，p=0.48 |
| **删除·编辑图**（与描述冲突） | 0.979 (95/97) | **0.835** (81/97) | **14 → 0，p=5.1×10⁻⁴** |
| sham·原图 | 0.957 (67/70) | 0.971 (68/70) | 0 → 1，p=1 |
| sham·编辑图 | 0.957 (67/70) | 0.971 (68/70) | 0 → 1，p=1 |

两个条件弃答都是 0。

**③编辑痕迹被排除了。** sham 用同一个填充器、同样大的面积、同一批图，
两个成员都没有掉——`0 → 1`，一次反向翻转，p=1。删除对上的 14 次翻转不可能是
「一张过了网络、一张没过」造成的，因为 sham 的两张也都过了网络，而它没掉。
这是这一轮最硬的一个结论：**它是排除性的，不需要相信填充器的质量，只需要相信两臂用的是同一个填充器。**

**①错误类型有支持。** 编辑器、验收门、问法风格全部固定，只换被改的那个原子：

| Tier B 臂 | image-only → prompted | 掉幅 | 翻转 |
|---|---|---:|---|
| 改色（§21，56 对） | 1.000 → 0.964 | 0.036 | 2 |
| **删除（97 对）** | 0.979 → **0.835** | **0.144** | 14 |
| sham（70 对，对照） | 0.957 → 0.971 | −0.014 | 0 |

**东西没了比颜色错了更能引出服从，差不多四倍。** §21 里「服从是像素证据弱时填进来的」
已经被 counting 推翻；这里补上的是另一半——像素同样清楚（image-only 都在 0.97 以上），
服从的强弱仍然差四倍，差的是**错法**。

**②可归因性没有被分开，必须写明。** 删除同时是「不同的错法」和「更像模型自己会犯的错」，
这一臂动了两个变量。要分开它，得让错法固定而来源变化——自然池的 existence 诊断题
（0.500 → 0.292，n=44）问的正是同一件事，但那是模型自己漏画的，图本身就含糊
（image-only 才 0.500），绝对值与构造的删除（0.979）不可比。**下一步要设计的是这一条，
不是再加对数。**

**别把 0.835 读成「服从很强」。** 自然池 prompted 下 counting 是 0.256、absence 0.355、
existence 0.292，都在 0.3 上下；构造的删除是 0.835。同一个模型，被我们抠掉一个物体时
它基本还是照实说，被它自己漏画时才大面积改口。这个落差本身就是②的证据方向，
但如上所述，这一轮分不开。

### 24. 服从跟着什么走：不是「谁犯的错」，是「这是不是我会犯的错」（2026-09-02）

> **术语更正（2026-09-03）：本节以前叫「分叉点」，改掉了。**
> proposal 里的**分叉点 = D\***，是训练步上的量（内部自洽仍在上升、外部可验证正确率停止上升的
> 最早一步），主交付是领先于它的无标签预警信号 D_g，主终点是 `Lead = D* − D_g > 0`。
> 本节讲的是**服从跟着什么走**，属于主张②（机理：意图泄漏）的静态证据，与 D\* 是两回事。
> 一个词指两件事会让「分叉点已找到」被误读成 D\* 已测出来——**D\* 至今没有测过**。
> 本页其余地方的「分叉」若指训练曲线，一律是 D\*。

> **状态（2026-09-03）：(b) 未获确证，降为探索性观察。** §25 的预注册检验在新语料上跑完，
> 方向对上了、效应量也接近，但单侧 p=0.062 没过预先声明的 0.05。按 §25 的规定不得抢救。
> 详见 §26，第三批语料的预注册见 §27。
> (a) 同 spec 配对臂不受影响，它是一个被推翻的假设，不是一个待确证的假设。

§23 留下①错误类型与②可归因性没分开。做了两件事，第二件才是答案。

**(a) 同 spec 配对臂（origin，26 对）：假设被推翻，方向还是反的。**

同一个 spec 下，一个种子漏画了物体、另一个种子画对了；后者由我们把同一个物体删掉。
同 prompt、同问句、同族、同答案（都是「没有」），**只差这个缺失是谁造成的**。

| | image-only | prompted | 掉幅 |
|---|---:|---:|---:|
| 生成器自己漏画的 | 0.615 | 0.308 | +0.308 |
| 我们删掉的 | 0.962 | 0.577 | +0.385 |

掉幅之差 −0.077（严格子集 16 对：0.250 vs 0.438，差 −0.188）。**我们造的错掉得反而更多。**
「服从跟着作者走」这个读法不成立。n=26，功效不足，但方向与假设相反，不是「没测出来」。

顺带一个副产品：这一臂里**我们删的那一侧掉了 0.385，而删除臂总体只掉 0.144**。
同样是我们删的，差了两倍多。差别在于这一臂的物体是按「生成器在别的种子上漏画过它」挑出来的。

**(b) 删除臂内部拆分（97 对，全是我们删的）：分界在这里。**

按「该 (spec, 物体) 在别的种子上是否被生成器漏画过」拆开。注意被打分的那张图本身是外部验证为
**正确**的，所以这个划分变量从不使用被打分的图——它来自同 spec 的其他种子。

| | n | image-only | prompted | 掉幅 | McNemar |
|---|---:|---:|---:|---:|---|
| 生成器漏画过这个物体 | 43 | 0.977 | **0.744** | **+0.233** | 10 → 0，p=0.0044 |
| 从没漏画过 | 54 | 0.981 | 0.907 | +0.074 | 4 → 0，p=0.134 |

**image-only 两组一样（0.977 / 0.981），像素同样清楚；服从差三倍。**

三条显而易见的替代解释都查了，都不成立：

- **洞的大小**：高服从组的洞反而更小（0.128 vs 0.151），编辑痕迹更少而服从更多，方向相反。
- **验收门宽严**：严格门占比 0.74 vs 0.67，高服从组反而更严。
- **名词身份**（jar 集中在一组、apple 集中在另一组）：只取两组都出现的 9 个名词重算，
  0.222（n=27）vs 0.067（n=45），同向同量级。

**结论：服从跟着「这是不是我这类模型在这个 prompt 上会犯的错」走，不跟着「这个错是不是我犯的」走。**
这是②可归因性的一个更窄的版本——是**对自己错误分布的先验**，不是对作者身份的识别。
它同时解释了 §23 里那个说不通的落差：构造删除整体只掉 0.144，而自然 counting 掉 0.568，
因为漏画正是这个生成器最常犯的错，counting 的诊断题几乎全落在它的高先验区。

**这一节是探索性的，必须这样标注。** 划分变量是在看到 (a) 的结果之后才选的，不是预注册的。
它对上面三条混淆稳健，但「漏画过」只由每个 spec 至多 4 个种子估计，估计量很粗。
**确证性检验需要在新语料上做**：用一批种子估计每个 (spec, 物体) 的漏画率，在另一批种子上建删除对，
按漏画率分层后再测。现有语料做不到这件事，因为 spec 不跨半边。

### 25. 预注册：§24 分叉点的确证检验（2026-09-02 写于看到任何结果之前）

§24 是探索性的：划分变量在看到配对臂结果之后才选，且「漏画过」由每个 spec 至多 4 个种子估计。
本节在**生成新语料之前**写定，此后不再修改；任何偏离都必须以「偏离」的名义追加记录，不得改写本节。

**假设（方向已定，单侧）**：在我们删除的物体上，prompted 相对 image-only 的掉幅，
在「生成器在别的种子上漏画过该 (spec, 物体)」的一层显著大于「从没漏画过」的一层。

**预期效应量（取自 §24，不作为通过条件，只用于定 n）**：掉幅 0.233 vs 0.074，差 0.159。
两比例检验 80% 功效、α=0.05 双侧需每层约 74 对。

**划分变量，完全来自旧语料**：`runs/v4/main-2plus1` 与 `main-1plus1plus1` 的 `verified.jsonl`。
一个 (spec, 单例名词) 若在旧语料任一已裁定种子上整个缺席，记为 prone，否则记为 never。
旧语料共 429 个这样的格子（prone 242 / never 187）。**新语料的图不参与这个标注。**

**新语料**：同一批 228 个 spec（`data/v4/corpus_*.jsonl`），K=6，`--seed-offset 1000000`，
种子与旧语料按构造不相交（旧语料种子跨度 < 500）。产出 1368 张，落在 `runs/v4/conf-*`。

**流水线与门槛一律不动**：同两个检测器、同 crop 级、同阶梯、同 `MAX_DELETE_SHARE=0.20`、
`MIN_DELETE_CENTRE=0.80`、同双检测器验收门、同问法、同两个条件。
`unnameable` 与 `pending_human` 照旧不供给试次。**不做人工裁定**——待裁定图直接排除，
但必须**分层报告排除率**；若两层的排除率相差超过 5 个百分点，则本检验判为不可解释，另行说明。

**每张图可出多对**（`--per-image`），因为划分变量是 (spec, 物体) 的属性，一张图可同时携带两层。
**同一张图出的多对不独立**：主检验按图聚类（每张图对每层至多贡献一个「翻转/未翻转」计数，
若一层内同图有多对，随机取一个，随机数种子记为 `20260902`）。不做这一步就会把图级噪声当成对级证据。

**主检验**：2×2 Fisher 精确检验，行=分层，列=（image-only 对且 prompted 错）与（其余）。
按上述假设方向单侧，同时报双侧。**判定：单侧 p < 0.05 且方向与 §24 一致，则确证；否则不确证。**

**预注册的次要分析**（不改变主判定）：
1. **同图配对**：只取同时含两层且两对都过门的图，McNemar 配对。旧语料上 242 张有 74 张同时带两层，
   新语料预计约 18 张能两对都过门——功效不足，只作方向性参考。
2. 分层报告 image-only 正确率（若两层不同，则「像素同样清楚」这条前提在新语料上不成立，须说明）。
3. 分层报告 `hole_share`、`centre_coverage`、严格门占比。
4. 只取两层都出现的名词重算。

**预先声明的失败条件**：主检验不显著、或方向相反、或两层 image-only 差超过 0.05、
或两层排除率差超过 5 个百分点——任一成立，§24 在论文中降为「探索性观察，未获确证」，
**不得改用次要分析或换分层阈值去抢救**。

**成本**：生成 1368 张 × 25 s ≈ 9.5 小时（GPU0），检测 2 × 1368 × 4.5 s ≈ 3.4 小时，
crop 与 verify 另计。全部排在 GPU0，GPU1 不占。

### 26. 确证检验的结果：方向对了，显著性没到，§24(b) 降为探索性（2026-09-03）

按 §25 一字不改地跑完。检验代码 [`scripts/v4_conf_fork.py`](scripts/v4_conf_fork.py)
**提交于任何答案存在之前**（`9e221e6`，语料当时还在检测），所以它不可能是照着结果写的。

**语料**：`runs/v4/conf-2plus1`（702 张，p=0.254，45 张待裁定）与
`runs/v4/conf-1plus1plus1`（666 张，p=0.277，56 张待裁定）。291 张已核验正确的源图，
规划 175 对，双检测器验收通过 159 对（0.909，高于 §23 的 0.752）。
按图聚类后 **prone 77 / never 62**。

**主检验（2×2 Fisher，按图聚类，种子 20260902）**：

| | n | image-only | prompted | 掉幅 | 翻转 |
|---|---:|---:|---:|---:|---:|
| 生成器漏画过这个物体 | 77 | 0.974 | 0.688 | **+0.286** | 22 |
| 从没漏画过 | 62 | 1.000 | 0.839 | **+0.161** | 10 |

**单侧 p=0.062，双侧 p=0.106。** 方向与 §24 一致，效应量 0.125 也接近 §24 的 0.159，
**但没过预先声明的 0.05**。四条失败条件里过了三条（方向对；image-only 差 0.026 < 0.05；
排除率 0.077 vs 0.081，差 0.004 < 5 个百分点），唯独主检验不显著。

**按 §25：§24(b) 降为「探索性观察，未获确证」。不做任何抢救。**

预注册的次要分析照报，且**不得用于翻案**：同图配对只有 14 张图同时带两层（2 对 1，p=1，功效为零）；
`hole_share` 0.128 vs 0.137、`centre_coverage` 0.984 vs 0.996、严格门占比 0.857 vs 0.742——
三条混淆仍然不支持替代解释；两层共有名词子集 0.293（58）vs 0.158（57），与主表同向同量级。

**为什么没到：功效不够，不是效应消失。** §25 按 §24 的效应量 0.159 算出每层需要约 74 对，
实得 77 / 62 已经贴着下限；而实测效应量是 0.125，比 0.159 小，所需样本随之涨到
**每层 136（单侧）/ 172（双侧）**。这一轮从设计上就够不着。
`MAX_DELETE_SHARE=0.20` 挡掉了 240 个候选（最大的一块损耗），但**动它就是事后改预注册**，不动。

**这条结论怎么写进论文**：§24(b) 作为探索性观察陈述，附这次的复制结果——
「方向与效应量在独立语料上复现，显著性未达预注册阈值」。
不写成「已确证」，也不写成「未能复制」；后者同样是错的，因为方向和量级都对上了。
要变成确证结论，需要每层约 136 对的第三批语料，那是另一次实验，不是这一次的重新分析。

**一处澄清（不是偏离）**：§25 写「必须分层报告排除率」，没说在什么单位上量。
实现取的是**候选**（新语料每张图 × 其 spec 的每个单例名词），不是成对样本：
没裁定的图根本产不出对，在成对样本上量会正好把这条检查要抓的偏差藏起来。

### 27. 预注册：第三批语料，按 §26 的实测效应量重新定 n（2026-09-03 写于生成任何图之前）

§26 唯一没过的是主检验的显著性，原因是功效：预注册按 §24 的 0.159 定 n，实测是 0.125。
本节在**生成第三批语料之前**写定，此后不再修改；任何偏离以「偏离」的名义追加记录，不得改写本节。
**术语**：本节讲的是 §24(b) 的**服从分界**，与 proposal 的分叉点 D\* 无关（见 §24 顶部的更正）。

**假设、划分变量、流水线、门槛、问法、条件、判定阈值——与 §25 逐字相同。**
唯一变的是样本量和种子。这是一次严格复制，不是一个新设计。

**n 的依据**：§26 实测 0.286 vs 0.161（差 0.125）。两比例检验 80% 功效需每层
**136 对（单侧 α=0.05）/ 172 对（双侧）**。§26 在 1368 张图上得到 prone 77 / never 62，
约束层是 never，按线性外推需 `1368 × 136 / 62 ≈ 3001` 张。取 K=14 × 228 spec = **3192 张**。
预期 prone ≈ 180、never ≈ 145。

**新语料**：同一批 228 个 spec，K=14，`--seed-offset 2000000`。
旧语料种子跨度 < 500，§26 用 `+1000000` 跨度 1368，本批跨度 3192，**三批按构造两两不相交**。
落在 `runs/v4/conf2-2plus1` 与 `runs/v4/conf2-1plus1plus1`。

**分层标注仍然只由最早那批语料（`runs/v4/main-*`）计算**，429 格 / 242 prone / 187 never，
`scripts/v4_conf_fork.py` 的 `proneness()` 一行不改。§26 的语料**不参与标注**，也不参与主检验。

**主检验**：与 §26 完全相同——按图聚类（每张图对每层至多一个翻转计数，同图同层多对时随机取一，
随机数种子 `20260902`），2×2 Fisher 精确检验，单侧 p<0.05 且方向与 §24 一致则确证。
**在第三批语料上单独跑，不与 §26 合并。**

**预先声明的次要分析**（不改变主判定）：
1. **按格子聚类**：每个 (spec, 名词) 格子每层至多一个计数。K 从 6 提到 14 会让同一格子出更多图，
   §26 实测每格 1.13 / 1.44 张，本批预计升到约 1.6 / 1.7。格子级是更严的单位，报出来作为
   伪重复的稳健性检查。**它比主检验严，因此不可能被用来抢救主检验。**
2. **§26 + 第三批的固定效应合并**（两批都在同一预注册协议下跑，合并是标准做法）。
   **此处预先声明，正是为了它不能在看到第三批结果之后才被想起来。** 合并结果单独报告，
   明示它不是主判定；若主检验不显著而合并显著，写成「两次复制合并后达到显著」，
   **不得写成「已确证」**。
3. §25 的四条次要分析照旧（同图配对、分层 image-only、`hole_share`/`centre_coverage`/严格门、
   共有名词子集）。

**预先声明的失败条件**：与 §25 逐字相同（主检验不显著 / 方向相反 / 两层 image-only 差 >0.05 /
两层排除率差 >5 个百分点）。任一成立，**§24(b) 在论文中永久写为「探索性观察，两次预注册确证均未通过」**，
**不再做第四批**。连着两次方向对而阈值不到，就是效应比设计能力小，继续加语料是在买显著性。

**成本**：生成 3192 张，两卡并行 ≈ 11 h；双检测器 2 × 3192 × 5 s，两卡并行 ≈ 4.5 h；
crop ≈ 1 h；plan / check / observe ≈ 1.5 h。合计约 18 h。
**先预热页缓存再起作业**，冷加载会让 16 GB 权重爬到 5 MB/s（§26 那天的教训）。

### 28. 主 backbone 一直是普通版 1.5B，不是 HQ（2026-09-03）

**怎么发现的。** Gate B 探针的 naive 臂启动时打印 `showlab/show-o2-1.5B @ 07ec1658`，
与本页抬头写的 `show-o2-1.5B-HQ @ d3a220ec` 不符。

**证据，不是推断。** `v4_run_pipeline.py` 的 generate 阶段调 `Showo2Adapter(device=..., lazy=False)`，
**没有传 `backbone_config`**，走默认的 `configs/backbones/showo2_1p5b.yaml`（`candidate_rank: 1`，
432px）。决定性的一条：v4 三批语料的图**全是 432×432**，而 HQ 配置是 512px。v3 时代的标定图
（`runs/v3/calib*`）是 512×512，那些确实跑在 HQ 上。所以切换发生在 v3 → v4 之间，无人察觉。

**v4 的设计是按 HQ 标定的。** 建 v4 流水线的提交 `bb8fa79` 明写「The corpus is 512x512」
（InternVL 单 448 tile 不做动态切片的理由——在 432px 下这个理由更成立，无害），以及
「48 prompts × K=4 through Show-o2-1.5B-HQ give p = 0.500 / 0.417 / 0.154 / 0.000 / 0.010」
——`TOTAL_OBJECTS = 3` 与单条目上限 2 这两个旋钮，是拿 HQ 标定出来、套在普通版语料上的。

**旋钮迁移的实测后果：迁过来了。** 普通版实测 p = 0.2201（468 张已裁定图），
C(p) = 0.4099，balanced_pool_rate 0.5385 / 0.6757，balanced_pools 63 + 75 = 138。
p 仍落在 C(p) 要求的 [0.13, 0.70] 内，Gate A 在 v4 仪器上通过。旋钮是按 HQ 定的，
但它在普通版上依然工作——这是实测，不是假设。

**决定（2026-09-03，用户）：论文报普通版 1.5B。** 三批语料 4560+ 张全部保留，不重新生成。
连带后果逐条处理：

| 项 | 原状态 | 处理 |
|---|---|---|
| §2 负对照清单含 rank-1（432px） | 它就是主 backbone | 移出清单，理由见下 |
| LoRA 目标清单（196 模块） | 审计于 HQ | **已在普通版上重建**：`runs/readiness/showo2-1p5b/a4-lora-targets-r1.json`，目标逐条相同，`selection_digest` 同为 `033751f8…` |
| §14 难度断崖（48×4） | 测于 HQ | 保留原值 + 注明来源。那 48 条 prompt 从未进 git，无法逐字节复现 |
| 10.15 GiB LoRA 峰值 | 测于 HQ | 保留 + 注明。目标清单相同，上界同样适用 |
| Proposal §3.1 的 7B 对照（16pt vs 10pt） | 基准是 HQ | 保留 + 注明。它支持的是「放大到 7B 没用」，不是普通版与 7B 的直接比较 |

**为什么可以把 rank-1 移出负对照清单。** 它当初的红是 v2.2 的 A3 自动检查
（`docs/EVIDENCE_LOG.md` §2.2：「A3 自动红，按预注册停止规则停在人工/A4 之前」），
而 §11 / §13 已经实测判定那一代的缺陷**在金标准、不在检测器**（换检测器动 +0.09，换金标准动 −0.48）。
v4 换掉金标准之后，同一个模型在 Gate A 上是绿的。冻结记录一字未改，`docs/EVIDENCE_LOG.md`
原样保留；改的是「当前成立的结论」这一页对它的归类。

**这条缺陷本身的教训。** `Showo2Adapter` 的 backbone 是个默认参数，而不是每个 run 必须显式声明、
并写进产物的字段。manifest 里没有 generator id，`verified.summary.json` 里也没有——所以切换能
悄悄发生并存活三批语料。**后续任何 run 的产物必须记录 backbone id + revision。**

---

### 29. Gate B：两条从未测过的判据，一过一不过（2026-09-03）

138 个平衡池 × 3 个判据 = 414 次 LoRA 前向+反向，18,464,768 个可训练参数，
GPU1 上 12 分钟跑完。产物 `runs/v4/gate-b/`。

**三个判据确实在分歧**（不是退化的同选）：

| 判据对 | 选到同一张图的比例 |
|---|---:|
| naive vs rfo | 96/138 = 0.696 |
| naive vs gold | 54/138 = 0.391 |
| rfo vs gold | 75/138 = 0.543 |

**梯度结果：**

| 量 | 余弦 | 95% CI | 宽度 |
|---|---:|---|---:|
| GDA-free = cos(g_naive, g_rfo) | 0.9678 | [0.8199, 0.9927] | **0.1728** |
| GDA-gold = cos(g_naive, g_gold) | 0.9705 | [0.8790, 0.9861] | 0.1071 |

分半 SNR 诊断（非判据）0.6794。

- **LoRA 子空间不退化：过。** 余弦 0.97 远离 0。
- **配对 bootstrap CI ≤ 0.10：不过。** 实测 0.1728。

**Gate B 未通过。** 按注册的补救动作（「增加探针中 informative pool 的数量直到 CI 收窄」），
CI 宽度按 1/√n 收窄，要到 0.10 需要 n ≈ 138 × (0.1728/0.10)² ≈ **412 个池**。
主语料只有 138 个；第二、三批语料（1368 + 3192 张）裁定完之后可以补上，第三批 K=14
意味着几乎每个 spec 都能凑出平衡池。**这不是加算力抢救，是注册时就写好的动作。**

**一个独立于判据的观察：余弦本身很高（0.968）。** 三个判据在 30% 的池上选了不同的图，
梯度方向却几乎平行。也就是说梯度方向主要由 prompt 与 loss 决定，「换一张图训练」只推动了很小的角度。
这对主张 (1) 是坏消息还是好消息，要等 CI 收窄后才能说——现在的 CI 宽到 0.17，
本来就不足以支持任何方向的结论。

**梯度是用 HQ 审计的目标清单取的**（`gradients.meta.json` 的 `lora_target_provenance` 记录了两侧）。
事后在普通版上重建的清单与之逐条相同、digest 相同（§28），所以这批梯度不受影响。

---

### 30. 通往 D* 的路上没有训练回路（2026-09-03）

`830cfa7`「Delete the v3 geometric instrument」删掉的不只是几何刺激栈，还包括**跑训练和按
checkpoint 出指标的整条回路**。那次删除本身是对的——这些文件从头到尾吃的就是几何刺激——
但决策树下面那句「其余 L0–L2 的脚本均已存在」是 `8f155ae` 写的，没有跟着更新，
于是记录里一直显示「只差一个运行器」。实际情况：

| 已删除 | 行数 | 原职责 | 阶段 |
|---|---|---|---|
| `src/selfsight/pilot/real_loop.py` | 819 | 配对训练主循环 | L1 |
| `src/selfsight/pilot/evaluate.py` | 580 | 按 checkpoint 计算 internal / external 指标 | L2 |
| `src/selfsight/analysis/gradient_gate.py` | 359 | 梯度门判定 | L0 |
| `scripts/run_gradient_gate.py` | 298 | L0 入口 | L0 |
| `scripts/evaluate_pilot.py` | 212 | L2 入口 | L2 |
| `scripts/run_local_pilot.py` | 151 | L1 入口 | L1 |
| `scripts/run_formal_e2.py` | 341 | 三 seed A800 编排 | C1/C2 |

`src/selfsight/pilot/` 现在只剩 `__pycache__`。

**活下来的是零件，不是回路**：`training/checkpoint.py`（存档 + RNG 状态）、`training/lora.py`
（挂 LoRA、断言只有 LoRA 可训）、`training/gradients.py`（采集、余弦、噪声区间、EMA）、
`training/paired.py`（配对 schedule 与同一性断言），共 460 行；
`analysis/breakpoints.py` 的 `estimate_d_star` / `estimate_d_g` / `estimate_lead` 也在。

也就是说：**测 D\* 需要的统计学已经写好，产生它输入的那条训练曲线没有代码可以产生。**
从这里到 Gate C 的工作量不是「拆一个单 seed 运行器」，是在 v4 仪器上重建 L1+L2。

**为什么不 revert `830cfa7`**：`real_loop.py` 的数据侧全部指向已删的 `data/renderer`、
`data/manifest`、`data/verifier`，恢复回来也编译不过；它的 gold 来自参考渲染器，而 v4 的
gold 来自三级升级裁定。可复用的是它的**骨架决定**（resume 语义、checkpoint 间隔、
配对两臂共享 schedule 与 latent seed、只训 LoRA），这些照抄，数据与 gold 侧重写。
参考实现取 `git show 830cfa7^:src/selfsight/pilot/real_loop.py`。

**教训（与 §28 同类）**：§28 是运行工件不记录 backbone 身份，这一条是删除操作不回写依赖它的
计划文档。两者都是「记录与实际脱钩」，都只在下一次有人照着记录行动时才暴露。

---


### 31. 228 个 spec 撑不起 D\* 的测量，差约一个数量级（2026-09-03）

重建 L1+L2（§30）时，`split` 阶段在真实语料上跑出的第一个数字就把问题挑明了：

```
76 train / 120 outcome / 32 probe
```

语料一共 **228 个 spec**（111 + 117）。main / conf / conf2 三批是同一批 spec 换种子重画，
所以批次再多也不增加 prompt。而通往 D\* 的两侧同时需要它：

**训练侧**。`configs/local_3090_showo2.yaml` 的 `rounds: 10 × prompts_per_round: 64 = 640`
个**互不相同**的 prompt。留出评测集之后只剩 76 个。

| 留出 | 可训练 | 需要的 `max_epochs` |
|---|---|---|
| outcome 120 + probe 32 | 76 | 9 |
| outcome 80 + probe 32 | 116 | 6 |
| outcome 60 + probe 24 | 144 | 5 |
| outcome 40 + probe 16 | 172 | 4 |

缩小评测集能换来训练 prompt，但同一批 prompt 走 4–9 遍意味着模型第二次见到它时
**记忆与能力混在一起**——而 D\* 恰恰是要区分「内部自评还在涨」和「外部正确率不涨了」。
用重复 prompt 训练出来的外部曲线会因为记忆而**上翘**，正好抵消要找的效应。

**评测侧**。外部正确率是个比例，基线 p = 0.2201（§28）。评测集固定、latent 种子在
两臂间共享（`evaluation_seed`），所以 checkpoint 之间是**配对**比较，精度比独立比例好，
但仍受 n 限制。参照效应量只有一个：决策树里记的 v2.2 探索闭环 **Naive +4.17 pt**
（单 seed、K=2、10 步，工程证据）。

| n_outcome | 每 checkpoint 配对 SE | 4.17 pt / SE |
|---|---|---|
| 120 | 3.54 pt | **1.2** |
| 228 | 2.56 pt | 1.6 |
| 400 | 1.94 pt | 2.2 |
| 800 | 1.37 pt | 3.0 |
| 1600 | 0.97 pt | 4.3 |

**n=120 时每个点的噪声和整段效应一样大。** 在这种曲线上拟合折点，`fit_segmented`
会给出一个数，但那个数是噪声的位置，不是 D\* 的位置。这与本节同批补的斜率下限是同一件事的
两面：下限挡住「平线被读成上升」，样本量决定「真上升能不能被定位」。

**成本**。评测开销是主项，不是训练：

| n_outcome | 图（两臂 × 10 checkpoint） | 检测调用 | 检测 @4.7s | 生成 @8s |
|---|---|---|---|---|
| 120 | 2400 | 4800 | 6.3 h | 5.3 h |
| 400 | 8000 | 16000 | 20.9 h | 17.8 h |
| 800 | 16000 | 32000 | 41.8 h | 35.6 h |

**结论**：要同时满足训练侧 640 与评测侧 n≈800，语料需要约 **640 + 800 + 32 ≈ 1470 个 spec**，
是现有 228 的 6–7 倍。写 spec 本身便宜（LLM 出文本，和这 228 条同一套流程），
贵的是评测那 40 小时本地 GPU。**这是 D\* 的限制项，且它不在 A800 上——先扩语料，再上云。**

**为什么现在才发现**：640 这个数写在 v3 的配置里，那时刺激是渲染器画的几何图形，
prompt 要多少有多少。v4 换成 LLM 写的自然场景之后，prompt 变成有限资源，
但配置里的数字没人重算过。与 §28、§30 同类——**换了仪器，跟着仪器走的数字没换**。

**同一个坑的第三处：`candidate_k: 2`。** 阳性对照 RFO-Gold 的选择器是判别阶梯，
它只能从**被判定为正确**的候选里挑。按 p = 0.2201，一个 K 大小的池子里至少有一张正确的概率是
1 − (1−p)^K：

| K | P(池中有正确的) | 64 个 prompt 里能用的 | 每轮检测调用 | 10 轮检测 | 10 轮生成 |
|---|---|---|---|---|---|
| **2** | **0.392** | **25** | 256 | 3.3 h | 2.8 h |
| 4 | 0.630 | 40 | 512 | 6.7 h | 5.7 h |
| 8 | 0.863 | 55 | 1024 | 13.4 h | 11.4 h |
| 16 | 0.981 | 63 | 2048 | 26.7 h | 22.8 h |

**K=2 时阳性对照在 61% 的 prompt 上弃权**，配对设计再把这些 prompt 从 Naive 臂一起丢掉，
每轮只剩 25 个 prompt 参与训练。更糟的是弃权**不是随机的**：它集中在难 prompt 上，
于是「完美选择器」只在容易的 39% 上完美，Gate C 比较的是两臂在简单子集上的表现。
`select_gold` 在池子全错时**弃权而不是随机挑一张**——随机挑会让阳性对照不再是阳性对照，
而且恰好在难例上失效，把比较偏向 Naive。

K 要开到 **8 以上**（0.863）才谈得上「完美选择器」，代价是 10 轮 25 小时的本地检测+生成。
这个数字和上面的语料规模是叠加的，不是二选一。

---

### 32. GPU1 画图的绝对速率，以及一条被推翻的预热优化（2026-09-03）

第 19 行早就记着「逐图检测慢 7.5 倍，但生成只慢 7%」，所以「GPU1 能画不能看」
不是新发现。**新的是一个绝对数**：GPU0 正在 68% 跑 InternVL 检测的同时，
GPU1 上 show-o2-1.5B 画图 **18.66 s/张**（8 张，不含 71.6 s 的加载与预热）。
排 L3 预告的班需要的是这个数，不是比例。

**但比例对不上，得说清楚**：对照语料图片时间戳反推的 GPU0 速率
（conf2-2plus1 的 1638 张跨 373.6 分钟 = 13.69 s/张），GPU1 慢 **36%**，不是 7%。
两个数不可直接比——13.69 那次 GPU0 也在被占用，而 18.66 这次 GPU1 是在
GPU0 满载检测时测的，两边的竞争条件都不干净。**没有干净的同机对照**，
第 19 行的 7% 也就没被推翻，只是在真实竞争条件下应按 36% 排班。
要定这件事需要两卡各跑一次空载基准，现在卡没空。

**可用的结论**：GPU1 画图、GPU0 裁决，墙钟取两者的大者而不是和。
这是 L3 预告能压进一天的唯一原因。规则相应细化为——
传输受限的活（detect / observe）留给 GPU0，计算受限的活（生成、梯度）可以给 GPU1。

**同时作废一条旧优化**：「先 `cat` 一遍 safetensors 预热页缓存」在磁盘有竞争时
是反优化。今晚它变成了两遍慢读——预热的顺序读和消费者的随机读互相寻道饿死，
盘掉到 0.8 MB/s，两张卡空转 50 分钟。杀掉预热和我自己那个读 51 GB 梯度 memmap
的 Gate B report 之后，盘回到 54 MB/s，InternVL 四个 shard 16 秒加载完。

还有一个诊断陷阱值得记下来：safetensors 是 **mmap 惰性加载**，
「Loading checkpoint shards 100%」那 16 秒只是建立映射，真正的 16 GB 磁盘读
发生在权重往 GPU 搬的时候，以缺页形式发生，**不计入进程的 ReadTransferCount**。
所以「进程只读了 0.14 GB 但盘在满速转」不是矛盾，是正常现象。
判断它在不在动，看显存增长速率（当时 ~16 MB/s，和盘的 17.7 MB/s 吻合）。

### 33. L3 预告的配置，以及它不能回答什么（2026-09-03）

`configs/v4_l3_preview.yaml`，划分冻结在 `runs/v4/l3-preview/split.json`
（76 train / 120 outcome / 32 probe，replay 池 726 对）。
`local_outcome`、`local_probe`、`seed` 与 `local_3090_showo2.yaml` 逐字相同，
所以 `split_digest` 一致，预告复用同一份划分而不是偷偷重抽。

规模 R=10 / P=7 / K=8 / max_epochs=1：70 个 prompt 槽对 76 个训练 prompt，
**零复用**。这是唯一值得花轮数去避免的污染——第二遍会把外部曲线往上弯，
而真实效应是把它往下弯，方向正好相反。K=8 让阳性对照的弃权率从 K=2 的 61% 降到 14%。
按实测速率（生成 18.66、双检测器 10.8、observe 1.5 s/张）墙钟约 **18 小时**，
其中评测占大头：每个 checkpoint 每臂光画 120 张 outcome 图就是 37 分钟。

**这份预告不能回答什么，必须先说清楚**：10 轮 × 3 步 = 30 次 LoRA 更新，
前 3 步还是 warmup。**这不足以让 backbone 动起来**，所以跑出两条平的曲线意味着
「没训练」，不是「内外没分叉」。预告能证伪流水线，不能证伪假设，
它的 null **不得**当成假设的 null 写进论文。

步数少不是旋钮拧低了，是被语料卡死的：76 个训练 prompt × 86% 覆盖
= 全程只有 65 张不同的被选图。想从这里买更多步数，只能把每轮那 6 张图多看几遍，
那训练的是「记住 6 张图」而不是「画对」。**解法是加 spec，不是加步数。**

---

### 34. L3 预告的停止规则（预注册，2026-09-04 00:50，写于生成任何曲线之前）

用户指令是「接着跑，直到找到分叉点或失败再停止」。为了让「失败」不是事后
才划的线，先把两个终态写死。**本节在 `runs/v4/l3-preview` 产出任何一行
`checkpoint_metrics.csv` 之前写成，之后不改。**

**终态 A —— 找到 D\*（预期不会发生）**
`divergence.json` 里至少一个臂给出非 None 的 `d_star`，且该臂的内部斜率
高于 `internal_noise_slope` 从曲线自身 SEM 推出的噪声下限。
即便命中，它也**只是描述性的**：单 seed、n=120、效应/噪声 1.2，
按 Hard Stop **不得**写成显著性结论，也不得据此宣称 Lead = D\* − D_g > 0。

**终态 B —— 失败,停止（预期发生）**,四条任一：

1. **没训练**：两臂的内部斜率都低于各自的噪声下限。这是 §33 预判的结果。
   报告必须写「30 次更新不足以移动 backbone」,**不得**写成「内外未分叉」。
2. **配对塌了**：任一轮 gold 弃权后剩余配对 prompt < 2。K=8 下每轮期望
   剩 6.0,低于 2 说明 p=0.2201 这个先验在训练分布上不成立,那是新信息,
   要单独登记而不是调 K 重跑。
3. **同一阶段连续两次不可恢复地失败**。允许一次重试(掉盘、别人的作业抢卡、
   CUDA OOM 都属于环境噪声),第二次同样的错就停,把 traceback 原样登记。
4. **墙钟超过 40 小时**。§33 估的是 GPU1 可用时 18 h、被占时 24.5 h,
   40 h 留了足够余量;超过就说明成本模型错了,那本身是要报告的发现。

**不构成停止理由**（写下来是因为这几条最容易被当成停止理由）：
结果是 null、曲线难看、磁盘慢、别人的作业占卡。前两条是结论不是故障,
后两条等就行。

**无论落在哪个终态,都不做的事**（沿用 Hard Stops）：不靠加算力/加 seed/
换 backbone 抢救；不把单 seed 轨迹写成显著性；不回头改本节。


---

### 35. 第三批语料的确证检验：未通过，§24(b) 永久降为探索性观察（2026-09-04）

§27 的预注册在第三批语料上跑完了。**判定：not confirmed，分界留在探索性。**
本节是新证据的登记，**§25 与 §27 一字不改**。

**主检验**（按图聚类，种子 `20260902`，2×2 Fisher）：

| 层 | n | 翻转 | 率 |
|---|---|---|---|
| prone | 173 | 21 | 0.121 |
| never | 149 | 15 | 0.101 |

单侧 p=0.3419，双侧 p=0.5983。四条预先声明的失败条件里三条干净通过、一条不过：

- `[ok]` 方向与探索性划分一致（prone 更高）
- `[FAIL]` **单侧 p < 0.05**
- `[ok]` 两层 image-only 差 0.004（≤0.05）
- `[ok]` 两层排除率差 0.003（0.091 vs 0.095，≤5 个百分点）

**不是功效不足。** §27 按 §26 的实测效应量定的是每层 136 对，实际拿到 173 / 149，
**两层都超过要求**。设计对 0.125 的差有 80% 功效，实测到的差是 **0.020**。

**效应量在三次独立取样上单调塌缩**：

| 来源 | prone | never | 差 | 单侧 p |
|---|---|---|---|---|
| §24 探索性（划分变量是看过数据之后选的） | — | — | 0.159 | 不适用 |
| §26 第二批 | 0.286 (77) | 0.161 (62) | 0.125 | 0.062 |
| §27 第三批 | 0.121 (173) | 0.101 (149) | **0.020** | 0.342 |

这就是选择性效应回归到零的形状。§27 已经写死：连着两次方向对而阈值不到，
说明效应比设计能力小，**继续加语料是在买显著性**。

**排除率那条检查这次真的起了作用。** 两个子语料的 `pending_human` 率差了近两倍
（`conf2-2plus1` 5.8%，`conf2-1plus1plus1` 10.5%），但**分层**的排除率是 0.091 vs 0.095。
也就是说这个零结果不是差别性流失造出来的。

**两条 §27 预先声明的次要分析。** `scripts/v4_conf_fork.py` 没有实现它们，
补跑在 `scripts/v4_conf_fork_secondary.py`。**它们是在看过主检验之后才跑的**，
这一点如实记在这里；能这么做的唯一理由是它们的设计和方向在语料存在之前就写死在 §27 里，
现在只能执行、不能选择。防漂移的保险是脚本自带的 `--expect` 断言：

- 重算的按图聚类表必须逐字复现冻结记录 `((21,173),(15,149))` —— **通过**
- 同一条代码路径重算第二批，得到 77 / 0.286、62 / 0.161、p=0.062，
  与 §26 已发表的数字一致 —— 说明两批用的确实是同一把尺子

*次要 1，按 (spec, 名词) 格子聚类（更严的单位）*：prone 11/99 = 0.111，
never 10/69 = 0.145，单侧 p=0.8135。**方向反了。** 格子级只可能缩小 n、不可能抢救主检验，
§27 事先就是这么写的；这里它还顺带说明主检验那 0.020 连符号都不稳。

*次要 2，§26 + 第三批固定效应合并*：Mantel-Haenszel OR 1.537，单侧 p=0.07405，
双侧 p=0.1481；合并计数 prone 43/250 = 0.172，never 25/211 = 0.118。
**合并之后也不显著。** 按 §27 的规定，这句话只能写成「两次复制合并后仍未达显著」，
无论如何不得写成「已确证」。

**结论与执行**：§24(b) 在论文中永久写为
**「探索性观察，两次预注册确证均未通过」**，不做第四批，不换 backbone，不加种子。

**顺带登记一条新测量，不改 §34。** 第三批语料的 p（每张图上某个单例名词缺席的比例）
实测 `conf2-2plus1` 0.249、`conf2-1plus1plus1` 0.287，而 §33/§34 用的是 0.2201。
新的 p 让 K=8 的覆盖率**变好**（约 0.93 vs 0.863），因此 §34 的 B.2 条件更不容易触发。
**预注册没有被削弱，方向是对它有利的那一边**，所以这里只登记，不回头改 §34。

### 36. 第三次静默断链：这次不是提前退出，是死锁（2026-09-04）

Gate B 报告的等待器在 **14:48:11** 判定「batch-3 pipeline 没了，哨兵从未写出」并退出，
而 `conf_fork.txt` 是 **14:57:35** 落的盘——**早退了 9 分钟**。原因是 liveness 检查的盲区：
pipeline 的最后一步是一个 python 分析进程，命令行不匹配 `*run_conf2_pipeline*`，
于是在 shell 退出到哨兵落盘之间存在一个「按这个定义没有任何东西活着、而哨兵还不存在」的窗口。

**真正的伤害不是早退，是早退路径不写哨兵。** 报告成功或失败都会写
`runs/v4/gate-b/REPORT_DONE`（当初就是为了「报告失败也要让 L3 起来，而不是永远堵住」），
但两条放弃路径直接 `exit 1`。L3 编排器（还活着）于是在等一个永远不会有人写的文件。
**这不是前两次的「提前杀死」，是一个死锁**：所有进程都健康，链就是不往下走。

修法两条，`scripts/run_gate_b_report_after.sh`：
1. 所有退出路径统一走 `give_up()`，一律写哨兵并带上原因。**堵死下游比让下游拿到一个过期的
   Gate B 数字更糟**——后者可以重跑，前者只会烧掉整夜的卡。
2. liveness 判负要连续命中两次（间隔 120 s）才算数，覆盖上面那个窗口形状的空档。
   这**没有**把检查变正确——更长的收尾步骤照样能骗过它——它只是让 1 的降级路径成为常态兜底。

顺带补上了这个脚本缺的 `source scripts/set_h_env.sh` 与 `PYTHONPATH`：链上每一步都从各自的
启动器继承环境，只有这个等待器是单独起的，少了这两行报告会在读到第一个字节之前就死在 import 上。

**教训与前两次同类但更钝**：编排里每一个「我依赖的东西还在不在」的判断，
既要问对问题（§32 那次是问了行数而不是问进程），也要保证**答错时的降级方向是放行、不是堵死**。

### 37. L3 预告第一次跑成了 B.3,是仪器故障不是结果（2026-09-04）

**B.3 是 §34 预先写下的终止状态之一,含义是「这次运行没能执行」。它确实发生了,
但它对「L3 会不会分叉」一个字也没说。** 记录归档在
`runs/_archive/l3-preview-b3-20260904T174548/`,原样保留;缺陷登记在这里;
重测引用该目录作为出处。

**症状**:两次尝试都死在 round 0 的同一处——裁定 gold 臂时,
`v4_run_pipeline.py detect --detector qwen3vl --device cuda:0` 退出码 **3221225477**,
即 `0xC0000005` 访问违例,停在 `Loading checkpoint shards: 0%| 0/4`。

**根因是容量,不是代码**:训练进程把 backbone 常驻在 cuda:0,裁定阶段要在**同一张卡**上
再起 Qwen3-VL-8B(bf16 约 17 GB),剩下的约 11 GB 装不下。
**Windows 上加载期的分配失败经常以访问违例的形式出现,而不是干净的 CUDA OOM**——
这就是它第一眼看起来像崩溃、而不是像容量问题的原因,也是它花掉一轮生成才被看懂的原因。

**为什么会落到一张卡上**:冻结配置 `v4_l3_preview.yaml` 写的是
`generator_device: cuda:1` / `observer_device: cuda:0`,两卡方案本来装得下。
但 `run_l3_preview.sh` 的 `pick_devices()` 要求 GPU1 空闲显存 <500 MiB 才用它,
而 GPU1 上有不属于本项目的作业(先是 `baseline_table.py` 3.3 GB,16:50 又加了
`cache_reports.py`,一度共 7.9 GB)。**那道守卫是对的——不该去挤别人的活——
错的是它降级之后没人检查降级后的方案还装不装得下。**

**当时的修法(已被推翻,保留在此以记录过程)**:`Showo2Adapter.parked()`,裁定期间把 transformer 停到 CPU。
只搬 transformer:它几乎就是那 13.5 GB 的全部,搬得越少出错的面越小。
LoRA 优化器状态留在卡上,期间不会有 optimizer step,所以安全。

整个修复压在一条 torch 保证上:**`Module.to` 原地搬参数,优化器持有的引用在往返之后
仍指向同一批对象**。这条**写成了测试钉住**而不是默认成立——它一旦不成立,失败是静默的:
训练会照常继续,只是对着过期参数,不会抛错。

**实测而非推断**。模型是经 `device_map` 加载的,accelerate 把 `.to` 包了一层并给 block
挂了 `AlignDevicesHook`;读代码只能确认包装器最终会调用原函数,不能确认显存真的还回来了。
所以有 `scripts/v4_check_parking.py`,量 `torch.cuda.mem_get_info`:

| | GPU0 占用 |
|---|---|
| 加载前 | 1269 MiB |
| backbone 常驻 | 7857 MiB |
| **停放中** | **2519 MiB(释放 5338,余 22056 MiB)** |
| 恢复后 | 7897 MiB |

余量 22 GB 对 17 GB 的裁定器足够。脚本最后还跑一次真实生成——
**参数被落在 CPU 上这种错只会在下一次 forward 暴露**,不测就等于没测。

**这一整套验证是错的,见下面 §37b。**

#### 37b. 停放被证伪,以及一个跑绿了的假验证（2026-09-04 18:52）

带停放的版本在 18:52:06 拿到**第二个 B.3**,失败形态与第一次不同:
round 0 的 112 张图**全部画完**、ladder manifest 18:51 写出,然后训练进程 **SIGSEGV**。
`detect.qwen3vl.log` 从未被创建(`run_stage` 会先建日志再执行),
所以裁定子进程根本没启动——**崩溃点就在 `parked()` 里的 `model.to("cpu")`**。
主机 64 GB 内存、空闲 39.4 GB,不是内存问题。原因是搬一个
**挂了 accelerate `AlignDevicesHook`、且已注入 LoRA** 的 dispatched 模型。

**真正要记的不是这个崩溃,是那个假绿灯。** `v4_check_parking.py` 在同一台机器上、
崩溃前一小时跑出 `VERDICT: parking gives the card back and survives a forward`。
它错在**没有还原真实调用点的条件**:它在模型加载后立刻停放,没有 LoRA、没有优化器状态、
没有刚跑完的一轮生成。它验证的是一个真实运行从不经过的状态。
**一个不还原条件的检查比没有检查更糟,因为它会被相信。** 脚本已删除,不留作纪念品。

**代价**:一小时生成 + 一次 B.3。**换来的判断**:这条链需要两张卡,没有单卡变通。

**现在的做法是拒绝,不是降级**:
- `pick_devices()` 改问「生成器在 GPU1 上放得下吗(空闲 ≥ 15000 MiB)」,
  而不是原来的「GPU1 空不空(占用 < 500 MiB)」。**后者从来不是要问的问题**;
  别人的作业照样不动,但有空间的卡就是有空间的卡。放不下就 `stop B.3`,不再自作主张降级。
- `stage_train` 拿到同卡的 `--device` / `--ladder-device` 会在**几秒内**拒绝,
  而不是画完一轮图之后崩。
- 生成器放共用的那张卡(它是两个消耗里较小的,13.5 GB vs 17 GB),
  裁定器独占另一张——这也正是冻结配置写的分配。

两次失败的产物分别归档在 `runs/_archive/l3-preview-b3-20260904T174548/` 与
`runs/_archive/l3-preview-b3-20260904T185206/`,均未删除。

**第二个缺陷,顺带暴露出来的**:按 Hard Stop 把失败运行归档之后,重开在 **3 秒**内死于
`split.json does not exist`。`split.json` 只存在于运行目录里,而编排器**不生成它**——
第一次是手工跑的 `split` 阶段。也就是说这条链从来不能从零重放。
重建之后与归档那份逐字一致,digest `2500a97c…` 也与冻结配置吻合,**分区没有被动过**;
但「一个阶段依赖一个没人负责生成的文件」本身要记一笔。

**还有一条记在 `runs/_archive/l3-preview-b3-.../RELEASED_EARLY.txt`**:
这次运行是靠手写哨兵放行、与 Gate B 报告并发起跑的(用户指示)。哨兵内容写的是
`released-early` 而不是退出码,不冒充报告结果。并发的实测代价与预期相反——
队列从 1.0 压到 3–6,H: 盘聚合读吞吐从 5–6 MB/s 升到 7.7–9.2 MB/s,
因为队列 1.0 时磁头在两次寻道之间是闲着的。**§32 里那条「并发会把读跌到 0.8 MB/s」
说的是另一种形状的争用(detect 作业的大量小读),不适用于这一种。**

### 37c. 第四次失败:不是容量,是抢在了一瞬间（2026-09-04）

**四次失败没有一次是重复的**:①单卡装不下(容量)→ ②`split.json` 缺失(链条不能从零重放)
→ ③我自己那个 parking 修法段错误 → ④**这一次:输掉了一次显存竞争。**

19:59:07,`caching_allocator_warmup` 报干净的 OOM:
`Tried to allocate 15.17 GiB … 22.76 GiB is free`。

**这两个数字不矛盾。** `torch.OutOfMemoryError` 里的 free 是**抛异常那一刻**算的,
不是**尝试分配那一刻**;中间那段时间里,别人的作业把显存拿走了又还回来。
所以「22.76 空闲却分不出 15.17」读作:分配发生时确实没有,报错时又有了。

**实测排除了分配器上限**:在同一张卡上直接申请 8 / 12 / 15.17 / 18 GiB 单块,**全部成功**。
所以不是碎片、不是上限,就是那一瞬间抢输了。这台机器上一直有不属于本项目的作业
(`baseline_table.py` 系列,当天 11:30 / 17:55 / 20:18 各起一个),**它们不归我们调度,
也不该被我们挤掉**。

**修法是等,不是抢**([`scripts/v4_run_pipeline.py`](scripts/v4_run_pipeline.py) 的 `await_room`):
detect 与 crop 两个阶段在 `load()` 之前先读卡,空闲不足 `DETECTOR_MIB = 18000` 就每 60 秒复查。
18000 = 那次失败的 15.17 GiB warmup 再加上下文与激活的余量;在 24 GiB 的卡上,
它的含义是「我们愿意和别人的约 6 GiB 并存,再多就等」。

**等待关不掉那个时间窗——任何东西都关不掉。** 它买到的是另外三分之二:
不再撞进一张肉眼可见没地方的卡,也不再当那个把别人挤下去的作业。

两条性质用测试钉住,因为它们错了不会报错:

- **读数走 `nvidia-smi`,不走 `torch.cuda.mem_get_info`。** 后者会在被问的那张卡上
  建 CUDA 上下文,几百 MiB。**一个只是在排队的进程不该占着显存。**
  这条测在语法树上而不是文本上——不这样做的理由本身写在一句必须提到该函数名的注释里。
- **等待的结局永远是「开始」,不是「拒绝」。** 拒绝会把一次排队意外写成 §34 的终止条件 B.3,
  而 B.3 是关于实验的陈述,不是关于队列的。上游 `adjudicate()` 本来就有三次重试。
  读不到卡(没有 nvidia-smi / 索引不存在 / `[N/A]`)一律算「未知」并立即开始,
  **这道闸门不能变成新的卡死方式**。

**这一条与 ①②③ 的性质不同,要分开记**:前三条是结构性缺陷,已经关死了
(拒绝而不降级、split 可重建并已校验、parking 已删)。
第四条是共用机器的噪声,**关不死,只能变便宜**——重试是可续的(detect 追加写,
已写的行下轮跳过),所以抢输的代价从「一轮生成」降到「一次等待」。

**同一天 21:26 又输了一次,而这一次留下了重试确实管用的记录**
(`runs/v4/l3-preview/rounds/round-000/ladder/rfo_gold/detect.internvl.log`)。
`Tried to allocate 96.00 MiB … 13.64 GiB is free`,栈在 `detectors.py:234` 的 `.to(device)`,
`8.99 GiB is allocated by PyTorch`。日志里有**两次** COMMAND:

| | 起步读数 | 结局 |
|---|---|---|
| 第一次 | `cuda:0: 23470 MiB free, need 18000 -- starting` | 装到 8.99 GiB 时 96 MiB 被拒 |
| 第二次(等 180s 后) | 同样 `23470 MiB free` | 一路装到 21:32 被人工停止时仍在跑 |

`train.log` 对应一行:`detect internvl failed, 2 tries left, waiting 180s for the card`。
**起步条件相同、第一次死第二次活,这正是竞争的签名,不是上限也不是缺陷的签名。**
本项目的 backbone 当时在**另一张卡**上(`backbone on cuda:1, ladder on cuda:0`),
所以挤掉我们的不是我们自己。

**进程级上限也排除了,这次是静态排除**:项目的 `*.py` / `*.sh` / `*.ps1` / `*.yaml` 里
没有任何 `set_per_process_memory_fraction`、`PYTORCH_CUDA_ALLOC_CONF`、
`max_split_size`、`expandable_segments`;启动这些阶段的 shell 环境里也没有
(只有 `CUDA_PATH_V12_1` 与 PATH,无分配器配置、无 `CUDA_VISIBLE_DEVICES` 限制)。
第一次(19:59)靠**实测**排除上限——同卡直接申请 8/12/15.17/18 GiB 单块全部成功;
这一次靠**通读配置**排除。两条互补:前者证明卡能给,后者证明我们没设过不让要。

**我先前把它判成「不是容量,也不是 §37c 的竞争」,那半句是错的**——当时只看了单次报错,
没看第二次 COMMAND,也没看 `train.log` 的重试行。它就是 §37c 的第二个实例,
**没有新的代码缺陷要修**,`await_room` 与三次重试都按设计工作了。
§37c 说过等待关不掉那个时间窗;这次的记录补上了它关得掉的那部分是真的。


### 38. 训练选择器没有在选择：天花板效应，以及它作废了什么（2026-09-04）

**主结论：现行选择器在两个独立子集上都贴着随机，原因是构造决定的天花板，
不是模型能力，也不是样本量。** 所有依赖「自评价选出了更好的图」的下游量，
在修好它之前量到的都是平分规则。

**本节每个数字都由 `scripts/v4_selector_resolution.py` 从冻结记录复算**
（`envs/core/python.exe scripts/v4_selector_resolution.py --expect`）。
`--expect` 把下面的表写死在脚本里：记录变了、或脚本口径漂了，它拒绝出数而不是
悄悄给一组新数。2026-09-04 全部复现。

**证据一，选中正确图的比例**（`runs/v4/gate-b/`，自然「有对有错」池）：

| | K=4（138 池） | K=6（94 池） |
|---|---:|---:|
| 均匀随机期望 | 40.2% | 34.2% |
| **naive 自评价** | **39.1%**（54/138） | **34.0%**（32/94） |
| rfo（Qwen2-VL-2B，冻结，盲） | 55.1%（76/138） | 51.1%（48/94） |
| gold（qwen3vl+internvl 阶梯） | 100% | 100% |

`gold` 就是外部正确性标准本身，100% 是构造出来的，**它不是观察者**。
`rfo` 与 naive 差的是**两件事**——换了模型 *且* 看不到提示词，两者拆不开。

**证据二，天花板。** naive 的分数只有三个取值：

| 分数 | 候选数 | 占比 |
|---:|---:|---:|
| 1.000 | 928 | **83.2%** |
| 0.667 | 171 | 15.3% |
| 0.333 | 17 | 1.5% |

140 个「全部候选同分」的池里，**135 个同分在 1.000**。池内比较：正确候选均分与
错误候选均分**完全相等的有 142/232（61.2%）**，能动的时候方向是对的（81 : 9），
平均差 +0.052。**尺子不是坏成随机，是坏成饱和——信号在，被压在天花板上。**

**机制，在构造里一眼可见**（`src/selfsight/v4/probe.py`）：exists 题的 gold 永远是
yes，count 题的 gold 永远是 `wanted`。**只要 gold 完全由 spec 决定，一个完美的
提示词复读机就在每个候选上都拿 1.0——这是数学后果，与题目多少、难易无关。**

顺带查实的三条覆盖缺口：计数题只在 `count > 1` 时构造（「要一个画了两个」没题问）；
计数题是 `wanted` vs `wanted-1` 二选一（要 2 画 5，答 two 照样算对）；
没被要求的物体多画出来无题可问。**「规则没检查该事实」与「模型看了仍答错」是两件事。**

**证据三，Gate B 与它一致。** `GDA-free` = cos(g_naive, g_rfo) = 0.9699，
CI 宽 0.1285 > 0.10，**Gate B 未通过的是这一项**。不同准则挑出的几乎是同一批图
（最高分唯一的池只有 8/232），梯度自然指向同一个方向。
`probe.py` 自己的 docstring 早就写下了这个失效签名。

**Gate B 失败后注册的补救是「增加 informative pools 直到 CI 收窄」——
而 informative pool 只有 8 个。补救办法要求的东西正是缺的东西。**

**`GDA-gold`（= cos(g_naive, g_gold) = 0.9806，CI 宽 0.0884）通过了宽度判据。
但它需要真值。**把它提为 D_g 的主检测器会给项目增加对标签的依赖，
**不能再沿用原 proposal 的无标签主张**；它只能作机制参照。

**未经筛选的自然池构成**（456 池，四个 run，源自各 run 的 `verified.jsonl`）：

| | 全对 | 有对有错 | 全错 |
|---|---:|---:|---:|
| 合计 456 | 8（1.8%） | 301（66.0%） | **147（32.2%）** |
| K=4（228） | 5（2.2%） | 138（60.5%） | **85（37.3%）** |

`runs/v4/gate-b/pools.jsonl` **本身已是筛过的**（232 个全是有对有错），
从它出发算不出这张表。**K=4 有 37.3% 的池里没有任何正确图可选**，
完美选择器在全部自然池上的天花板也只有 62.7%。只报有对有错子集会让 39.1% 被断章取义。

#### 38.1 一次探索性观察：提示词让选样变好，方向与假设相反

`answers.prompted.jsonl` 与 `answers.jsonl` 已存在：**同一模型、同一批图、同一套题，
只差看不看得到提示词。**134 个自然有对有错池（main 两个 run；conf 两个 run 缺
prompted 条件）：

| | 选中正确图 |
|---|---:|
| 均匀随机 | 41.7% |
| 看得到提示词 | 59.0%（79/134） |
| 看不到 | 49.3%（66/134） |

精确 McNemar（不一致 20 : 7）双侧 **p = 0.0192**。

**这是未预注册的事后比较，按本项目自己的 Hard Stop 只能记为探索性观察，
不得当作结论**——§24(b) 正是这么栽的。三条保留必须同时写下：题集是从**检测**
构造的（gold 随图变），不是训练选样用的那套；只有 4 个 run 里的 2 个有 prompted 条件；
平分规则（取最小 candidate_index）是本次分析选的，不是项目既有约定。

即便如此，它足以说明一件事：**proposal 里「意图泄漏腐蚀自反馈选样」的符号，
在唯一能直接检验它的地方是反的。**这条要写进 proposal 作为待检验问题，
不是作为已知机制。

#### 38.2 两条需要更正的既有表述

**§31 的样本量规划锚在 +4.17 pt 上。** 那个数是 v2.2 探索闭环、单 seed、K=2、
10 步的工程证据。它不足以支撑精确的样本规划，这一点 §31 自己写了。
**但目前没有证据把它归因于平分规则**——本节量的是 K=4/K=6 下当前评分器的并列率，
把结论跨设置外推到 K=2 的旧运行是推断，不是测量。修好评分器后需要重测效应量，
§31 的表随之重算。

**`estimate_d_star` 的实现。** 它不是「对两条曲线各定位一个折点再求交集」。
它取两组**允许搜索的折点位置**的交集，在每个共同位置上同时拟合两条曲线，
要求 `internal.slope_after > 0` 且 `external.slope_after <= 0`，取残差最小者
（`src/selfsight/analysis/breakpoints.py:104`）。**脱钩条件已经写在代码里了**，
要补的是它上面的推断层：外部斜率的 CI 含零不等于停滞（需要预先定义
最小有意义改善并做等价检验）；「内部−外部」斜率为正不等于脱钩（两条都涨、
内部更快也满足，且两者量纲不同）；配对不保证降方差
（`Var(R−Q)=Var(R)+Var(Q)−2Cov(R,Q)`，取决于协方差）；
每个 checkpoint 检一次要处理重复监测的误报。

**下一步的预注册见 `docs/prereg/2026-09-04-selector-resolution.md`**，
阈值在改 `probe.py` 之前定死。

**Proposal 已据此改到 3.5（2026-09-05），只改论证不改设计**：①③降为待检验命题、
D\* 重定义为效果脱钩并要求与普通收敛/随机波动分开、D_g 与在线报警步 A 拆开
（`Lead = D* − A`，四向记账，`T_max` 兜底删除）、Gate B 的补救改为「先修评分器」、
Gate C 加四条前置条件、§38.1 作为 §1.1 与 Q8 正面写入。
### 39. 重跑前的三个阻塞项：一个修好了，一个是缺口，一个不是缺陷（2026-09-05）

**39.1 两臂在第 0 轮不是从同一权重出发（已修）**

`stage_train` 用一个常驻 backbone 轮流跑两臂，靠「载入该臂上一轮的 checkpoint」换权重。
第 0 轮两臂都没有上一轮，于是三处 `if previous.exists(): load_checkpoint(...)` 全部
**什么也不做**——而 `ARMS = ("naive", "rfo_gold")`，naive 先训练并改掉共享参数，
`rfo_gold` 的第 0 轮**从 naive 更新过的权重起步**，此后每一轮通过它自己的 checkpoint 继承下去。

三处里其实只有第三处（训练）真的被污染：前两处（生成候选、选样）在第 0 轮时权重确实还是 base，
因为生成和选样都不改权重。但**三处走同一个 helper 才是对的**，
`if ... exists()` 而没有 else 是这个缺陷的书面形式，手写第四次照样会错，而且不报错。

修法：`capture_base_state` / `restore_base_state` / `restore_arm_state`
（`src/selfsight/training/checkpoint.py`），在 `attach_lora` 之后、任何训练之前抓一次 base；
没有上一轮 checkpoint 时**恢复 base，而不是什么都不做**。
RNG 一并抓——第 1 轮起 `load_checkpoint` 本来就会恢复各臂的 RNG，
第 0 轮若跳过，两臂会在唯一应当完全相同的那一轮里抽到不同的 LoRA dropout。

**过程中发现的第二件事，比第一件更值得记**：`lora_state_dict` 里的 `.detach().cpu()`
在 CPU 张量上是**恒等操作，不是拷贝**，所以第一版 `capture_base_state` 存的是活引用，
`optimizer.step()` 会把「base」一起改掉。`save_checkpoint` 侥幸没事，因为它马上序列化。
**后果是第一版测试全绿而毫无内容**——比较的两边是同一个张量，恒等永远成立。
测试里的取值也改成 `clone()`，并单独跑了一遍旧行为确认它真的会红
（旧代码下 rfo_gold 从 `[11.0, 8.0]` 起步，base 是 `[1.0, -2.0]`）。
守住它的是 `tests/test_round_zero_weights.py`（8 项），含一条语法树检查：
`stage_train` 内不得再直接调用 `load_checkpoint`。

**39.2 被优化的那个信号，从未在结果集上测过（缺口，未修）**

这里有三个量，此前被当成两个：

| 记号 | 是什么 | 在哪测 |
|---|---|---|
| `s_select` | 各臂的选样奖励（naive：看得到提示词的原子题正确率） | **训练池**，每轮，存在 `SelectionDecision.scores` |
| `internal_cycle` | `log p(prompt \| image)` | 结果集，每 checkpoint |
| `external_correct` | 判别阶梯的正确率 | 结果集，每 checkpoint |

**D\* 用的是 `internal_cycle`，不是 `s_select`，而且这是有理由的既定选择，不要改**：
原子题形式正是 RFO 臂的选样目标，内部曲线若也用它，画的就是某一臂自己的目标函数
（`tests/test_v4_evaluate.py:162` 写着理由，v2.3 正是这么错过一次）。

**但缺口是真的**：过度优化的故事是「**被优化的那个信号**继续涨、真值不涨」，
而 `s_select` 只在训练池上按轮记录，不进 `CheckpointMetrics`，不进任何曲线。
所以当前设计能回答「循环一致性与正确率是否脱钩」，**不能**回答「自评价奖励与正确率是否脱钩」——
后者才是摘要里那句话。补法是**加第三条曲线，不是替换**：每个 checkpoint 在结果集上另测
`s_select`。这需要模型重新答题，是 GPU 成本。Proposal 3.5 §4.1 已登记。

**39.3 internvl 的 96 MiB 拒绝：不是缺陷，是 §37c 的第二个实例**

见 §37c 末尾的补记。两次 COMMAND、起步读数相同、第一次死第二次活，是竞争的签名。
`await_room` 与三次重试都按设计工作了，**没有代码要改**。我先前判它「不是 §37c 的竞争」是错的。

### 40. 第 3 步：把 ② 换成开放计数，以及审计给出的坏消息（2026-09-05）

**40.1 改了什么**

`spec_questions` 原先对每个物体出一道存在题，只对 `count > 1` 的物体追加一道
`A. two / B. one` 的二选一计数题。现在**每个物体两道**：存在题仍是二选一，
计数题改成开放式（`How many Xs...? Answer with a single number.`，gold 是 `str(obj.count)`）。

刀落在**回答方式**，理由要说准：**天花板不是靠「让复读答错」打破的**。
复读按构造就和 spec 一致，而 spec 就是 gold。它是靠**让「看」能给出一个不同的答案**打破的，
而二元形式恰恰禁止了这件事——图里画了五个杯子，模型只能答「二」或「一」。

配套两处：`data/questions.py` 的 `NUMBER_WORDS` 从 zero–four 扩到 twelve（含 none/no），
否则一个看得见的「six」会解析失败→弃答，而 `observation_score` 把弃答从分母里剔除，
这个错就不花候选任何代价——正是要修的那个失效模式。另外旧代码把计数题的 `family` 写成
`existence`（存量记录里可查），意味着它的回退词表是 yes/no；靠字母匹配才没出事。现已改为 `COUNT`。

**40.2 覆盖审计（`scripts/v4_question_coverage.py`，CPU，232 池）**

| | 之前 | 之后 |
|---|---|---|
| 每池题数 | 3（全部 232 池） | 4（116 池）/ 6（116 池） |
| 可达分值数 | 4 | 5 / 7 |
| 计数原子 | 116 | 580 |
| 无人过问的单数物体 | 464（占全部物体 80.0%） | 0 |

语料只有两种形状，各 116 池：`2plus1`（两物体，其一 count=2）得 2 存在 + 1 计数；
`1plus1plus1`（三个单数物体）得 3 存在 + **0 计数**。所以此前**有一半的池，一道计数题都没有**，
三道题全是「图里有没有 X」而 X 正是提示词点名要的东西。
每池 3 题也解释了 §38 那个三值分布：`correct/3` 只能取 0、1/3、2/3、1。

**40.3 一个被证伪的预测（保留，不回填）**

按上面的说法，自然预测「没有计数题的那 116 池最退化」。**测出来是反的**：

| | 池数 | ceiling | all_tied |
|---|---|---|---|
| `2plus1`（有 1 道计数题） | 116 | 91.1% | 75.9% |
| `1plus1plus1`（0 道计数题） | 116 | 74.9% | 44.8% |

预测漏掉了自己构造里的混淆：两种形状差的不只是题型，还有**场景本身多难画对**。
三个互不相同的单数物体比「两个杯子加一个盘子」更容易画错，于是存在题变化更多
（21.6% vs 11.6%，答错率 8.6% vs 3.9%）。这是场景难度效应，不是题目设计效应。

**固定住池**的对照才说明问题——同一批 `2plus1` 池、同一批候选、同一个观察者：

| 题型 | 在候选间给出不同答案 | 答错率 |
|---|---|---|
| 存在题 | 11.6% | 3.9% |
| 二元计数题 | **5.2%** | 3.3% |

二元计数题是这套题里**最不能区分**的一道，区分力不到存在题的一半。改它是对的，
但成立的理由是这条对照，不是 40.3 开头那个被证伪的预测。

**40.4 新题要付的账（本次审计的主要产出）**

`all_tied` 精确等于「没有任何一道题在候选间变化」（44.8%/75.9% 逐位重现，
没有两处差异互相抵消的池）。据此可以算出二元计数题究竟买到了什么：
`2plus1` 的存在题单独留下 77.6% 的池不可分，加上计数题后是 75.9%——
**二元计数题在 116 个池里只解开了 2 个（+1.7pp）**。

于是：`all_tied` 现在 60.3%，预注册要求 ≤ 25%；而存在题单独就留下 61.2% 的池不可分，
**新构造完全没有动存在题**。也就是说，新的计数原子必须**几乎独自**解开这 61.2% 里的绝大部分。
这是个很硬的目标。它在重跑前登记于此，不在重跑后才被发现。

**40.5 这是一次仪器变更**

按 Hard Stop：旧记录原样保留，变更登记在此，任何重新测量的数都标注来源。
§38 与参考锚点（58.6% 天花板等）读的是存量 `correct` 字段，不受影响。
`scripts/v4_selector_resolution.py --expect` 仍复现 §38 全部数字（已验证）。

**40.6 一个只有上 GPU 才能测的风险**

开放计数比二选一更容易答出词表外的东西→弃答，而弃答被移出分母。
分母变小会**抬高**满分率。方向已知，大小未知，必须实测。

### 41. 第 4 步实测：分辨力判据未通过，但仪器确实动了（2026-09-05）

`runs/v4/gate-b-openct/`。判定脚本 `scripts/v4_resolution_verdict.py`，
**在看到任何新数之前提交**（`96fce93`），门槛照抄预注册第 2 节，未改动。

**41.1 跑的是什么**

232 池 / 1116 候选，逐条比对确认与冻结集**同一批池、同一批图、同一批裁定、同样顺序**，
只有题变了（696 → 1160 题）。观察者 ID 与 revision 两次运行完全一致
（rfo: Qwen2-VL-2B @ 895c3a49 盲；naive: show-o2-1.5B @ 07ec1658 带提示词）。
两臂各 5556 条回答，**0 错误、0 弃答**。rfo 在 cuda:0、naive 在 cuda:1，并行。

**未改动的那一半仪器复现了自己**：存在题逐条一致率 rfo 99.89%（1812/1814）、
naive 99.60%。所以下面指标的变化归因于计数题，不是漂移——rfo 这次还换了卡。

**41.2 判定：未通过（naive 臂，即预注册表里的「现行」那一列）**

| 判据 | 门槛 | 冻结现行 | 本次 | | 池数 |
|---|---|---:|---:|---|---|
| \*正确 > 错误 的池 | ≥ 60% | 34.9% | **61.6%** | PASS | 81 → 143（门槛 140，**只多 3 池**） |
| \*正确 = 错误 的池 | ≤ 25% | 61.2% | 29.7% | FAIL | 142 → 69（门槛 58，**差 11 池**） |
| 全部候选同分 | ≤ 25% | 60.3% | 25.9% | FAIL | 140 → 60（门槛 58，**差 2 池**） |
| 候选落在满分 | ≤ 70% | 83.2% | **59.9%** | PASS | 锚点 58.6% |

**按预注册第 3 节记录未通过。不得在看过数之后调题目直到通过。**
两条 PASS 里有一条是擦边过的：预注册自己写了 n≈232 时 SE≈3.3pp，61.6% 距门槛不到半个 SE，
不应当作稳健通过来引用。

分值分布从三值变成七值：
`{1/3:17, 2/3:171, 1:928}` → `{1/4:1, 1/3:1, 1/2:68, 2/3:73, 3/4:122, 5/6:183, 1:668}`。

**41.3 机制确实按 §40.1 说的那样工作**

计数题答案里，**旧二元形式结构上无法给出的**占 rfo 21.6% / naive 16.1%
（最常见是 `gold=1 说=2`：要一个、看见两个）。计数题答错率 3.3% → 32.8%(rfo) / 17.4%(naive)。
盲臂错得比带提示词臂多一倍,这个差本身是提示词泄漏的一个度量。

**§40.6 登记的弃答风险没有兑现**：0 弃答，两个模型都答裸数字。
`NUMBER_WORDS` 扩表因此没有成为承重件——它仍是对的防御，但不能记为它带来了改善。

**41.4 盲观察者 rfo 臂（门槛未注册，不构成判定）**

| 判据 | 门槛 | 旧题 | 新题 |
|---|---|---:|---:|
| 正确 > 错误 | ≥ 60% | 72.0% | **84.9%** |
| 正确 = 错误 | ≤ 25% | 27.2% | **5.2%** |
| 全部同分 | ≤ 25% | 25.9% | **3.4%** |
| 候选落在满分 | ≤ 70% | 67.0% | **32.6%** |

四条全部远超门槛，三条优于参照锚点。**但这不是判定**：预注册把门槛挂在 naive 臂上
（34.9/61.2/60.3/83.2 只在该臂逐位吻合），rfo 的门槛从未注册，
事后拿它宣布通过就是看过数之后换判据。**登记为下一版预注册应当处理的问题**：
门槛当初挂在了最差的那个臂上，而机制真正用的是 rfo 臂，它在旧题下就已接近达标。

**41.5 代价，一并报告**

「错误候选反而更高分」的池两臂都涨了：naive 9 → 20，rfo **2 → 23**。拆开看：

| | 原平分池中新解开 | 方向正确 | 方向错误 | 原本正确、现在反了 |
|---|---|---|---|---|
| naive | 78 | 67（85.9%） | 11 | 2 |
| rfo | 56 | 46（82.1%） | 10 | **12** |

新解开的池八成以上朝正确方向，但 rfo 有 12 个池**从正确翻成了错误**。
分辨力提高必然同时提高判错的机会，这条不能只报前半句。

**41.6 选中正确图的比例（结果，不是判据，预注册明令不设门槛）**

| | 随机期望 | naive 旧→新 | rfo 旧→新 |
|---|---:|---|---|
| K=4 (n=138) | 40.2% | 39.1% → 48.6% [40.4, 56.8] | 55.1% → **65.9%** [57.7, 73.3] |
| K=6 (n=94) | 34.2% | 34.0% → 41.5% [32.1, 51.6] | 51.1% → **62.8%** [52.7, 71.9] |

naive 的区间两次都包含随机期望，**不能说它优于随机**。
rfo 的区间两档都排除随机期望，且比旧题明显提高。
三判据一致率：naive_vs_rfo 68.5% → 50.9%，naive_vs_gold 37.1% → 43.1%，rfo_vs_gold 52.6% → 59.1%。

**41.7 本轮不做的事**

梯度阶段没跑：预注册第 4 节把它排除在本轮之外（它在评分器下游）；
且磁盘只剩 43.9 GB，三个梯度文件需要 51 GB。冻结的 `runs/v4/gate-b/` 原样保留。

### 42. 计分缺陷：题目问的是类别，gold 却按单个条目算（2026-09-06）

**复核发现，不是我发现的。** §41 的数字**原样保留**——它们是「有缺陷的仪器产出了什么」的
冻结记录，不重写。本节登记缺陷本身，并更正 §41 里**我对结果的解释**（数字对，解释错）。

**42.1 缺陷**

`spec_questions` 问的是 `How many {noun}s`（无颜色限定的类别总数），
gold 却取某一个颜色条目的 `obj.count`。`canonical_noun` 按设计把 `notebook` 归到 `book`
（`spec.SYNONYMS`，那个映射本身是对的，见其 docstring）。于是
`a red book, a blue notebook, a yellow candle` 生成了**两道一字不差的题**，
都要求答 1，而正确的图里就是 2 本书。

**看对图、数对数的模型两道全失。** 该池上限:完美观察者看完美图只能得 4/6。
另有 2 个池（`main-2plus1:v4a-0089`、`conf-2plus1:v4a-0043`，均为「一本红书+两本 notebook」）
同一句话同时要求答 1 和 2——**逻辑上无解**，无论图里是什么、模型说什么，必有一分拿不到。

范围:**24/232 池、112 张候选图、每臂 224 条计数回答**。

这正是本次改动要消除的失效模式，结果却制造了它:**它把「看对了」判成错**。

**42.2 它改变了 §41.5 的解释**

保留原题、原回答、原权重、原选图规则，只在 CPU 上把目标数改成题目所问类别的**总请求数**
（诊断重算，不替代预注册结果）:

| | 反向池 | 正向池 | 平分池 |
|---|---|---|---|
| naive | 20 → **12** | 143 → 152 | 69 → 68 |
| rfo | 23 → **12** | 197 → 212 | 12 → 8 |

两臂各有约一半反向池来自计分错误。**§41.5 把这些反转整体解释为「分辨力提高的代价」是错的。**
即便如此修正，naive 的平分池与全同分池**仍未达到原门槛**，§41 的「未通过」裁定不变。

**42.3 §41 中其他需要更正的表述**

| §41 的说法 | 更正 |
|---|---|
| 计数偏离率 rfo 32.8% / naive 17.4% | 全量是 **34.20% / 16.45%**。我引用的是跑到约六成时的部分数据，不该那样引用。 |
| 把它叫「答错率」 | 它是**与当前 spec 目标不一致率**，不是看图答错率。二者必须分开统计（见 42.4）。 |
| `gold=1 说=2` = 「要一个、看见两个」 | 不一定。rfo 这类回答 460 条中有 **140 条**，题目所指类别在请求中本就共 2 个——那些回答其实看对了图。 |
| 两臂之差是提示词泄漏的度量 | **不成立**:rfo 用 Qwen2-VL、naive 用 Show-o2，模型与提示条件同时变化，不能单独归因。 |
| 存在题完全没改，变化全归因于计数题 | **过强**。旧代码为二元计数题多消耗一次 RNG 抽样，共享随机流错位，**7 道存在题的 A/B 顺序变了**。高度复现（99.89%）成立，「完全没改」不成立。 |
| naive 两档区间都包含随机期望 | K=4 下界 **40.36%**，随机期望 **40.22%**，区间实际排除。算术错了。但 0.15pp 的边界差同样支撑不了结论。 |
| rfo 达标说明门槛挂错了臂（未决问题 6） | **过强**。验收哪一臂应由它在研究中承担什么测量任务决定，不能由「哪个臂达标」倒推。已改写该条。 |

**42.4 「偏离请求」与「数错图」是两个数**

用 `verified.jsonl` 的 `detections`（图里实际检出什么）作独立 gold，重判同一批回答:

| | 偏离请求 | 数错图 |
|---|---:|---:|
| naive（带提示词） | **16.5%** | **32.1%** |
| rfo（盲） | 34.2% | 24.7% |

**带提示词的臂更忠于提示词、更不忠于图;盲臂反过来。**
臂间比较仍受「模型与条件同时变化」的混淆，**但臂内那个反向差不受**——
同一个模型、同一批回答、两套 gold。这比 §41 里那句被否掉的「泄漏度量」有根据得多。
`v4_resolution_verdict.py` 现在把两个数并排报告。

**42.5 修复与测试**

题目加颜色限定:`How many red books are in this picture?`，gold 是该条目的 count。
这既让题与答对应，又保留了颜色×数量的绑定检查——而且比无限定的类别总数**更难靠复读答出**。
另外按题面短语分组求和:本语料中颜色+规范名同时相同的条目有 **0 条**（2280 条 spec 全查），
所以分组永不合并;写它是为了让「正确的图被正确观察必得满分」**结构上成立**，
而不是碰巧因为语料里没有坏样本。

测试补了 4 个函数 / 8 项（`tests/test_v4_probe.py`），核心是 `_truthful_answers`:
按 gold 判定用的同一套 (颜色, 规范名) 多重集规则从 spec 造出场景，
如实回答每道题，**要求满分**;对三个真实同义词提示词逐一跑。
**先验证对旧代码全红**:7 项失败，覆盖全部 4 个新测试函数。
（`v4b-0018` 的矛盾性检查在旧代码下正确地保持绿色——它两个 book 条目都要 1，
只有 count 不同才构成矛盾，这说明该测试判别力对。）

重建题目后:**重复计数题 24 池 → 0，矛盾 gold 2 池 → 0**;
存在题与上一轮**逐道完全一致**（A/B 与题面均未变），所以重测在未改动的那一半上完全配对。

**42.6 有边界的重测:仍未通过,但每个数都更好了**

`runs/v4/gate-b-openct2/`。全新回答(未复用任何旧答案),两臂各 5556 条,**0 错误 0 弃答**。
232 池与冻结集逐条配对一致。

| 判据 | 门槛 | 冻结现行 | 缺陷版 | **修正版** | |
|---|---|---:|---:|---:|---|
| \*正确 > 错误 | ≥60% | 34.9% | 61.6% | **64.7%** | PASS |
| \*正确 = 错误 | ≤25% | 61.2% | 29.7% | **29.3%** | FAIL |
| 全部候选同分 | ≤25% | 60.3% | 25.9% | **26.7%** | FAIL |
| 候选落在满分 | ≤70% | 83.2% | 59.9% | **64.2%** | PASS |

**裁定不变:四条过两条,未通过。** 缺陷不是「未通过」的原因——修掉它之后仍未通过。

池数(n=232):

| | 正向 | 平分 | 反向 | 全同分 |
|---|---|---|---|---|
| naive 冻结/缺陷/修正 | 81 / 143 / **150** | 142 / 69 / **68** | 9 / 20 / **14** | 140 / 60 / **62** |
| rfo 冻结/缺陷/修正 | 167 / 197 / **209** | 63 / 12 / **10** | 2 / 23 / **13** | 60 / 8 / **9** |

**未决问题 7 的答案**:反向池 naive 20→14、rfo 23→13。§41.5 报的量约一半来自计分缺陷,
但剩下的一半是真的(相对旧二元题的 9 和 2 仍是上升)。当初没有为这个量设过门槛。

选中正确图(结果,非判据):rfo K=4 **70.3%** [62.2, 77.3]、K=6 **69.1%** [59.2, 77.6],
两档都排除随机期望(40.2% / 34.2%);naive 51.4% [43.2, 59.6] / 45.7% [36.0, 55.8]。
判据一致率 rfo_vs_gold 52.6% → 59.1% → **64.7%**。

> 后补更正（§42.10）：以下旧事实错误率及颜色限定的因果解释失效，原文留作记录。
> 上面的全232池实测分辨力判定不受本次事实诊断修复影响。

计数题:naive 偏离请求 12.9% / 数错图 31.1%;rfo 27.9% / 22.8%。
注意 naive 的偏离请求**下降**了(16.5%→12.9%)而数错图几乎没变(32.1%→31.1%)——
**加颜色限定让看得到提示词的臂更容易复读**(gold 几乎都是 1 或 2,直接读提示词即可),
同时让盲臂更准。这是颜色限定的一个副作用,当初没有预计到。

**42.7 剩下的分辨力不足来自哪里:已测,不是题的问题**

> 后补更正（§43.1、§42.10）：本节旧事实恢复与全池“完美观察”有缺陷；
> “短板全在观察者”“题目不需要改”的排他性推论撤回。下表不作现行证据引用。

`scripts/v4_residual_diagnosis.py` —— 仍平分的池,按「本来有东西可看吗」拆:

| | 仍平分 | 题内可达 | 题外 | 不可见 |
|---|---|---|---|---|
| naive | 68 | **66** | 2 | 0 |
| rfo | 10 | **10** | 0 | 0 |

几乎全部是**题内可达**:图之间确实不同,题也正指着那个短语问了,**回答却没把差异反映出来**。
「不可见」为 0——没有一个池是「多重集层面根本没差别、只能靠 image-gold」。

`scripts/v4_observation_ceiling.py` —— 把回答换成 `detections` 给出的真值(观察 100% 准确),
同一套题、同一套 gold、同一套计分:

| 判据 | 门槛 | naive | rfo | 完美观察 |
|---|---|---:|---:|---:|
| 正确 > 错误 | ≥60% | 64.7% | 90.1% | **99.1%** PASS |
| 正确 = 错误 | ≤25% | 29.3% | 4.3% | **0.9%** PASS |
| 全部同分 | ≤25% | 26.7% | 3.9% | **0.9%** PASS |
| 满分候选 | ≤70% | 64.2% | 41.0% | **38.4%** PASS |

**四条全部通过。题目能表达这个裁定;短板是观察准确率。**

必须同时声明的保留:这是**诊断,不是实验结果**。读检测器答案的观察者不在测自我确认,
且 detections 与裁定同源,所以这是「spec 侧评分器最多能从该裁定里恢复多少」的**上界**,
不是真实观察者的预期值。**任何情况下不得把 99.1% 当作实验结果引用。**

**结论:下一刀不落在题目上。** 我先前提的「改存在题 or 转 image-gold」两个方向针对的都是题面,
都打偏了——题已经问对了地方(题内可达 66/68、10/10),且完美观察下四条全过。
真实观察者数错图的比例是 rfo 22.8%、naive 31.1%,这个噪声足以吃掉全部差距。
功夫该花在**观察准确率**上,而不是重写题目。具体做法需要新的预注册,
且必须在看数之前决定验收哪个臂(未决问题 6)。


**42.8 差多少:约 1.2 个百分点的观察准确率**

> 后补更正（§42.10）：74.8% 继承未知当零的缺陷，且统计全部原子题，并非只统计计数。
> “差1.2pp”及更强观察者曲线会更有利的推论撤回。原表保留，不作为新实验目标。

`scripts/v4_accuracy_sensitivity.py`。把回答按比例换成 `detections` 的真值(20 种子平均),
横轴取**与 detections 的实测一致率**——那才是真实可移动的量。

naive 臂(受判据约束的那个):

| 混入真值 | 数对图 | 正确>错误 ≥60% | 正确=错误 ≤25% | 全部同分 ≤25% | 满分候选 ≤70% |
|---|---:|---:|---:|---:|---:|
| 0%（实测） | 74.8% | 64.7% ✓ | 29.3% ✗ | 26.7% ✗ | 64.2% ✓ |
| 2% | 75.4% | 67.9% ✓ | 26.5% ✗ | 23.9% ✓ | 62.6% ✓ |
| **5%** | **76.0%** | 71.3% ✓ | **23.7% ✓** | **21.2% ✓** | 61.2% ✓ |

**四条同时达标的交叉点在数对图约 76%,即比现状高约 1.2 个百分点。**
rfo 臂在 0%（80.8%）就已四条全过。

三条必须同时说明的保留:
- **这不改变裁定。** 预注册的判定是「未通过」,本表是给下一版预注册设目标用的规划输入,
  不是结果,更不是「差一点就过了所以算过」。
- **不是预测。** 它假设被修好的错误是随机子集;更强的观察者会先修容易的,
  真实曲线在低比例处大概率更有利,此表低估了小幅改善的作用。
- **继承循环性。** 混入的真值与裁定同源,与 §42.7 的天花板同一条保留。

另外交叉点处的余量很薄(23.7% / 21.2% 对门槛 25%),n=232 时 SE≈3.3pp,
下一版预注册若以此定目标,应当把余量而不是交叉点当作目标。

**42.9 §42.8 那 1.2 个百分点买不到:naive 臂的错误不是噪声,是复读**

> 后补更正（§42.10）：本节旧事实分层含314条未知计数事实；“同臂无混杂”、
> 已识别提示词泄漏、强化观察者不能改善等推论均超出证据，撤回。原文保留。

`scripts/v4_recitation_split.py`。§42.8 把回答按比例换成真值,这只有在被换掉的错误是
**噪声**时才是「更强观察者」的合理模型。这一节检验这个前提,办法是按一个观察者无法控制的
变量分组:图碰巧是否符合请求。

| 臂 | 图 == 请求 n=1830 |  | 图 != 请求 n=948 |  |
|---|---:|---:|---:|---:|
| | 说请求值 | 数对图 | **说请求值** | **数对图** |
| naive（Show-o2-1.5B,看得到提示词） | 95.8% | 95.8% | **70.3%** | **16.9%** |
| rfo（Qwen2-VL-2B,盲） | 93.3% | 93.3% | **31.1%** | **46.1%** |

只有右半边有判别力:左半边两个答案重合,答对不说明看了。**同一臂内左右对比是干净的**
——同一个模型、同一套题,唯一差别是图碰巧是否符合请求。(跨臂比较不干净,模型与条件一起变,
见 §42.3;这里不用它。)

**naive 在图与请求分歧时,70.3% 说的是请求的数,只有 16.9% 说对了图。**
错误方向也一致:说的减真实为 +1 的 507 次、+2 的 183 次,−1 只有 71 次——
噪声会散在真值两侧,这是聚在请求值上。两臂在「图!=请求」时都退化(难度是真的),
但 naive 退 78.9pp 且退向请求值,rfo 退 47.2pp 且散开。

**推论:§42.8 的交叉点在 naive 臂上不可达。** 那张表按比例混入真值,建模的是一个
把错误随机改对的干预;而 naive 的错误里约 77% 是复读 spec 的数(全部计数错误口径),
在决定性的那 948 题上是 70.3%。更强的观察者、重复采样、更高分辨率都不动这个量——
要动它得拿走提示词,而拿走提示词它就是 rfo 臂了。

**§42.7「短板是观察准确率」需要按臂拆开重述**:
- **rfo 臂**:成立。它是盲的,观察者是自由变量,可以换更强的模型(`Qwen2.5-VL-7B` 已在盘上)。
  但它在现有题下四个数已经是 90.1 / 4.3 / 3.9 / 41.0——**门槛没有注册在 rfo 上,
  这不构成通过**(预注册 §2 的「现行」列只在 naive 上复现)。
- **naive 臂**:不成立。它的缺口不是观察准确率,是提示词泄漏,而泄漏正是这条臂被设计出来
  要展示的东西。**对它做观察者侧的改进是在修一个不是缺陷的东西。**

§42.8 原样保留(旧记录不回填),但**不得再单独引用它的「差 1.2pp」作为 naive 臂的目标**。

这把未决问题 6 从设计偏好变成了可测的事:naive 臂在计数上的行为与「泄漏对照」一致,
与「被验收的选择器」不一致。**但由此直接把门槛改挂到 rfo 上仍然是由结果倒推判据**
(§42.3),该决定属于下一版预注册,且必须在动手前作出。

**42.10 争议事实的历史诊断遗漏与重算（2026-09-06，后补更正）。**

用户再次指出 disputed 缺项造成的假零。复核代码历史：旧 `40d4625` 的
`detections()` 仅保留物体计数，丢掉 `resolution/disputed` 元数据；不是核验器把
已知零算错，而是诊断把整图共识记录中仍未知的类别当成零。`factual_truth` 与
`v4_observation_ceiling.py` 已在 `8ee6b97` 修复，并包含于本次审查前 HEAD `e351211`；
但 `v4_recitation_split.py` 和生成§42.8的 `v4_accuracy_sensitivity.py` 漏改，仍导入
已删除的旧函数，在当前版本会直接 ImportError。用户指的风险成立，当前遗漏文件是
后两者；不重复修改已修好的 ceiling，也不修改运行中的12项冻结实验源码。

本次两脚本统一按实际题面和完整 verification 取事实：`agreed_verdict` 中范围相关的
disputed 返回未知，`resolved_by_crop` 已解决的历史争议仍可用。缺记录、错误、弃答、
不可表达的旧二元事实和零分母分别保留，不让“没有事实”变成“观察错了”。所有重算
输出在 `review-packets/factual-diagnostics-20260906/`，旧run原样保留；19个实际输入
文件的前后SHA256一致，见 `counting-and-ceiling.json`。原注册232池四条过两条的
**未通过判定不变**；本次没有生成新回答，也没有事后调门槛。

openct2每臂2778道计数题中，可判2464条（88.7%），未知314条：274 disputed_scope、
40 unnameable。以下两个率严格使用同一2464条分母；是检测器/crop派生的可判子集，
不是人工真值，也不能外推全体。旧全分母数字不能直接当作同总体改善量。

| 臂 | 回答不符合记录请求目标 | 回答不符合可判计数事实 |
|---|---:|---:|
| naive | 261/2464 = 10.59% | 556/2464 = **22.56%** |
| rfo | 610/2464 = 24.76% | 323/2464 = **13.11%** |

计数分层重算也改变§42.9的分母。下列“相等”仅指**本题所问计数**，不等于整图正确。

| 所问事实与请求的关系 | n | naive说请求值 / 说事实值 | rfo说请求值 / 说事实值 |
|---|---:|---:|---:|
| 相等 | 1830 | 1754 / 1754 | 1708 / 1708 |
| 不等 | 634 | 449 / 154（70.82% / 24.29%） | 146 / 433（23.03% / 68.30%） |
| 事实未知 | 314 | 不计事实准确率 | 不计事实准确率 |

原来948条“不等”里混入了314条未知。回答聚集于请求值的描述仍存在，但不能单由
这个分层识别提示词因果效应；同臂不同层的难度、目标数、颜色和答案先验可以不同，
跨臂又同时改变模型与提示条件。撤回“无混杂”“更强模型/投票/分辨率都不能改善”
和“只能去掉提示词”的结论。

ceiling及随机替换曲线只在**同一79/232个完整可判池、378候选、1804道原子题**比较。
全体5556题里4928条事实可判、628条未知；完整池排除可能有选择性，不能将79池诊断
与232池注册判定混为一谈。以下是分辨力统计，不是实际选中正确图比例。

| 79池条件诊断 | 正确均分>错误均分 | 二者相等 | 全部候选同分 | 候选满分 |
|---|---:|---:|---:|---:|
| naive实际 | 54.43% | 37.97% | 35.44% | 68.25% |
| rfo实际 | 88.61% | 3.80% | 3.80% | 47.88% |
| 全部换成可判事实答案 | 97.47% | 2.53% | 2.53% | 47.09% |

这79池的全部原子题事实一致率是 naive 85.75%、rfo 93.07%；它既不是旧74.8%所指
全体，也不是上面2464条计数题的准确率。naive随机替换5%回答后，20种子均值为
86.51%事实一致、32.53%池平分、30.25%全同分，仍没有达到旧表声称的状态。
不再报告一个“换更强观察者只需提升若干pp”的定价。各替换比例共享稳定题ID的
嵌套随机掩码；p=0逐候选复现冻结selection分数，p=1与同池ceiling一致。种子最小/
最大值只是模拟随机性，不是抽样置信区间；事实与整图裁定同源，不能证明真实干预收益。

验证：新增23项回归覆盖争议未知、crop已解决、实际题面、旧数字词、不可表达选择、
缺答/错误分母及原始文件保护；全量CPU **484项通过**（2个既有Pillow弃用警告）。
完整性及两端独立复算记录见 `packet-summary.json`，SHA256
`dcfcdb09c72eb6a7af0d375215f56be8a7840f6b57e7b18c04ac9c49dd89ea7b`。

### 43. 接手动态实验：真值修复、训练隔离与固定 pilot（2026-09-06）

用户在确认本地双 3090 的条件性工期后明确要求接手、尽快启动，并授权并行子任务。
本节及新预注册在任何新训练更新前写入。新目录为 `runs/v4/decoupling-pilot-20260906`，
旧 `gate-b`、两次开放计数重测和停止的 `l3-preview` 均保留。

**43.1 修正事实真值口径。** `agreed_verdict` 只确定整图 verdict；它保存的 detections
会去除未决 disputed 类别，不能将缺项当零。`unnameable` 同样不提供逐题答案。
新的 `factual_truth` 按实际题面确定颜色/类别范围，区分已知/未知与原因，保留
`resolved_by_crop` 已解决事实。旧存在题的 atom_id 缺颜色，新 oracle 不能仅按该 ID 答题。
旧脚本由此改变 56 条存在答案；旧“完美观察”实际上还有 15 个反向池。
四项聚合比例通过不代表没有语义缺陷，更不能推出优化题目没有价值。

旧 openct 计数每臂共有 2778 条，其中 321 条触及未决争议、40 条来自不可命名图，
事实可判 2417 条。可判子集的事实错误率为 naive 551/2417、rfo 348/2417。
这些子集数不能与全体旧错误目标的不一致率混为同一总体或提示词因果效应。
原 32.1%/24.7%，以及早先仅排 unnameable 得到的准确率 68.9%/76.4%，均不能继续作为
全体真实观察准确率引用。诊断脚本现报告未知覆盖，并让实际/理想比较使用同一批完整可判池。
本节写入时§42.9及error-pricing旧推论尚未重算；现已在后补§42.10完成更正，仍不作为
新实验方向的因果验收证据。

**43.2 动态实现修复。** 原理解 replay 从全部输入语料取正确图片，未排除 outcome/probe，
会造成留出泄漏。新训练只使用 split.train 的 replay，并按 seed/round 可复现轮换。
LoRA 初始化、真实 base checkpoint、两臂参数/优化器/RNG 恢复统一；非零轮缺 checkpoint
直接拒绝。每轮记录真实参数 delta/digest 和有限梯度，部分失败轮保留归档证据。
训练及 outcome 的原子分数固定全题数分母，弃答/错误/缺失另报；同批 outcome
同时记录 `s_select` 和原 `internal_cycle`，跨检查点固定 latent 以便逐 spec 配对。

**43.3 固定小规模实验。** seed=20260906，主语料 228 个不同 spec ID 划分为
132 train /64 outcome /32 probe；十轮，每轮 12 个不重复训练 prompt、K=8，
每臂八次优化更新，共 80 次。两臂为 naive 与 rfo_gold。冻结探针含 16 个独立
held-out spec、80 图、404 条问题/臂，无 train/outcome 交叉；RFO 冻结，Naive 每 checkpoint
重答同一批图。梯度保留 cos(mean gradients) 定义、配对 bootstrap 与相对 base 的配对差 CI，
仅保存 Gram/标量，约 3.3 GiB 临时向量在成功后清理，不累积每点 51 GiB 文件。

行为候选按最近三个 checkpoint 的配对首尾变化：Δs_select≥0.02 且 Δexternal≤0.02；
区间支持另列。梯度候选按相对 base 的 GDA-free 下降≥0.01，连续两点；无真实配对差 CI
只能标 point-only。所有查看均为探索性，不能把单 seed 曲线写成已确证 D* 或可靠提前量。
无事件、漏报、同步、提前和滞后分别记录。完整规则与 60 小时/磁盘/配对/失败停止条件见新预注册。

**43.4 验证与启动。** 407 项 CPU 测试通过（两条既有 Pillow 弃用警告），冻结 §38
`--expect` 原样复现。独立 runtime-check 在 GPU0 成功加载 InternVL 并完成一张真实图推理，
消除了“上次未证明能加载”的当前启动疑点，不宣称所有长程资源故障均已排除。
新 supervisor 逐轮 train → 评测/裁定 → 梯度 → 报告，保存状态/日志/源码及配置指纹，
独占同一 run 的调度锁；失败暂停诊断。运行完成一个 pilot 不等于研究目标已完成。

**43.5 开跑后的独立复审（尚无 outcome 曲线）。** 四个子任务分别核查训练状态、事实真值、
统计报告和梯度。真实 base 的 392 个 LoRA 张量及全部 RNG/优化器状态已在 CPU 验真；
参数 digest 与存档一致，196 个 LoRA B 张量均为零。另在空闲 GPU0 做四池反向计算 canary，
仅验证运行路径，主 16 池仍按既定设备重新测量。

spec ID 和原文 prompt 隔离不等于场景隔离：按规范化的物体/颜色/数量比较，计划训练与
实际 replay 会接触 7/64 outcome、5/32 probe，以及实际 bank 的 2/16 场景。
`audit-splits/scene_overlap.json` 已在任何 outcome 文件出现前冻结匹配证据及 57 项
不重合 outcome 敏感性子集；64 prompt 内还有 4 组同场景、共 9 prompt，实际为 59 场景。
主划分不回填；不能把全 64 项称为未见场景，补充分析须考虑场景聚类。

统计复审发现二值配对变化全零时 percentile bootstrap 会退化为 [0,0]，不能据此可靠排除
2pp 以上改善；少量完整可判 spec 也不能代表大量 unknown 的全体 outcome。报告修复已经
独立验证：保留原注册 bootstrap 旗标，补逐 spec 未知完成界及完整 N 项的保守二值界。
64 项全零的补充上界为 6.618pp，不能据此证明小于 2pp；2/64 可判同样不能获支持。
Clopper–Pearson 反演与 SciPy exact 在 25 组边界/常规情形相差不超过 1.04e-13。
原门槛不变；内部连续量仍使用注册 bootstrap，重复查看未校正，补充筛查仍非确证。

另在任何 outcome manifest 出现前固定 54 个独立场景代表题（每场景字典序首 spec，剔除
训练重合场景），见 `audit-splits/scene_representatives.json`。原 64/57 项及场景聚类结果
全部保留。梯度的独立 CPU 敏感性脚本剔除两项训练重合，目标为 14 池、14 场景，
只对 Gram 切片并做同场景联合重采样，不新增 GPU 计算或宣称预警确证。

437 项 CPU 检查全过。GPU0 额外完成真实 train_arm/AdamW canary：2 次更新、12 个 T2I
及 4 个 replay microbatch，参数 delta L2=0.114798，峰值 reserved 9.04 GiB，退出 0。
主 base/config/split 哈希未变，全部 canary 排除于主轨迹。

报告/调度修复采用一次性边界接续：当前训练子进程继续使用启动时的代码；成功退出后旧
supervisor 的源码守卫会暂停。`repair-staging/resume-after-reviewed-repair.ps1` 只在首轮
成功、暂停原因精确匹配且所有已审查文件哈希一致时，启动带修复登记的新版 supervisor。
真实训练失败或未经审查的改动不会自动重试。每个后续测量阶段还输出场景敏感性报告及
独立 PNG 轨迹图；当前无主轨迹数据时明示 no_data，不把 canary 冒充测量结果。

LoRA B 从零初始化还带来解释限制：base 的 A 梯度为零，训练后 A 梯度出现，余弦变化可以
部分来自参数化几何。早期 GDA 下降不能单独证明观察能力脱钩，须结合行为轨迹和 Gold 对照。

修正后的 openct2 CPU 诊断在 `audit-openct2/`：完整可判仅 79/232 池（67 个 spec），
同池实际选对 naive 43/79、rfo 65/79、理想观察 77/79。理想观察仍会因未问额外物体而按
并列规则选错；故“题目全好，短板全在观察者”不成立。628/5556 个逐题事实实例未知
（548 disputed、80 unnameable）。全 232 池实际比例 114/232 和 162/232 不能直接与
79 池理想比例比较，旧注册未通过判定保持不变。

**43.6 首次真实更新与裁定覆盖修复（2026-09-05 20:04 UTC）。** 首轮 12 个自然池中
3 个无已认证正确图，按冻结配对规则排除；两臂都训练其余 9 个池。naive 所选图为
4 正确、4 错误、1 未决，Gold 为 9 正确；两臂 4 次同选、5 次不同。这个 Naive 轨迹
以 Gold 有可选正确图为条件，不能当成未过滤全部自然池的 Naive 训练结果。
两臂各 8 次 optimizer step、48 个 T2I 与 16 个 replay microbatch，参数 delta L2 分别
为 0.9348818562 / 0.9350694554；保存的 392 个 LoRA 张量、Adam 状态与 RNG 已独立核验。

主检测器 96 条记录中 1 条 JSON 解析失败，故 verified 实际为 95 条：89 已知、6 pending。
完整候选全集的未知数应为 7（6 pending + 1 missing），旧日志只报 6。缺项为 v4b-0052
候选 6，naive 选 7、Gold 选 5，因此未改变已完成的首轮训练选择，不重跑该轮。
`read_verdicts` 现显式保留缺失候选的未知状态；后续 `stage_score` 使用生成 manifest
统计全集覆盖，缺失不当错误。整图 `unnameable=False` 是已知生成失败；逐题事实仍未知。
CSV 新增 `external_coverage_policy` 区分旧 row-only 与 manifest 语义，旧原始结果不回填。
447 项 CPU 测试通过，含 10 项新增的缺失裁定、已知失败和 CSV 连续读写检查。

首次边界接续的 PowerShell helper 因 `Get-FileHash` 不可用而未启动新 supervisor；
19:46 UTC 人工核验成功 sentinel、全部已审查哈希及 checkpoint 后已恢复（PID 8124）。
本次覆盖修复改用 `repair-staging/missing-verdict/continue_after_repair.py` 做一次性接续：
核验原进程身份，等当前 base 生成成功及精确 source-guard 暂停后才恢复；真实阶段失败
不自动重试。全部审查输入另锁 SHA256，保留最初 started_unix 与 60 小时上限，不延长预算。

20:08 UTC 本次 Python 接续已成功：原 owner 自然退出，新 PID 1884 接管并开始 base
图像的 Qwen3-VL 裁定；run_manifest 登记 `aded444`，全部 12 个源码哈希与审查版一致。
64 张 base 图及全部 316 个回答完整可用，真实 base 参数 digest 相符；64 项 cycle 全部
有限，均值 -2.304931640625。此为单 checkpoint 的基线完整性证据，尚无外部结果或分叉。
接续与 cycle 核验在 `runtime-check/coverage-repair-handoff-verified.json`，只读快照在
`runtime-check/base-naive-integrity.json`（后者写入时 cycle 尚未齐，保留 partial 标签）。

**43.7 首份留出基线裁定（2026-09-05 20:43 UTC）。** naive 的真实 base 64 图完成双检测、
crop 和 verify：两检测器各 64 条有效记录、零错误/缺失；15 条 crop 回答覆盖 8 图，全部
有效。独立 CPU 重放与保存的 64 条裁定逐字段一致。57 图已知，其中 16 正确；7 图因
crop 未能确认争议物体而仍为 pending_human，无 unnameable 或 missing。
正式外部口径为 16/57=28.07%，未知覆盖 7/64=10.94%；全 64 图完成界为
[16/64,23/64]=[25.00%,35.94%]。证据在 `runtime-check/base-naive-verdict-integrity.json`。

旧 `verified.summary.json` 的 p=31.25% 把 pending 的临时布尔字段也纳入，不是本轮正式
外部指标，不能引用；新 manifest 计分和主轨迹报告按未知处理。基线逐图平均自评分为
0.9270833，316 个问题中 293 个答案符合 spec 目标，回答覆盖完整。原子符合率与整图
完全正确率不是同一种指标；这两个基线数的落差不等于训练脱钩。
7 张待人工图如实保留未知；当前动态协议要求覆盖/边界，没有按 pending 数自动停机的
条款。第二份 base 重复运行正在继续；20:42 的 49 对已生成图片 PNG/RGB 全部逐对一致，
完整配对及梯度结果尚待后续测量。该部分快照保留在 `runtime-check/base-paired-images-20260905T204250Z.json`。

**43.8 完整 base 配对与主梯度（2026-09-05 21:19 UTC）。** 两臂完整的 64 对 base 图
逐对 PNG/RGB、spec、seed 一致；316 个原始/归一化回答逐条一致，时间戳证明分别记录。
逐图 cycle、自评分及 64 个 verdict 状态也一致，7 个 pending 在两次裁定中相同。
正式 CSV 两臂均为 16/57 正确、7 未知，使用 manifest_image_verdict_v1。
完整证据为 `runtime-check/base-paired-integrity-complete.json` 与
`runtime-check/base-paired-answer-integrity.json`；仅 step0 的报告已另存不可回填快照
`runtime-check/step-00000-reports/`。主报告如实标 insufficient_checkpoints，没有产生图或报警。

主 base 梯度用冻结 16 池、80 图，每臂 404 个观察回答；全部可解析，无弃答/错误。
从原始回答用精确分数重算三个准则的选择均一致：Naive 7/16、RFO 12/16、Gold 16/16
选中正确图。这是平衡探针的静态选择率，不是训练后效果。选择复用后实际反向 26 次，
loss、norm 及 Gram 全部有限，参数 digest 对应保存的真实 base。
GDA-free=0.7749844，prompt bootstrap 95% 区间 [0.2190707,0.9887376]；
GDA-gold=0.8020402，区间 [0.2284272,0.9790155]。联合 Gram 谱分解所得等价向量
独立重采样 2000 次，统计量与原报告最大差 8.9e-16。
剔除训练重合的 14 池敏感性分别为 0.7146706 / 0.7455354，区间独立重算最大差 2.8e-15；
base 与自身的配对差严格为零。这些单点及宽区间不构成预警；后续必须比较同一 bank 的配对变化。
38 项只读完整性检查通过，3.302 GiB 临时梯度向量已清理，阶段耗时 223.4 秒。
证据为 `runtime-check/base-gradient-integrity.json`。21:11 UTC 已自动开始 naive step8
留出图生成；当前没有达到冻结停止条件，不修改实验协议。

**43.9 Naive 首轮后的第一个留出点（2026-09-05 21:57 UTC）。** step8 的实际参数 digest
为 `436ed56e…c0cf4`，与保存的 naive round000 一致，区别于 base 和 Gold。64 个 outcome
ID、固定 seeds、题目及记录关联全部通过；316/316 答案可用，无错误/弃答/缺失。
64 项 cycle 有限，所有图像像素均相对 base 改变。逐图平均原子自评分从
0.9270833 降至 0.9140625（−1.302pp）：3 图升、6 图降、55 持平；符合 spec 的答案
293→290。cycle 从 −2.3049316 增至 −2.2130127（+0.091919），行为判据仍使用原注册
原子自评分。这两个内部量不是可按结果替换的同一指标。

两个检测器各 64 条有效记录；24 条 crop 回答覆盖 13 图，全部有效。独立重放的 64 条
裁定与原记录无差异。54 图已知，其中 14 正确；10 pending、0 missing/unnameable。
正式外部为 14/54=25.93%，完整 N=64 的水平范围为 [14/64,24/64]=[21.875%,37.50%]。
旧摘要 p=32.8125% 含 7 个 pending 临时 true，不能引用。

base→step8 共同已知 54 图：14 对→对、39 错→错、1 对→错（v4b-0062）、0 错→对。
配对变化 −1/54=−1.85pp；bootstrap 区间 [−5.56,0]pp，而稀疏配对的补充保守区间约
[−11.24,+7.77]pp，不能据 bootstrap 上界零认定外部停滞。基线 7 个 pending 全部仍未决，
新增 v4b-0061/0082/0094；未知 union 为 10，完整 N 任意完成的变化界为
[−9/64,+8/64]=[−14.06,+12.50]pp。只有两个 checkpoint，且当前原子自评分未上涨，
不满足三点行为候选的识别条件。Gold step8 尚待评测，不推断两臂训练效果差。
证据在 `runtime-check/naive-step00008-internal-integrity.json` 与
`runtime-check/naive-step00008-verdict-integrity.json`；主 CSV/报告仍按原顺序在两臂测量齐后更新。

Naive step8 梯度及成功 sentinel 随后齐备，独立审查 24/24 项通过：16 个共同 prompt、
404 条观察回答、27 次实际 backward，全部有限，参数 digest 和选择重算匹配，临时向量
已清理。GDA-free 为 0.7361814，相对 base −0.0388031，真实配对差区间
[−0.0659287,+0.0230266]；GDA-gold 为 0.7712710，差 −0.0307692，区间
[−0.0603098,+0.0280263]。等价向量独立重算配对差与区间，最大误差 1.17e-14。
free 单点降幅超过 0.01，但区间含零，且只有一个训练后 checkpoint，不能称两点预警。
相对 base 的选择仅 Naive 改了 1/16：v4a-0012 从候选 3（正确）改选候选 0（错误），
使探针选对数 7→6；RFO 12/16、Gold 16/16 的选择不变。该固定 bank 上的梯度变化
同时包含选择变化和权重变化，不能据此归因于观察能力单一机制。
证据为 `runtime-check/naive-step00008-gradient-integrity.json`。主流程已自动转入
rfo_gold step8 的留出生成；继续原定日程，没有调整阈值或规模。

**43.10 Gold step8 内部量核验（2026-09-05 22:34 UTC，外部/梯度尚未完成）。** 64 个
outcome ID、固定 seeds、图像与题目哈希均匹配；所有记录的参数 digest 为
`be43b23a…c325d`，关联已审计的 Gold round000。316 个原始回答重新规范化和计分全部
一致，289 个符合 spec，零错误/弃答/缺失；64 项 cycle 有限。逐图平均原子自评分
0.91015625，较 base −1.6927pp（1 升/5 降/58 平），较 Naive step8 −0.390625pp
（1 升/2 降/61 平）；cycle 为 −2.23828125，较 base +0.06665039，较 Naive −0.02526855。
这些是两个时点的内部量描述，不构成外部效果或分叉判断。
证据在 `runtime-check/rfo-gold-step00008-internal-integrity.json`，未重做已完成的
checkpoint 审计、未使用 GPU 或改写既有测量。当前独立检测仍正常进行，源文件和冻结
config/protocol 哈希一致，剩余空间约 40.53 GiB；主流程继续自动完成裁定、梯度与整轮报告。

**43.11 首轮完整两臂比较（2026-09-05 23:10 UTC）。** Gold step8 的 64 张图和两个
检测器各 64 条记录均完整，18 个预期 crop 任务齐全；64 条裁定独立 CPU 重放零差异。
58 已知中 17 正确、6 pending、0 missing/unnameable；正式正确率 17/58=29.31%，
全 64 图完成界 [17/64,23/64]=[26.5625%,35.9375%]。旧摘要 p=32.8125% 含 4 个
pending 临时 true，不是正式正确率。

Gold base→step8 共同已知 54 图：15 对→对、39 错→错，0 改善/变差；未知 union10，
完整 N 变化界 [−6/64,+7/64]=[−9.375%,+10.9375%]。零差异的 bootstrap [0,0] 退化，
补充保守区间约 ±7.79pp，不能证明平台。Naive8→Gold8 共同已知 53 图：14 对→对、
38 错→错，仅 v4b-0062 错→对（Gold 较好），点差 +1/53=+1.887pp；未知 union11，
全 N 变化界 [−7/64,+9/64]=[−10.9375%,+14.0625%]。不同已知子集的 17/58 与 14/54
不能证明 Gold 优越；现有配对差也不能当作稳健的两臂效果差。外部证据在
`runtime-check/rfo-gold-step00008-verdict-integrity.json`。

Gold step8 梯度独立审计 29/29 项通过，16 池/404 回答/27 backward，参数关联、有限性、
选择重算和临时向量清理均正常。GDA-free 对 base 的差为 −0.0502337，95% 配对区间
[−0.0794752,+0.0171469]；GDA-gold 差 −0.0419114，区间 [−0.0717640,+0.0209830]。
独立重算最大误差 <3.7e-15。两训练臂在 step8 的三套准则选图逐池完全相同，相对 base
都只有 Naive 准则在 v4a-0012 从正确候选 3 变为错误候选 0；因此两臂当前 GDA 差不能
归因于选择了不同图片。Gold−Naive 的直接配对差也包含零：free −0.01143
[−0.02305,+0.00723]，gold −0.01114 [−0.02486,+0.00798]。
14 池敏感性的两臂四个差值及区间全部独立复算一致，均含零。证据在
`runtime-check/rfo-gold-step00008-gradient-integrity.json`。

主报告正确保留了两个 checkpoint、空行为窗口、单点幅度 flag=true/区间支持=false，
以及两点报警为空；没有 D* 或 D_g 结论。首轮完整快照为 `runtime-check/step-00008-reports/`，
首图为 `plots/decoupling-20260905T224248552121Z.png`，已视觉检查。图的外部折线使用
各点已知子集、覆盖另列；两臂梯度面板纵轴范围不同，比较应读数值与配对区间，不能按
视觉斜率判断优劣。22:42 UTC 主流程已自动启动 round001；当前固定方案继续，没有因
首轮趋势调整方法、阈值或规模。首轮完整核验后，后续以既有不变量及变化/异常检查为主，
不重复重算已经通过的基线审计。

**43.12 第二轮完成后的调度修复（2026-09-06 00:21 UTC）。** 我写的 supervisor 将
`--max-rounds index+1` 当成“跑到此轮”，但 train CLI 的既有语义是“本次新增多少个未完成
轮次”。第一轮的参数 1 没有暴露问题；第二次调用的参数 2 使 round001 完成后，继续
提前生成 round002，尚未执行要求的 step16 评测。这是调度实现缺陷，不是新科学协议。
00:14 UTC 在核验进程身份和 DONE 后停止 owned child27664，其 parent1884 退出并如实
保留 exit15/needs_diagnosis。停止前 round002 只有 9 张 PNG，没有该轮 checkpoint、DONE、
selection 或训练更新；9 文件全部按原哈希移入
`runtime-check/round-limit-repair/round-002-early-20260906T001426Z/`。不删除、不回填、不复用
提前生成的数据；原故障日志及 `round-limit-incident-20260906T001426Z.json` 留档。

round001 两臂各 8 次真实更新，累计 step16，9/12 个新的训练 prompt 完成配对；与上一轮
12 个 ID 不重复且全部来自 train。Gold 选中 9 个已确认正确图；两臂 1/9 同候选槽位、
8/9 不同槽位。训练后的两臂生成图不同，不能把 Gold 图像裁定转用为 Naive 图像标签。
两个 step16 checkpoint 独立审计 32/32 通过：文件哈希、392 个 Adam state 的 step16、
有限性、scheduler16 和各自上轮关联均匹配；实际 digest 为 Naive `9813785e…4c83` / Gold
`6d06ec80…08de`，参数 delta L2 精确复现为 1.3007776172 / 1.3019357355。
因此保留并复用这两个 checkpoint，不重跑第二轮。证据在
`runtime-check/round001-checkpoint-integrity.json` 和 `round001-selection-integrity.json`。

修复引入明确的 `--round-index N`：只执行指定轮；其 DONE 已存在则在模型导入/训练输入
加载前返回，不挑选下一未完成轮。与 `--max-rounds` 互斥，后者保留旧新增轮数语义。
supervisor 改传明确 index。只改为 `--max-rounds 1` 不够：本次恢复时 round001 的 DONE
已存在而阶段 sentinel 尚缺，那个写法会跳到 round002。新路径通过正常的完成返回让
supervisor 补记成功并先测 step16，不伪造旧进程成功状态。
新增 CLI/轮次 12 项及真实 main→execute→run 恢复顺序 2 项先在旧代码失败，修复后全过；
全量 CPU 461 项通过（两条既有 Pillow 警告）。真实 showo2 环境禁用 CUDA 后重复请求
已完成 round1，0.6 秒内退出 0，8 个已完成工件哈希未变、没有创建下一轮目录，见
`runtime-check/round-limit-repair/exact-round-noop.json`。冻结 config/prereg/split 和原
started_unix/60 小时预算不变；随后登记源码修复并恢复 step16 评测。

00:22 UTC 恢复已实测成功：新 supervisor PID26356 登记源码修复 `d5fd716`，精确 round1
调用正常退出并写完成 sentinel，紧接进入 `naive.step-00016.generate`（child20832）。
原 exit15 日志保留；没有伪造旧调用成功、没有重跑第二轮、没有重新创建 round002。
全部新源码哈希一致，原 started_unix1788631464.2176192 和截止1788847464.2176192 保持
不变。恢复记录为 `runtime-check/round-limit-repair/resume-verified.json`。

**43.13 Naive step16 与首个三点窗口（2026-09-06 01:35 UTC，Gold16 尚在评测）。**
新增 Naive16 内部记录完整：64 张图的固定 spec/seed/题目逐项一致，实际 RGB 与回答绑定
一致，权重 digest 对应已核验的 step16 checkpoint；316 个回答全部有效，302 个匹配
请求，0 error/abstain/missing。全 64 图等权自评分为 727/768=0.9466145833，对 base
89/96 的增幅精确为 5/256=1.953125pp，低于 2pp 门槛，不能四舍五入成通过。16 图上升、
9 图下降、39 图不变；cycle 增幅 0.20263671875 仅作辅指标，不替换原子自评分。证据在
`runtime-check/naive-step00016-internal-integrity.json`。

外部双检测器各 64 条有效、0 error，10 个预期 crop 任务全部齐全，64 条最终裁定 CPU
重放无差异。正式正确率为 21/61=34.4262%，另 3 图 pending；完整 N 水平界为
[21/64,24/64]=[32.8125%,37.5%]。`verified.summary.p=35.9375%` 含两条 pending 的
临时 true，不能引用为正式外部正确率。

冻结窗口 [0,8,16] 使用首尾共同已知的 57 个 spec：内部增幅 11/684=1.608187pp，
bootstrap 95% 区间 [−2.33918,+5.55556]pp；外部 3 错→对、2 对→错、14 一直对、38
一直错，配对差 +1/57=1.754386pp，区间 [−5.26316,+10.52632]pp。外部点值满足 ≤2pp，
内部点值未达 ≥2pp，因此候选为 false，bootstrap/补充支持也为 false。未知 union7，
全 64 图外部完成差值界 [−2/64,+8/64]=[−3.125,+12.5]pp；仍不能排除外部实质改善。
不能拿不同分母的 21/61−16/57 代替配对变化，也不能把 4 个 base 未知、现在正确的图
记成 4 次已知改善。证据在 `runtime-check/naive-step00016-verdict-integrity.json`。
这是单臂先完成后的有限审查，未覆盖或提前改写等待两臂齐全的主报告。

Naive16 梯度的有限 CPU 审计 22/22 项通过，16 池/80 图/404 回答/27 次 backward，实际
checkpoint 关联正确，Gram 与梯度有限，临时向量已清理。GDA-free=0.5232923868，
对 base 的配对差 −0.2516920295，95% 区间 [−0.2798543965,+0.3324524816]；独立复算
误差 <1.5e-15。按原规则，base/8/16 点值旗标为 false/true/true，首个连续两点候选
报警可用时点是 step16；区间支持报警仍为空。行为事件尚不存在，不能报告 D_g 或提前量，
也不能把 no-event 写成 k=0。

点估计大幅下降与较宽区间并非算术 bug：原定义是 cos(mean gradient)，大范数与跨池
相消会影响均值方向。v4b-0108 占 Naive/RFO 平方梯度范数之和的 33.7%/29.5%；仅作事后
影响诊断、同时从 current/base 去掉该池时，配对点差变为 +0.01114。2000 次 bootstrap
中 705 次未抽到该池，这些重复的当前 free 中位数为 0.92267；全部重复的配对差中位数
约 −0.04035。注册的 16 池、计算定义与主结果全部保留，不能选取删池后的数替代它。
step8→16 的 Naive 选图变了 2 池，正确选中数反而 6→7，冻结 RFO/Gold 选择未变，
因此不能直接解释成“选图质量下降”。证据在
`runtime-check/naive-step00016-gradient-integrity.json`。接着等待 Gold16，对照两臂是否
共同变化；当前无须暂停或改变冻结方案。

**43.14 两臂 step16 完整报告（2026-09-06 02:12 UTC）。** Gold16 的效果、梯度、主报告、
14 池敏感性与图在 01:52 UTC 全部完成，随后才启动 `--round-index 2`。监督进程26356、
子进程21972 正常运行，原预算和冻结 config/protocol/source 哈希未变。主报告/CSV/图
来源及阶段顺序的 36 项检查通过；完整快照保存在 `runtime-check/step-00016-reports/`，
避免后续累积报告覆盖当前证据。新图 `plots/decoupling-20260906T015226554457Z.png` 已
视觉检查，外部折线仍是各 checkpoint 的已知子集，不能替代配对变化；梯度两面板轴范围
不同，比较应使用数值和直接配对区间。

主报告的两臂行为候选、bootstrap 规则支持、补充稳健性支持均为空；与训练场景不重叠的
57 spec 子集（含 54 个场景）及 54 个场景代表题的补充分析也没有行为事件。两臂连续梯度点值报警的最早
时点同为16，区间支持报警都为空，`point_only_lead` 保留 `no_event` 和空 lead，未把
无事件写成零提前量。这是两臂共有的点值变化，尚不能归因于 Naive 特有的自我确认机制。
第三轮继续按冻结计划采集；没有根据接近阈值或单池影响而修改判据、bank 或梯度定义。

Gold16 的 64 图/316 回答来源正常，302 个答案匹配请求，0 error/abstain/missing。
全 64 图自评分均值与 SEM 恰与 Naive16 相同并非结果复用：两臂 64 个实际 RGB 和
request_id 全部不同，权重分别关联自己的 step16 checkpoint。62 图同分，另两图为
v4a-0014 从 Naive 的 1 分到 Gold 的 0.75 分、v4a-0078 从 0.75 分到 1 分；两条计数
回答的相反变化使汇总精确相同。固定题目、spec 和 seed 均一致。

Gold16 双检测器各64条有效，15 个 crop 任务齐全，0 error/missing/结构异常；按新增
疑点检查原则未重复整套 verifier 重放。正式外部为19/59=32.2034%，5条未知，全64图
完成水平界[29.6875%,37.5%]。Gold base→16共同已知55：1改善、2变差，配对差
−1/55=−1.8182pp，未知union9，全64差值界[−6.25,+12.5]pp。Naive16→Gold16共同59：
1改善、2变差，配对差−1/59=−1.6949pp，未知union5，全64差值界[−7.8125,+4.6875]pp。
Gold首窗口内部配对增幅3/220=1.363636pp<2pp，外部点条件虽满足，bootstrap区间
[−7.272727,+3.636364]pp仍不能排除超过2pp的改善。不能据此说Gold优于或劣于Naive。
证据在 `runtime-check/rfo-gold-step00016-outcome-integrity.json`。

Gold16 的 GDA-free=0.6248799534，对 base 配对差 −0.1501044629，95% 区间
[−0.2341402626,+0.3173267291]。Gold16−Naive16 的直接同池配对差 +0.1015875666，
区间 [−0.0641961108,+0.0892103966]，包含零；百分位区间在该非线性统计量下不保证
包含原始点估计。两臂三套准则的选图逐池完全一致，Naive/RFO/Gold 正确选中数都是
7/12/16，因此当前臂间梯度差不能归因于选中了不同的 probe 图。
Gold16 同样受到 v4b-0108 的高影响，Naive/RFO 平方范数份额为 35.35%/25.73%；仅诊断性
同时从 current/base 去掉该池后，Gold 的 free 对 base 点差变为 +0.03954。注册主结果
保留全 16 池。这些变化尚不支持 Naive 特有的退化或可靠预警。
Gold16 梯度来源与数值检查27/27通过，GDA-gold对base差−0.1214910，区间
[−0.2193455,+0.3218865]；两臂14池敏感性对base的四个差值区间也全部含零。
证据在 `runtime-check/rfo-gold-step00016-gradient-integrity.json`。所有本轮审查均为CPU，
没有干预运行中的第三轮或改动实验源码。

**43.15 对测量能力批评的复核与执行范围收束（2026-09-06）。** 用户转来三项批评，
本次分别用统计报告、真实训练池和逐图/逐题 outcome 作 CPU 复核。它们足以要求改变
投入顺序：原运行保留为测量能力受限的先导轨迹，决定在当前第三轮及 step24 既定测量
完成后停止自动追加训练，先验收测量和选择收益。此为看到先导结果后的新执行停止决定，
不是原十轮已经完成，也不是预注册的确认性早停。配置、判据和旧结果不回填；后续如改变
科学设计，须另立运行版本。具体安全边界接续及真实退出证据在完成后单独登记。

第一项批评对“继续训练不能补评测精度”的判断成立，而且主报告还有一个硬条件：
`v4_decoupling_report.py` 的场景重复 guard 会将原64 spec/59场景及57 spec/54场景分支
的 `robustness_supported` 固定为 false；其后再加 checkpoint 也不能解除。只有54个场景
代表题分支能获得条件性支持。描述性 candidate/bootstrap 标记不是这一个旗标，不能混说
全部规则永远不触发。约843配对样本是令真实差为0、保持5/57不一致率、用普通正态区间
估半宽2pp的粗略计算，并非统计功效或当前CP判据的保证。未知同样造成无法靠重复同分布
采样消除的识别范围，但决定条件的是最坏补全上端，不是未知百分比或区间宽度单独决定。
当前64题完成界上端12.5pp、54代表题9.26pp；若比例和变化结构维持，增加n也不能变成
≤2pp。54题无未知、零变化时保守CP上界仍7.79pp；强烈外部恶化可能通过，所以不能
把“无法精确证明轻微停滞”说成任何未来数据都不可能支持。

第二项把 outcome 分数相近推成训练选图相同，实际不成立。前两轮18对实际训练图片中
14对不同；round0同图4/9，round1同图0/9。round0两臂候选逐像素相同时，Naive选择
4正确/4错误/1未知，Gold9/9正确，确有训练质量差异。之后两臂各自产生候选，不能拿
同seed的Gold图标签代替Naive图。step16固定probe三套准则选图相同、outcome仅两条
答案不同，均不能替代真实训练池分析。

第三项的饱和现象成立，但“分数接近1所以学不到”“从未耦合”超出证据。s_select只
用于排序挑图，随后对选中图做生成SFT和replay，score或1-score并不乘入loss。Naive
候选满分44/96→61/96，最高分并列池11/12→12/12；前两轮18/18张选中训练图均满分，
其挑最高分的区分能力确实不足。round0九个正确/错误混合池的描述性池均AUC约0.744，
仍有有限信号，不等于完全没耦合。

逐图 outcome 给出的证据比两个聚合率之差更直接：step16两臂都有26/40张已知错误图
获得满分；Naive的50张满分图为21正确/26错误/3未知，另外14张非满分图全部错误。
0.9466是逐图等权的原子题符合分，不是94.66%整图正确，跨spec AUC约0.675也不是
同prompt的K选1收益。base→16存在题均分95.0521%→100%，计数题90.3646%→89.3229%，
总分92.7083%→94.6615%；总分上涨全部来自存在题，不能解释成计数理解改善。固定共同
可判cohort后，AUC变化Naive +0.0022、Gold −0.0082，配对区间均含零；尚无可靠的
训练后分辨力下降证据，也无强的“先耦合后脱钩”轨迹证据。
逐图/分题型及条件性统计留在 `runtime-check/selector-coupling-review-20260906.json`。

下一步只做两项小验收：审计现成“满分但整图错误”案例，分清题目没覆盖、可判事实与
观察回答冲突、事实仍未知；再核同prompt候选池的实际选择收益，并补独立裁定后两轮
Naive所选图片的质量。同时让下一版评测规模/未知处理对应明确要识别的最小变化，不能
把当前pilot的未支持结果当成靠多加训练轮数必然能解决的问题。

第一项小验收已在CPU完成，见 `runtime-check/fullscore-wrong-case-review-20260906.json`。
使用修复后的 factual_truth、实际题面与原双检测器/crop证据，不把整图 agreed_verdict
当成完整事实。Naive26张满分错图共136题：75条与可判事实匹配、47条冲突、14条事实
未知；Gold26图134题分别为73/39/22。按图至少一道已问问题存在冲突的是23/26与19/26；
存在未知的是7/26与11/26，两类可重叠。全部所问事实都已知且符合目标、整图仍错的为
Naive0图、Gold1图：v4b-0083多出了未问的银色lid。这些是检测器推导证据，不是人工
真值；冲突率只能在当前精选案例的可判题上计算，不能外推全体。当前主要已有“题目
问到、回答却与可判图像证据不符”的实例，优先问题不应简单写成再增加题量。

03:41 UTC 已设置实测的执行边界。第三轮已在03:26:11正常完成，两臂各新增8步，9个
配对样本，参数delta为0.6396394/0.6387588；随后的Naive step24生成正在运行。
一次性helper `runtime-check/method-review-hold-20260906/finish_step24.py` 经独立代码
审查及真实 `--check-only` 验证后，以PID18492启动。它先持有真实child19412句柄、核验
PID创建时间/命令/冻结哈希，再仅结束原supervisor26356（明确记录forced exit，非正常
成功），保留生成child继续工作。现场确认原父已退出，child仍在GPU1正常运行。

helper只会在该child真实exit0且64图/316题/cycle/checkpoint关联全部校验后，记录带接管
来源的阶段完成证据，再启动同源supervisor `--through-round 3`，完成两臂step24既定
评测/梯度/报告后自然停止，不进入round003。原started/60小时和配置/源码不变，锁文件
由OS释放，未删除或重算既有输出。接续前 `state.json` 暂留旧父的最后状态且受hash保护，
应先查helper的 `events.jsonl`，不能据旧PID消失而另起任务。失败只留诊断，不猜退出成功
或自动重试；本次记录尚不代表step24测量已经完成。最终CLI可能称 `canary_complete`，
它在此只代表有界运行结束，这三轮仍是主pilot数据，不改标为启动canary。

**43.16 历史诊断修复后，按用户要求接续原pilot（2026-09-06）。** 用户明确说
“修一下，然后继续跑吧”，本次按完成§42.10的CPU修复后恢复原定10轮/80步执行，
这取代§43.15的临时step24停止边界。统计能力、选择器饱和及梯度不确定性等结论没有
因此改变；继续取得探索性轨迹，不承诺一定找到两个事件，也不将缺少支持解释为不存在。
原started=1788631464.2176192、60小时截止=1788847464.2176192保持不变，不追加预算。

旧有界helper已如实等到原generation child19412退出0，并验证完整64图/316题后启动
supervisor26896；Naive step24检测/crop/verify也完成。随后gradient因缺失
`SELFSIGHT_MODEL_ROOT` 在模型路径解析时失败，supervisor真实退出1，helper据此拒绝
继续，没有生成hold-complete。原因是接续启动时漏传原环境，属于本次执行失误；
失败目录仅写出run.meta，尚无观察或梯度结果。原helper、事件及失败日志完整保留，
副本位于 `review-packets/factual-diagnostics-20260906/continuation/`。

修复恢复入口显式继承原 `scripts/set_h_env.ps1` 的模型/cache/tmp路径与离线设置，
CPU核查本地冻结模型及仓库revision、12项源码/配置/协议hash、原期限和无活跃owner，
再启动同源supervisor `--through-round 10`。依原调度顺序先重试Naive step24梯度、
完成Gold step24与配套报告，才进入第四轮；不重跑已完成训练、不改失败为成功，
不要求旧helper补写一个并未成功的hold-complete。尚未启动的旧等待入口保留但弃用。

04:32:57 UTC 已实际启动修复入口，launcher32704确认新supervisor18576拥有运行；
现场state为 `naive.step-00024.gradient`，child28688，through_round=10，原started未变。
入口 `resume_with_original_env.py` 的SHA256为
`357c1a6c55ba39dd090c9b33cfc646fc11ca4832d2fb68c355a61b9c5b9b5dc7`；
recovery-expected SHA为 `738bb78114ec88057a0202b4470f3543011d92c77d16963e6f2822b3867be9d2`。
真实CPU预检退出0，新命令已追加到原阶段日志，旧KeyError证据仍保留。此处确认的是
恢复启动，尚不代表step24梯度、十轮pilot或研究目标已完成；30分钟兜底跟进已更新。

**43.17 第24步两臂完整测量与新窗口核验（2026-09-06）。** 05:50 UTC 两臂step24
效果、梯度、主报告、14池敏感性及轨迹图全部完成，随后原调度进入第四轮round003。
本次把当时的CSV、主报告、敏感性、run manifest和PNG另存到
`review-packets/step24-review-20260906/`，不覆盖原轨迹。原始数据来源哈希见该目录
`snapshot-inputs.json`，图经视觉检查可读。第四轮仍按原协议运行，本段不代表已做完80步。

| step24 | naive | rfo_gold |
|---|---:|---:|
| 固定全题分母自评分 | 92.578125% | 92.968750% |
| 存在题回答符合请求 | 158/158 | 158/158 |
| 计数题回答符合请求 | 139/158 | 140/158 |
| 整图正确/已裁定 | 20/59 | 20/57 |
| 未裁定图 | 5/64 | 7/64 |
| 满分图中的正确/错误/未知 | 20/22/3（45图） | 20/20/6（46图） |

每臂64图、316回答完整，无缺答、错误或弃答。计数题行是所有题汇总比例，第一行是
逐图等权平均，两者不混用。Naive自评分比step16下降2.0833pp，Gold下降1.6927pp。
外部率分母不同，只用于描述点位；不能直接比较两个边际率推导配对改善，也不使用
含pending临时布尔的verified.summary.p。

新窗口[8,16,24]在两端共同已裁定的spec上独立重算如下：

| 主64题分析 | naive | rfo_gold |
|---|---:|---:|
| 共同已裁定配对数 | 52 | 54 |
| 自评分变化 | +1.7628pp | +1.6975pp |
| 外部正确率变化 | +3.8462pp | +1.8519pp |
| 原行为candidate | false | false |
| 全64未知标签的配对完成界 | [-6.2500,+17.1875]pp | [-4.6875,+15.6250]pp |

两个原bootstrap/robustness支持旗标也为false；不能把未支持解释成已证明外部停滞。
54个独立场景代表题的固定ID/hash/代表选择均一致，两臂也无candidate。主64和57题的
重复场景保护保留，54题只是预先冻结的补充分析。`outcome-audit.json`中333项CPU
一致性检查通过，含独立配对重采样及CP界复算；SHA256
`4712dc0761ac92c7e5544556171b756b33778d5fe3f993a602e32829244f9936`。

Naive24 GDA-free=0.7020929433，对base差−0.072891473，95%配对区间
[-0.261439133,+0.453497140]；Gold24为0.7773375450，差+0.002353129，区间
[-0.261544963,+0.387877680]。两臂从16步均回升；Naive当前点条件成立，Gold当前
点条件不再成立。历史最早点报警时间仍为16，但不能把它描述为持续报警或可靠D_g。
Gold−Naive同池free差+0.075244602，区间[-0.307559289,+0.164792713]；14池敏感性
及gold-reference差也都含零。两臂三套probe选择逐池相同，正确数分别9/12/16。
Gold两个高范数池合计约74.4%/72.6%的两侧平方范数，注册主结果不删池。
`gradient-audit.json`中21项CPU检查通过，SHA256
`8ea8a1863eafd72373cce9b63f4e97baad5bf329ed484ee97924f9d68bee14c3`。
本次仍无D*、区间支持的D_g或可报告提前量；没有事件不写k=0。

用户要求继续后，测量验收与原有界pilot并行：首轮同池选择收益转为可复核脚本，
并增加同一共同可判问题上的“回答符合请求/事实符合请求/回答符合事实”CPU轨迹诊断。
这三者用于区分自评分、图像符合和观察错误，不替换已注册整图结果或事后宣布分叉。
原始输入与12项运行源码保持冻结；需要新科学设计时另立版本。

**43.18 初始选择收益验收与同尺度事实诊断（2026-09-06）。** 首轮两臂从同一base
出发，实际96对候选图片的解码RGB相同，允许将对应Gold候选裁定用于Naive选图评估。
这不适用于后续两臂各自生成的候选。新增 `scripts/v4_selection_gain.py` 把该条件、
逐图来源和同prompt随机基线都做成检查；报告在
`review-packets/selector-gain-20260906/round000.json`。

| 首轮口径 | Naive选中正确率范围 | 同池均匀随机期望范围 | 配对收益联合完成界 |
|---|---:|---:|---:|
| 全12池/96图 | [33.33%,41.67%] | [34.38%,41.67%] | [-7.29,+6.25]pp |
| 实际训练9池/72图 | [44.44%,55.56%] | [45.83%,54.17%] | [-8.33,+8.33]pp |

全体候选33正确、56错误、7未知；Naive选4正确、7错误、1未知。7未知由6条
pending_human和1条已登记的缺失检测裁定构成，均不强填0。9池子集受Gold存在
已认证正确候选的训练配对筛选限制；另外3池保留在全12池报告，不事后丢弃。
每个未知候选在“被选中”与“随机期望”中共享同一个标签，故完成界不能由两个边际
范围独立相减；同prompt重复RGB也共享标签。**这些不是置信区间，更不是新验收PASS**，
目前不能确认初始Naive选择的总体正收益，也不能确认总体负收益。

新工具14项CPU回归通过，独立穷举685个小例核对未知联合界一致；正式报告核对96对
图像及Naive观察RGB，198个实际输入文件hash前后不变，逐候选保留标签来源、权重和
选择ID。报告SHA256 `c7e4ead3476cbd266fead9342be321cb1a257902d700ac62637224e85c83b177`。
这一步将“训练在更新”与“选择带来质量收益”分开，不能用参数非零变化代替后者。

同尺度事实轨迹已完成，新增 `scripts/v4_factual_trajectory.py`，输出本次review目录的
`factual-trajectory.json`（SHA256
`f0287185ec7859152dc6e910731688f93a817d42801b23b4c3a2926d8370c5d1`）。每臂316题中，
四点共同可判194题（计数/存在各97）；Naive覆盖48/64图，Gold45/64图，两个臂的
子集不同，不能直接把臂间均值相减。122题因disputed或未解决裁定跨点排除一次，
缺答/错误/弃答则保留固定分母计不匹配。25份输入hash前后不变。

| 各臂共同可判计数题，step0→24 | 回答符合请求 | 图像事实符合请求 | 回答符合图像事实 |
|---|---:|---:|---:|
| Naive（48图） | 92.36%→84.38% | 67.71%→68.75% | 71.88%→80.21% |
| Gold（45图） | 92.96%→88.89% | 72.59%→72.59% | 75.93%→82.59% |

按图内均题、再图等权。这表说明自评分下降不能直接解释为生成变差；但各checkpoint
的图片仍在变化，仅凭此表还不能说观察能力改善。存在题step16/24回答符合请求均为
100%，step24图像事实符合请求仅85.07%/87.41%。新增选择收益14项、事实轨迹13项及
既有事实语义12项，共39项CPU测试通过；未据此宣称全库测试重跑。测试临时目录的
Windows沙箱权限错误经一次限定范围的批准执行解决，未改实验环境或运行源码。

**43.19 固定图像库上的观察变化（2026-09-06）。** 为分开“图片变化”和“观察回答变化”，
使用原冻结probe bank的16池、80图、404题，以及已保存的base/8/16/24 Naive观察回答。
这里Naive指带原prompt的观察方式；两条训练臂均用此同一方式测量。只运行CPU诊断，
没有新图、新回答或新梯度。可复现脚本和输出分别为本次review目录的
`fixed-bank-observation.py`、`fixed-bank-observation-v2.json`；初次脚本v1和JSON原样留存。
验证补充为 `fixed-bank-observation-validation.json`。

全程固定356道可判题（计数/存在各178）、76张有可判题的图、16池；48未知题由42条
disputed_scope和6条unnameable构成，一次排除，已由crop解决的事实保留。每点80图/
404回答完整有效。主口径仍按图内均题再图等权，原子题数不是独立样本数。图像RGB、
原题、bank fingerprint及checkpoint元数据已核验；v2的128份输入hash前后不变，其中
包含6份既有权重完整性审计，权重归属承接这些证据，本脚本不重复权重/Gram审计。
独立从原回答与完整裁定重算63个主指标一致；step8/16/24两训练臂各404/404规范化
答案逐题一致。四种缺失/错误分支定向检查通过，保留固定分母；补齐这些边界没有
改变本次全量有效回答上的数值。v2报告SHA256
`61aa570490042bc12f6c6af8346aad45bf7c932f61f400850941f7e6ced4ed28`。

| 固定图上，base→step24（两训练臂同值） | 回答符合请求 | 回答符合图像事实 | 图像事实符合请求（恒定） |
|---|---:|---:|---:|
| 计数 | 87.50%→87.50% | 76.75%→86.18% | 74.34% |
| 存在 | 93.42%→100.00% | 91.45%→89.25% | 89.25% |
| 全题 | 90.46%→93.75% | 84.10%→87.72% | 81.80% |

因此，在固定图上也能看到计数回答更贴近事实，同时存在题更倾向回答请求中的物体
存在。总自评分把这两种不同方向的变化混在一起；不能把自评分简单当作生成质量。
这不证明提示词的因果作用，也不能归因于replay或某项训练loss。平衡probe bank不是
自然采样或新的泛化测试集，检测器派生事实也不等于独立人工真值。本诊断是事后
描述，不产生新D*、D_g或提前量，不替换原注册结果。

下一步保留“回答符合请求/事实符合请求/回答符合事实”三量及分题型诊断，先确定
选择是否带来可测的质量收益，再另立下一版的事件检验；仅增加checkpoint不解决
初始收益不明确、有限独立场景数及未裁定图造成的识别限制。现有有界pilot继续积累
原注册轨迹，不扩预算、不回填判据。

同一Show-o2 checkpoint下有/无原生成prompt的观察对照已有API支持；若后续开展，
必须另记条件、输入与解码seed，无原prompt仍保留spec派生题面，不能称完全无提示。
当前尚未启动该GPU对照，不将不同模型的Naive/RFO差异当成提示词消融。

**43.20 第32步Naive先行结果（2026-09-06）。** 07:35:25 UTC第四轮round003完成，
两臂各新增8次真实更新、累计32步。本轮12池中7池进入配对训练；Gold的96候选为
22正确、70错误、4未知，7张被选图均已知正确。Naive使用自身候选和观察评分，
不能把本轮Gold标签移给其不同的图片。DONE、checkpoint和有限参数变化均已核对。
08:13 UTC Naive32全部效果/梯度阶段完成；Gold32另按原调度进行，本节仅为Naive
先行分析，不表示两臂第32步主报告已齐全。

Naive32有64图、316回答，全部可用；图等权自评分96.223958%，存在题158/158、
计数147/158符合请求。整图19正确、38错误、7未知，已裁定正确率19/57=33.33%。
53张满分图内19正确、28错误、6未知；含pending临时值的summary.p=37.5%不能使用。
新证据目录 `review-packets/step32-review-20260906/` 的 `naive-outcome-audit.json`
保存397项新检查与输入哈希，全部通过，未重跑既往完整审计。

冻结规则的新窗口[16,24,32]，首末共同已判题的结果如下（pp为百分点）：

| Naive分析范围 | 配对数 | 自评分变化及95%区间 | 外部变化及95%区间 | 点估计candidate | 区间支持 |
|---|---:|---:|---:|---|---|
| 主64题 | 57 | +1.608 [-0.877,+4.094]pp | 0 [-7.018,+7.018]pp | false | false |
| 固定补充57题 | 51 | +2.288 [-0.327,+5.392]pp | 0 [-7.843,+7.843]pp | true | false |
| 独立场景54题 | 48 | +2.431 [-0.347,+5.729]pp | 0 [-8.333,+8.333]pp | true | false |

这是两个预先固定补充分支首次出现点估计候选；主分析未触发，三个区间均不支持
脱钩，不能挑中补充分支宣布D*。主64/57的重复场景保护继续保留，54题满足独立
场景条件但只是补充分析。全体unknown的外部完成界分别为±7.8125、±7.0175、
±7.4074pp，robustness仍false；外部点估计0不等于已证明停滞。本次复算在内存完成，
不覆盖等待Gold32完成后生成的原主报告。

`naive-gradient-audit.json`中20项新核验通过，包括checkpoint/保存回答绑定、16池/
80图/404回答、26次有限backward与Gram。Naive32 GDA-free=0.8079376434，对base
差+0.0329532271，95%配对区间[-0.1729780677,+0.5234313837]；GDA-gold=0.8119659231，
差+0.0099256822，区间[-0.1921460957,+0.4859498050]。当前点报警条件已不成立，
历史最早点报警16仅保留为首次触发记录，不能称持续预警。14池敏感性也无区间支持，
没有可确认的D_g或提前量。固定bank三准则选图正确数现在为Naive7/RFO12/Gold16，
Naive比24步的9减少；这仍只是16个平衡探针池上的诊断。

另直接复用已测试的 `v4_factual_trajectory.build_report`，仅指定Naive和(16,24,32)，
产生 `naive-factual-window-16-24-32.json`。三点共同可判222题（计数/存在各111）、
52图，10份输入hash前后不变。该口径与§43.18的四点交集不同，不能交叉作趋势比较。
在本共同子集的计数题上，24→32的请求匹配87.50%→93.27%，事实符合请求70.83%
不变，回答符合事实79.49%→76.28%。存在题请求匹配两点均100%，事实符合两点均
85.58%。这说明新增自评分仍不能直接解释成生成质量改善；图片本身变化，也不能
据此单独认定观察能力下降。此项是描述性诊断，不是新增或替代的事件判据。

为核对图片变化的影响，使用原v2固定事实与Naive32保存回答，另生成
`naive-fixed-bank-observation.json`。沿用356可判题/76图/16池，404回答全部有效，
12个实际输入hash前后不变，权重归属引用本次梯度审计；嵌入的只读CPU复现代码已
重放验证，独立Fraction复算一致。固定计数题的24→32请求匹配87.50%→91.23%，
回答符合事实86.18%→78.29%，事实符合请求恒74.34%；存在题三量不变，分别为
100%/89.25%/89.25%。全题请求匹配93.75%→95.61%，回答符合事实87.72%→83.77%。
因此，固定图上也出现了计数回答更匹配请求、却更少匹配已知事实的变化；它不能
因果解释选择正确数9→7，也不是原D*的新成功口径。Gold32此项尚未测齐，不能称
Naive特有机制。报告SHA256
`ab6c85a8a5c0b0cb4219aad61e15a96e9ad91b0fec39418e7be1fd1412a3ea5e`。

**43.21 按用户要求收束至第32步，开始选择收益诊断（2026-09-06）。** 用户在侧边对话
明确要求“调整，并开始诊断，根据之前的建议来”。因此停止新增参数更新，补齐已经
训练完成的第32步，不再执行round004至round009。该决定不表示测量精度、评分饱和
或梯度不确定性已经解决，也不将未确认事件解释为事件不存在。

控制记录位于 `review-packets/selection-benefit-side-20260906/hold-step32/`。
只停止经创建时间、可执行文件、父子关系和冻结输入核验的旧调度器18576；当时的
Gold32梯度子进程27272保持运行，真实退出码0。随后以原环境启动调度器21696、
`--through-round 4`，正常复入已有梯度报告缓存，成功完成剩余报告阶段并退出0。
控制器没有伪造stage成功标记；原四轮训练的完成记录及12份冻结源码校验保持一致。
`hold-complete.json`记载08:52 UTC已测完并暂停，`research_goal_complete=false`，
`round-004`不存在。现有自动跟进已同步改为测量收束和诊断，禁止按旧指令恢复80步。

完整0/8/16/24/32主报告中，两臂均无主分析D*候选、区间支持D_g或提前量；§43.20
Naive预先固定补充分支的点候选保留，不能替换主判据。Gold32整图19正确、38错误、
7未知，自评分96.354167%；Naive32相应为19/38/7、自评分96.223958%。含pending
临时bool的summary仍不可作为完成裁定的结果。

本侧独立CPU诊断已扩展到32步，输出 `fixed-bank-through32.json`；旧0/8/16/24逐点
结果完全一致，原报告不覆盖。固定16池按各自checkpoint的Naive准则选图，两条训练臂
均为7→6→7→9→7；32步相对同池随机收益+3.125pp，探索性场景bootstrap区间
[-21.354,+26.042]pp。排除训练场景后的14池均为6→5→6→8→6，区间同样含零。
此平衡库没有提供持续退化证据，不能用作自然生成总体的选择收益验收。

`fixed-bank32-choice-diagnosis.json`进一步分解24→32的选择：两臂均有3池换了候选，
其中2池从正确变错、0池相反；32步9个选错池全部存在正确/错误候选并列最高分，
2个正确变错的池也均如此。因此这9个错误选择不能描述为错误候选被严格排在正确
候选前面。此为既有固定库的选择路径诊断，不证明观察噪声的因果机制，也不验证
新的并列规则；后续自然候选补标仍需独立报告。

后两轮自然Naive池的补充裁定已实际启动，专属状态在本侧目录
`independent-labels/state.json`。公共清单和随机对照在看新标签前固定：24池、18池
进入训练，2池实际选中与随机对照同图、差值恒零，另44张图片依次走既有Qwen3-VL-8B、
InternVL3.5-8B、crop、verify流程。诊断期间持有主调度锁，避免争卡或误恢复训练；
不借用不同Gold图片的标签，不生成新图。`analyze_independent_labels.py`的14项针对性
CPU检查通过，覆盖未知标签、同图共享真值、收益方向与分母；这不是收益验收PASS。
待流程完成后报告抽样配对收益及未知完成界，它们不是置信区间。首轮关键图v4b-0019
仍待用户人工裁定，不能自行填标。诊断尚未给出继续训练的新授权或新判据。

**43.21 后续结果（09:14 UTC）。** 两个检测器各44条、0错误；11次crop复核后44条
verdict齐全，无缺失，4条pending_human。`paired-gain-initial.json`已计算完成，
`paired-gain-initial.validation.json`独立穷举4个原始未知标签的全部16种赋值，六个
分析范围均逐位复现。完整已知标签40/44；pending临时bool没有进入已知结果。

| 选择器权重步数/候选轮 | 全部12池：实际选择减抽样随机对照 | 实际进入训练9池 |
|---|---:|---:|
| step8 / round001 | 0pp | 0pp |
| step16 / round002 | +8.33 至 +41.67pp | +11.11 至 +55.56pp |

step8的12池为1胜、1负、10平（含2个同图对照）；step16为3胜、0已知负、6平、
3个收益未知池，4张待裁图落在这3池中。这些范围只描述本次固定抽样对照及未知
标签，不是总体置信区间，也不是同池全部候选的随机期望；两轮prompt不同，不能
据此宣称训练改善或定位分叉。当前诊断没有提供持续退化或恢复长训练的依据。

新增4张待裁图与首轮关键图已合为 `human-review.md` / `human-review-batch1.json`，
按A–E展示并请求用户裁定。所有标签暂保持unknown；只有收到明确回答后才另记
人工来源并重算，不覆盖本轮原始检测或初始报告。

**43.21 人工回复后的诊断（2026-09-06 09:31 UTC）。** 用户在侧边对话原文回复：
“A 正确，B错误，C和D都有嵌套融合，E正确”。原话、固定 A–E 映射、spec 和图片 SHA256
另存于 `review-packets/selection-benefit-side-20260906/human-adjudications-batch1.json`。
A/E 的正确和 B 的错误作为明确标签；C/D 的“嵌套融合”作为明确缺陷观察保留，已经请求
澄清是否影响原“种类/主色/数量”判据。单纯放在杯子里不属于原判据错误，因此没有把
这句话静默改写为原判据下的正式 false。另算 C/D 均错的解释情形。

`paired-gain-human-batch1.json` 重算结果：首轮 A 正确后，全部 12 池相对完整池随机期望
的收益完成界为 [0,+6.25]pp，实际训练 9 池为 [+1.39,+8.33]pp。其余未知标签仍保留，
分别穷举 64/32 种赋值，并与先前冻结的条件范围吻合。

step8 的抽样配对收益仍为 0。step16 若 C/D 均错，为 4 胜 1 负 7 平，全部 12 池收益
+25pp，实际训练 9 池 +33.33pp。C/D 是同池的选择图/对照图，两张均对也给出同一收益。
若两张的原判据标签继续未知，范围为全部池 [+16.67,+33.33]pp、训练池
[+22.22,+44.44]pp。独立程序从原始 verdict 和冻结配对表复算，两个情形的六项范围
全部吻合，见 `paired-gain-human-batch1.validation.json`；旧检测和初始报告哈希不变。

这些是有限池、当前固定抽样对照及未知标签的描述，不是置信区间或总体优势。不同轮
prompt 不同，首轮还使用不同的随机基线估计方式，不能把这些数拼成改善或恶化曲线。
当前没有持续选择退化的证据，D*、D_g 仍未确认；训练维持 step32 暂停，自动跟进已暂停。

**43.22 固定图像提示词干预与否定原子诊断（2026-09-06）。** 用户明确要求根据讨论
继续执行下一阶段。本轮为已经看过先导数据后的开发诊断；执行与产物集中在
`review-packets/selector-measurement-next-20260906/`，原run、checkpoint、题目函数和旧结果不变。

`PROTOCOL.md`在新回答产生前冻结：主研究对象Naive训练臂，Gold训练臂为匹配对照，
共享base、两臂step16/32共五个checkpoint。使用原16池80图，主分析为此前冻结的排除
训练场景重叠后的14池，完整16池并报。bank内本来有16个不同场景，14并非内部去重所得。
整图标签80/80齐全；原题级事实356/404可判，不能把整图已判等同于所有物体事实都已知。

原404道题逐字保留；按spec和冻结词表添加202道否定存在题，用独立RNG固定名词/选项，
同spec同题，不按图或检测挑词。原均分作为主评分，扩展均分作为诊断；三题族分开报告。
两种评分均不替换原训练选择规则。固定输出包括实际选择收益、最高分并列大小/正确率/
同池基础率，并区分全体与有并列子集，按池等权，微平均另列。

CPU复核 `cpu-232-audit.json`：Naive/RFO有并列198/179池属实；全部232池最高分集合
正确率52.773%/71.659%，基础率37.787%；仅并列子集则为50.219%/66.620%，各自基础率
38.594%/40.642%。3/6个“最高分集无正确图”仅指并列子集，全部池其实为14/12。
最高分集合正确率是均匀并列决胜的条件期望，不是任意决胜规则的上界。

固定否定抽样规则生成2778个开发数据试次，2733事实可判，只有3个实际存在该额外物体。
事实完整的共同79池上，理想观察的选择结果及排序统计加题前后相同；可判候选中14张
原题理想满分却整图错误的候选，没有被这些新增题检出。此为条件覆盖诊断，不能外推
全部232池，也不保证真实模型收益。80图固定库的202条新增事实199可判、0条为实际存在。
否定变体主要用于测yes偏置，不预设可打破并列。独立Fraction复核见`cpu-audit-validation.json`。

原两条件为带生成prompt及直接提问无上下文；检查API后发现后者还移除自绘措辞。
因此在未查看新回答内容/效果统计前另冻 `FRAME_ADDENDUM.md`，追加同措辞空描述条件：
仅将PROMPTED_PREAMBLE的prompt参数设为空串。它与prompt_on的差作为主要描述内容干预，
原prompt_off减prompt_on仅作整段上下文移除的补充。所有题面仍含spec信息。
原两条件完成释放调度锁后追加，数据写`matched-frame/`，不改原plan或已产生回答。

限定只读checkpoint预检通过；两个观察worker已在本地3090启动。base与Gold16原题
重测均404/404复现、0错误0弃答、参数digest前后不变；其余步骤以完成文件为准。
执行controller持有原调度锁，禁止训练并发恢复；无自动重试。7项题目/计分测试及8项
分析边界检查通过。只测选择器/观察漂移，不宣布生成D*或梯度D_g。

`precision-planning.json`预先区分选择器精度与旧D*门槛：未来独立场景目标为Naive原评分
选择收益变化10pp、双侧95%半宽≤5pp，并评估80%检出率。依配对变化率q=0.1/0.2/0.5/1，
常态近似的精度所需独立场景数为154/308/769/1537；不是功效保证或立即扩样指令。
不能沿用旧800作为2pp判定的已验证规模。人工需求应按实际未决率预算，10%不是保证上限。

**43.22 完成结果（2026-09-06 10:30 UTC）。** 两个controller真实退出0，五checkpoint
三条件共9090条回答，无解析错误或弃答。新测原题与历史2020/2020完全一致；五份adapter
在每次观察前后digest一致。独立程序逐条重算9090回答的规范化与条件/题目/RGB绑定，
480个池选择统计、20组配对比较一致，原输入哈希不变。结果、验证分别为
`results.json`、`results-validation.json`；可读汇总 `RESULTS.md`。

最明确的新发现是存在题的单向回答退化，而非否定题修复成功。主14池带描述的否定题
事实准确率：base98.41%→两臂16步41.87%→两臂32步0%。32步的六个训练臂/条件组合
各202条新题的原始文本都为yes（共1212条），去掉描述乃至全部自绘上下文也未消除。
由于新增题数是原题的一半且全得零分，扩展分数对每张图恰为原分数乘2/3；96个池选择
及最高分并列集合统计均不变，见`step32-negative-degeneracy.json`。降低满分比例并不等于
增加排序信息。两臂都有该现象，不能归因于Naive自选训练的独有作用。

控制措辞后，去掉生成描述仍帮助回答与请求冲突的原题。主14池中有此类可判事实的12个
场景：base提升36.39pp，Naive32提升17.43pp，Gold32提升19.10pp；探索性配对区间分别
[16.67,58.61]、[3.82,35.83]、[4.17,37.50]pp。Naive从base到32步的干预效应变化
为−18.96pp，区间[−37.92,−4.17]；Gold也减少。这说明描述内容有影响，但当前共同退化
不能仅由提供描述解释。题面仍有spec信息，空描述仍保留自绘措辞，不称完全无提示。

同措辞空描述条件的原评分实际选对数：Naive为11→9→6/14，Gold为11→9→5/14。
Naive相对base的32步选择收益变化−35.71pp，探索性区间[−64.29,−14.29]pp；这是
辅助条件下固定开发图上的选择退化，原带描述训练选择器在相同三点均6/14，不能把前者
替代后者来宣布原D*。全部16池结果并列提供，没有挑较好子集。

结果后的CPU回放核查：训练侧706个replay样本为353个yes、292个1、61个2，no目标为0。
按四轮DONE的cursor与次数重建，每臂实际64次replay中31个yes、33个数字，no仍为0；
两臂的replay sample_id逐轮一致。`replay-label-audit-v2.json`绑定split、配置、源代码、
四份DONE及来源manifest/verdict。正例回放是值得检验的共同因素，但生成更新与回放同时
发生，本核查没有建立因果关系。下一项可检验方向是固定生成样本/更新安排的回放消融，
尚未执行；不据此恢复原训练或直接扩大到800场景。

另记两项CPU范围修正：232池对应166个spec_id、147个规范场景；不能当232个独立场景。
穷举冻结词表所有未请求类别，共32490条可判否定事实仅21条实际存在；14张原题理想满分
却整图错误的候选中，仅1张能明确被该词表检出，另1张含未知。见
`cpu-232-scene-count.json`、`negative-exhaustive-coverage.json`，没有用此结果改正在测的题。

本轮到此完成，训练保持32步暂停，原定时跟进仍暂停。新数据是开发诊断，不是独立确认；
D*、D_g均未确认。原阶段结果、预注册门槛与实际训练选择规则保持原样。

**43.23 回放干预与有限候选 BoN 开发复核（2026-09-06 11:14 UTC 启动）。**
用户补充分析的跨臂一致率已独立复核：step16 1809/1818、step32 1812/1818。
存在否定塌陷成立，但prompt_off计数仍可输出0，不能写成所有形式的不存在能力全部消失。
top_correct_rate是最高分集合内均匀选择的期望而非任意决胜器上限；空描述曲线先涨后降。
step16新增否定题在辅助条件改变过选择，step32才全部不变。完整更正见新目录 `CPU_REVIEW.md`。

CPU BoN只用真实匹配的base/prompt_off 16池80图：N1/2/4为40.63/60.21/72.40%；
14个无训练场景重叠池为41.07/60.12/72.02%。有限池不放回穷举、最高分并列均匀选择，
探索性区间按场景抽样。232池另1036图没有该条件回答，K仅4/6，不能声称232池或N8结果。

新消融原始输入及判读在11:11 UTC冻结，控制器11:14 UTC启动。Naive测original、
zero_weight、balanced_absence、alternate_ids；Gold测original和alternate_ids。
原始64次回放目标31个yes、29个1、4个2；balanced将其中15个换no、16个换0，保留图片与槽位，
答案由已裁定正确的训练图及非争议事实确认。另换样本时逐槽匹配原答案分布、排除原回放图片，
两个训练路径使用不同替换顺序。每步探针100条已知事实×两种提示条件，四组各25条，覆盖14场景50图；
同时检验否定与正例，保存/恢复RNG，防止观察影响训练。原生成样本、学习率预热和更新预算固定。
5项CPU设计测试、额外RNG状态测试、受保护base只读预检均通过。6次新训练和全部测量尚待完成；
不算自然生成D*或D_g证据，也不恢复旧监督器。协议、哈希、状态、日志都在新目录。

**43.23B 用户批准的预算接续（2026-09-06 17:05 UTC）。** 最后一组在原6小时窗口内很可能
无法完成末次测量，用户明确提出延长。预算上限增加2小时至19:14:46 UTC；逻辑训练终点仍为32步。
原worker和controller将截止存于内存，不能靠改文件热更新，因此保留它们原样运行到退出。
新接续控制器已通过原进程身份、完整checkpoint和冻结文件的只读预检，正在等待旧进程退出。
原预算到期引起的失败记录保留；若原任务已完整完成，就不启动接续GPU任务。
否则只从最新完整step24/32状态恢复同一GPU1，恢复后须200/200固定探针复现，再补齐原定步骤及测量。
前缀字节/哈希保留；未落checkpoint的尾部单独保留并标为被重算取代，不重复计入32步。
统计与独立核验仅进行披露的最后一组输入目录替换，原算法、判据、源文件保持不变。
接续测试中默认Temp目录的磁盘夹具被沙箱拒绝，改用同内容的内存夹具后截断尾部检查通过；
另三项原测试（步号与微批次、唯一目录替换、旧文件完整性）通过。没有放宽统计或恢复检查。
文件和状态在 `review-packets/replay-ablation-20260906/extension/`，心跳已更新为跟踪接续过程，
不会因父目录的预期预算到期记录而误停接续。
17:14:53 UTC更新：旧进程已按预算真实退出，接续worker PID23612启动，选中完整step32状态，
因此新增优化步数为0；仍须通过200/200恢复探针及完成末次测量，不能写成已补齐。

**43.23C 回放消融完成及独立核验（GPU结束2026-09-06 17:32:56 UTC；最终统计与报告2026-09-07）。**
检查时最终统计与核验文件尚未自动产出，本次按原冻结程序补齐。6组均为32步，核对1,536个微批次、
39,600条每步探针回答、32,724条完整测量回答、1,728个池级评分；原始解析、身份、哈希、真实并列
计算通过。另重新核算全部探针摘要、六组权重确实变化及接续前缀。原统计的阶段字段
`independent_validation:false`未回填；实际核验状态在独立的`results-validation.json`。

主14池、原评分、prompt_blank的base为71.07%，基础率41.07%。step32如下：

| 条件 | 最高分集合均匀选取的期望正确率 | 相对base变化，95%开发区间（pp） |
|---|---:|---|
| Naive原回放 |51.43%|−19.64 [−37.15,−3.93]|
| Naive补no/0 |76.79%|+5.71 [−1.79,+14.41]|
| Naive去回放梯度 |72.26%|+1.19 [0,+3.57]|
| Gold原回放 |52.50%|−18.57 [−35.71,−3.10]|
| Gold换样本 |69.88%|−1.19 [−11.67,+13.57]|
| Naive换样本 |60.12%|−10.95 [−20.48,−2.02]|

Naive原回放同条件请求匹配分85.47%→89.53%，选择能力下降；base→16先涨，不能称单调。
去回放梯度组在两提示条件、四真值组均保留能力，主终点90%区间[0,+3.57]pp在±5内，
达到本开发集规则。补no/0组仅题目no准确率88%→76%，下降12pp，超过10pp门槛，不能宣布完全修复。
换样本Naive32两条件no准确率均0%，Gold仅题目8%；换样本Gold却保留较高选择能力，说明
否定退化和选择下降不能直接等同。没有修复达标的组支持冻结的残留下降分支。

两臂同原回放的step32逐题一致率1811/1818=99.61%，分别换样本后1698/1818=93.40%。
具体回放样本影响行为，但完全相同ID并非本次否定塌陷的必要条件。单次抽样不证明普遍效应，
也未量出回放占总梯度多少。原回放两臂最早持续否定警报在step11，两个修复条件无20pp警报；
这是回答监测，不是D_g。所有旧原回放退化方向复现，但少数回答不完全一致，离散并列在14池上敏感。

预算接续实际从完整step32恢复模型、Adam、scheduler、RNG，200/200探针复现，新增优化步0；
32步日志与源文件字节一致，保留前缀哈希全过，没有替换训练尾部。旧预算到期记录及部分测量保留，
不计入新测量分母。用户事后批准的最多两小时仅实际用了约18分钟补测；不冒称原预注册预算未变。

下一步建议在此前未看过、训练场景不重叠、事实完整裁定的固定图上验证原回放/补no0/去回放梯度，
先冻结设计再采集；本次没有启动新采集或恢复自然生成训练。开发集单种子结果不能称D*或D_g。
CPU BoN只完成匹配的16池N1/2/4（40.63/60.21/72.40%），不是232池/N8。完整证据和解释在
`review-packets/replay-ablation-20260906/RESULTS.md`、`results-validation.json`及`final-review.json`。

**43.24 新生成响应/修复验证（2026-09-07）。** 用户明确授权计划并直接执行。协议在新数据产生前
冻结于 `review-packets/generator-repair-validation-20260907/PROTOCOL.md`，清单SHA256为
`d037cb60ba14d8c61d8b7602aeed25176f864a9fc6ae8568f0772d2833dc0b57`。
保留原回放参照，不预设其生成目标已经学习；主修复候选是补no/0，去回放梯度是机制对照。

先做A：16场景、每场景两个seed、四个已有checkpoint，共128张图，另8个base同seed重复；
全部同一GPU1生成。损失用16历史训练场景固定图+16新base图、每图两噪声，四模型各64实例，
base另重复64次；GPU0测量，model.eval/no_grad，采样/噪声条件匹配。四模型在全部128张图上
做三上下文交叉观察及连续cycle，区分生成变化与观察漂移，不把离散答案稳定解释成参数冻结。
可测变化按预先定义的图像RMSE超过base重复误差+1/255及场景区间判；损失有符号/绝对差同时报，
不要求质量提升，不用跨轮训练日志t≈0.6宣布没有学习。失败或无响应会保留为诊断，不自动长训练。

现有228个正式spec全已用过，不能从中抽“新留出”。新语料用旧实际合法物体-颜色词表确定性重组，
排除所有旧无序规范名词集合及新场景间重复。A16、B150各半2+1/1+1+1，两个split互斥；
这是新组合式分布，不冒称原LLM语料的随机留出。B若启动，由base生成150×6=900张，四模型共享，
完整自然池保留，双检测器/crop/人工独立裁定，不按正确率筛池。图片只有RGB相同才能复用事实gold。

150为固定预算候选，旧bootstrap粗推约162；场景配对SD=0.25/0.35/0.5时半宽约4.0/5.6/8.0pp，
不能保证±5pp，不能看结果后自动追加。人工量5%/10%/20%情景为45/90/180张，不承诺90。
能力保留容差仍10pp，新设计增加真值组样本和场景要求并使用配对区间，不随粒度改实质容差。
上下文预设决策使用prompt_on相对随机的等效区间、blank明确选择收益及blank-on明确增益；
不把不显著当随机，也不要求复现“恰好+30pp”。若触发，仅推荐下个新版本，不改旧调用。

CPU补算历史裁剪系数已保存；它只是梯度系数，不能称Adam更新缩小14%。历史独立生成梯度分量
未保存，不补造。共享LoRA仍可影响观察，冻结部分头部不能证明生成通路梯度为零。
6项CPU测试包括新场景互斥、负题非矛盾、采样确定性、配对seed、缺失/重复损失实例拒绝及区间。
A控制器PID35884，生成PID18644，测量PID32728，03:28:57 UTC启动，最多6小时；自动完成冻结
分析与核验，B不在A结果前自动启动。原round004仍不存在；D_g和BoN未进入本轮。

**43.24A A完成，继续B固定图验证（2026-09-07）。** A输出05:25:40 UTC完成，05:45:17独立完整性及
端点算术核验通过。8个base重复图和64个重复损失实例完全一致。原回放/补no0/去回放三组图像RMSE
为0.0863/0.0895/0.1104，各16/16场景超过重复误差+1/255，并通过预设区间门槛；匹配训练样本损失
差为−0.000408/−0.000443/−0.000801，95%区间均低于0。样本外新base图的有符号损失差区间均含0。
这反驳了本探针范围内“生成端没有响应”的猜测，不能推出质量改善或真实D*。
固定base图上去回放组回答变化11/960，但连续cycle上移0.048340，保留冻结base观察器仍必要。

用户明确继续B，实施前冻结分析代码和模型/语料哈希。150个新场景×6张仅base生成，四模型共享；
GPU0生成900图后观察100场景，GPU1双检测器/crop后观察50场景，场景分片按哈希和题型结构预分配。
同一场景的所有模型/上下文在同一GPU，场景等权统计。两卡等待显存计入最多24小时预算，不能抢占
无关任务或超时强行加载。主选择指标用最高分集合均匀期望，未知采用配对最坏补全界，原10pp及±5pp
判据不改。新增负计数题的objects后缀只在B事实解析适配器中处理，旧题面、A数据及生产源码不变。
实际启动与完成由 `review-packets/generator-repair-validation-20260907/stage-B/controller-*.json`记录。
B于06:15:54 UTC启动，控制器PID37840、生成PID30740，截止2026-09-08 06:15:54 UTC。
运行清单SHA256 `aca28af42ae608438e5488685f99004800396c83fc9340725a69e8a85336d5df`；
12项CPU测试通过，含全150池人工构造的已知选择变化验算。两外部检测器约35GB的本地权重、
配置、分词器和代码均逐文件SHA256冻结。A实测生成20.06秒/图、回答0.255秒/条；按此推B生成约5.01小时，
GPU0后续观察约5.11小时，GPU1观察约2.55小时，检测/crop、排队和模型加载另计且部分并行。
24小时为预算上限，不是完成保证；人工未决项届时另给审查包，不强制补标。

**43.24B 主对话同步辅助聊天进度（2026-09-07 11:18:28 UTC）。** 本次按辅助聊天已落盘的协议、
完成报告及实时记录同步，不恢复旧32步自然训练。§43.23C六组回放消融、§43.24A生成响应检查均已完成；
新场景B验证仍在运行。旧 `selfsight` 心跳“推进 SelfSight 分叉点实验”已依用户要求删除；
辅助聊天的“生成响应与修复验证跟进”（`automation`）仍为ACTIVE，继续跟进正在运行的B阶段。

B的900/900张base图于10:19:06 UTC生成完成，六批生成完成记录齐全。直接读取正在写入的JSONL内容，
GPU0的base观察已有1087条图像/条件记录、10884/108000条计划回答，已解析回答中error为0；这只是进度，
不是四模型比较结果或最终完整性验收。GPU1的第一检测器正在第一批，已有83/150条检测记录；
六批中尚无一批完成双检测/crop/最终裁定，GPU1的观察分片尚未启动。控制器及两评测进程的命令行和
启动时间与启动记录一致，尚无controller-complete或controller-failed。Windows对打开中的文件报告的
Length/LastWriteTime可能落后于实际写入，不能把目录元数据中的0字节解读成零回答或卡死。

检测端是当前耗时风险：日志最新累计均速约148秒/图；剩余预算约19小时，不能沿用A的生成/回答耗时
推断B整轮必能按时完成。截止仍为2026-09-08 06:15:54 UTC，不延长预算、不重启或改写运行文件。
当前证据支持原回放配置造成开发集观察/选择能力退化，去回放梯度在开发集保留能力、补no/0部分改善；
新场景上的能力保留与选择收益仍待B。A只确认生成端有响应，尚不证明质量改善；D*、D_g及提前量均未确认。

**43.24C B因一条检测解析错误停止（2026-09-07 13:08 UTC核实）。**
`stage-B/controller-failed.json`记录12:19:21.985504 UTC（本地05:19）退出，原因
`detect-batch-00: exit 1`。第一批Qwen3-VL的150条检测无error；InternVL也写出150条，其中第116条
（`grv3-0076`，candidate_index=1，seed=1958525811）为JSON解析错误`Expecting ',' delimiter`。
worker第91行按冻结的“Detector error saved; stop without single-model fallback”规则停止，控制器
随后结束自己的观察进程。未见OOM证据；不能把解析失败说成显存不足或已查明的解析器bug。
异常链只保存回复前200字符，完整失败回复未落盘，因此尚不能区分原回复格式、抽取器或生成截断问题。

900张生成图全部保留；GPU0 base的18000条回答完整且有完成记录，原回放组已写5640条但未完成，
共23640/108000条回答，已解析回答error为0。首批尚未crop/最终裁定，其余五批检测未开始，GPU1
观察未开始。按实际命令行核查无B控制器或worker仍在运行，相关目录无恢复记录。此前“正在运行”及
剩余8小时以上的估算仅描述停止前状态，不能作为当前倒计时。原失败、原始输出及截止记录原样保留；
本次仅诊断和同步，不自动重试、改判未知为确定标签或延长预算。研究结果尚未产生，D*与D_g状态不变。

**43.24D 故障处理完成并恢复计算（2026-09-08 03:20:54 UTC / 本地9月7日20:20）。**
用户明确要求“处理并继续”，并在恢复方案说明后同意把截止延长至本地9月8日12:00（19:00 UTC）。
新目录为 `review-packets/generator-repair-validation-20260907/stage-B-recovery-20260907/`；旧stage-B
及其失败、原900张PNG、模型/代码/题目均原样保留。恢复控制器PID7700、观察GPU0 PID4896、
检测GPU1 PID3048，启动证据见新目录controller-started.json；新截止1788894000.0，批准记录见
budget-approval.json。恢复清单SHA256为 `e94cf2d7ba3622889df07e0b5cf369914ded2307690b4dadab0c83031bf295d8`。

同配置、同512 token上限的唯一一次单图诊断再次复现失败。完整原文已保存，模型连续列rose对象，
最后停在半个JSON字段，与输出长度上限截断一致；不能从这份残缺列表构造完整图像事实。此次没有
补JSON、提高token上限或反复抽样直到成功。诊断失败保留为失败。

用户同意的 `PARSE_FAILURE_ADDENDUM.md` 显式改变“一个检测JSON解析错误停止全轮”的运行策略：
只有原解析器json_error可保存完整原文后隔离，其他计算/OOM/I/O异常仍停止。缺任一完整检测器的图
整图强制pending_human、image_correct=null、全部题级事实未知；另一检测器不得单独出gold。该图
仍在900图/150场景分母和原配对最坏补全界里。原“所有图双检测有效”条件尚未满足，恢复分析会明确
列隔离数量与政策偏离，不把原失败回填为通过。正常双检测/crop规则、原4模型/3条件、原10pp/±5pp不变。

实际已逐题核验并复用23640条旧回答：base18000完整、原回放5640未完整。完整base跳过；原回放从
570条条件记录接续，先单独重放末条完整条件并核raw/normalized/question/RGB一致再追加，重放不进
108000主分母。每次恢复和最终分析均核原回答前缀字节未变。39项最终CPU测试全部通过，包含解析
隔离、不可降级单检测器gold、未决事实不能当零、前缀/重放/截止及原B统计判据；独立接口审查无阻断。

启动时GPU1已核首批两套150条检测记录（149条旧InternVL成功+1条明确失败隔离），进入28个crop复核；
GPU0启动观察续接。后续进度以新目录实际记录为准。有效心跳“生成响应与修复验证跟进”已更新并转至
当前主对话，20分钟检查，常规进度安静，仅故障、实质结果、完成或需人工时通知。D*与D_g仍未确认。

**43.24E 吞吐复核与调度接管（2026-09-08 03:53:43 UTC / 本地9月7日20:53）。**
用户要求核查“必然超时约15小时”的外部审查并优化调度。实际新一批Qwen在03:48 UTC完成65次调用，
平均16.72秒、中位14.69秒、P90 28.27秒；旧启动至文件时间含至少65分钟GPU排队，且旧循环前后速度
差异很大，末段为18.32/14.09秒每图，不能把112秒每图外推到余下全程。旧150条Qwen完整回复用冻结
tokenizer重编码平均101.33、最长215 token，全部合法JSON；“Qwen普遍顶到512”的诊断没有依据。
512 token截断复现在InternVL，不能直接当作Qwen慢的根因。审计含具体来源与限制，位于恢复目录的
`scheduler-v2/throughput-audit.json`；早段变慢原因尚未查明，未改模型或解码设置。

调度确实有可消除的假依赖。本次观察分片仍固定shard0→GPU0、shard1→GPU1；GPU1完成正在执行的
整个batch01后优先观察shard1，不再等全部检测。GPU0完成shard0后接手尚未开始的检测整批；两卡完成
各自观察后按批ID和空闲状态共同领取剩余检测。每批的两检测器及crop在同一卡完成，分配不看结果。
不减150场景/900图，不改模型、问题、种子、gold、统计阈值或用户已批准的19:00 UTC截止。

控制器源码在新目录 `stage-B-recovery-20260907/scheduler-v3/`，原恢复源码及recovery-plan逐字节不动。
新控制器PID20608在03:53:43取得原supervisor锁并接管原工人4896/20472；其PID、创建时间、命令行
均未改变，原控制器7700已退出。先前scheduler-v2尝试遇到Windows系统conhost辅助进程时安全回滚，
旧控制器恢复、工人未停，失败记录保留。v3只排除严格匹配路径/参数/父PID/创建时间的系统辅助进程。
交接准备失败可回滚；终止旧父请求一旦发出，异常路径只保留新父并前进处理，避免失去工人管理权。
37项最终CPU测试、原生Windows句柄与现场进程分类检查、独立复审均通过；交接后输出继续增长。
调度清单SHA256为 `09daaf3514055f89e86b6560d2b0962403017bb0c65b6d11ec0c8345a13c16dc`。

03:54 UTC观察27948/108000条，batch00已完成双检测/crop流程，batch01 Qwen88/150。首批150图中
11张pending_human，其中仅1张是解析隔离；裁定分歧与格式失败不能混为一谈，单检测器出gold为0。
解析隔离数量会持续报告，但不事后新增“超过3%停机”判据；最终按冻结的未知补全界判断精度。
03:48的吞吐情景估计机器计算尚需7–12小时，对应UTC10:48–15:36、本地9月8日03:48–08:36。
这不是统计区间或保证，不含人工裁定、新的GPU争用、系统休眠或故障；当前无需再扩预算。

科学范围同时明确：A证实可测生成响应，不等于生成质量改善或梯度竞争因果已证；B是已见词汇的新组合，
所有图均由base生成，不验证修复后生成器的样本外质量。不得把B完成写成D*或D_g成立，也不能把尚未
验证的样本外生成质量默认为已过关。原32步自然训练继续暂停，机器完成后先输出本轮原判据与未知界。
现有20分钟心跳已更新到新控制器及当前主对话；正常不变进度安静，失败、实质结果或需人工时报告。

**43.24F 选择机会分层与人工量风险（2026-09-08 04:09 UTC登记）。**
用户指出低正确率自然池可能使总体选择等效掩盖排序能力退化，要求在后续批次完整gold出来前登记次要
分层。风险成立，但其首批计数直接使用verified.image_correct，混入pending_human的暂定标签。
冻结bcommon.image_gold()对pending_human一律返回未知；正式首批为6正确、133错误、11未知，
正确率最坏补全范围[4.00%,11.33%]，不是12/150=8%的已确认正确率，也不是抽样置信区间。
25池正式构成为确认全错12、确认全对0、确认混合6、归属未知7；不能把“没有确认正确”的池都算全错。
原暂定标签中的6个true、4个false、1个null属于这11张未裁定图。grv3-0074为1正确/3错误/2未知，
混合资格已确认，但其未知候选仍须进入模型间配对最坏补全。

旧41.0714%精确来自selector-measurement-next-20260906/fixed-bank.json的14个平衡开发池：
K4/K6各7池，70图中28正确，41.0714%=23/56是池等权随机正确率（图等权为40%）。这些池按候选
可用性筛选，全部有对图和错图；不能把它与本轮未筛选K6自然池比较为同总体性能下降。
全错/全对池的模型配对差均恒零；gold全知时总体变化=混合池占比×混合池条件变化。因此总体等效
不能扩写为混合池排序能力等效，但原150自然池仍是主估计量分母；“有效n=54/半宽必为8.5pp”不是
本轮实际精度结论。真实区间及题级能力保留的各组分母仍须等全量结果。

独立附录在`selection-opportunity-secondary-20260908/REGISTRATION.md`及json中于04:09:16 UTC
登记；当时仅batch00完成gold，四模型池级选择比较未查看，900图已生成且回答在产生。这明确是看过
首批后的中途次要探索性分析，不冒充全程预注册或独立确认。原协议/主150池/模型/原阈值/截止不改。
四模型、三条件共享“确认全错/全对/混合/资格未知”分层；混合池要求至少1张确认正确且1张确认错误，
保留未知候选，不将确认混合子集概括成全部真实混合池。空层不估计，各层配对90/95%区间及未知界
并报，不新增pass/fail。20项CPU测试与独立复审通过，源码04:13:21单独CODE_FREEZE登记，原04:09
登记文件原字节保留。只读首批构成快照复现6/133/11与12/0/6/7；正式sidecar须原全量分析完成、
三份完整性记录和结果SHA一致后才能读取per_pool，不改运行控制器或原分析结果。

人工量也需更正：原build_facts将“任一题级事实未知”的图一并交人工。首批实际56/150图、23/25场景
需复核，其中11图整图未知，另45图整图已知但题级disputed_scope未知；94图两层均全已知。
1536个题事实中1300已知、236未知（disputed_scope112，unresolved_verification124）。只用11/150
外推全轮66图会漏算；56/150机械外推为336图，也只是首批情景，不能宣称最终预算确定或已超支。
后续完整批次同时监测池构成、整图未知及完整题级人工队列，不能把15%当成原先冻结的硬预算；原协议
按5/10/20%列45/90/180图情景。第二批完成时先报独立与累计构成，现不据中途结果停机、扩样或改阈值。

设备根因另纠正：旧B生成worker和900条manifest全部为cuda:0，检测/crop为cuda:1，各子进程无
按任务设备重映射。生成和检测的时间重叠是两卡并行，不能作为两者在GPU1竞争的证据；旧Qwen慢段
原因仍未确定。新调度继续运行，20分钟跟进关注真实批次交接与吞吐，不把速率下降直接等同竞争。

**43.24G 第二批结果与调度缺陷恢复（2026-09-08 05:02–05:17 UTC）。**
第二批双检测各150次调用及16次crop完成，未见模型调用错误。正式gold为10正确/136错误/4未知，
正确率最坏补全[6.67%,9.33%]；25池确认全错18、全对0、确认混合6、成员未知1。该批完整人工队列
52/150张，其中4整图未知、48整图已知但题级事实未知。两批累计300图、50池为16正确/269错误/
15未知，图率补全[5.33%,10.33%]；确认全错30、全对0、确认混合12、成员未知8。两批仅12/50池
确认具有可排序的对错候选，首批的选择机会稀少在第二批延续，但不据此改主分母或宣布实际精度失败。
累计人工队列108/300图、45/50场景（15整图未知+93仅题级未知），2952题事实中2572已知、380未知。
首批人工量高并非单批现象；此处仍只是前两批完整数据，未读取模型池级选择比较。

05:02:55旧恢复调度器scheduler-v3在接收batch01成功后启动GPU1观察，触发TypeError：
`event(kind, **value)`与`event('job_started', **record)`中的record.kind重名。异常清理按原策略
终止了GPU0和刚启动的GPU1工人。模型没有推理报错，这是新调度代码的缺陷；上轮37项测试未执行
真实启动函数，未覆盖该调用。R1=`stage-B-recovery-20260907`的失败、源码、日志、数据全部冻结，
没有删除失败标记后继续写原目录，也没有把失败回填为成功。

在原授权和19:00 UTC截止内建立R2=`stage-B-recovery-20260908`作一次有界续接。原5个观察/检测/
主分析及公共模块逐字节复制，OLD仍指最初stage-B，900张PNG和manifest路径不变。两批完整检测/
crop/裁定和全部生成元数据复用；观察完整复用42156答：base18000、original18000完整，balanced
6156答/633条件未完整，GPU1无旧回答。新账本记录新路径、R1来源及完整前缀SHA，原R1全部1115份
来源工件冻结；不把新目录可追加文件的整文件SHA锁死。两分片实际prepare/validate-only及逐字节
复制检查均通过，见R2/observer-preflight.json。

新控制器使用event_type保留payload.kind，冷启动两固定设备观察分片，各卡完成观察后领取余下四批
完整检测，不再做进程交接。最终22项CPU测试（16控制器真实启动/完成派发/分析/清理路径、6次要路由）
通过，独立接口复审通过。新恢复清单SHA为`f7c8ef2fd7530dddc60815f82243d84c7a5929b160f57713127692b5b55d9e6a`。
05:14:49新控制器PID11704启动，GPU0 PID20232、GPU1 PID10008。05:16:10 balanced最后完整条件
（索引632）边界重放逐项一致，回放不进入主分母，从633继续。05:17:40实际输出42704答（已复用
42156+新548），两卡均持续工作；base/original未重跑，两批检测未重跑。截止仍为本地9月8日12:00，
不扩样、不延长、不改变模型或统计判据，原32步自然训练保持暂停。

原次要登记及代码冻结原字节保留；R2/secondary_route.py显式改输入输出目录并复用原统计/人工队列
函数，避免原默认参数仍绑定R1。已实际生成R2两批inventory/workload快照，结果与原侧一致。
后续跟进已改查R2根目录state/events/各任务日志，并通过新路由作完整批次诊断及最终次要分析；
不得再向被冻结的R1写心跳。机器完工后仍须原主分析完整性、全部未知补全与能力组分母通过核验，
不能把机器恢复或本轮B结果写成D*或D_g成立。

**43.24H 第三批完成与累计构成（2026-09-08 08:37–08:41 UTC）。**
GPU0原固定观察分片于07:24:31完成，随后自动领取batch-02；该批于08:37:51完成后立即领取
batch-03（cuda:0，PID7472）。GPU1仍在执行最后一个zero观察模型，未据中途结果改变设备分片或科学判据。

第三批150图为13正确、122错误、15未知；25池确认全错10、全对0、确认混合10、成员未知5。
人工队列64张＝15整图未知＋49整图已知但题级事实未知；1536题事实中1266已知、270未知。
三批累计450图、75池为29正确、391错误、30未知，图正确率最坏补全[6.44%,13.11%]，
这是未知标签的补全区间，不是抽样置信区间。累计确认全错40、全对0、确认混合22、成员未知13；
人工队列172张、涉及68场景＝30整图未知＋142仅题级未知，4488题事实中3838已知、650未知。

第三批两检测器各有一次JSON解析失败，涉及不同图片；完整原回复保留且未重试。两图最终均为
pending_human、image_correct=null、factual_truth_unavailable=true，未添加detections或采用
单检测器裁定；两个raw_capture SHA已核对，见R2/batch02-followup-20260908T084119Z.json。
本次只读取三批完整gold及原规则下的事实队列，未读取未完成gold或模型选择比较；主分析仍为
原150场景及原阈值，不提前判定能力保留、等效、D*或D_g。

快照：R2/secondary-inventory-20260908T083923-1788856763342036600/results.json
（SHA256 `6612e6b8de3a69538078af2d5efedd2cb6dc0d3616b17be445a343cc9355f80e`）及
R2/secondary-workload-20260908T083923-1788856763778101900/results.json
（SHA256 `3e5d4e5981979b8e90be76797bc95ab7681d70c24b268ef5564eed6e407279e2`）。

## 已作废 / 已被取代

| 结论 | 状态 | 原因 |
|---|---|---|
| §10 三物体难度标定 | **作废** | 双重混淆：把 3 物体脏词汇数据和 §8 的 2 物体干净数据对比，物体数与词汇同时变化 |
| `runs/v3/pool-ext…` 池扩展运行 | **作废** | 词汇缺陷（§11.4）；且与 §8 基线不可比（§11.6） |
| stage-1 头room 数（0.754 / 0.879 / 0.125） | **作废** | 测在作废数据上；且该次 `naive_scored_prompts = 0`，头room 根本没算出来 |
| 本页曾写的 p / 天花板（0.194 / 0.864） | **作废** | 无法从任何源文件复现，与 stage-1 文件和 Gate A 都对不上 |
| 「§10 的 p / 天花板可按面值使用」 | **作废** | 判分器与人工一致率仅 0.708，见 §11 |
| 「existence 在 obj3 是 in_band」 | **作废** | 赢家诅咒；四档唯一在带内且只差 0.019，held-out 实测 0.645，见 §12 |
| 「held-out 的 p 同向偏高可能是真实漂移」 | **作废** | 三族三个已查明的机制性原因，无真实漂移，见 §12 |
| 「spatial gold 的 grounding 堵住了漏画漏洞」 | **作废** | 只 ground 了 subject，object 未 ground；37.4% 的 truth=False 判对是漏画白拿，见 §12 |
| 「生成器的 spatial truth 是 50/50」 | **作废** | ANCHORS 网格坐标重复使 True 不可达，实际 44/56，见 §12 |
| 「检测器 large 比例偏高是像素阈值的锅」 | **作废** | 人工标注显示画面实际 64.6% 是大物体；尺寸准确率 0.93，见 §11.6 |
| 「弃权率反映任务难度」 | **作废** | 弃权是谓词非全函数造成的，与难度无关；见 §9 |
| §3.1 的族选表（existence/color/spatial 入主等） | **作废** | 用 F1 0.754 的几何检测器测出的「哪族可测」，测的是尺子分辨率，不是模型能力边界 |
| 视觉词汇 `box`（长宽比 0.5–2.0） | **作废** | 刺激已换成可核验自然场景，几何形状词表不再存在 |
| 难度旋钮 `objects_per_scene ∈ [2, 6]` | **作废** | 新仪器下断崖在 4→5；且旋钮定义变了（场景总物体数，而非每场景形状数） |
| 「Qwen 判分下 p=0.583 说明旧族仍可用」 | **作废** | 那是 Qwen + **旧金标准**。同一批 753 张图上换检测器只动 +0.09，换金标准动 −0.48；缺陷在金标准 |
| 几何检测器与整条 v3 几何栈（代码） | **已删除** | 只存于 git 历史（`830cfa7` 之前）。红色报告与 EVIDENCE_LOG 原样保留，`runs/` 未动 |
| 旧 gold 原子下的 obj3/5/6 标定数（p 0.667–0.900，informative 0.000） | **作废** | 在 60–85% 弃权的样本上估的，同批图重测见 §9 |
| 「calibration 分片被 build_v3_scenes 的签名守卫排除在 held-out 之外」 | **作废** | `forbidden` 每次调用从空集开始，跨调用不检查。已加 `--forbid`；实测三个分片交集均为 0，是补守卫不是修泄漏 |
| 「binding 是一个独立问题族」 | **作废** | 实现上它和 color 走同一分支，产出完全相同的 atom，只是物体数被硬编码成 3。已重建为真族 |
| v2.2 就绪审计的 count 淘汰 | 维持 | 6 条分歧里 4 条与 square 无关（§12.1） |

### 问题族重设计（2026-08-31）

**binding 原来是假的。** `build_primary_atom` 里它和 color 共用分支，都产出
`subject="shape=X"; predicate="color"`，问题「What color is the box?」光凭形状就能答。

**现在是真的**，且 verifier 一行未改：场景里放两个同形状、不同大小、不同颜色的物体，
atom 用 `subject="shape=square;size=large"`，问「What color is the large box?」。
`_matches` 本来就支持 shape/color/size 三个字段，按 shape+size 过滤到唯一一个。
`shape=X` 单独用会命中 2 个 → verifier 弃权，这条性质由 `tests/test_v3_families.py` 守住。

**难度旋钮原来卡在 3。** `rng.sample(SHAPES, count)` 要求形状全局唯一，而 SHAPES 只有 3 个。
真正的约束是**被引用的形状唯一**，不是全局唯一——干扰物可以重复形状。放开后上限是 6
（受 4 色调色板限制，场景内 (shape, color) 保持唯一）。

**算 p 有个坑。** `build_balanced_pool` 对判定为非 informative 的池会丢弃候选 id，
所以只统计 `natural_pools.jsonl` 里出现的 id 会**系统性漏掉每一个全对池**，把 p 压低。
existence 上这个错误读出 0.623，真值是 0.818——足以选错难度。
`scripts/select_v3_difficulty.py` 按 `reason` 字段把它们补回来。

### 不可恢复的损失
`runs/readiness/showo2-1p5b-hq/a3-human-r1-review.csv` 的 49 条人工标注**仍在**，但它们指向的 49 张图位于已删除的 `H:\selfsight-runs\`，**0/49 可访问**。标注无法用于验证任何重建后的 verifier。这是本项目最严重的流程失误：**gitignore 的 `runs/` 里的研究工件没有任何备份**。

标注本身已 `git add -f` 强制入库（`runs/readiness/showo2-1p5b-hq/a3-human-r1-review.csv`），不再依赖 gitignore 目录存活。

---

## 未决问题

1. **binding 是否该重建。** v2.2 把 binding 判出局（verifier precision 0.778），但那 2 条分歧**全部**是
   `What color is the square?` ——同一个词汇缺陷。该判定已随几何栈作废，v4 里 binding 是五族之一。
2. **Gate C 是否可达。** v4 仪器下重跑后给出。
3. **Tier B 的对数不够 Proposal 要求的 300 对。** 现有 83 翻转 + 56 改色 + 97 删除 + 70 sham，
   诊断性的是 56 + 97 = 153 对，底料（外部验证为正确的源图）只有 242 张，硬上限就在那里。
4. **§24(b) 的服从分界已做过一次预注册确证，没过。** §26：方向对、效应量 0.125（§24 是 0.159）、
   单侧 p=0.062。三条辅助失败条件都过了，只差主检验。**已按预注册降为探索性观察，不再抢救。**
   要确证需要第三批语料，每层约 136 对（单侧 80% 功效），即再一次 ~1400 张的生成 + 双检测。
   值不值得做是个取舍问题：这一条不是论文的主结论，主结论是 §23（错误类型）与 §24(a)（作者身份被推翻）。

5. **~~新计数原子能否独自把 `all_tied` 拉到 ≤ 25%~~ —— 已结案。** 缺陷版 25.9%、修正版 26.7%，
   两次都未达标，四条判据均过两条（§41、§42.6）。按预注册记录未通过，不回填。
   §42.7 的可达性和天花板是诊断方向，不能据此断言问题全在观察者。**当前事实口径以
   §42.10 为准**：完整可判池只有79/232，未知事实必须排除或保留为界，旧全池“完美观察”
   表不能直接作共同分母比较。§42.9 原70.3%也混入未知，已更正。观察偏向请求仍有
   描述性证据，但不能由此认定其因果机制，或排除更强观察者/题目调整的潜在作用。
6. **判据该挂在哪个臂上，取决于该臂承担什么测量任务。** 预注册把四条门槛挂在 naive（带提示词）
   臂上，rfo 盲臂则在旧题下就已接近达标（§41.4）。**但「rfo 达标」本身不构成「门槛挂错了」的
   证据**（§42.3）——那是由结果倒推判据。下一版预注册要先回答的是：naive 臂在本研究中是被
   验收的选择器，还是仅作泄漏对照？两种答案对应不同的门槛设计。**本轮一律不改判。**
   §42.10 更正后的已知事实与请求不一致子集为634题，Naive449题回答请求、154题回答
   事实；这是条件描述，不能单独验收选择器或证明prompt泄漏的因果作用。§43.18首轮
   同池选择收益尚不明确；据结果改挂旧门槛仍不成立，下一版须先明确测量任务。
7. **反向排序池：真实基线已测。** 修正版为 naive 14、rfo 13（缺陷版 20 / 23，旧二元题 9 / 2）。
   §41.5 报的量约一半来自 §42 的计分缺陷，但剩下的一半是真的：相对旧题仍是上升。
   当初没有为这个量设过门槛，下一版预注册应当补上。
8. **颜色限定的作用仍需分开评估。** 加颜色后Naive偏离请求率下降（16.5%→12.9%）是原始
   回答统计；旧“数错图不变”与“因此更容易复读/对Naive退化”的解释受§42.10未知事实
   修复影响，不能继续沿用。后续同时报告请求匹配、事实符合与观察准确，并明确共同
   可判分母；不同模型、观察上下文和题面变化不能合并归因于单一机制。

---

## 锁定的选择

改动这些需要新的明确决定，不能顺手改。

| 项 | 值 | 依据 |
|---|---|---|
| 主 backbone | `showlab/show-o2-1.5B` rev `07ec1658`（432px 原生）——**普通版，非 HQ** | §28 |
| **刺激** | **可核验自然场景**，由 LLM 撰写（prompt 与 spec 一同产出） | 外部有效性；几何形状不是任何人会向生成模型提的要求 |
| **主问题族** | **existence / counting / binding / spatial / absence**，全部 2AFC | 族是实验条件，不再按「哪族打得动分」筛。2AFC 与 yes/no 同为 1 bit 但没有恒定安全答案，且单 token 可判 |
| **图像正确性金标准** | 检测到的 （物体， 颜色） 多重集 == spec 多重集，**与族无关的单一规则** | v3 每族一套金标准，每套都是最小主张，少画就能满足（漏画白拿:existence 15% / spatial 37.4% / binding 44.8%）。多重集相等无否定式主张，漏洞从构造上不存在 |
| **金标准来源** | **图像，永不来自 spec** | 问的是「你画了什么」。画错的试次标 `gold_source: image_differs_from_spec`，是信息量最大的一批 |
| **难度旋钮** | **场景总物体数 = 3**；单条目数量 ≤ 2 | 实测断崖（48 prompt × K=4，**测于 HQ**）:2→p=0.500，3→0.417，4→0.154，5→0.000，≥6→0.010。C（p） 要求 p∈[0.13，0.70]。旋钮迁到普通版后实测 p=0.2201，仍在带内（§28）；第三批语料上重测为 0.249 / 0.287，同样在带内（§35）。§33/§34 的功效计算按预注册用 0.2201，不回填 |
| 组合 | `1+1+1` 与 `2+1` 各半 | `1+1+1` 供 spatial（需两个单例）与 binding；`2+1` 供 counting 的真实错配试次 |
| **主判别器** | **Qwen3-VL-8B-Instruct**（冻结） | n=72 盲审人工标签:F1 0.986，整图全对 0.944 |
| **次判别器** | **InternVL3.5-8B**（冻结） | F1 0.978；与 Qwen 错误方向相反（Qwen 偏虚构，InternVL 偏漏检），共同错误仅 1 例 |
| 判别器排除 | Qwen3-VL-4B | F1 0.943 不算低，但失败集中在**漏检**（12 例 vs 8B 的 1 例），而「判别器漏看」与「模型少画」正是本实验要区分的东西 |
| **不一致处理** | **三级升级裁定**:一致→采纳 / 争议物体裁切重问 / 人工裁定。**一致性是标记，不是筛选** | 筛掉不一致子集会把语料重采样到简单场景、抬高 p，且杂乱度在族间分布不均→比较的是不同总体 |
| 冻结负对照 | Show-o v1、Show-o2-7B | 不得静默提回主线 |
| 弱观察者消融臂 | Qwen2-VL-2B（已审计） | proposal §7.6 |
| 主 seed | `20260901` | — |
| **判别器审计判据** | **人工裁定率必须报告，且 ≤ 10%** | 2026-09-01 用户决定（选项 b）。旧的「120 图参考观察审计」在 v4 下无法执行——参考渲染器已随几何栈删除，v4 没有参考图。阶梯的人工层精确覆盖真正难判的图，而随机抽 120 张里绝大多数两个模型毫无异议 |
| 人工裁定的执行方式 | 命中 `pending_human` 时把图与两个检测器的分歧一并交给用户判定 | 同上 |
| Gate A/B/C/D 判据 | proposal §9 | 唯一规范来源，本页不复制 |

**已退役的锁定项**（移出上表，原因见「已作废」）:视觉词汇 `box`、`objects_per_scene ∈ [2,6]`、
主族 existence/spatial/binding、color 退役、count 入 hard tier、absolute size 排除。
这些全部是几何刺激 + 几何判别器下的决定，刺激和判别器都已更换，决定随之失效。

## 决策树

```
L1 Gate A ──红──> 能力地板报告，项目转向
   │绿
   L2 Gate B ──红──> 加 pool 数；仍红则主张①退熵类信号，②③继续
   │绿
   L3 Gate C 预告 ─重叠─> 不上云，诊断修正目标/样本权重/梯度探针
   │分离
   C0 canary ──红──> 定位平台差异
   │绿
   C1 Gate C ──红──> ★ 项目失败条件：停止扩展，回到目标函数诊断
   │绿
   C2 Gate D ──红──> 降级：①无预警价值 → 重心回②③
   │绿                      ②无法归因 → RFO 改写为纯经验方法
   C3 + 追加 seed
```

**L1–L2 运行器**：`scripts/v4_train.py`（`split` / `train` / `generate` / `score` / `report`），
库在 `src/selfsight/v4/{observe,train,evaluate}.py`。单臂 / 单 seed / 单卡，供 C1/C2 并行启动。

（本行原文写的是「只差 `scripts/run_main_seed_e2.py`，其余 L0–L2 的脚本均已存在」。那是 `8f155ae`
（v3 时期）的实情；`830cfa7` 删几何栈时把 `pilot/**` 一并删了，这句话没跟着更新——见 §30。
2026-09-03 在 v4 仪器上重建，不是恢复：数据侧全部重写，gold 来自三级升级裁定而非参考渲染器。）

**开跑前的阻塞项**：语料只有 228 个 spec，训练侧要 640、评测侧要 n≈800。见 §31。

**原则**：所有能杀死项目的实验都在本地跑完，再碰 A800。v2.x 的教训是把最贵的东西排在最前面——先审计观察者、再审计生成，最后才发现**根本没有可选的东西**（79% 的候选池是退化的）。

### 已注册的规则（在看到结果之前定的，不能事后改）

| 规则 | 内容 |
|---|---|
| 种子域分离 | 候选搜索 `700000000+`，梯度/dropout `900000000+`，两域不相交。代码里由 `v3.bank.assert_disjoint_seed_domains` 强制 |
| 候选库上限 | M=16。**只允许提到 24 一次**，且必须在报告中记录这次提升；仍不过则停 |
| 自然采样对照 | 每个 bank 的前 K 个候选即未筛选抽样，因此自然率与库率在**同一批 prompt** 上比较 |
| Gate B 资源 | 18.4M LoRA 参数 × 128 prompt × 3 准则，float16 memmap ≈ 14 GB 磁盘；Gram 分块峰值内存 < 1 GB |
| Gate B 现值参照 | identical 选择余弦 1.000（要求 ≥0.999）；不相交半批余弦 [−0.082, 0.322]，**只报告不当门** |
| L3 判据 | Gold 明显高于 Naive → 上云；两条曲线重叠 → **不上云**，回目标函数诊断；Gold 低于 Naive → 同上，且优先怀疑 SFT 目标对「正确候选」的加权方式 |

L3 这一步能省下 120–200 A800 小时。参照：v2.2 探索闭环里 Naive +4.17 pt、RFO-Self +2.08 pt（单 seed、K=2、10 步，只是工程证据）。

## Hard Stops

- 未经明确批准，不下载任何新模型或权重。需要时先暂停并报告 model/revision、磁盘占用、用途、替代方案。
- 不重写、不重新决定 v2.1 / v2.2 / v2.3 的已冻结证据。红色报告保留为不可变记录。**发现仪器缺陷时的正确做法是：旧记录原样保留 + 缺陷作为新证据登记 + 重新测量并注明来源**（先例见 §11）。
- Show-o v1 与 Show-o2-7B 是冻结负对照，不得静默提回主线。
- 不把 Qwen2-VL / Qwen3-VL 的输出当作「统一 backbone 能看见自己的画」的证据；它们始终是冻结外部观察者/检测器。
- 不在 Gate C 红之后靠加算力、加 seed 或换 backbone 来抢救。
- 单 seed 的描述性轨迹不得写成显著性结论。
- 候选库平衡池只用于梯度探针；训练与 external correctness 曲线必须用自然采样池，论文中显式区分。
- **绝不把已修正的探针 resume 进被隔离的输出目录**：`run_bank_probe` 按 `packets/{index}-{scene_id}.json` 断点续跑，而词汇修复不改变 scene_id，会静默复用旧图。
- **不删除 `runs/` 下的研究工件**（它们不在 git 里，删了就没了）。归档到 `_archive/`，不要 `rm`。

---

## 目录导航

四份文档各有一个职责，互不重复。**不要再新增计划类文档。**

```
Can You See What You Drew….md   规范 —— proposal v3.0，含 Gate A–D 判据
STATUS.md                       现状 —— 本页
docs/EVIDENCE_LOG.md            证据 —— 全部实测（§1–§12），只记录不重解释
docs/RUNBOOK.md                 操作 —— 3090 / A800 手册
docs/source-notes/              改变过设计的用户输入原件

src/selfsight/v3/               当前代 —— 全部新工作写在这里
src/selfsight/v23/              v2.3 冻结实现，不要修改
src/selfsight/{data,analysis,backbones,observers,pilot,training}/  共用基础设施

runs/readiness/                 §1 §2 就绪审计证据（冻结）
runs/exploratory-post-gate/     §3 探索闭环
runs/v2.3-rfo-gold/             §4 v2.3 机制门
runs/v3/                        §8–§11 当前代
runs/canaries/ review-packets/ gate-minus-1/   环境 canary 与盲审包
runs/_archive/                  已归档：mock-pilot ×3、audits、host、manifests、wandb
data/_archive/                  已归档：selfsight-v1、v2.3 失败模板
```
