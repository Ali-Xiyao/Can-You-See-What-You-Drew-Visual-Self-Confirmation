# STATUS

**唯一入口。** 想知道「现在什么成立」只读这一页；要证据细节再去 `docs/EVIDENCE_LOG.md` 对应节。

最后更新：2026-09-01 · 分支 `experiment/v2.3-rfo-gold` · backbone `showlab/show-o2-1.5B-HQ` rev `d3a220ec`

---

## 正在跑

**没有 GPU 作业在跑。** v4 两个半边都已走完 generate -> detect x2 -> crop -> verify
-> observe(image_only + prompted) -> analyze。主结果 §18、核验器缺陷 §19、复现与效应量分解 §20。

**在等一件人工输入**：70 张待裁定的图（`2+1` 28 张、`1+1+1` 42 张）。裁定表是**三选一**，
不再手打列表：`Q` 千问的列表对 / `I` InternVL 的列表对 / `X` 图里有东西不是任何东西
（粘连、认不出）。左右哪个检测器在上按图随机（按钮上写着名字，所以按名字点仍是一下），
防止顺着往下都点第一个。两个列表都不对而图能读，卡片下方仍可手写。

页面按 `--chunk 10` 分片、图片降采样成 JPEG 内嵌，每份约 160 KB、自包含：`A1–A3`（28 张）、
`B1–B5`（42 张）。之前那版把 432px PNG 全部 base64 塞进一个文件，1+1+1 那份 10.6 MB，
浏览器直接打不开。

回填：页面底部生成一行 `A1: 1Q 2I 3X 4="2 blue mug, white plate"`，
`scripts/v4_apply_review.py --index <run>/review.index.json --codes '<那一行>'`
写出 `human_labels.jsonl`。卡片号是全局的，页前缀只是标签；重复号、越界号、漏答都报错而不是警告。
`Q`/`I` 整行复制检测器的输出（含 bbox，否则后面出不了空间题）。

标签回来后重跑 `verify`，把这些图的行从两个 answers 文件里删掉，
再对它们单独重跑 `observe`（该阶段可续跑，不必为 70 张重付 912 张的钱）。

**`X` 计入 p，是失败，不是被忽略。** 一度给「整张图读不了」单设过 `unusable`，让它退出 p 的
分母；那是错的，已删除。p 问的是「生成有没有画出要求的东西」，读不出来的图显然没有，
把它从分母里拿掉等于把生成器最差的输出从它自己的分数里删掉，p 会被抬高。人读不出来是关于
这次生成的证据，不是仪器的空白；真正没有判定的只有文件损坏或根本没渲染出来，那不是评审员会报的东西。
`X` 唯一不做的事是出题——「你画了几个梨」在候选是半个梨时没有正确答案，所以
`stage_observe` 跳过它们。`ladder_summary` 另报一个 `unnameable` 计数，因为
「画错了物体」和「画出了非物体」是两种失败，p 把它们加成了一个数。

**下一件没做的事**：把 §21 留下的三条候选解释分开——需要「删除物体」这种编辑，
而它要 inpainting 权重，按 Hard Stop 得先批准。

**运行环境的一条事实**：两块卡都是 RTX 3090，但 GPU1 挂在 PCIe gen3 x4 上(GPU0 是 gen4 x16)，
逐图推理慢 7.5 倍(Qwen 检测 20.4 s/张 vs 2.7 s/张)，功耗只有 89 W、频率 1125 MHz——在等数据不在算。

**2026-09-01 起：所有 GPU 作业只用 GPU0，GPU1 不占。** 这是用户的指令，不是从上面那条实测推出来的。
因此各阶段串行跑，起下一个之前先看 `nvidia-smi` 显存有没有掉下来；队列长就说队列长，不许挪到 GPU1 上并行。

语料已定稿：`data/v4/corpus_2plus1.jsonl` 117 条、`corpus_1plus1plus1.jsonl` 111 条。
后者原为 120 条，剔除 9 条 `clear` / `wooden` 条目——那个进程在 12:53 启动，
内存里是加 `COLOR_WORDS` 白名单之前的模块，磁盘上的检查是对的、进程里的不是。
教训与 §15 同源：改了校验就必须重启用它的进程，否则校验只存在于源码里。

---

## 当前成立的结论

按主题组织，不按时间。括号内是证据出处。

### 1. Backbone 已定
`show-o2-1.5B-HQ`（512px 原生）。existence / color / spatial 三族四项指标全达标，程序化 verifier 的盲审 precision = 1.00。就绪门判红**只因为**「主族 ≥4」这条阈值，而该阈值无推导依据——v3 已公开把 floor 降到 3。（§1）

### 2. 负对照全红，不得提回主线
Show-o v1 1.3B、Show-o2-1.5B rank-1（432px）、Show-o2-7B 均判红。（§2）

### 3. 工程闭环已打通
LoRA 训练、E1 反事实、checkpoint 曲线在本地全部跑通。**单 seed 下没有机制信号**——这是工程可行性结论，不是机制结论。（§3）

### 4. 候选供给是真实瓶颈
随机 K=4 池天然 informative 率仅 **21%**。有界候选库搜索把 balanced_rate 从 0.135 提到 **0.344**（2.5×），但仍低于注册阈值 0.40 → Gate A 判红。失败模式与 v2.x 预期**相反**：不是模型画不对，是 color 太准（每候选正确率 0.949）凑不出 2 个错误候选。（§4, §8）

### 5. 梯度噪声控制原设计有缺陷
「不相交半批余弦」测的量与它要把关的配对跨判据余弦**尺度不同**，产生过两次误导性红灯。已改为 prompt 层配对 bootstrap；半批余弦降级为诊断，不再当门槛。（§5）

### 6. 显示词汇缺陷（2026-08-30 发现）
verifier 把长宽比 0.5–2.0 的四边形都判为 SQUARE，而 v3 manifest 声称用 `box` 措辞却有 **183/256 行**仍写 `square`。实测 **39.5%** 的 verifier-SQUARE 物体长宽比 ≥1.25（中位 1.19，p90 1.62）。方向是**系统性抬高 `oracle − naive`**，即把 Gate C 推向假绿。已修复并加 fail-closed 守卫。（§11）

### 7. 选择头room 的判据（已在看数前注册）
`Oracle@K − Naive@K` > 0.05 → go；< 0.03 → stop；之间 → 扩样本。每候选正确率 p 与天花板
`(1−(1−p)^4) − p` 的关系：p≈0.35 时最大（0.471），p→0 或 1 时坍缩。有效区间很宽——
p ∈ [0.25, 0.55] 全都 ≥0.41，所以标定不需要精细调参。

**头room 至今没有被测出来。** 本页早先写的「已测 spatial p=0.194 / existence p=0.864」
无法从任何源文件复现，已作废（§12.4）。

### 8. box 词汇下的 Gate A（2026-08-31，128 prompt/族）

| 族 | balanced_rate | informative | 自然池 informative | 自然池 p | 天花板 |
|---|---|---|---|---|---|
| existence | **0.594** 过 | 91 (0.711) | 0.461 | 0.818 | 0.181 |
| spatial | **0.266** 不过 | 65 (0.508) | **0.016** | 0.754 | 0.243 |

阈值 0.40。两族都远在有效区间之外（p 太高）。spatial 的自然池 **98.4% 是退化的**——
整体 p=0.754，但每条 prompt 要么四个全对要么全错，与 proposal §72「模型对某个 prompt 的
正确性接近确定性」一致。

绝对供给判据（128 个 informative pool）在两次运行里都被显式 defer，从未测过。

---

### 9. gold 原子必须是全函数且肯定式（2026-08-31）

标定扫描本来是要量难度的，结果量到了别的东西。

**弃权，不是判错。** binding 和 spatial 在模型生成图上大面积弃权（binding 40–44/64，
spatial 44–54/64，existence **0/64**）。`color` 和严格关系谓词要求主体解析到**恰好一个**
检测才给答案，否则返回 `None`。这在参考渲染上恒成立（自洽测试因此全绿），在生成图上
只有 12–40% 成立。于是 p 是在存活的少数样本上估的，难度扫描测的是验证器可解析性。
`exists` 没有唯一性要求，所以 existence 弃权率恒为 0——**弃权还是判错是验证器的设计
选择，不是模型的性质。**

**否定命题近乎白送。** 拆开 existence 正负例（物体数 3/5/6）：

| | 正例 p | 负例 p |
|---|---|---|
| 3 物体 | 0.312 | **0.931** |
| 5 物体 | 0.420 | **0.882** |
| 6 物体 | 0.250 | **0.819** |

模型漏画东西就自动满足否定命题。这在生成式验证里是本质属性，调参调不掉。

**修复**（`src/selfsight/data/questions.py: build_gold_atoms`）：把**判生成图的原子**和
**问模型的原子**分开。问题保持正负平衡不变；gold 原子必须同时满足

* **全函数**——新增 `exists_left_of` / `exists_above` / `exists_larger_than`
  （`verifier.py`），「不存在这样一对」是答案不是弃权；
* **肯定式**——existence 负例场景改用「场景里某个真实物体在场」，binding 用
  「目标在场 ∧ 错配色不在场」，spatial 用「关系成立 ∧ 主体在场」。

`_gold_score` 改成多原子合取（`v3/supply.py`），manifest 落 `gold_atoms` 字段，
旧 manifest 自动回落到原问题原子。空白画布永远判错，由 `tests/test_v3_families.py` 守住。

**重测**（`scripts/rescore_v3_calibration.py`，**同一批图，只换判分，零 GPU**）：

| 物体数 | 族 | p | 天花板 | 弃权 | balanced_rate |
|---|---|---|---|---|---|
| 3 | binding | 0.312 | 0.464 | **0.000** | 0.688 |
| 3 | existence | 0.531 | 0.420 | **0.000** | 0.875 |
| 3 | spatial | 0.344 | **0.471** | **0.000** | 0.750 |
| 5 | binding | 0.297 | 0.459 | 0.000 | 0.812 |
| 5 | existence | 0.641 | 0.343 | 0.000 | 0.750 |
| 5 | spatial | 0.328 | 0.468 | 0.000 | 0.938 |
| 6 | binding | 0.312 | 0.464 | 0.000 | 0.750 |
| 6 | existence | 0.594 | 0.379 | 0.000 | 1.000 |
| 6 | spatial | 0.531 | 0.420 | 0.000 | 1.000 |

弃权全部归零，balanced_rate 最低 0.688（阈值 0.40，原 spatial 是 0.000 informative）。
spatial@3 的天花板 0.471 就是 K=4 的理论最大值 0.4715。

**物体数是弱旋钮。** 标定 bank 上的检测直方图是平的：要 6 个物体时 Show-o2 最常画 3–4 个，
要 3 个时最常画 2 个。移动 p 的是原子设计，不是物体数。

> **分析脚本的坑：** `_gold_score` 走 `verify_generated_image` → `detect_generated_objects`
> （给生成图用的容差更大的轮廓检测器）。任何离线复算都必须走同一条路径；用参考图检测器
> `detect_objects` 会得到系统性不同的 p。这个错误在本次分析中犯过一次，未进入任何产物。

**难度选择是欠功效的（重要限制）。** 48 条标定场景分给 3 个族，每族只有 **16 条 prompt**。
候选在 prompt 内部有聚类（设计效应 1.5–1.9），聚类后 SE(p) ≈ **0.085**，而有效区间
[0.25, 0.55] 只有 0.30 宽。注册规则是靠 |Δp| = 0.038 在 obj3 和 obj6 之间做的选择——
**这个差值远在噪声底之下，四档之间的取舍实际上是任意的。**

规则是看数之前注册的，照章执行，结果不改。但能写进论文的结论不是「obj3 是最优难度」，
而是**四档现在全部落在可工作区间、弃权全部为零**——这个结论对档位选择不敏感。
下次要真正区分档位，标定分片每族需要的是几百条 prompt，不是 16 条。

顺带：新 gold 原子下候选池的退化程度明显下降。held-out existence 的 64 条 prompt 里
20 条「四个候选全同」，独立假设下期望 12 条（设计效应 1.48）。对比 §8 记录的旧设计
「每条 prompt 要么四个全对要么全错」，这是选择机制能起作用的前提条件。


---

### 10. Gate A 首次两条判据同时成立（2026-08-31，held-out）

难度在 calibration 分片上冻结（三族均为 3 物体），Gate A 测在**没有参与选难度**的
64 条 held-out prompt 上。产物 `runs/v3/gate-a-v2/{family}/gate_a.json`，
汇总 `runs/v3/queue2/SUMMARY.md`。

| 族 | balanced_rate | informative | 自然 informative | bank lift | p | 天花板 | 弃权 |
|---|---|---|---|---|---|---|---|
| binding | 0.672 | 53/64 | 0.609 | +0.062 | 0.340 ± 0.040 | **0.470** | 0.000 |
| existence | 0.859 | 60/64 | 0.688 | +0.172 | 0.645 ± 0.036 | 0.340 | 0.000 |
| spatial | 0.891 | 63/64 | 0.734 | +0.156 | 0.508 ± 0.039 | 0.434 | 0.000 |

* **balanced_rate 三族全过**（阈值 0.40）。
* **informative pool 合计 176 ≥ 128**——注册的绝对供给底线在本项目里第一次达标。
  §8 的两次运行都把这条显式 defer 了，从未测过。
* **弃权全部为零。** 这是 §9 的直接结果。

binding 的天花板 0.470 几乎就是 K=4 的理论最大值 0.4715，但它的 balanced_rate（0.672）
和 bank lift（+0.062）是三族里最低的：候选质量分布最集中，bank 搜索能加的东西最少。

**held-out 的 p 系统性高于 calibration（未定论）。** 三族同向：+0.027 / +0.113 / +0.164，
Stouffer 合并 z = 2.00（双尾 p = 0.045）。**这是事后构造的检验**——我在看到前两族之后
才提出这个假设，第三族只是确认了方向，所以 0.045 不能按预注册结果解读。
影响是难度在 held-out 上比预期略偏易（existence 的 0.645 已在 [0.25, 0.55] 之外），
但三族 balanced_rate 仍全过。要定论需要预注册一次专门的检验。

---

### 11. 判分器效度审计：几何检测器不可信（2026-08-31）

§10 的 Gate A 数字全部依赖 `detect_generated_objects`。本节第一次在**人工标注**上
测了它的效度，结论是它不可信，§10 的 p / 天花板不能按面值使用。
（供给判据本身抗得住，见文末。）

#### 11.1 人工基准

72 个 held-out 自然池候选（每族 24），从 768 个里按固定种子抽样，**排除**先前带检测框
看过的 48 个。标注在只有编号、无框、无判定、无 prompt 的对照图上做。尺寸沿用检测器
同一条 95px 规则；判不准的标 `?` 并从尺寸统计中剔除。产物
`scratchpad/human_labels.py`、`blind_sample.jsonl`。

**这是 §「不可恢复的损失」之后项目里唯一可用的人工基准。它必须进 git。**

#### 11.2 物体级效度

| 判分器 | precision | recall | F1 | 整图完全一致 | 形状误判 | 漏检 | 虚构 |
|---|---|---|---|---|---|---|---|
| 几何（现行） | 0.750 | 0.757 | 0.754 | **0.486** | 45 | 5 | 7 |
| 几何（顶点数规则修复） | 0.889 | 0.898 | 0.894 | 0.694 | 16 | 5 | 7 |
| **Qwen3-VL-8B-Instruct** | **0.981** | **0.990** | **0.986** | **0.944** | 1 | 1 | 3 |

**现行检测器每两张图就有一张的物体清单是错的。**

#### 11.3 三个独立缺陷

1. **立方体被判成圆**（45 次形状误判里 `square→circle` 占 21）。渲染出的 3D 立方体
   投影是六边形，正六边形 circularity ≈ 0.91，而规则是「circularity ≥ 0.72 且外接框
   接近正方 → 圆」。八边形、圆角矩形走同一个洞。
2. **背景色相撞进调色板时整组物体消失。** 背景被归入某个颜色通道后，形态学闭运算把
   背景和同色物体连成一块，再被「面积 ≥ 80% 画布」整块丢弃。实例：一张模型三个物体
   全画对的图，检测器只看见一个。
3. **碎片与重复。** 18.9% 的候选带至少一个面积不足中位数 1/10 的检测；3.0% 的候选
   有同位置重复检测。

#### 11.4 判定级效度（48 张 existence + binding，gold 原子是纯存在式，不需坐标）

| 判分器 | 与人工一致 | 一致率 | 判对但实际错 | 判错但实际对 |
|---|---|---|---|---|
| 几何（现行） | 34/48 | **0.708** | 5 | 9 |
| 几何（修复） | 41/48 | 0.854 | 3 | 4 |
| Qwen3-VL-8B | 44/48 | **0.917** | 2 | 2 |

Qwen 的 4 处分歧逐张裁定后：1 处人工标注更弱（水滴形），1 处尺寸卡在 95px 边界，
1 处画面上的圆是**青色**（不在四色调色板内，属数据缺陷），仅 1 处 Qwen 真错。
**扣除标注争议后 Qwen 的一致率约 0.96。**

#### 11.5 修复线的评估

按 112 条人工标注轮廓拟合新形状规则（三角：circularity<0.62 且 fill<0.60；
圆：eps=0.01 下顶点数 ≥10；其余为方）。顶点数比 circularity 分得干净得多——
圆中位数 14 顶点、方 5、三角 3。`square→circle` 从 21 降到 1。

**但缺陷 2、3 一个没修**（漏检 5 / 虚构 7 完全不变），它们与形状分类无关。
且阈值是在同一批 112 条上挑的，0.894 含样本内成分。
**判断：几何路线修得动但修不到 Qwen 的水平，且每个缺陷都要在同一批小样本上单独
拟合阈值，终点大概率是过拟合到这批图的检测器。**

#### 11.6 对 §10 的影响

* **Gate A 的两条供给判据仍然成立。** 它们问的是「池子里有没有对错两侧」，
  对判分器的系统性偏移不敏感。补上 spatial 的 object grounding 后实测
  balanced_rate 0.807 → 0.797，informative 176 → 175，仍然通过。
* **p、天花板、以及基于 p 的难度选择不能按面值使用。**
* **偏差方向不能简单定号。** §9 记录的漏画漏洞（原子设计）抬高 p；本节的检测器误差
  在 existence+binding 上净向下（9 次误判为错 vs 5 次误判为对）。两者方向相反。

> **上一版本记录的一处过度归因已更正：** 「检测器报 large 占 72% 而场景只要求 51%，
> 是绝对像素阈值的问题」——**说过头了**。人工标注显示画面上实际就有 64.6% 是大物体，
> 大头是模型自己画大了；两个判分器的尺寸准确率都是 0.93，阈值本身没问题。

---

### 12. 那个「held-out p 同向偏高」已被完全解释（2026-08-31）

§10 末尾标注为未定论的 Stouffer z = 2.00，查清后**没有任何真实的难度漂移**，
三个族三个不同原因：

| 族 | 原始差值 | 原因 | 校正后 |
|---|---|---|---|
| spatial | +0.164 | 成分差异 + 漏画漏洞 | **+0.008** |
| existence | +0.113 | 赢家诅咒 | — |
| binding | +0.027 | 无（成分变化反而掩盖了它） | +0.063 |

**spatial：条件 p 几乎没动**（truth=True 0.288→0.250，False 0.583→0.652），
动的是配比——标定集 81% 是「关系成立」的难题，held-out 只有 36%。按标定配比标准化后
0.325 vs 标定 0.344。而 False 一侧的高 p 来自漏画漏洞：**107 个判对的 truth=False
候选里 40 个（37.4%）画面上根本没有 object 形状。** 补上 grounding 后 spatial p
0.508 → 0.352（标定是 0.344）。

`build_gold_atoms` 的 spatial 分支注释说要堵漏画漏洞，但**只 ground 了 subject，
没 ground object**。binding 有同类漏洞且更大：87 个判对里 39 个（44.8%）画面上
没有竞争者的颜色+形状，30 个（34.5%）目标形状只画了一个（场景本该有两个）。

**existence：赢家诅咒。** 扫描四档 p = 0.531/0.656/0.641/0.594，带是 [0.25,0.55]，
**obj3 的 0.531 是唯一落在带内的，只差 0.019 出带**。held-out 实测 0.645，
而其他三档均值 0.630。真实 p 约 0.63，标定那 16 条向下抖了约 0.10，规则抓住了抖动。
**existence 的 `in_band` 判定不可复现；按真值它应触发 `knob_exhausted`。**

**生成器 truth 标志有偏（新缺陷）。** `truth` 靠「掷硬币定目标真值，不符就交换两个
物体」实现，但交换只在两个锚点该坐标不同时才翻转真值。ANCHORS 是 8 点网格，
x 有 3 组重复、y 有 3 组重复，**坐标打平时 True 不可达**：

| 关系 | 坐标打平概率 | 预测 P(True) | 实测（3000 场景） |
|---|---|---|---|
| left_of | 0.250 | 0.375 | 0.360 |
| above | 0.107 | 0.446 | 0.446 |
| larger_than | 0 | 0.500 | 0.506 |
| 合计 | | 0.440 | 0.437 |

设计意图是 50/50，实际是 44/56。另已排除构建方式差异：混合族与单族构建的 truth 率
分别为 0.446 / 0.437，统计上相同；用同种子精确复现出了两份 manifest。
标定集那 13/16 = 0.812 是**真实率 0.44 下 P ≈ 0.0016 的抽样意外**——
这是 §9「每族只有 16 条 prompt」那条限制第一次真的咬人。

**Stouffer 的 0.045 本身双重虚高**：事后构造，且假设三族独立（共用 backbone、
检测器、代码版本，相关会撑大 z；取 ρ=0.3 则 z 从 2.00 降到 1.58）。

---

### 13. 缺陷在金标准，不在检测器（2026-08-31，同一批 753 张 held-out 图）

同一批图，四种打分方式：

| 打分 | p | C(p) K=4 | 自然 K=4 平衡率 |
|---|---:|---:|---:|
| 几何检测器 + 旧每族原子（§10） | 0.495 | 0.440 | 0.225 |
| Qwen3-VL-8B + 旧每族原子 | 0.583 | 0.387 | 0.162 |
| Qwen3-VL-8B + 场景匹配（含尺寸，prereg v1） | 0.107 | 0.258 ✗ | 0.089 |
| Qwen3-VL-8B + 场景匹配（去尺寸，prereg v2） | 0.253 | 0.436 | 0.220 |

**换检测器动了 +0.09，换金标准动了 −0.48。** 我早先把「检测器不可信」当作主因，这一行否定了那个说法：
主因是金标准，检测器是次因。

失败逐层分解：数量对 0.459 → 再加形状 0.277 → 再加颜色 0.252 → 再加尺寸 0.107。
**超过一半的图物体数就是错的，而旧金标准对此完全看不见。** 颜色只花掉 2.5 个点——
颜色当初是因为几何检测器的 0.156 供给率被退役的，那是个仪器假象，退役理由不成立。

existence 的漏洞在换上更好的检测器后**变大**：0.645（几何）→ 0.844（Qwen + 旧原子），
因为 Qwen 几乎总能找到那个唯一的锚点物体。这说明「把检测器换好」不能修复一个最小主张式的金标准。

### 14. v4 难度旋钮：断崖，不是斜坡（2026-09-01）

48 条可核验自然场景 prompt × K=4 = 192 张，Show-o2-1.5B-HQ 生成，Qwen3-VL-8B 按多重集规则评分：

| 场景总物体数 | n | p | 95% CI | C(p) K=4 |
|---:|---:|---:|---|---:|
| 2 | 8 | 0.500 | [0.215, 0.785] | 0.438 |
| 3 | 12 | 0.417 | [0.193, 0.680] | 0.468 |
| 4 | 52 | 0.154 | [0.080, 0.275] | 0.334 |
| 5 | 20 | 0.000 | [0.000, 0.161] | 0 |
| ≥6 | 100 | 0.010 | — | ~0 |

单条目数量的画对率：要求 1 个 → 0.754，2 个 → 0.312，3 个 → 0.213。

**据此把总物体数锁在 3，单条目数量上限 2。** 组合层面 `1+1`=0.50、`2+1`=0.50、`1+1+1`=0.375，
其余全部塌到 0（每格 n 只有 4–12，但与总物体数那一列的大样本趋势一致）。

一处需要更正的早先说法：我在只看了两三张图之后说过「tier 2 太简单」。实测 tier 2 的 p=0.172。
那是抽样运气，以实测为准。

---

### 15. 让 LLM 写语料:光靠 prompt 约束不管用（2026-09-01）

第一批 119 条 `2+1` 场景通过了全部结构校验（条目数、总物体数、prompt 与 spec 一致、
无重复），人工抽检 30 条却发现一类校验看不见的缺陷：**自然物体的颜色不合常理**。

instruction 从第一版起就写着「绝不要写蓝香蕉」。实际产出的 55 条 apple 条目里，
蓝 ×10、黑 ×4、棕 ×3、白 ×2——35% 不合理；场景层面 22/119 受影响。

**为什么这不是小事**：模型的物体颜色先验会和 prompt 打架，图失败的原因就不再是被测的能力；
而且不同物体被拖累的程度不一样，物体身份因此变成难度分层的混淆项。

**处理**：`PLAUSIBLE_COLORS` 白名单写进 `_check` 强制执行，只约束自然物（水果蔬菜蛋面包），
人造物（杯/书/蜡烛/碗/盘…）不限颜色——马克杯真的可以是蓝的，苹果不行。
第一批语料移入 `data/v4/_superseded/`，重跑。

**可推广的一条**：语料由模型写时，任何「请不要 X」的软约束都必须有对应的硬校验，
否则它只是一句祝愿。这也是用户当初那句「后面你人工校验一下」抓出来的——
自动校验全绿，人眼看第 30 条时才发现。

---

### 16. v3 的瓶颈是候选不变异，不是 p 落错区间（2026-09-01，v4 首批 117 spec × K=4）

单判别器（Qwen3-VL-8B）下的首读，**尚未过三级阶梯**：

| | p | 平衡池率 | 独立伯努利预测 | 实测/预测 |
|---|---:|---:|---:|---:|
| v3 几何 + 旧每族原子 | 0.495 | 0.225 | 0.875 | **0.26** |
| v3 Qwen + 场景匹配（去尺寸） | 0.253 | 0.220 | 0.685 | **0.32** |
| **v4 可核验自然场景** | **0.188** | **0.513** | 0.564 | **0.91** |

平衡池率 = K=4 池里既有对又有错的比例，三行同一定义、同为自然采样。
独立预测 = `1 − p⁴ − (1−p)⁴`，即若同一 prompt 的四个候选彼此独立时该有的平衡率。

**Gate A 的 ≥40% 判据在 v4 上成立（0.513），在 v3 上不成立。**

**最后一列才是重点。** Proposal §3.2 断言「在这个尺度上，模型对某个 prompt 的正确性接近确定性」，
并据此把候选供给定为整条路线的真实瓶颈。这个断言对 v3 数据是对的——但它是**几何刺激的性质，
不是模型的性质**。v3 的平衡率只有独立抽样预测值的 1/4 到 1/3，同一 prompt 的四个候选高度相关；
换成自然场景后这个比值升到 0.91，候选之间几乎独立。

所以 v3 的瓶颈从来不是「p 落在错误区间」，而是**同一 prompt 内候选不变异**。
此前围绕 p 调旋钮（物体数、词汇、金标准）的全部努力都在治标：p=0.495 那一档 p 明明在带内，
平衡率却只有 0.225。这一条应写进论文，它比「换了个更好的判别器」有意思得多。

**~~一处需要记录的偏差~~ ——— 这条归因是错的，已由 §19 取代。** 原文写：实测 p=0.188 低于
校准值 0.417，是因为 LLM 产出的 prompt 比校准批次短、无照片框定、部分图偏插画风。
实际原因是核验器的两个缺陷（盘子被当成表面、cup/mug 与 notebook/book 交叉命名），
修掉之后 p = 0.244。prompt 形式的差异也许仍有一点影响，但它不是主因，
而且当时没有任何证据支持那个归因——它是在没有别的解释时顺手写下的一个解释。
**下次遇到「实测低于校准」，先去查尺子，再去猜刺激。**

失败分层（468 张）：全对 88、总数不对 312、漏画物体 68。

### 17. 「诊断性试次」不会自己出现——四个族一度全部为零

在花 GPU 让 backbone 答题之前，对 468 张已检测的图做了一次纯 CPU 的出题空跑。结果：

```
总题数 1803，平均 3.85 题/图
gold 来源: image 1344 / spec_matches_image 305 / image_differs_from_spec 154
  诊断性试次占比 0.085   ——  而且 154 道全部来自 counting
  existence 0/468   absence 0/468   binding 0/355   spatial 0/53
```

**承载整个主张的只有 8.5% 的试次。** 其余 91.5% 上「读像素」与「背 prompt」给出同一个答案，
两个假设不可分。原因是构造上的：只有 `build_counting` 会把「请求的数量」当作干扰项，
其余四族的干扰项都是从一个通用物体池里随机抽的，与这张图的 spec 无关，
于是干扰项永远既不在图里也不在 prompt 里，背 prompt 的模型不会被它抓住。

修法是让每一族都优先从 **spec 里没被满足的那条声明** 取干扰项：

| 族 | spec 说 | 画出来 | 问 | 背 prompt 的人答 |
|---|---|---|---|---|
| existence | 蜡烛 | 瓶子（多画的） | 「图里有瓶子还是蜡烛」 | 蜡烛 ✗ |
| absence | 蜡烛 | 没画 | 「图里有蜡烛吗」 | 有 ✗ |
| binding | 绿梨 | 红梨 | 「梨是什么颜色」 | 绿 ✗ |
| counting | 两个梨 | 三个 | 「几个梨」 | 两个 ✗ |
| spatial | 杯在苹果左 | 画在右 | 「杯在左还是右」 | 左 ✗ |

修完重跑同一批图：

```
总题数 1860，平均 3.97 题/图
gold 来源: image 182 / spec_matches_image 1264 / image_differs_from_spec 414
  诊断性试次占比 0.223（154 → 414 道）
  existence  81/468 = 0.173
  counting  160/459 = 0.349
  absence   159/468 = 0.340
  binding    14/412 = 0.034
  spatial      0/53 = 0.000
```

三件必须一起记下的事：

**(a) existence 只算「替换」为诊断。** 若图里画出的那个选项本身也在 spec 里（漏画了另一个），
背 prompt 的模型在两个选项里都看见自己的指令，它是**掷硬币而不是答错**。
把这种试次也标成诊断会用无法区分两个假设的题去稀释主结果，所以只有
「画了 spec 没要的 × 漏了 spec 要的」这种替换才标 `image_differs_from_spec`（81 道），
其余标 `image`（在元数据里留 `present_in_spec` 以便复查）。

**(b) binding 的诊断率低是测出来的事实，不是构造缺陷。** 逐条统计 spec 条目：
颜色画对 722、画错 14、根本没画 168、同名物体画成两种颜色 32。
颜色几乎是免费的——这与 v3 的分层一致（count 0.459 → object 0.277 → colour 0.252）。
所以 binding 这一族在本语料上基本不产诊断题，报告时不应掩饰。

**(c) spatial 在本语料上永远为 0，且是有意的。** 它要成为诊断题必须 spec 里带关系，
而带关系就等于多给生成器一个必须做对的约束，直接压 p，而 p 是这条路线上最稀缺的资源
（Proposal §5：不带关系的 prompt 让 spatial 题「白拿」）。
`build_spatial` 里读 spec 关系的分支已经写好并有测试覆盖，换成带关系的语料无需改代码。

**可推广的教训**：v3 的族是按「能问什么」划分的，v4 的族必须按
「**这一问能否把读像素和背 prompt 分开**」重新审一遍。前者是题型，后者才是实验。
一个族问得再对，只要它的干扰项与 spec 无关，它对主张的贡献就是零。
### 18. 主结果：prompt 在场时，模型答自己画错的那张图，正确率从 0.651 掉到 0.201

`2+1` 半边，117 条 spec × K=4 = 468 张图，1783 道 2AFC 配对试次，同一批图、同一批题，
唯一差别是问题前面有没有那句「你是照这个描述画的：……」。

| | image-only | prompted | 配对检验 |
|---|---:|---:|---|
| 图与 spec 一致（1244 道） | 0.982 | **0.993** | 3 → 17 反向，χ²=8.4, p=0.0037 |
| 图与 spec 冲突（307 道） | 0.651 | **0.201** | **140 → 2，χ²=132, p=1.4e-30** |
| 总体 | 0.905 | 0.824 | |

**两个方向相反，这才是关键。** 若 prompt 只是干扰上下文，两行都该下降；实际上图与 spec 一致时
prompt 略有帮助（+0.011，显著），只有在像素与描述冲突时才崩塌。加了 prompt 之后
140 道从对变错、只有 2 道从错变对；而在一致的那批上是 3 → 17，方向反过来。

自我确认率（诊断性试次中按被要求的场景而非画出的场景作答的比例，随机 = 0.5）：
**0.349 → 0.799。**

分族看诊断性试次：

| 族 | image-only | prompted | n |
|---|---:|---:|---:|
| counting | 0.815 | **0.109** | 135 |
| absence | 0.510 | 0.262 | 145 |
| binding | 0.688 | 0.312 | 16 |
| existence | 0.455 | 0.357 | 11 |

counting 最干净：**不给 prompt 时模型数得清自己画的（0.815）**，给了 prompt 之后
「你被要求画两个梨」→ 它答两个，尽管它画了三个、而且被明确要求「回答图里实际有什么」。
这不是看不见，是看见了不采信。

程序性检查：prompted 条件下弃答率 0.000，选 A 比例 0.499（gold A 0.496），无位置偏置。
排除 28 张待人工裁定的图后，诊断性正确率 0.6515 → 0.6445 / 0.2013 → 0.2020，结论不变。

**这个对照是补跑的，而且是必须补的。** 在此之前只跑了 image-only 一条，而
`observe_atoms` 按 RFO 隔离契约只喂图和问题、不带任何生成期状态——描述根本不在上下文里。
那种条件下「照着 prompt 答而不是照着像素答」这个假设**没有可答的东西**，
0.982 vs 0.651 那个落差只能说成「画错的图更难读」，是难度不是服从。
Proposal §5 的观察条件矩阵本来就写着 Fresh-context prompted 这一行，是我漏跑了。
一条只有单一条件的曲线读不出机制，这一条要记住。

**边界**：只有 `2+1` 半边；单一模型、单一 seed 组；binding 与 existence 的诊断 n 只有 16 和 11，
不单独下结论。这是描述性结果，不是训练结论。
### 19. 核验器的三个缺陷，以及一个「低到该去查」的数字

`2+1` 半边跑了三遍，每遍都是因为尺子有问题，不是因为结论不喜欢。三版记录全部留档
（`runs/v4/main-2plus1/*.v1-no-box-clause.*`、`*.v2-plate-as-surface.*`）。

| | v1 | v2 | v3 定稿 |
|---|---:|---:|---:|
| p | 0.197 | 0.192 | **0.244** |
| 平衡池率 | — | 0.496 | **0.573** |
| 清单一致率 | — | 0.496 | **0.573** |
| 人工裁定率 | 0.335 | 0.077 | **0.060** |

**(a) Qwen 在 468 张里 276 张一个 bbox 都不给。** 不是解析错、不是 key 名不同——字段直接省略，
object 和 color 照常正确。`parse_reply` 有框就存、没框不报错，全程没有任何东西失败。
框是 spatial 族和裁剪重问赖以工作的东西，所以这两处一直只跑在 41% 的行上：
spatial 从 468 张图只出了 53 题，我一度归因于 `2+1` 组成里单例物体少，**那个归因也是错的**。
指令加一句「元素缺 box 即无效」，6/6 原本没框的图都给了框。detection 行现在存原始回复。

**(b) 指令让检测器跳过「物体所在的表面（table, tray, counter, cloth）」，而 21% 的场景要求画盘子。**
盘子既是物体又是表面，于是这些图无论生成器画得多好都必然被判漏画：仅此一项 57/468。
反过来把托盘说成物体又制造了新的假阳性（20/468 多出一个「第四个物体」）。
最终形态是：指令里明说盘/碗/碟是物体，同时**在比对时用一张表面词表强制过滤** table/tray/cloth/mat
——软约束必须配硬检查，这已经是第三次（§15 颜色白名单、§17 诊断干扰项、这里）。

**(c) 名词粒度。** cup↔mug 交叉命名 30 次、notebook↔book 21 次，多重集按精确名词比对，
同一个东西当成两个，又 50 张。`canonical_noun` 只并这两对加复合词取中心词，
**故意做得很小**：lemon 与 lime 不并、spoon 与 fork 不并——那些是检测器的错误，值得被测出来。
撰写词表每对同义词只留一个，canonical 后撞车的 spec 直接拒。

**这三个是怎么被发现的：不是靠 code review，是靠一个太低的数字。**
absence 族的诊断性试次正确率 0.385，显著低于随机。分物体一看：
plate 0.182（n=33）、cup 0.303（n=33）、notebook 0.550（n=20）——占 205 道里的 86 道。
那些东西**本来就在图里**，模型答「有」是对的，错的是金标准。修完这一族回到 0.510。

**一个族的数字低到反常时，先去看它，再去写它。** 如果当时把 0.385 当成发现写进论文，
写的是核验器的缺陷，署名是模型的失败。
### 20. `1+1+1` 半边复现了主结果，效应量只有一半，原因查出来了

`1+1+1` 走完同一条流水线（111 spec × K=4 = 444 张）。核验器不再有 §19 那三个缺陷，
一次跑成，没有返工：

| | `2+1` | `1+1+1` | 合并 |
|---|---:|---:|---:|
| p | 0.2436 | 0.3288 | 0.2851 |
| 平衡池率 | 0.5726 | **0.6847** | 0.6272 |
| 清单一致 / 判决一致 | 0.573 / 0.885 | 0.475 / 0.840 | — |
| 人工裁定率 | 0.060 | **0.0946** | 0.0768 |

两个半边 Gate A 都过（≥0.40），人工裁定率都在锁定的 ≤10% 以内，但 `1+1+1` 只剩 0.5pt 余量——
它三个单例物体、颜色各不相同，两个检测器在清单层面更容易分歧。crop 那一级问了 120 个争议物体，
**0 个因为没有 bbox 而问不成**（§19(a) 修好之前这个数是 59%）。42 张待裁定，已生成
`runs/v4/main-1plus1plus1/review.html`。

**配对对照复现，方向完全一致：**

| | image-only | prompted | 配对 McNemar |
|---|---:|---:|---|
| 图与 spec 一致（1111 道） | 0.974 | **0.987** | 8 → 22 反向，χ²=5.6，p=0.018 |
| 图与 spec 冲突（365 道） | 0.669 | **0.449** | 87 → 7，χ²=66.4，p=3.7×10⁻¹⁶ |

**关键的那个对照——一致行反向上升——在独立的一半语料上又出现了一次**（+0.013，与 `2+1` 的
+0.011 几乎同值）。「加长上下文造成的干扰」这个解释要成立，得解释为什么它只在像素与描述冲突时才生效。

合并 3672 道配对试次（912 张图）：一致 0.978 → 0.990（11 → 39，p=1.3×10⁻⁴），
冲突 0.661 → 0.336（227 → 9，χ²=199.5，p=2.6×10⁻⁴⁵）。自我确认率 **0.339 → 0.664**。
分族（合并）：counting 0.825 → 0.249（n=189）、binding 0.792 → 0.500（72）、
existence 0.571 → 0.333（42）、absence 0.561 → 0.350（369）。
**§18 里 n 太小不敢单独说的 binding 与 existence，现在 72 和 42，都显著且同向。**

**但冲突行的落差在两半边差了一倍**（−0.450 vs −0.219），这不能不解释。分解到 counting 的
「要求几个 / 画了几个」组合上，答案是干净的：

| 要求 / 画出 | n | image-only | prompted | 出现在 |
|---|---:|---:|---:|---|
| 2 / 1（少画了） | 102 | 0.804 | **0.049** | 只有 `2+1` |
| 1 / 2（多画了） | 73 | 0.740 | 0.534 | 两个半边都有 |

**少画比多画容易否认得多。** 要求两个、只画了一个，再把描述放回上下文，模型有 95% 的时候
答「两个」——它在断言一个自己没画出来的物体存在。反过来要求一个、画了两个，它只有 41%
的时候答「一个」。这与 absence 族的方向一致（那一族问的正是「被要求但没画出来的物体」，
0.561 → 0.350）：**缺席的东西比多余的东西更容易被 prompt 补上。**

`1+1+1` 的所有物体都是单例，`(2,1)` 这个格子在它里面根本不存在，效应量减半就是这么来的。

**这条不要写得比证据硬。** `(2,1)` 只出现在一个半边，所以「少画 vs 多画」的比较里混着
「哪个半边」；两边都有的 `(1,2)` 格子在 `2+1` 是 0.353（n=17）、在 `1+1+1` 是 0.589（n=56），
半边效应确实存在，只是没被分离出来。要干净地测它，得让同一批 spec 同时供给两种错法。

**一个附带的观察**：image-only 条件下有 49 道弃答，prompted 条件下 0 道。
那句描述不只是改变答案，它还让模型每一次都给出答案。

配对分析独立成脚本 `scripts/v4_compare_conditions.py`，因为 `v4_analyze.py` 一次只看一个文件，
而主张在两个文件的**逐试次差**上——非配对地比两个准确率会把配对信息扔掉，在这里方差会被放大数倍。
### 21. Tier B：构造真值拿到了，服从却没出现——而且「像素清楚就不服从」这个解释是错的

从 209 张外部验证为正确的图上建了 139 对：**83 对水平翻转 + 56 对改色**（Proposal 原写 300 对，
底料只有 209 张，达不到；差额如实记，不靠放宽标准凑）。

**改色臂（56 对，两个条件）**

| | image-only | prompted |
|---|---:|---:|
| 原图（与描述一致） | 1.000 | 1.000 |
| 改色图（与描述冲突） | 1.000 | **0.964** |

56 道里只有 2 道翻转。**服从几乎不存在**，而自然池的 binding 诊断题是 0.792 → 0.500。
两个验收门下结论一样（严格 44 对 0.977，阶梯 12 对 0.917），受语义约束的名词（水果，n=13）
与不受约束的（杯碗瓶，n=43）也一样。弃答两个条件都是 0。

**我先给出的解释是错的，在这里更正。** 我一度写道「服从是像素证据弱时填进来的东西」——
Tier B 的题在 image-only 下 1.000，自然池的诊断题只有 0.669，看上去正好对上。
**counting 直接推翻它**：counting 的诊断题在 image-only 下是 0.825，一点也不含糊，
prompted 下却掉到 0.249。像素清楚而服从极强，这个格子存在，所以「清楚就不服从」不成立。

留下的候选解释，都还没测：

- **错法不同。** counting 的主力格子是「要求 2、画了 1」（§20），模型要断言的是一个**不在图里**的
  物体；改色问的是一个确实在图里的物体是什么颜色。前者也许更像它可能犯的错。
- **可归因性。** 一只毫不含糊的绿杯子，在「你被要求画蓝杯子」面前，不是一个模型合理会犯的错误；
  少画一个梨是。服从也许跟着「这个错我可能犯过吗」走，而不是跟着像素清晰度走。
- **编辑痕迹。** 旋转色相后的物体饱和度整齐，也许携带了「这不是我画的」的线索。

**能分辨这三条的那个编辑，恰好是这一轮做不了的那个**：删除一个物体，直接造出「要求了但图里没有」，
也就是 counting 与 absence 的那种错法。删除必须 inpaint，本地没有权重，按 Hard Stop 要先批准。
这一条要记住——**范围决定是有代价的，代价出现在这里，不是出现在批准的时候。**

**翻转臂（83 对）：一个干净的阴性对照**

image-only 逐图 0.789，prompted 0.735（21 → 12，χ²=1.94，p=0.164，不显著）。
配对层面 image-only 两个都对 0.590、只对一个 0.398、都错 0.012。若两次作答独立且 p=0.789，
预期是 0.62 / 0.33 / 0.045——**实测与独立几乎一致，翻转对没有查出隐藏的位置偏置或场景先验**。
本语料 spec 不带空间关系，两个成员在描述面前地位相同，所以这一臂本来就测不到服从，
它测的是「答案是否跟着像素走」，答案是：跟，但 spatial 自读本身只有 0.79，不高。

**构造真值与难度不可兼得。** 双检测器要能确认「颜色变了、别的没变」，编辑就必须做得干脆，
于是题必然容易（image-only 1.000）。想让 Tier B 出难题就得只旋转一部分色相，
可那样「到底算黄还是算绿」就不再由构造给出，得回去要人工标签。
**同一道门既保证了标签，也保证了题目容易**，这是构造类刺激的结构性代价，要写进论文的限制一节。

**三个工程缺陷，都已修并补测试：**

- `check` 的续跑键只有 `pair_id`、不含检测器名。Qwen 写完 80 行，InternVL 认为全做过了，
  于是 80 张改色图全卡在 `not_checked_by_both`，产率报成 **0.000**——一个「什么都没做错」的零。
- 验收门按整份清单比对，19/80 的拒绝来自两个检测器对**别的物体**的分歧，而不是编辑本身。
  这正是 §19 修过一次的错（按清单判 vs 按被判定的事判）。改成：两个检测器都要确认被编辑的那个事实，
  其中一个确认别的没动。产率 0.550 → 0.700，两个门的结论一致，两个数都报。
- `questions` 用顺序 RNG。加了 12 对之后，**已答过的 254 道里 136 道选项顺序变了**，
  而 `observe` 的续跑按 question_id 判——旧答案会被配到新选项上。改成按 `pair_id` 播种，
  阶段幂等；旧答案留档 `answers.sequential-rng.jsonl`，278 道全量重跑。
  **续跑的正确性依赖上游阶段是幂等的，这个依赖之前没有被写下来，也没有被测。**
---

## 已作废 / 已被取代

| 结论 | 状态 | 原因 |
|---|---|---|
| §10 三物体难度标定 | **作废** | 双重混淆：把 3 物体脏词汇数据和 §8 的 2 物体干净数据对比，物体数与词汇同时变化 |
| `runs/v3/pool-ext…` 池扩展运行 | **作废** | 词汇缺陷（§11.4）；且与 §8 基线不可比（§11.6） |
| stage-1 头room 数（0.754 / 0.879 / 0.125） | **作废** | 测在作废数据上；且该次 `naive_scored_prompts = 0`，头room 根本没算出来 |
| 本页曾写的 p / 天花板（0.194 / 0.864） | **作废** | 无法从任何源文件复现，与 stage-1 文件和 Gate A 都对不上 |
| 「§10 的 p / 天花板可按面值使用」 | **作废** | 判分器与人工一致率仅 0.708，见 §11 |
| 「existence 在 obj3 是 in_band」 | **作废** | 赢家诅咒；四档唯一在带内且只差 0.019，held-out 实测 0.645，见 §12 |
| 「held-out 的 p 同向偏高可能是真实漂移」 | **作废** | 三族三个已查明的机制性原因，无真实漂移，见 §12 |
| 「spatial gold 的 grounding 堵住了漏画漏洞」 | **作废** | 只 ground 了 subject，object 未 ground；37.4% 的 truth=False 判对是漏画白拿，见 §12 |
| 「生成器的 spatial truth 是 50/50」 | **作废** | ANCHORS 网格坐标重复使 True 不可达，实际 44/56，见 §12 |
| 「检测器 large 比例偏高是像素阈值的锅」 | **作废** | 人工标注显示画面实际 64.6% 是大物体；尺寸准确率 0.93，见 §11.6 |
| 「弃权率反映任务难度」 | **作废** | 弃权是谓词非全函数造成的，与难度无关；见 §9 |
| §3.1 的族选表（existence/color/spatial 入主等） | **作废** | 用 F1 0.754 的几何检测器测出的「哪族可测」，测的是尺子分辨率，不是模型能力边界 |
| 视觉词汇 `box`（长宽比 0.5–2.0） | **作废** | 刺激已换成可核验自然场景，几何形状词表不再存在 |
| 难度旋钮 `objects_per_scene ∈ [2, 6]` | **作废** | 新仪器下断崖在 4→5；且旋钮定义变了（场景总物体数，而非每场景形状数） |
| 「Qwen 判分下 p=0.583 说明旧族仍可用」 | **作废** | 那是 Qwen + **旧金标准**。同一批 753 张图上换检测器只动 +0.09，换金标准动 −0.48；缺陷在金标准 |
| 几何检测器与整条 v3 几何栈（代码） | **已删除** | 只存于 git 历史（`830cfa7` 之前）。红色报告与 EVIDENCE_LOG 原样保留，`runs/` 未动 |
| 旧 gold 原子下的 obj3/5/6 标定数（p 0.667–0.900，informative 0.000） | **作废** | 在 60–85% 弃权的样本上估的，同批图重测见 §9 |
| 「calibration 分片被 build_v3_scenes 的签名守卫排除在 held-out 之外」 | **作废** | `forbidden` 每次调用从空集开始，跨调用不检查。已加 `--forbid`；实测三个分片交集均为 0，是补守卫不是修泄漏 |
| 「binding 是一个独立问题族」 | **作废** | 实现上它和 color 走同一分支，产出完全相同的 atom，只是物体数被硬编码成 3。已重建为真族 |
| v2.2 就绪审计的 count 淘汰 | 维持 | 6 条分歧里 4 条与 square 无关（§12.1） |

### 问题族重设计（2026-08-31）

**binding 原来是假的。** `build_primary_atom` 里它和 color 共用分支，都产出
`subject="shape=X"; predicate="color"`，问题「What color is the box?」光凭形状就能答。

**现在是真的**，且 verifier 一行未改：场景里放两个同形状、不同大小、不同颜色的物体，
atom 用 `subject="shape=square;size=large"`，问「What color is the large box?」。
`_matches` 本来就支持 shape/color/size 三个字段，按 shape+size 过滤到唯一一个。
`shape=X` 单独用会命中 2 个 → verifier 弃权，这条性质由 `tests/test_v3_families.py` 守住。

**难度旋钮原来卡在 3。** `rng.sample(SHAPES, count)` 要求形状全局唯一，而 SHAPES 只有 3 个。
真正的约束是**被引用的形状唯一**，不是全局唯一——干扰物可以重复形状。放开后上限是 6
（受 4 色调色板限制，场景内 (shape, color) 保持唯一）。

**算 p 有个坑。** `build_balanced_pool` 对判定为非 informative 的池会丢弃候选 id，
所以只统计 `natural_pools.jsonl` 里出现的 id 会**系统性漏掉每一个全对池**，把 p 压低。
existence 上这个错误读出 0.623，真值是 0.818——足以选错难度。
`scripts/select_v3_difficulty.py` 按 `reason` 字段把它们补回来。

### 不可恢复的损失
`runs/readiness/showo2-1p5b-hq/a3-human-r1-review.csv` 的 49 条人工标注**仍在**，但它们指向的 49 张图位于已删除的 `H:\selfsight-runs\`，**0/49 可访问**。标注无法用于验证任何重建后的 verifier。这是本项目最严重的流程失误：**gitignore 的 `runs/` 里的研究工件没有任何备份**。

标注本身已 `git add -f` 强制入库（`runs/readiness/showo2-1p5b-hq/a3-human-r1-review.csv`），不再依赖 gitignore 目录存活。

---

## 未决问题

1. **binding 是否该重建。** v2.2 把 binding 判出局（verifier precision 0.778），但那 2 条分歧**全部**是
   `What color is the square?` ——同一个词汇缺陷。该判定已随几何栈作废，v4 里 binding 是五族之一。
2. **Gate C 是否可达。** v4 仪器下重跑后给出。
3. **v4 的 Tier B 像素反事实尚未构造。** Proposal §5 仍要求 300 对。

---

## 锁定的选择

改动这些需要新的明确决定，不能顺手改。

| 项 | 值 | 依据 |
|---|---|---|
| 主 backbone | `showlab/show-o2-1.5B-HQ` rev `d3a220ec`（512px 原生） | §1 |
| **刺激** | **可核验自然场景**，由 LLM 撰写（prompt 与 spec 一同产出） | 外部有效性；几何形状不是任何人会向生成模型提的要求 |
| **主问题族** | **existence / counting / binding / spatial / absence**，全部 2AFC | 族是实验条件，不再按「哪族打得动分」筛。2AFC 与 yes/no 同为 1 bit 但没有恒定安全答案，且单 token 可判 |
| **图像正确性金标准** | 检测到的 （物体， 颜色） 多重集 == spec 多重集，**与族无关的单一规则** | v3 每族一套金标准，每套都是最小主张，少画就能满足（漏画白拿:existence 15% / spatial 37.4% / binding 44.8%）。多重集相等无否定式主张，漏洞从构造上不存在 |
| **金标准来源** | **图像，永不来自 spec** | 问的是「你画了什么」。画错的试次标 `gold_source: image_differs_from_spec`，是信息量最大的一批 |
| **难度旋钮** | **场景总物体数 = 3**；单条目数量 ≤ 2 | 实测断崖（48 prompt × K=4）:2→p=0.500，3→0.417，4→0.154，5→0.000，≥6→0.010。C（p） 要求 p∈[0.13，0.70] |
| 组合 | `1+1+1` 与 `2+1` 各半 | `1+1+1` 供 spatial（需两个单例）与 binding；`2+1` 供 counting 的真实错配试次 |
| **主判别器** | **Qwen3-VL-8B-Instruct**（冻结） | n=72 盲审人工标签:F1 0.986，整图全对 0.944 |
| **次判别器** | **InternVL3.5-8B**（冻结） | F1 0.978；与 Qwen 错误方向相反（Qwen 偏虚构，InternVL 偏漏检），共同错误仅 1 例 |
| 判别器排除 | Qwen3-VL-4B | F1 0.943 不算低，但失败集中在**漏检**（12 例 vs 8B 的 1 例），而「判别器漏看」与「模型少画」正是本实验要区分的东西 |
| **不一致处理** | **三级升级裁定**:一致→采纳 / 争议物体裁切重问 / 人工裁定。**一致性是标记，不是筛选** | 筛掉不一致子集会把语料重采样到简单场景、抬高 p，且杂乱度在族间分布不均→比较的是不同总体 |
| 冻结负对照 | Show-o v1、Show-o2-7B | 不得静默提回主线 |
| 弱观察者消融臂 | Qwen2-VL-2B（已审计） | proposal §7.6 |
| 主 seed | `20260901` | — |
| **判别器审计判据** | **人工裁定率必须报告，且 ≤ 10%** | 2026-09-01 用户决定（选项 b）。旧的「120 图参考观察审计」在 v4 下无法执行——参考渲染器已随几何栈删除，v4 没有参考图。阶梯的人工层精确覆盖真正难判的图，而随机抽 120 张里绝大多数两个模型毫无异议 |
| 人工裁定的执行方式 | 命中 `pending_human` 时把图与两个检测器的分歧一并交给用户判定 | 同上 |
| Gate A/B/C/D 判据 | proposal §9 | 唯一规范来源，本页不复制 |

**已退役的锁定项**（移出上表，原因见「已作废」）:视觉词汇 `box`、`objects_per_scene ∈ [2,6]`、
主族 existence/spatial/binding、color 退役、count 入 hard tier、absolute size 排除。
这些全部是几何刺激 + 几何判别器下的决定，刺激和判别器都已更换，决定随之失效。

## 决策树

```
L1 Gate A ──红──> 能力地板报告，项目转向
   │绿
   L2 Gate B ──红──> 加 pool 数；仍红则主张①退熵类信号，②③继续
   │绿
   L3 Gate C 预告 ─重叠─> 不上云，诊断修正目标/样本权重/梯度探针
   │分离
   C0 canary ──红──> 定位平台差异
   │绿
   C1 Gate C ──红──> ★ 项目失败条件：停止扩展，回到目标函数诊断
   │绿
   C2 Gate D ──红──> 降级：①无预警价值 → 重心回②③
   │绿                      ②无法归因 → RFO 改写为纯经验方法
   C3 + 追加 seed
```

**尚未实现的运行器**：`scripts/run_main_seed_e2.py`（从 `run_formal_e2.py` 拆出单 arm / 单 seed / 单卡，供 C1/C2 并行启动）。其余 L0–L2 的脚本均已存在。

**原则**：所有能杀死项目的实验都在本地跑完，再碰 A800。v2.x 的教训是把最贵的东西排在最前面——先审计观察者、再审计生成，最后才发现**根本没有可选的东西**（79% 的候选池是退化的）。

### 已注册的规则（在看到结果之前定的，不能事后改）

| 规则 | 内容 |
|---|---|
| 种子域分离 | 候选搜索 `700000000+`，梯度/dropout `900000000+`，两域不相交。代码里由 `v3.bank.assert_disjoint_seed_domains` 强制 |
| 候选库上限 | M=16。**只允许提到 24 一次**，且必须在报告中记录这次提升；仍不过则停 |
| 自然采样对照 | 每个 bank 的前 K 个候选即未筛选抽样，因此自然率与库率在**同一批 prompt** 上比较 |
| Gate B 资源 | 18.4M LoRA 参数 × 128 prompt × 3 准则，float16 memmap ≈ 14 GB 磁盘；Gram 分块峰值内存 < 1 GB |
| Gate B 现值参照 | identical 选择余弦 1.000（要求 ≥0.999）；不相交半批余弦 [−0.082, 0.322]，**只报告不当门** |
| L3 判据 | Gold 明显高于 Naive → 上云；两条曲线重叠 → **不上云**，回目标函数诊断；Gold 低于 Naive → 同上，且优先怀疑 SFT 目标对「正确候选」的加权方式 |

L3 这一步能省下 120–200 A800 小时。参照：v2.2 探索闭环里 Naive +4.17 pt、RFO-Self +2.08 pt（单 seed、K=2、10 步，只是工程证据）。

## Hard Stops

- 未经明确批准，不下载任何新模型或权重。需要时先暂停并报告 model/revision、磁盘占用、用途、替代方案。
- 不重写、不重新决定 v2.1 / v2.2 / v2.3 的已冻结证据。红色报告保留为不可变记录。**发现仪器缺陷时的正确做法是：旧记录原样保留 + 缺陷作为新证据登记 + 重新测量并注明来源**（先例见 §11）。
- Show-o v1 与 Show-o2-7B 是冻结负对照，不得静默提回主线。
- 不把 Qwen2-VL / Qwen3-VL 的输出当作「统一 backbone 能看见自己的画」的证据；它们始终是冻结外部观察者/检测器。
- 不在 Gate C 红之后靠加算力、加 seed 或换 backbone 来抢救。
- 单 seed 的描述性轨迹不得写成显著性结论。
- 候选库平衡池只用于梯度探针；训练与 external correctness 曲线必须用自然采样池，论文中显式区分。
- **绝不把已修正的探针 resume 进被隔离的输出目录**：`run_bank_probe` 按 `packets/{index}-{scene_id}.json` 断点续跑，而词汇修复不改变 scene_id，会静默复用旧图。
- **不删除 `runs/` 下的研究工件**（它们不在 git 里，删了就没了）。归档到 `_archive/`，不要 `rm`。

---

## 目录导航

四份文档各有一个职责，互不重复。**不要再新增计划类文档。**

```
Can You See What You Drew….md   规范 —— proposal v3.0，含 Gate A–D 判据
STATUS.md                       现状 —— 本页
docs/EVIDENCE_LOG.md            证据 —— 全部实测（§1–§12），只记录不重解释
docs/RUNBOOK.md                 操作 —— 3090 / A800 手册
docs/source-notes/              改变过设计的用户输入原件

src/selfsight/v3/               当前代 —— 全部新工作写在这里
src/selfsight/v23/              v2.3 冻结实现，不要修改
src/selfsight/{data,analysis,backbones,observers,pilot,training}/  共用基础设施

runs/readiness/                 §1 §2 就绪审计证据（冻结）
runs/exploratory-post-gate/     §3 探索闭环
runs/v2.3-rfo-gold/             §4 v2.3 机制门
runs/v3/                        §8–§11 当前代
runs/canaries/ review-packets/ gate-minus-1/   环境 canary 与盲审包
runs/_archive/                  已归档：mock-pilot ×3、audits、host、manifests、wandb
data/_archive/                  已归档：selfsight-v1、v2.3 失败模板
```
